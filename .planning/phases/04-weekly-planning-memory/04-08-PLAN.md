---
phase: 04-weekly-planning-memory
plan: 08
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - recipe-planner/src/pages/WeeklyPlans.tsx
  - recipe-planner/src/lib/planning/dates.ts
autonomous: false
requirements: [WEEK-01, WEEK-02]
must_haves:
  truths:
    - "The New Plan dialog collects start_date (native date input, default upcoming Monday) and people_multiplier (number, step 0.5, min 0.1, default 1) and persists both on create/update"
    - "people_multiplier <= 0 is rejected/clamped client-side (Security V5)"
    - "The plan list secondary line and plan header show the formatted 'Week of {Mon D, YYYY}' date derived from start_date"
    - "A pure dates helper computes the upcoming Monday and formats start_date, reusable by the wizard"
  artifacts:
    - recipe-planner/src/lib/planning/dates.ts
    - recipe-planner/src/pages/WeeklyPlans.tsx
  key_links:
    - "start_date entered here feeds history/LRU (04-04) and the wizard's staples 'previous plan' lookup (04-09)"
    - "people_multiplier entered here is what Outputs (04-07) reads to scale outputs"
---

<objective>
Add the `start_date` picker and `people_multiplier` control to the New Plan dialog, persist both, and display the formatted week date in the plan list and header. Provide a small pure dates helper (upcoming Monday + format) reused by the wizard.

Purpose: WEEK-01 (dated plans, shown in list/header) and the WEEK-02 control surface (the multiplier knob). This is the manual-plan path; the wizard (04-09) reuses the same dialog + dates helper.
Output: dialog fields + list/header date + `src/lib/planning/dates.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-UI-SPEC.md
@recipe-planner/src/pages/WeeklyPlans.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: dates helper (upcoming Monday + format)</name>
  <files>recipe-planner/src/lib/planning/dates.ts</files>
  <read_first>
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (A1 PocketBase date serialization note — space vs T separator; A2 date math is a plain one-liner, no date library)
    - .planning/phases/04-weekly-planning-memory/04-UI-SPEC.md (Copywriting: "Week of {Mon D, YYYY}"; start-date label "Week starting (Monday)")
  </read_first>
  <action>
    Create `src/lib/planning/dates.ts` exporting two pure functions: `getUpcomingMonday(from?: Date): string` returning an ISO `YYYY-MM-DD` for the next Monday on/after `from` (default today), suitable as a native date-input value; and `formatWeekOf(startDate: string): string` returning `Week of Jul 6, 2026`-style text from a stored start_date (normalize a possible space/T separator per A1 before parsing; guard empty/null → return "" so callers can conditionally render). No external date library.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run build && grep -c "getUpcomingMonday\|formatWeekOf" src/lib/planning/dates.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes
    - both functions exported; formatWeekOf tolerates both `2026-07-06 00:00:00.000Z` and `2026-07-06T00:00:00.000Z` and empty input
  </acceptance_criteria>
  <done>Reusable pure dates helper exists.</done>
</task>

<task type="auto">
  <name>Task 2: New Plan dialog fields + list/header date display</name>
  <files>recipe-planner/src/pages/WeeklyPlans.tsx</files>
  <read_first>
    - recipe-planner/src/pages/WeeklyPlans.tsx (plan dialog state ~95-98; handleSavePlan 356-391; dialog markup 953-987; plan list ListItemText 584-587; header 792-794 — all verified-accurate citations)
    - .planning/phases/04-weekly-planning-memory/04-UI-SPEC.md ("New Plan dialog additions" + "Plan list + header" sections: field placement, labels, inputProps step 0.5 min 0.1, helper text, 44px touch floor)
    - recipe-planner/src/lib/types.ts (WeeklyPlan.start_date/people_multiplier from 04-02)
    - recipe-planner/src/lib/planning/dates.ts (Task 1 helper)
  </read_first>
  <action>
    Extend the plan dialog: add a `TextField type="date" label="Week starting (Monday)"` below the Plan Name field (margin="dense" fullWidth), defaulting new plans to `getUpcomingMonday()`; and a `TextField type="number" label="People multiplier"` with `inputProps={{ step: 0.5, min: 0.1 }}`, default `1`, and the helper text from the UI-SPEC. Add matching dialog state; on submit reject/clamp people_multiplier <= 0 (Security V5) and persist both `start_date` and `people_multiplier` in handleSavePlan's create/update payload.
    Display the date: in the plan list ListItemText secondary line render `formatWeekOf(plan.start_date)` (only when present), and in the plan header (currently `{selectedPlan.name || "Unnamed Plan"}`) add a `Typography variant="body2" color="text.secondary"` line beneath the name with the same formatted date. Respect the 44px touch floor on any new tappable element per the UI-SPEC.
    Do NOT add the "Fill Week" wizard-launch button here — that belongs to 04-09 (same file, later wave) to avoid a merge conflict.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run build && grep -c "people_multiplier\|start_date\|formatWeekOf\|getUpcomingMonday" src/pages/WeeklyPlans.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes
    - dialog persists start_date (default upcoming Monday) and people_multiplier (default 1) on create/update; people_multiplier <= 0 is rejected/clamped
    - plan list secondary line and header both show the formatted "Week of …" date when start_date is set
    - the Fill Week button is NOT added in this plan (reserved for 04-09)
  </acceptance_criteria>
  <done>Manual plans carry a date + multiplier and the date is visible in list and header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies date + multiplier dialog and date display</name>
  <action>Human-verify gate: exercise the New Plan dialog to confirm the default Monday, multiplier persistence + guard, and the formatted date in list/header per how-to-verify.</action>
  <what-built>New Plan dialog now has a Monday date picker (default upcoming Monday) and a people-multiplier field; the plan list and header show the formatted week date.</what-built>
  <how-to-verify>
    In the app: open the New Plan dialog — the date field defaults to the upcoming Monday and the multiplier defaults to 1. Create a plan; confirm the list row and the header both show "Week of {Mon D, YYYY}". Edit the plan, set multiplier to 1.5, save, reopen — value persists. Try setting multiplier to 0 or a negative — confirm it is rejected/clamped. Confirm existing (backfilled) plans display their dates.
  </how-to-verify>
  <resume-signal>Type "approved" if the date + multiplier fields, persistence, guard, and date display all work, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client dialog → PocketBase | User-entered start_date/people_multiplier written to the plan record. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08a | Tampering | negative/zero people_multiplier | medium | mitigate | Client-side reject/clamp <= 0 plus inputProps min 0.1 and the PocketBase field Min 0.1 (04-02) — defense in depth (Security V5). |
| T-04-08b | Tampering | malformed start_date string breaking history/format | low | mitigate | Native date input constrains format; formatWeekOf guards empty/malformed input. |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase (native date input, no @mui/x-date-pickers). |
</threat_model>

<verification>
`npm run build` passes; human-verify confirms default Monday, multiplier persistence + guard, and formatted date in list/header.
</verification>

<success_criteria>
WEEK-01 dates are collected + displayed on the manual plan path and the WEEK-02 multiplier control exists with a validity guard; a reusable dates helper backs both this and the wizard.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-08-SUMMARY.md` when done.
</output>
