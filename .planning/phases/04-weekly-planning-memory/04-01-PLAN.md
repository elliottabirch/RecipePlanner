---
phase: 04-weekly-planning-memory
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - recipe-planner/src/lib/planning/history.test.ts
  - recipe-planner/src/lib/aggregation/aggregation-multiplier.test.ts
  - recipe-planner/src/lib/units.test.ts
  - recipe-planner/scripts/backfill-plan-dates.test.js
autonomous: true
requirements: [WEEK-01, WEEK-02, WEEK-03, WEEK-04]
must_haves:
  truths:
    - "Vitest collects four new/extended test specs covering the LRU service, pool resolution, multiplier scaling, D-03 pull-list correctness, D-04 rounding, and the backfill date resolver"
    - "Each spec fails only because its implementation is not yet written (RED), not because of a syntax error"
  artifacts:
    - recipe-planner/src/lib/planning/history.test.ts
    - recipe-planner/src/lib/aggregation/aggregation-multiplier.test.ts
    - recipe-planner/scripts/backfill-plan-dates.test.js
  key_links:
    - "Test signatures match the exact function shapes the downstream implementation plans will export (orderPoolByLRU/computeLastPlannedDates/poolForSlot, scaleQuantity, buildPullLists 3-arg, resolvePlanDate)"
---

<objective>
Author the Wave 0 Nyquist test scaffolds enumerated in 04-VALIDATION.md so every downstream Phase 4 implementation task has an automated `<verify>` target that flips RED→GREEN. These specs are intentionally RED until the paired implementation plan lands.

Purpose: Lock the observable contracts (AC#6 LRU tie-break, AC#7 pull-list correctness, AC#8 no-regression, D-04 discrete/continuous rounding, WEEK-01 backfill parse) as executable assertions before any implementation.
Output: Two new TS test files, one new JS test file, and appended cases in `units.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@.planning/phases/04-weekly-planning-memory/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LRU + pool-resolution spec (RED)</name>
  <files>recipe-planner/src/lib/planning/history.test.ts</files>
  <read_first>
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 5 exact function shapes + Code Examples "LRU tie-break unit test shape"; Pattern 6 poolForSlot)
    - recipe-planner/scripts/seed-usda.test.js (project vitest style: describe/it/expect, fixtures as plain objects)
    - recipe-planner/src/lib/types.ts (WeeklyPlan, PlannedMeal, TemplateSlot shape to import as types)
  </read_first>
  <action>
    Create `src/lib/planning/history.test.ts` importing `computeLastPlannedDates`, `orderPoolByLRU`, and `poolForSlot` from `./history` (module does not exist yet — import failure is the expected RED).
    Cover AC#6 (WEEK-04): (a) a never-planned recipe sorts before one planned last week; (b) a recipe planned 3 weeks ago sorts before one planned this week; (c) two recipes with equal last-planned dates sort by name asc then id asc, deterministic across a call with reversed input array; (d) two never-planned recipes tie-break by name asc then id asc. Use ISO date strings like "2026-06-01" for start_date fixtures. Follow the exact test shape in RESEARCH Code Examples.
    Cover WEEK-03 poolForSlot: given a `recipeTags: Map<string,string[]>` and a slot with `pool_tags: ["tag-a"]`, only recipes whose tag list intersects pool_tags are returned (match-any). Include a case where a recipe has none of the pool tags and is excluded.
    Do NOT implement history.ts — this task authors specs only.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/planning/history.test.ts; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "orderPoolByLRU\|computeLastPlannedDates\|poolForSlot" recipe-planner/src/lib/planning/history.test.ts` returns >= 3
    - `npm run test -- src/lib/planning/history.test.ts` fails on the missing `./history` module (RED), not on a syntax/parse error in the test file itself
  </acceptance_criteria>
  <done>history.test.ts is collected by vitest and fails only because ./history is unimplemented.</done>
</task>

<task type="auto">
  <name>Task 2: Multiplier scaling + D-03 pull-list + D-04 rounding specs (RED)</name>
  <files>recipe-planner/src/lib/aggregation/aggregation-multiplier.test.ts, recipe-planner/src/lib/units.test.ts</files>
  <read_first>
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 2 regression-check requirement + the three assertion cases; Pattern 3 scaleQuantity signature + AC#7 exception; Pattern 1 threading)
    - recipe-planner/src/lib/aggregation/aggregation-lineid.test.ts (existing buildPullLists call-site shape, makePlannedMeal helper, RecipeGraphData fixture construction)
    - recipe-planner/src/lib/units.test.ts (existing units test style + imports)
    - recipe-planner/src/lib/aggregation.ts:102-205 (current buildPullLists / buildProductFlowGraph signatures)
  </read_first>
  <action>
    Create `src/lib/aggregation/aggregation-multiplier.test.ts` reusing the fixture helpers from `aggregation-lineid.test.ts`. Author these cases against `buildPullLists(plannedMeals, recipeData, peopleMultiplier)` (new 3rd arg, not yet added):
    (a) D-03 regression — a `quantity: 2` meal with a JIT assembly step and a continuous-unit (e.g. "g") input node emits pull-list quantity = base × 2 at peopleMultiplier default; assert this is DOUBLE today's unscaled output (the correctness fix, independent of the multiplier).
    (b) `quantity: 1` meal × `peopleMultiplier: 1.5` scales the pull-list item ×1.5 (continuous, exact).
    (c) `quantity: 2` meal × `peopleMultiplier: 1.5` scales ×3 (compounding).
    Also cover WEEK-02/AC#8 for `buildProductFlowGraph(..., peopleMultiplier)`: a multiplier of 1 reproduces byte-identical product/step quantities versus the current 2-arg call (no-regression), and a multiplier of 2 doubles a continuous product total.
    Append to `src/lib/units.test.ts` a `describe("scaleQuantity")` block importing `scaleQuantity` from `./units` (not yet exported): continuous unit (e.g. "g", "cup") returns exact `qty*factor` including fractional (3 × 0.5 → 1.5); `each`-dimension unit ceils (3 × 0.5 → 2); the literal "discrete" third arg ceils regardless of any unit; and container-float-drift is defeated (2 × 1.0 stays 2, never 3 from a 2.0000000002-style input — assert `scaleQuantity(2, 1.0000000001, "discrete") === 3`? use a value that would round up to document the ceil, and separately assert exact integers stay put).
    Do NOT implement scaleQuantity or the multiplier threading.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/aggregation/aggregation-multiplier.test.ts src/lib/units.test.ts; echo "expect RED on multiplier/scaleQuantity assertions"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "peopleMultiplier\|scaleQuantity" recipe-planner/src/lib/aggregation/aggregation-multiplier.test.ts recipe-planner/src/lib/units.test.ts` returns >= 4 across the two files
    - aggregation-multiplier.test.ts contains a case asserting a `quantity: 2` meal doubles pull-list output (the D-03 regression), distinct from any multiplier=1 no-op case
    - units.test.ts scaleQuantity block asserts BOTH exact-continuous and ceil-discrete behavior
  </acceptance_criteria>
  <done>Both specs are collected and fail only on unimplemented scaleQuantity / un-threaded multiplier.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill date resolver spec (RED)</name>
  <files>recipe-planner/scripts/backfill-plan-dates.test.js</files>
  <read_first>
    - recipe-planner/scripts/seed-usda.test.js (pure-resolver test pattern: imports resolveSeedRow from the sibling .js, in-memory Map fixtures, no live DB)
    - .planning/phase-docs/phase-4-week-memory.md §3.6 + item 1b (backfill algorithm: parse "Week of …" → date, else descending Mondays by created, tie-break id asc)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Runtime State Inventory: nullable-first → backfill → required; A2 regex-only parse)
  </read_first>
  <action>
    Create `scripts/backfill-plan-dates.test.js` importing a pure resolver (name it `resolvePlanDate`) from `./backfill-plan-dates.js` (module not written yet — RED). Following the `seed-usda.test.js` pure-function convention, assert:
    (a) a plan named like "Week of Jan 6, 2026" resolves to that Monday's ISO date;
    (b) a plan with a non-matching name falls back to an assigned Monday derived from its position in the created-order sequence (feed the resolver the index + a reference/base Monday so it stays pure and deterministic — the plan's own `main()` will compute base/index from the live getFullList order);
    (c) descending-Mondays ordering is monotonic and tie-breaks deterministically by id asc for equal created timestamps.
    Keep every assertion against the pure resolver only — no PocketBase, mirroring seed-usda.test.js's "no live DB" stance.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- scripts/backfill-plan-dates.test.js; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolvePlanDate" recipe-planner/scripts/backfill-plan-dates.test.js` returns >= 1
    - test covers BOTH the "Week of …" parse path and the descending-Monday fallback path
    - fails only on the missing `./backfill-plan-dates.js` import (RED)
  </acceptance_criteria>
  <done>backfill-plan-dates.test.js is collected and RED pending the script's pure resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none (test authoring) | This plan writes test files only; no runtime input crosses a boundary and no packages are installed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | test fixtures | low | accept | Tests are deterministic, in-memory, no live DB or network; false-green risk mitigated by requiring RED-for-the-right-reason in acceptance criteria. |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase (04-RESEARCH Package Legitimacy Audit: zero new deps); nothing to verify. |
</threat_model>

<verification>
Run `npm run test` — the four new/extended specs are RED; the pre-existing suite stays GREEN (no production code touched). Confirm each failure is an unimplemented-symbol/import failure, not a parse error.
</verification>

<success_criteria>
All four Wave 0 specs from 04-VALIDATION.md exist, are collected by vitest, and fail only because their implementations are pending.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-01-SUMMARY.md` when done.
</output>
