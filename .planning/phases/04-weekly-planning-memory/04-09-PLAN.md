---
phase: 04-weekly-planning-memory
plan: 09
type: execute
wave: 3
depends_on: [04-02, 04-04, 04-08]
files_modified:
  - recipe-planner/src/components/WeekWizard.tsx
  - recipe-planner/src/pages/WeeklyPlans.tsx
autonomous: false
requirements: [WEEK-03, WEEK-04]
must_haves:
  truths:
    - "A 'Fill Week' button on the WeeklyPlans header opens WeekWizard for the selected dated plan"
    - "WeekWizard is a one-page MUI Accordion (not a modal stepper): active slot expanded with a 4px accent left border, others collapsed showing an 'N of {count} picked' outlined badge + CheckCircle when full; auto-advances on completion"
    - "Slot #1 Staples is pre-expanded with last week's picks pre-toggled and a one-tap 'Confirm Staples' action; every other slot lists its pool least-recently-planned-first via history.ts"
    - "Picking = tap-to-toggle recipe Chips (filled/outlined tag-color idiom); a docked '+ Add other recipe' Autocomplete reaches off-pool recipes; a slot is never blocking (Skip this slot / Next slot always available)"
    - "Each pick writes a planned_meal with the slot's meal_slot (never null), day (null for week-spanning), quantity 1, and template_slot = slot.id"
    - "A failed pick write reverts the Chip and shows an inline error Alert (non-optimistic write, no retry queue)"
    - "Empty states render for no-template-seeded and zero-eligible-pool per the UI-SPEC"
  artifacts:
    - recipe-planner/src/components/WeekWizard.tsx
  key_links:
    - "poolForSlot + orderPoolByLRU (04-04) drive pool contents/order; template_slots (04-06) drive slot walk; created planned_meals feed the same Outputs aggregation as manual meals"
    - "day threaded through so day-specific slots yield non-empty pull lists (AC#9); meal_slot required so writes never violate the constraint"
---

<objective>
Build the guided-fill wizard (`WeekWizard.tsx`) and its "Fill Week" launch button, implementing D-02's locked accordion mechanics and the 04-UI-SPEC visual states. The wizard walks the seeded template's slots, pre-fills staples from last week, orders each pool least-recently-planned-first, and writes picks as `planned_meals` identical in shape to manual ones.

Purpose: WEEK-04 (guided fill, staples-first, LRU order) and the WEEK-03 consumer surface (tag pools become usable). This is the phase's headline UX.
Output: WeekWizard.tsx + Fill Week button in WeeklyPlans.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@.planning/phases/04-weekly-planning-memory/04-UI-SPEC.md
@recipe-planner/src/pages/WeeklyPlans.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: WeekWizard.tsx — accordion, pools, staples pre-fill, writes</name>
  <files>recipe-planner/src/components/WeekWizard.tsx</files>
  <read_first>
    - .planning/phases/04-weekly-planning-memory/04-UI-SPEC.md (FULL — Accordion states table, wizard pick Chip states, "N of {count}" badge, empty states, error state copy, "Confirm Staples"/"Skip this slot"/"Next slot"/"+ Add other recipe" copy, spacing/typography/color, 44px touch floor)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 6 poolForSlot + staples previous-plan lookup + write shape; Code Examples: wizard create() call, Chip toggle idiom; Anti-Patterns: LRU query needs meals from ALL plans, not plan-scoped)
    - recipe-planner/src/pages/WeeklyPlans.tsx:743-778 (Chip filled/outlined tag-color idiom), :998-1018 (Add-Meal Autocomplete to reuse), :412-423 (handleSaveMeal create() shape)
    - recipe-planner/src/lib/planning/history.ts (computeLastPlannedDates, orderPoolByLRU, poolForSlot from 04-04)
    - recipe-planner/src/lib/planning/dates.ts (formatWeekOf from 04-08)
    - recipe-planner/src/lib/types.ts (WeekTemplate, TemplateSlot, PlannedMeal.template_slot), recipe-planner/src/lib/api.ts (weekTemplates/templateSlots/plannedMeals collections)
    - recipe-planner/src/constants/mealPlanning.ts (SLOT_COLORS fallback for untagged chips)
  </read_first>
  <action>
    Create `src/components/WeekWizard.tsx` accepting the target dated plan (and its id). On open, load: the single week_templates row + its template_slots (sorted by sort_order), all recipes, recipe_tags (build the recipeTags Map once, expand: "tag"), all weekly_plans + all planned_meals (unfiltered — LRU needs every plan; do NOT reuse the plan-scoped fetch). Compute lastPlanned via computeLastPlannedDates once.
    Render a one-page MUI Accordion (NOT a modal stepper). For each slot in sort_order:
    - Staples (prefill_from_last_week): position #1, pre-expanded; resolve the previous plan (max start_date < this plan's start_date) and pre-toggle its picks for this slot (via template_slot === slot.id, else tag-membership fallback); show a prominent "Confirm Staples" action that advances without re-tapping.
    - Other slots: pool = poolForSlot(slot, recipes, recipeTags) ordered by orderPoolByLRU(pool, lastPlanned); render tap-to-toggle recipe Chips (filled when picked = tag color / SLOT_COLORS fallback, outlined otherwise; sx fontSize 0.875rem, height 40, px 1.5 per UI-SPEC); dock a "+ Add other recipe" Autocomplete (reuse the Add-Meal Autocomplete) writing into this slot's meal_slot/day; a "Skip this slot"/"Next slot" text button is always available (never blocking).
    - Accordion visual states per UI-SPEC: active = white summary + 4px primary.main left border; collapsed = #fafafa summary + outlined grey "N of {count} picked" Chip, plus an accent CheckCircle icon (only) when N === count. Auto-advance to the next slot on reaching count.
    On each pick toggle-on, write a planned_meal via create(collections.plannedMeals, { weekly_plan: plan.id, recipe: recipe.id, meal_slot: slot.meal_slot, day: slot.day ?? null, quantity: 1, template_slot: slot.id }); on toggle-off delete that meal. Writes are direct/non-optimistic — on failure revert the Chip to un-toggled and show an inline `Alert severity="error"` with the UI-SPEC copy. Track created meal ids per (slot, recipe) so toggle-off can delete precisely.
    Empty states: if no week_templates row exists, render the "No week template yet." heading/body; if a slot's pool is empty, render "No recipes tagged for this slot yet." with the tag name, keeping the off-pool Autocomplete available.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run build && grep -c "orderPoolByLRU\|poolForSlot\|template_slot\|Accordion" src/components/WeekWizard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc) passes
    - `grep -c "orderPoolByLRU\|poolForSlot" recipe-planner/src/components/WeekWizard.tsx` returns >= 2 (uses the LRU service)
    - create(collections.plannedMeals, ...) call always sets meal_slot from the slot and day = slot.day ?? null and template_slot = slot.id
    - all-plans/all-meals fetch is unfiltered (not weekly_plan-scoped) for the LRU computation
    - failed write reverts the Chip and renders an inline error Alert; both empty states are implemented
  </acceptance_criteria>
  <done>The accordion wizard walks slots, pre-fills staples, LRU-orders pools, and writes correctly-shaped planned_meals with error handling and empty states.</done>
</task>

<task type="auto">
  <name>Task 2: "Fill Week" launch button in WeeklyPlans.tsx</name>
  <files>recipe-planner/src/pages/WeeklyPlans.tsx</files>
  <read_first>
    - recipe-planner/src/pages/WeeklyPlans.tsx (plan header ~792-794 + the existing "Add Meal" button placement to sit beside; the plan-refresh/reload path after meals change)
    - .planning/phases/04-weekly-planning-memory/04-UI-SPEC.md (Copywriting: "Fill Week" contained accent-green button, AutoAwesome/PlaylistAddCheck icon, beside Add Meal)
    - recipe-planner/src/components/WeekWizard.tsx (Task 1 props/open API)
  </read_first>
  <action>
    Add a "Fill Week" button (variant="contained", accent green, AutoAwesome or PlaylistAddCheck icon) on the WeeklyPlans plan header beside the existing "Add Meal" button, enabled only when a dated plan is selected. Wire it to open WeekWizard for the selected plan (dialog/overlay mount + open state). On wizard close, refresh the plan's meals so wizard-created meals appear in the week view exactly like manual ones. Keep the existing manual Add-Meal flow untouched.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run build && grep -c "WeekWizard\|Fill Week" src/pages/WeeklyPlans.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes
    - a "Fill Week" button opens WeekWizard for the selected plan; on close the week view reflects wizard-created meals
    - manual Add-Meal flow still present and unchanged
  </acceptance_criteria>
  <done>The wizard is reachable from the plan header and its results integrate with the existing week view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human walks the full wizard flow</name>
  <action>Human-verify gate: walk the full wizard on an empty dated week and confirm staples pre-fill, LRU order, tap-toggle, off-pool add, skippability, correct meal_slot/day, non-empty day-specific pull lists, and error revert per how-to-verify.</action>
  <what-built>The guided-fill WeekWizard (accordion, staples pre-fill, LRU pools, off-pool add, skippable) launched via "Fill Week".</what-built>
  <how-to-verify>
    On an empty dated week, click "Fill Week":
    1. Staples is slot #1, pre-expanded, with last week's picks already toggled; "Confirm Staples" advances in one tap.
    2. Each other slot lists its pool least-recently-planned-first (a never-planned recipe on top, last week's at the bottom); tapping Chips toggles picks; the collapsed header shows "N of {count} picked" and a green check when full; it auto-advances at count.
    3. "+ Add other recipe" pulls in an off-pool recipe into that slot.
    4. Skip a slot (0 picks) — it writes nothing and is not blocking.
    5. Finish → the week view shows the created meals in the right meal-slot rows; a day-specific slot produces a NON-empty pull list on Outputs (AC#9); go to Outputs and confirm pull lists are populated.
    6. Simulate a write failure (e.g. drop connection) — the Chip reverts and an inline error appears.
    Confirm meal_slot is always set and day is set for day-specific slots.
  </how-to-verify>
  <resume-signal>Type "approved" if staples pre-fill, LRU order, tap-to-toggle, off-pool add, skippability, correct meal_slot/day, non-empty pull lists, and error handling all work — or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client wizard → PocketBase | Wizard creates/deletes planned_meals records directly (non-optimistic). |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09a | Tampering | planned_meal write with null meal_slot | high | mitigate | meal_slot sourced from the slot (required field, seeded valid enum in 04-06); wizard never leaves it null (AC#9, Pitfall 4). |
| T-04-09b | Availability | silent pick-write failure | medium | mitigate | Non-optimistic write reverts the Chip and shows an inline error Alert (UI-SPEC error state); no false "picked" state. |
| T-04-09c | Tampering | day left null → empty pull lists | medium | mitigate | day threaded from slot.day; day-specific slots verified to yield non-empty pull lists (AC#9). |
| T-04-09d | Tampering | LRU computed from plan-scoped meals (wrong order) | low | mitigate | Unfiltered all-plans/all-meals fetch for the history computation (Anti-Pattern). |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase (MUI already present). |
</threat_model>

<verification>
`npm run build` passes; human-verify walks the full wizard: staples pre-fill, LRU order, tap-toggle, off-pool add, skip, correct meal_slot/day, non-empty day-specific pull lists, and error revert. (No jsdom component harness exists — wizard is manual/UAT per 04-VALIDATION.md.)
</verification>

<success_criteria>
WEEK-04 guided-fill wizard is complete: blank-start accordion walking template slots, staples pre-filled from last week with one-tap confirm, pools least-recently-planned-first, skippable, writing correctly-shaped planned_meals that behave identically to manual ones (WEEK-03 pools now usable end-to-end).
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-09-SUMMARY.md` when done.
</output>
