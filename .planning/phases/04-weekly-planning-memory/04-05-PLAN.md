---
phase: 04-weekly-planning-memory
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - recipe-planner/scripts/backfill-plan-dates.js
autonomous: false
requirements: [WEEK-01]
user_setup:
  - service: pocketbase
    why: "Backfill writes to live weekly_plans records on both instances (superuser creds from gitignored .env.local), and the final tighten-to-required is a manual admin-UI step."
    env_vars:
      - name: PB_SUPERUSER_EMAIL
        source: "recipe-planner/.env.local (existing, gitignored)"
      - name: PB_SUPERUSER_PASSWORD
        source: "recipe-planner/.env.local (existing, gitignored)"
must_haves:
  truths:
    - "A pure resolvePlanDate function parses 'Week of …' names to the correct Monday and otherwise assigns descending Mondays by created order, deterministic and unit-tested"
    - "The script supports --dry-run (report only, no writes) and takes a pb.backups.create() snapshot before any mutation, targeting PB_URL (default prod, override to :8091 for test)"
    - "After the backfill runs on both instances, every weekly_plans record has a non-null start_date"
    - "start_date is then tightened to REQUIRED in the admin UI on both instances and re-mirrored to pb_schema.json"
  artifacts:
    - recipe-planner/scripts/backfill-plan-dates.js
  key_links:
    - "resolvePlanDate is exported and import.meta-guarded so the Wave 0 test imports it without running main() (seed-usda.js pattern)"
---

<objective>
Backfill `start_date` on every existing `weekly_plans` record across both PocketBase instances, then tighten the field to required. The parse/assign logic is a pure, unit-tested resolver; the script follows the established dry-run + backup + PB_URL-override safety pattern.

Purpose: WEEK-01 — existing undated plans must get a monotonic best-effort date so history/LRU has signal, and the field becomes required going forward. Flips the Wave 0 `backfill-plan-dates.test.js` GREEN.
Output: `scripts/backfill-plan-dates.js`; both instances fully dated; field required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@recipe-planner/scripts/seed-usda.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backfill-plan-dates.js with pure resolvePlanDate + safety scaffolding</name>
  <files>recipe-planner/scripts/backfill-plan-dates.js</files>
  <behavior>
    - resolvePlanDate("Week of Jan 6, 2026", ...) → that Monday's ISO date
    - resolvePlanDate on a non-matching name → assigned Monday derived from created-order index + a base Monday, monotonic descending, id-asc tie-break
    - (all covered by scripts/backfill-plan-dates.test.js from 04-01)
  </behavior>
  <read_first>
    - recipe-planner/scripts/seed-usda.js:20-48,300-430 (PB_URL override, DRY_RUN flag, authWithPassword _superusers, pb.backups.create abort-on-failure, import.meta.url main-guard, exported pure functions)
    - recipe-planner/scripts/backfill-plan-dates.test.js (the resolver contract from 04-01)
    - recipe-planner/scripts/sync-to-test.js:1-11 (DB_URLS prod/test constants)
    - .planning/phase-docs/phase-4-week-memory.md item 1b + §3.6 (algorithm: parse "Week of …" else descending Mondays by created, tie-break id asc)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Runtime State Inventory backfill row; A2 regex-only; Security: backup-before-mutate)
  </read_first>
  <action>
    Write `scripts/backfill-plan-dates.js` modeled on seed-usda.js:
    - Export a PURE `resolvePlanDate(nameOrRow, index, baseMonday)` (no PocketBase/I-O) implementing the "Week of …" regex parse → Monday, else `baseMonday` shifted back `index` weeks; return an ISO date string; deterministic. Keep the exact signature the 04-01 test imports.
    - `main()` (guarded by `import.meta.url === file://${process.argv[1]}`): authenticate superuser, `getFullList` weekly_plans, sort by created asc (tie-break id asc), compute each record's resolved date via resolvePlanDate for records lacking start_date, print a per-row disposition report (matched-parse vs assigned-fallback). Support `--dry-run` (report only, zero writes) and take `pb.backups.create()` before any write (abort if it does not confirm). Respect `PB_URL` (default prod :8090; override to :8091 for test) exactly like seed-usda.js.
    - Do NOT tighten the field here (that is the human-action Task 3).
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- scripts/backfill-plan-dates.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- scripts/backfill-plan-dates.test.js` passes (both parse and fallback paths)
    - `grep -c "export function resolvePlanDate\|--dry-run\|backups.create\|PB_URL" recipe-planner/scripts/backfill-plan-dates.js` shows dry-run, backup, PB_URL override, and the exported resolver all present
  </acceptance_criteria>
  <done>Pure resolver GREEN; script has dry-run + backup + PB_URL safety and does not run on import.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human runs backfill against test then prod</name>
  <action>Human-run gate: execute the backfill script (dry-run first, then apply) against test :8091 then prod :8090 per the ordered steps in how-to-verify; supervised because it writes to live records.</action>
  <what-built>backfill-plan-dates.js (dry-run + backup + PB_URL override). It must now be RUN against test first, then prod, because superuser DB writes to live records are supervised per the Phase 1 precedent.</what-built>
  <how-to-verify>
    Run in this order and confirm each step:
    1. Dry-run TEST: `cd recipe-planner && PB_URL=http://192.168.50.95:8091 node scripts/backfill-plan-dates.js --dry-run` — review the per-row disposition report; confirm every currently-undated plan gets a sensible date.
    2. Apply TEST: rerun without `--dry-run` (takes a backup first); confirm it reports every weekly_plans record now has a non-null start_date.
    3. Dry-run PROD: `PB_URL=http://192.168.50.95:8090 node scripts/backfill-plan-dates.js --dry-run` — review.
    4. Apply PROD: rerun without `--dry-run`; confirm all dated.
    Then type "backfilled both" or report anomalies.
  </how-to-verify>
  <resume-signal>Type "backfilled both" once every weekly_plans record on prod AND test has a non-null start_date, or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Human tightens start_date to required on both instances</name>
  <action>Manual admin-UI gate: after backfill confirms every plan is dated, set weekly_plans.start_date to Required on both instances; Claude then re-mirrors the required flag into pb_schema.json.</action>
  <what-built>All plans are now dated. The start_date field must be tightened from nullable to REQUIRED on both instances and re-mirrored.</what-built>
  <how-to-verify>
    In the PocketBase admin UI on BOTH :8090 and :8091, edit weekly_plans.start_date and set it to Required. Save on both. Confirm the app still loads plans without error (open the Weekly Plans page). Then Claude re-mirrors the required flag into pb_schema.json (a follow-up auto edit). Type "required on both" when done.
  </how-to-verify>
  <resume-signal>Type "required on both" once start_date is required on prod AND test.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → PocketBase (both instances) | Superuser-authenticated writes to live weekly_plans records. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05a | Tampering / Repudiation | prod write without backup | high | mitigate | pb.backups.create() before any mutation (abort if unconfirmed); dry-run + test-instance rehearsal first (Security Domain). |
| T-04-05b | Tampering | wrong/duplicate assigned dates breaking monotonic order | medium | mitigate | Deterministic descending-Monday assignment with id-asc tie-break, unit-tested; dry-run report reviewed by human before apply. |
| T-04-05c | Information Disclosure | superuser creds | low | accept | Sourced from gitignored .env.local at runtime, never printed/committed. |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
Wave 0 backfill resolver test green; dry-run reports reviewed on both instances; post-apply every weekly_plans record non-null; field required on both; pb_schema.json re-mirrored.
</verification>

<success_criteria>
WEEK-01 backfill complete: every existing plan on prod + test has a non-null start_date, the field is required going forward, and the parse logic is unit-tested via a pure resolver.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-05-SUMMARY.md` when done.
</output>
