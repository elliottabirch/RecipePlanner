---
phase: 04-weekly-planning-memory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_schema.json
  - recipe-planner/src/lib/types.ts
  - recipe-planner/src/lib/api.ts
  - recipe-planner/scripts/sync-to-test.js
autonomous: false
requirements: [WEEK-01, WEEK-02, WEEK-03]
user_setup:
  - service: pocketbase
    why: "Schema changes are made manually via the PocketBase admin UI on both prod (:8090) and test (:8091) — there is no ORM/migration tool in this project (locked convention)."
    dashboard_config:
      - task: "Create weekly_plans.start_date (date, NULLABLE for now), weekly_plans.people_multiplier (number, default 1, min 0.1), week_templates + template_slots collections, and planned_meals.template_slot (relation, nullable) per the field spec in Task 1"
        location: "PocketBase admin UI, http://192.168.50.95:8090 AND http://192.168.50.95:8091 (both instances, identical)"
must_haves:
  truths:
    - "Both PocketBase instances expose weekly_plans.start_date (nullable) and weekly_plans.people_multiplier (default 1, min 0.1)"
    - "Both instances expose week_templates and template_slots collections with template_slots.meal_slot marked REQUIRED, day optional, pool_tags relation→tags (multi), sort_order, count, label, prefill_from_last_week"
    - "planned_meals has a nullable template_slot relation on both instances"
    - "pb_schema.json mirrors every new field/collection; api.ts collections map gains weekTemplates + templateSlots; types.ts gains the matching interfaces"
    - "sync-to-test.js COLLECTIONS_TOP_DOWN lists week_templates then template_slots BEFORE weekly_plans/planned_meals"
  artifacts:
    - pb_schema.json
    - recipe-planner/src/lib/types.ts
    - recipe-planner/src/lib/api.ts
    - recipe-planner/scripts/sync-to-test.js
  key_links:
    - "template_slots.meal_slot REQUIRED matches planned_meals.meal_slot REQUIRED so wizard writes never violate the required constraint (Pitfall 4)"
    - "sync-to-test insertion order satisfies template→slot AND slot→planned_meals.template_slot relation dependency (Pitfall 2)"
---

<objective>
Land the additive schema foundation for Phase 4: the two mandated `weekly_plans` fields, the two new template collections, and the optional `planned_meals.template_slot` link — created manually in both PocketBase instances, then mirrored into `pb_schema.json`, the `collections` map, TypeScript types, and the test-copier's ordering.

Purpose: Every other Phase 4 plan (multiplier plumbing, LRU history, wizard, backfill, seed) depends on these fields/types existing. `start_date` is created NULLABLE here; it is tightened to required in 04-05 after the backfill runs.
Output: Schema live on both DBs; code surface (types/api/sync) updated to match.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@.planning/phase-docs/phase-4-week-memory.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human creates Phase 4 schema in both PocketBase instances</name>
  <action>Manual admin-UI gate: the human creates the fields/collections in the field spec below on both instances; Claude cannot proceed to Task 2's mirror/type work until this is confirmed. See how-to-verify for the exact field spec.</action>
  <what-built>Nothing yet — this is the manual schema-creation gate. Claude cannot click through the PocketBase admin UI (locked convention: schema is human-created in the admin UI on both instances, no ORM push).</what-built>
  <how-to-verify>
    In the PocketBase admin UI, create the following on BOTH http://192.168.50.95:8090 (prod) AND http://192.168.50.95:8091 (test), identically:

    1. weekly_plans — add two fields:
       - `start_date`: type Date, OPTIONAL/nullable for now (backfill in 04-05 tightens it to required later).
       - `people_multiplier`: type Number, default value `1`, Min `0.1` (belt-and-suspenders against zero/negative — 04-RESEARCH Security V5).

    2. NEW collection `week_templates`:
       - `name`: Text, required.

    3. NEW collection `template_slots`:
       - `template`: Relation → week_templates, required, maxSelect 1, cascadeDelete off.
       - `label`: Text, required.
       - `count`: Number, required.
       - `meal_slot`: Select (breakfast, lunch, dinner, snack, micah), **REQUIRED** — must match planned_meals.meal_slot's enum + required flag (Pitfall 4).
       - `day`: Select (mon, tue, wed, thu, fri, sat, sun), OPTIONAL.
       - `pool_tags`: Relation → tags, maxSelect > 1 (multi), cascadeDelete off.
       - `sort_order`: Number.
       - `prefill_from_last_week`: Bool, default false.

    4. planned_meals — add one field:
       - `template_slot`: Relation → template_slots, OPTIONAL/nullable, maxSelect 1, cascadeDelete off.

    Confirm each collection/field saved without error on both ports. Then type "created on both" (or report which failed).
  </how-to-verify>
  <resume-signal>Type "created on both" once all fields/collections exist on prod AND test, or describe any field that failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify live schema, mirror pb_schema.json, extend types + api + sync order</name>
  <files>pb_schema.json, recipe-planner/src/lib/types.ts, recipe-planner/src/lib/api.ts, recipe-planner/scripts/sync-to-test.js</files>
  <read_first>
    - pb_schema.json (existing weekly_plans + planned_meals collection blocks — copy their exact JSON shape/field-schema style for the new fields/collections)
    - recipe-planner/src/lib/types.ts:131-149 (WeeklyPlan, MealSlot, Day, PlannedMeal — extend these)
    - recipe-planner/src/lib/api.ts:52-70 (collections map — add weekTemplates/templateSlots)
    - recipe-planner/scripts/sync-to-test.js:27-44 (COLLECTIONS_TOP_DOWN + DEFERRED_RELATION_FIELDS)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pitfall 2 exact insertion position; Pattern 5 PlannedMealExpanded already has weekly_plan expand)
  </read_first>
  <action>
    Read back the live schema from both instances (superuser auth via the gitignored recipe-planner/.env.local PB_SUPERUSER_* creds, same pattern as seed-usda.js) using the PocketBase collections API, to confirm the human-created fields/collections match the spec (names, types, required flags, meal_slot enum values). If anything diverges, stop and report before writing.
    Then mirror into `pb_schema.json`: add the two weekly_plans fields, the week_templates and template_slots collection definitions, and planned_meals.template_slot, matching the existing JSON field-schema style byte-for-byte in structure.
    Extend `src/lib/types.ts`: WeeklyPlan gains `start_date?: string` and `people_multiplier?: number`; PlannedMeal gains `template_slot?: string`; add `WeekTemplate extends BaseRecord { name: string }` and `TemplateSlot extends BaseRecord { template: string; label: string; count: number; meal_slot: MealSlot; day?: Day; pool_tags: string[]; sort_order?: number; prefill_from_last_week?: boolean }`. Add a `TemplateSlotExpanded` with `expand?.pool_tags` only if a consumer needs it (defer otherwise).
    Extend `src/lib/api.ts` collections map with `weekTemplates: "week_templates"` and `templateSlots: "template_slots"`.
    Extend `sync-to-test.js` COLLECTIONS_TOP_DOWN by inserting `"week_templates"` then `"template_slots"` immediately after `"step_to_product_edges"` and BEFORE `"weekly_plans"` (Pitfall 2 — this satisfies both template-before-slot and slot-before-planned_meals.template_slot). Do not add planned_meals to DEFERRED_RELATION_FIELDS (the earlier insertion makes it unnecessary).
  </action>
  <verify>
    <automated>cd recipe-planner && grep -c "weekTemplates\|templateSlots" src/lib/api.ts && grep -c "WeekTemplate\|TemplateSlot\|people_multiplier\|start_date" src/lib/types.ts && node -e "const c=require('fs').readFileSync('scripts/sync-to-test.js','utf8'); const i=c.indexOf('week_templates'), j=c.indexOf('template_slots'), w=c.indexOf('weekly_plans'); process.exit(i>0 && i<j && j<w ? 0 : 1)" && echo "sync order OK"</automated>
  </verify>
  <acceptance_criteria>
    - Live read-back from BOTH instances confirms all fields/collections exist with correct types and required flags (template_slots.meal_slot required, start_date nullable, people_multiplier default 1 min 0.1)
    - `pb_schema.json` contains week_templates, template_slots, planned_meals.template_slot, and the two weekly_plans fields
    - `grep -c "weekTemplates" recipe-planner/src/lib/api.ts` returns >= 1 and types.ts exports WeekTemplate + TemplateSlot
    - sync-to-test COLLECTIONS_TOP_DOWN lists week_templates before template_slots before weekly_plans (the node -e ordering check exits 0)
    - `npm run build` (tsc) passes with the new types
  </acceptance_criteria>
  <done>Both DBs, pb_schema.json, types.ts, api.ts, and sync-to-test.js all agree on the new schema surface; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → PocketBase (both instances) | Superuser-authenticated read-back of live schema crosses into the DB; write access is admin-UI only here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02a | Tampering | template_slots.meal_slot left optional | high | mitigate | Human-action spec explicitly marks meal_slot REQUIRED; Task 2 read-back verifies the required flag on both instances (Pitfall 4). |
| T-04-02b | Tampering | negative/zero people_multiplier | medium | mitigate | PocketBase field Min 0.1 set server-side; client guard added in 04-08 (Security V5). |
| T-04-02c | Information Disclosure | superuser creds | low | accept | Creds sourced at runtime from gitignored .env.local, never printed/committed (established Phase 1/3 pattern). |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase (04-RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
Read-back diff of live schema vs the field spec on both instances is clean; `npm run build` passes; sync-to-test ordering check exits 0.
</verification>

<success_criteria>
The two weekly_plans fields, week_templates, template_slots (meal_slot required), and planned_meals.template_slot exist identically on prod + test, are mirrored into pb_schema.json, typed in types.ts, mapped in api.ts, and copyable in dependency order by sync-to-test.js.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-02-SUMMARY.md` when done.
</output>