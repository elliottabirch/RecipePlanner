---
phase: 04-weekly-planning-memory
plan: 06
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - recipe-planner/scripts/seed-week-template.js
autonomous: false
requirements: [WEEK-03]
user_setup:
  - service: pocketbase
    why: "Seeds the household's single week_templates row + its template_slots on both instances (D-01: seed once, no in-app editor). Superuser creds from gitignored .env.local; a human runs it and confirms the pool tags exist."
    env_vars:
      - name: PB_SUPERUSER_EMAIL
        source: "recipe-planner/.env.local (existing, gitignored)"
      - name: PB_SUPERUSER_PASSWORD
        source: "recipe-planner/.env.local (existing, gitignored)"
must_haves:
  truths:
    - "One week_templates row exists on both instances with an ordered set of template_slots"
    - "Slot #1 is Staples with prefill_from_last_week=true and sort_order first; remaining slots (dinner/salad/etc.) carry meal_slot (required, never null), optional day, and pool_tags resolved from existing tags by name"
    - "The seed is idempotent (re-running does not duplicate the template/slots) and resolves pool_tags to real tag ids, failing loudly if a named tag is missing"
    - "Script supports --dry-run and backup-before-mutate, PB_URL override for test-first"
  artifacts:
    - recipe-planner/scripts/seed-week-template.js
  key_links:
    - "template_slots.meal_slot values match the planned_meals.meal_slot enum so wizard writes satisfy the required constraint (Pitfall 4)"
    - "pool_tags reference existing tags/recipe_tags — tagging a recipe with the slot's tag makes it pool-eligible with no other change (WEEK-03)"
---

<objective>
Seed the household's de-facto week template (one `week_templates` row + ordered `template_slots`) on both PocketBase instances via a one-off idempotent script (D-01 — no in-app editor this phase). Tagging a recipe with a slot's pool tag is the only action needed to make it eligible.

Purpose: WEEK-03 — the wizard needs a real template to walk. Staples must be slot #1 with copy-forward; other slots key to existing tags.
Output: `scripts/seed-week-template.js`; one template + slots live on prod + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@recipe-planner/scripts/seed-usda.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: idempotent seed-week-template.js</name>
  <files>recipe-planner/scripts/seed-week-template.js</files>
  <read_first>
    - recipe-planner/scripts/seed-usda.js:20-48,300-430 (PB_URL override, DRY_RUN, superuser auth, backup abort, idempotent upsert-by-key pattern, main-guard)
    - recipe-planner/scripts/sync-to-test.js:1-11 (DB_URLS)
    - .planning/phase-docs/phase-4-week-memory.md §3.2-3.3 (week_templates/template_slots field shapes; Staples prefill_from_last_week=true; meal_slot required; day optional)
    - .planning/phases/04-weekly-planning-memory/04-CONTEXT.md (D-01 seed-once; Claude's Discretion on slot set — Staples + dinner/salad keyed to existing tags)
    - pb_schema.json (tags collection — to resolve pool tag names → ids; confirm existing household tags before hardcoding names)
  </read_first>
  <action>
    Write `scripts/seed-week-template.js` (seed-usda.js safety pattern: PB_URL override, --dry-run, superuser auth, pb.backups.create before writes, main-guarded):
    - Define the template as a small in-file config: template name (e.g. "Standard week") and an ordered slot list. Slot #1 = Staples { label:"Staples", count, meal_slot, day:null, prefill_from_last_week:true, sort_order:0, pool_tags:[<staples tag name(s)>] }; then dinner/salad/etc. slots each with a required meal_slot from the planned_meals enum, optional day, sort_order incrementing, and pool_tags by tag NAME.
    - Resolve each pool_tag name to a real tag id by querying the tags collection; if a named tag does not exist, FAIL LOUDLY (print the missing tag, exit non-zero) rather than seeding a dangling/empty pool.
    - Idempotent: upsert the single template by name and its slots by (template, sort_order) so re-running does not duplicate. Print a dry-run disposition of what would be created/updated.
    - The exact slot set / tag names are Claude's Discretion (D-01) — inspect the live tags collection first and choose the household's real recurring shape; document the chosen slots in the SUMMARY.
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/seed-week-template.js && grep -c "prefill_from_last_week\|--dry-run\|backups.create\|PB_URL\|sort_order" scripts/seed-week-template.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/seed-week-template.js` passes (valid JS)
    - script defines a Staples slot with prefill_from_last_week=true at sort_order 0 and resolves pool_tags to real tag ids (fails loudly on a missing tag)
    - dry-run + backup + PB_URL override present
  </acceptance_criteria>
  <done>Idempotent, safe seed script exists; Staples-first slot ordering and tag resolution implemented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human runs the template seed against test then prod</name>
  <action>Human-run gate: execute the seed script (dry-run first, then apply) against test :8091 then prod :8090 per how-to-verify; the human confirms the chosen slot set + pool tags reflect the real household week before applying.</action>
  <what-built>seed-week-template.js. It must be RUN against test then prod to create the household template; the human confirms the chosen pool tags reflect the real week shape and that tagged recipes then appear in the right pools.</what-built>
  <how-to-verify>
    1. Dry-run TEST: `cd recipe-planner && PB_URL=http://192.168.50.95:8091 node scripts/seed-week-template.js --dry-run` — review the template + slot list and resolved tag ids; confirm the slot set matches the household's real week and no pool tag is missing.
    2. Apply TEST: rerun without `--dry-run`.
    3. Dry-run + Apply PROD: repeat with `PB_URL=http://192.168.50.95:8090`.
    4. In the app/admin: confirm a recipe carrying a slot's pool tag is eligible for that slot (spot-check against recipe_tags).
    Type "seeded both" or report which tag/slot needs adjustment.
  </how-to-verify>
  <resume-signal>Type "seeded both" once the template + slots exist on prod AND test with correct pool tags, or describe adjustments.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → PocketBase (both instances) | Superuser-authenticated writes creating template/slot records. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06a | Tampering | slot with null/invalid meal_slot | high | mitigate | Slot config uses only valid planned_meals meal_slot enum values; template_slots.meal_slot is required (04-02); wizard write would otherwise fail (Pitfall 4). |
| T-04-06b | Tampering | dangling pool_tags to non-existent tag | medium | mitigate | Script resolves tag names to ids and fails loudly on a missing tag rather than seeding an empty pool. |
| T-04-06c | Tampering / Repudiation | prod write without backup | high | mitigate | pb.backups.create() before writes; dry-run + test-first via PB_URL. |
| T-04-06d | Tampering | duplicate template on re-run | low | mitigate | Idempotent upsert by name / (template, sort_order). |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
`node --check` passes; dry-run reports reviewed on both instances; one template + ordered slots (Staples first, prefill on) live on prod + test; a tagged recipe is pool-eligible.
</verification>

<success_criteria>
WEEK-03 template exists on both instances via an idempotent seed; Staples is slot #1 with copy-forward; every slot's meal_slot is a valid required enum value and pool_tags reference real tags.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-06-SUMMARY.md` when done.
</output>
