---
phase: 04-weekly-planning-memory
plan: 07
type: execute
wave: 2
depends_on: [04-02, 04-03]
files_modified:
  - recipe-planner/src/pages/Outputs.tsx
autonomous: false
requirements: [WEEK-02]
must_haves:
  truths:
    - "Outputs derives selectedPlan by selectedPlanId and passes selectedPlan.people_multiplier ?? 1 into buildProductFlowGraph AND buildPullLists"
    - "Both useMemo dependency arrays include peopleMultiplier so switching plans / changing the multiplier re-derives outputs live (Pitfall 3)"
    - "A secondary-orange '×{value} servings' Chip renders adjacent to the plan selector ONLY when people_multiplier !== 1; at 1 the page is pixel-identical to today"
  artifacts:
    - recipe-planner/src/pages/Outputs.tsx
  key_links:
    - "Threading the plan's multiplier here is the point where WEEK-02 becomes visible end-to-end: shopping, prep, containers, and (per D-03) pull lists all scale together"
---

<objective>
Wire the live `people_multiplier` from the selected plan into the aggregation call sites on the Outputs page, fix the useMemo dependencies, and render the multiplier badge per the UI-SPEC (only when ≠ 1).

Purpose: WEEK-02 end-to-end — the pure scaling from 04-03 only takes effect once Outputs passes the real value. This is the "make scaling visible where it takes effect" step.
Output: Outputs.tsx threads peopleMultiplier + renders the ×N badge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@.planning/phases/04-weekly-planning-memory/04-UI-SPEC.md
@recipe-planner/src/pages/Outputs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread peopleMultiplier + useMemo deps + multiplier badge</name>
  <files>recipe-planner/src/pages/Outputs.tsx</files>
  <read_first>
    - recipe-planner/src/pages/Outputs.tsx (verified lines: plans state ~101; getAll(collections.weeklyPlans) ~212-213; buildProductFlowGraph useMemo ~399-402; buildPullLists useMemo ~472-475; selectedPlanId already in scope; where the plan selector renders)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 4 exact selectedPlan/peopleMultiplier derivation + both useMemo edits; Pitfall 3 dependency array)
    - .planning/phases/04-weekly-planning-memory/04-UI-SPEC.md (Outputs multiplier badge: secondary.main Chip size=small "×{value} servings", render only when !== 1, no reserved space at 1)
    - recipe-planner/src/lib/types.ts (WeeklyPlan.people_multiplier from 04-02)
  </read_first>
  <action>
    In Outputs.tsx add `const selectedPlan = useMemo(() => plans.find(p => p.id === selectedPlanId), [plans, selectedPlanId]);` and `const peopleMultiplier = selectedPlan?.people_multiplier ?? 1;` (both `plans` and `selectedPlanId` already in scope). Update the buildProductFlowGraph useMemo (~399) to pass `peopleMultiplier` as the 3rd arg and add it to the dependency array; likewise the buildPullLists useMemo (~472). Do not touch the downstream memos (shoppingList/overlaidShoppingList/groupedShoppingList/batchPrepSteps/storedItems/mealContainers) — they inherit correctness through productFlowGraph (Pitfall 3).
    Render the multiplier badge adjacent to the plan selector: `{peopleMultiplier !== 1 && <Chip size="small" label={`×${peopleMultiplier} servings`} sx={{ backgroundColor: "secondary.main", color: "white" }} />}`. At multiplier 1 render nothing (no placeholder/empty space) so an unscaled week is pixel-identical to today.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run build && grep -c "peopleMultiplier" src/pages/Outputs.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc) passes
    - `grep -c "peopleMultiplier" recipe-planner/src/pages/Outputs.tsx` returns >= 3 (derivation + two useMemo call sites; deps included)
    - buildProductFlowGraph and buildPullLists are each called with the 3rd peopleMultiplier arg and both useMemo dependency arrays include peopleMultiplier
    - the ×N badge is gated behind `peopleMultiplier !== 1`
  </acceptance_criteria>
  <done>Live multiplier flows into both aggregation entry points, memos re-run on change, and the badge appears only when scaled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies live scaling + multiplier badge</name>
  <action>Human-verify gate: exercise the running app to confirm the multiplier scales all four output families live and the ×N badge toggles on the ≠1 boundary per how-to-verify.</action>
  <what-built>Outputs.tsx now scales all outputs by the selected plan's people_multiplier and shows a ×N badge when ≠ 1.</what-built>
  <how-to-verify>
    In the running app, open Outputs for a plan and note shopping/prep/container/pull-list quantities. Set that plan's people_multiplier to 2 (via the New Plan / edit dialog once 04-08 lands, or directly in admin) and reopen Outputs: (1) shopping, batch-prep, container, and pull-list quantities all scale ×2 without a full page reload; (2) the "×2 servings" orange badge appears next to the plan selector; (3) set it back to 1 → badge disappears and quantities match the original. Confirm switching between two plans with different multipliers re-derives immediately.
  </how-to-verify>
  <resume-signal>Type "approved" if outputs scale live and the badge behaves, or describe what did not update.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PocketBase → client render | people_multiplier value read from the plan record drives all displayed quantities. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07a | Tampering | stale outputs after multiplier/plan change | medium | mitigate | peopleMultiplier added to both useMemo dependency arrays; human-verify confirms live re-derivation (Pitfall 3). |
| T-04-07b | Tampering | undefined multiplier → NaN quantities | low | mitigate | `?? 1` fallback for absent people_multiplier (WEEK-02 "absent ⇒ 1"). |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
`npm run build` passes; human-verify confirms ×2 scales all four output families live and the badge toggles on the ≠1 boundary.
</verification>

<success_criteria>
WEEK-02 is visible end-to-end on Outputs: the selected plan's multiplier scales shopping/prep/container/pull-list outputs live, and the ×N badge renders only when scaled.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-07-SUMMARY.md` when done.
</output>
