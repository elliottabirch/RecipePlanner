---
phase: 04-weekly-planning-memory
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - recipe-planner/src/lib/planning/history.ts
autonomous: true
requirements: [WEEK-03, WEEK-04]
must_haves:
  truths:
    - "computeLastPlannedDates maps each recipe id to the max start_date of any dated plan containing it, ignoring undated plans"
    - "orderPoolByLRU sorts never-planned recipes first, then ascending last-planned date, tie-broken by name asc then id asc — deterministic and order-independent (AC#6)"
    - "poolForSlot returns recipes whose recipe_tags intersect the slot's pool_tags (match-any) — WEEK-03 pool eligibility"
    - "All three are pure functions with no PocketBase import"
  artifacts:
    - recipe-planner/src/lib/planning/history.ts
  key_links:
    - "Consumed by WeekWizard (04-09) for pool ordering + staples pre-fill; string ISO-date comparison is chronologically correct without Date parsing (Pattern 5)"
---

<objective>
Implement the pure planning-history module: last-planned computation, deterministic LRU ordering (AC#6), and tag-pool resolution (WEEK-03). Flip the Wave 0 `history.test.ts` GREEN.

Purpose: Centralize the rotation-freshness and pool-eligibility logic in one unit-tested place the wizard consumes, keeping the "planned = cooked" rule (last-cooked = max start_date of a containing plan) in a single tested function.
Output: `src/lib/planning/history.ts` exporting computeLastPlannedDates, orderPoolByLRU, poolForSlot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure LRU + pool-resolution module</name>
  <files>recipe-planner/src/lib/planning/history.ts</files>
  <behavior>
    - computeLastPlannedDates: recipe in a plan dated 2026-06-01 and another dated 2026-06-15 → maps to 2026-06-15; a recipe only in an undated (null start_date) plan → absent from the map
    - orderPoolByLRU: never-planned before planned-last-week; planned-3-weeks-ago before planned-this-week; equal dates and never-planned pairs tie-break name asc then id asc, identical result on a reversed input array
    - poolForSlot: recipe tagged "tag-a" is returned for a slot with pool_tags ["tag-a"]; recipe with only "tag-x" is excluded; match-any across multiple pool_tags
  </behavior>
  <read_first>
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 5 full reference implementation of computeLastPlannedDates/orderPoolByLRU/tieBreak + ISO string-comparison note; Pattern 6 poolForSlot)
    - recipe-planner/src/lib/planning/history.test.ts (the contract authored in 04-01)
    - recipe-planner/src/lib/types.ts (WeeklyPlan with start_date, PlannedMeal, TemplateSlot, Recipe — added/confirmed in 04-02)
  </read_first>
  <action>
    Implement `src/lib/planning/history.ts` per RESEARCH Pattern 5/6, importing only types from ../types (no PocketBase):
    - `computeLastPlannedDates(plans: WeeklyPlan[], plannedMeals: PlannedMeal[]): Map<string,string>` — build planDateById from plans that have start_date; for each meal, record the max start_date per recipe via lexicographic string comparison.
    - `orderPoolByLRU<T extends {id:string; name:string}>(recipes: T[], lastPlanned: Map<string,string>): T[]` — copy-then-sort: both never-planned → tieBreak; one never-planned → it sorts first; else ascending date; equal date → tieBreak. Private `tieBreak` = name.localeCompare then id.localeCompare.
    - `poolForSlot(slot: TemplateSlot, recipes: Recipe[], recipeTags: Map<string,string[]>): Recipe[]` — filter recipes whose tag-id list intersects a Set(slot.pool_tags) via Array.some (match-any).
    Never mutate inputs (spread before sort). Keep it side-effect free.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/planning/history.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/lib/planning/history.test.ts` passes all LRU tie-break and poolForSlot cases
    - `grep -c "export function" recipe-planner/src/lib/planning/history.ts` returns >= 3
    - history.ts imports no PocketBase (pure) — verified by the tests running with no DB and `npm run build` passing
  </acceptance_criteria>
  <done>The three pure planning functions are implemented and the Wave 0 history spec is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none (pure functions) | Operates on already-fetched in-memory data; no external input crosses a boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04a | Tampering | non-deterministic ordering across renders | medium | mitigate | Total-order tie-break (name asc, id asc) verified by an order-independence test (AC#6). |
| T-04-04b | Tampering | undated plans polluting LRU signal | low | mitigate | Plans without start_date are excluded from planDateById (Pattern 5). |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
`npm run test -- src/lib/planning/history.test.ts` green; `npm run build` passes.
</verification>

<success_criteria>
Deterministic LRU ordering (AC#6) and match-any pool resolution (WEEK-03) are implemented as tested pure functions ready for the wizard.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-04-SUMMARY.md` when done.
</output>
