---
phase: 04-weekly-planning-memory
plan: 03
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - recipe-planner/src/lib/units.ts
  - recipe-planner/src/lib/aggregation.ts
  - recipe-planner/src/lib/aggregation/builders/product-builder.ts
  - recipe-planner/src/lib/aggregation/builders/step-builder.ts
  - recipe-planner/src/lib/aggregation/builders/flow-builder.ts
  - recipe-planner/src/lib/aggregation/utils/product-utils.ts
  - .planning/REQUIREMENTS.md
  - .planning/phase-docs/phase-4-week-memory.md
autonomous: true
requirements: [WEEK-02]
must_haves:
  truths:
    - "scaleQuantity(qty, factor, unitOrDiscrete) returns exact fractional for mass/volume and Math.ceil for each-dimension and the literal 'discrete' marker (D-04)"
    - "buildProductFlowGraph, processRecipeProducts, and processRecipeSteps each accept a peopleMultiplier (default 1) and multiply it into their one local mealCount derivation (D-02 three-site plumbing)"
    - "buildPullLists scales each item by node.quantity × (meal.quantity||1) × peopleMultiplier — a quantity>1 meal's pull-list output changes even at multiplier 1 (D-03 correctness fix)"
    - "flow-builder container-instance counts and product-builder instance counts ceil identically via scaleQuantity('discrete'), defeating the 2.0000000002 float-loop drift"
    - "A multiplier of 1 reproduces byte-identical continuous-product output (AC#8 no-regression)"
    - "REQUIREMENTS.md folds PULL-F1 into WEEK-02 and phase-doc AC#7 no longer excludes pull lists"
  artifacts:
    - recipe-planner/src/lib/units.ts
    - recipe-planner/src/lib/aggregation.ts
  key_links:
    - "peopleMultiplier defaults to 1 at every new parameter so existing 2-arg/3-arg callers (and the whole existing test suite) keep compiling and passing untouched until Outputs.tsx wires the real value in 04-07"
    - "flow-builder receives the already-multiplied mealCount as a parameter — it applies the D-04 ceil but must NOT re-apply peopleMultiplier (double-count hazard, Pattern 1)"
---

<objective>
Implement the people-multiplier scaling math and the D-04 rounding split as pure aggregation-layer changes, and fix `buildPullLists` to honor quantity × multiplier (D-03). Flip the Wave 0 `aggregation-multiplier.test.ts` and `units.test.ts` scaleQuantity specs GREEN. Reconcile the two docs that D-03 supersedes.

Purpose: This is the trustworthy-scaling core of WEEK-02. All new parameters default to 1 so the change is invisible to existing callers/tests until 04-07 threads the live plan value.
Output: scaleQuantity helper + multiplier threading at exactly the enumerated sites + D-03 pull-list fix + doc reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-weekly-planning-memory/04-RESEARCH.md
@recipe-planner/src/lib/aggregation.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scaleQuantity helper (D-04)</name>
  <files>recipe-planner/src/lib/units.ts</files>
  <behavior>
    - scaleQuantity(3, 0.5, "g") → 1.5 (continuous mass stays exact fractional)
    - scaleQuantity(3, 0.5, "cup") → 1.5 (continuous volume exact)
    - scaleQuantity(3, 0.5, "each") → 2 (discrete count ceils — never under-buy)
    - scaleQuantity(2, 1.5, "discrete") → 3 (explicit discrete marker ceils; used for container instances regardless of the product's own unit)
    - integer inputs with an integer-producing factor stay put (scaleQuantity(2, 1, "discrete") === 2), and a hair over an integer ceils up (documents the deliberate container ceil vs float-loop drift)
  </behavior>
  <read_first>
    - recipe-planner/src/lib/units.ts:20-108 (Dimension, Unit, UNIT_DIMENSIONS, getDimension — reuse getDimension as the classifier; add the new export alongside promoteUnit/chooseDisplayUnit)
    - recipe-planner/src/lib/units.test.ts (the scaleQuantity describe block authored in 04-01 — this is the contract)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 3 exact scaleQuantity signature + rationale)
  </read_first>
  <action>
    Add and export `scaleQuantity(qty: number, factor: number, unitOrForceDiscrete: Unit | "discrete"): number` to units.ts per RESEARCH Pattern 3: `isDiscrete` is true when the third arg is the literal "discrete" OR `getDimension(arg) === "count"`; return `Math.ceil(qty*factor)` when discrete, else the exact `qty*factor`. Document with a JSDoc that states the continuous-exact / discrete-ceil rule and the container-drift rationale by concept (do not paste any literal that a negative grep gates on).
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/units.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/lib/units.test.ts` passes including the scaleQuantity block
    - `grep -c "export function scaleQuantity" recipe-planner/src/lib/units.ts` returns 1
  </acceptance_criteria>
  <done>scaleQuantity is exported and the units scaleQuantity spec is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread peopleMultiplier + apply scaleQuantity at the enumerated sites</name>
  <files>recipe-planner/src/lib/aggregation.ts, recipe-planner/src/lib/aggregation/builders/product-builder.ts, recipe-planner/src/lib/aggregation/builders/step-builder.ts, recipe-planner/src/lib/aggregation/builders/flow-builder.ts, recipe-planner/src/lib/aggregation/utils/product-utils.ts</files>
  <behavior>
    - buildProductFlowGraph(meals, data, 2) doubles a continuous product total vs multiplier 1
    - buildProductFlowGraph(meals, data, 1) reproduces byte-identical output to the pre-change 2-arg call (AC#8)
    - a fractional multiplier on an each-dimension product ceils (3 eggs × 0.5 → 2), diverging from raw hand-multiply per the documented AC#7 exception
    - container-instance counts from product-builder and flow-builder agree exactly (same ceil)
  </behavior>
  <read_first>
    - recipe-planner/src/lib/aggregation.ts:169-205 (buildProductFlowGraph mealCount line 183, processRecipeProducts call 186, processRecipeSteps call 189, createProductToStepFlows/createStepToProductFlows mealCount args 193-204)
    - recipe-planner/src/lib/aggregation/builders/product-builder.ts:26-70,177-190 (buildAggregatedProduct: calculateProductQuantity call line 38, instances line 62; processRecipeProducts mealCount line 183)
    - recipe-planner/src/lib/aggregation/builders/step-builder.ts:24-96,193-205 (extractStepInputs/Outputs quantity lines 52,86; processRecipeSteps mealCount line 199)
    - recipe-planner/src/lib/aggregation/builders/flow-builder.ts:38,80 (instances = (node.quantity||1)*mealCount — consumer of the passed mealCount)
    - recipe-planner/src/lib/aggregation/utils/product-utils.ts:69-79 (calculateProductQuantity)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 1 threading + corrected line numbers; Pattern 3 three call-site categories; Anti-Patterns: do NOT re-multiply in flow-builder)
  </read_first>
  <action>
    Add a `peopleMultiplier: number = 1` parameter to `buildProductFlowGraph`, `processRecipeProducts`, and `processRecipeSteps`. In each of the three files change the single local mealCount derivation to `mealCount = (meal.quantity || 1) * peopleMultiplier` (aggregation.ts:183, product-builder.ts:183, step-builder.ts:199), and have buildProductFlowGraph forward its peopleMultiplier into the processRecipeProducts/processRecipeSteps calls.
    Apply the D-04 ceil via scaleQuantity at the three call-site categories (Pattern 3): (1) product total — replace `calculateProductQuantity(quantity, mealCount)` at product-builder.ts:38 with `scaleQuantity(quantity, mealCount, nodeUnit as Unit)` (nodeUnit already in scope); (2) container instances — replace `(node.quantity||1)*mealCount` at product-builder.ts:62, flow-builder.ts:38, and flow-builder.ts:80 with `scaleQuantity(node.quantity || 1, mealCount, "discrete")`; (3) step in/out — replace `(node.quantity||0)*mealCount` at step-builder.ts:52,86 with `scaleQuantity(node.quantity || 0, mealCount, node.unit as Unit)`.
    In flow-builder the incoming `mealCount` is ALREADY multiplied (passed from aggregation.ts) — apply only the ceil there; do NOT reference peopleMultiplier in flow-builder (double-count hazard). Import scaleQuantity (and Unit) from ../../units where needed. Leave calculateProductQuantity in product-utils.ts as-is unless the ceil requires touching it; if you keep calling it elsewhere it stays exact.
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/aggregation && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/lib/aggregation` passes including aggregation-multiplier.test.ts (multiplier scaling + AC#8 no-regression + each-ceil)
    - `grep -c "peopleMultiplier" recipe-planner/src/lib/aggregation.ts recipe-planner/src/lib/aggregation/builders/product-builder.ts recipe-planner/src/lib/aggregation/builders/step-builder.ts` returns >= 1 in each of the three files
    - flow-builder.ts uses scaleQuantity for its instance counts and does not reference peopleMultiplier (verified by the passing no-double-count test, not by a file-wide negative grep)
    - `npm run build` passes; the full pre-existing aggregation suite stays green
  </acceptance_criteria>
  <done>Multiplier reaches all three derivation sites, all three instance-count sites ceil identically, and AC#8 holds for continuous products.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildPullLists D-03 fix + doc reconciliation</name>
  <files>recipe-planner/src/lib/aggregation.ts, .planning/REQUIREMENTS.md, .planning/phase-docs/phase-4-week-memory.md</files>
  <behavior>
    - buildPullLists(meals, data, 1) on a quantity:2 meal with a continuous-unit JIT input emits DOUBLE the pre-fix quantity (the latent correctness fix)
    - buildPullLists with peopleMultiplier 1.5 on a quantity:2 meal scales ×3 (compounding)
    - continuous units stay exact; each-dimension pull quantities ceil (never under-pull an indivisible item)
  </behavior>
  <read_first>
    - recipe-planner/src/lib/aggregation.ts:102-161 (buildPullLists — the unscaled `quantity: node.quantity` at line 139 is the target)
    - recipe-planner/src/lib/aggregation/aggregation-multiplier.test.ts (the D-03 regression cases authored in 04-01)
    - .planning/REQUIREMENTS.md:39-42,63-77 (WEEK-02 line + Deferred PULL-F1 block)
    - .planning/phase-docs/phase-4-week-memory.md:119-122,161,167-175 (§4 item 5 pull-list exclusion, §6 AC#7, §7 Open Question)
    - .planning/phases/04-weekly-planning-memory/04-RESEARCH.md (Pattern 2 exact fix + regression requirement; Pitfall 5)
  </read_first>
  <action>
    Add `peopleMultiplier: number = 1` to `buildPullLists`. Change the item push (line ~139) to compute `quantity: scaleQuantity(node.quantity ?? 0, (meal.quantity || 1) * peopleMultiplier, node.unit as Unit)` — this honors per-meal quantity AND the multiplier, keeps continuous units exact (satisfying the ×2/×1.5/×3 regression assertions) and ceils each-dimension pulls per D-04's never-under-buy intent. Import scaleQuantity/Unit as needed.
    Reconcile docs (D-03 requires this): in REQUIREMENTS.md, remove PULL-F1 from the Deferred/Future block and note it is folded into WEEK-02; update WEEK-02's text to state pull lists are now included. In phase-doc phase-4-week-memory.md, revise §4 item 5's "deliberate exclusion", §6 AC#7, and §7's Open Question to reflect that pull lists now scale by quantity × multiplier (reference D-03).
  </action>
  <verify>
    <automated>cd recipe-planner && npm run test -- src/lib/aggregation/aggregation-multiplier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/lib/aggregation/aggregation-multiplier.test.ts` passes including the D-03 doubling regression AND the compounding ×3 case
    - `grep -c "peopleMultiplier" recipe-planner/src/lib/aggregation.ts` shows buildPullLists now takes the param
    - REQUIREMENTS.md no longer lists PULL-F1 as excluded/deferred and WEEK-02 mentions pull lists; phase-doc AC#7 no longer excludes pull lists
  </acceptance_criteria>
  <done>Pull lists scale by quantity × multiplier, the regression is covered by a dedicated test, and the two docs match D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none (pure functions) | All changes are in-process pure aggregation math; no external input or network. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03a | Tampering | double-counted multiplier in flow-builder | high | mitigate | flow-builder applies only the ceil, never peopleMultiplier; enforced by the no-double-count test (Pattern 1 anti-pattern). |
| T-04-03b | Tampering | silent pull-list regression absorbed as no-op | medium | mitigate | Dedicated quantity>1 doubling regression test separate from the multiplier=1 no-op case (Pitfall 5). |
| T-04-03c | Tampering | float-loop container drift (2.0000000002) | medium | mitigate | Explicit Math.ceil via scaleQuantity('discrete') before any instance loop, at all three instance sites. |
| T-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
`npm run test` full suite green (existing + new multiplier/scaleQuantity specs); `npm run build` passes. Multiplier=1 reproduces continuous output byte-for-byte; each-dimension items follow the documented ceil exception.
</verification>

<success_criteria>
WEEK-02's scaling math is trustworthy: multiplier threads to all three derivation sites, D-04 rounding is centralized in one helper used at every enumerated site, D-03 pull-list correctness is fixed and regression-tested, and the superseded docs are reconciled.
</success_criteria>

<output>
Create `.planning/phases/04-weekly-planning-memory/04-03-SUMMARY.md` when done.
</output>
