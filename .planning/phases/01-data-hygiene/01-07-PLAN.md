---
phase: 01-data-hygiene
plan: 07
type: execute
wave: 4
depends_on: ["01-02", "01-05", "01-06"]
files_modified:
  - pb_schema_updated.json
autonomous: false
requirements: [DATA-04, DATA-05]
user_setup:
  - service: pocketbase
    why: "Superuser access is required to add schema fields via Admin UI and to run backup/merge scripts against the test instance during rehearsal"
    env_vars:
      - name: PB_SUPERUSER_EMAIL
        source: "PocketBase test instance (:8091) superuser account"
      - name: PB_SUPERUSER_PASSWORD
        source: "PocketBase test instance (:8091) superuser account"
    dashboard_config:
      - task: "Add products.canonical_unit (select, nullable) and products.dimension (select volume/mass/count, nullable) fields"
        location: "PB Admin UI on prod :8090 AND test :8091 -> products collection -> Fields"
must_haves:
  truths:
    - "products.canonical_unit and products.dimension select fields exist on BOTH prod :8090 and test :8091"
    - "The full migration flow (find-duplicates -> merge -> normalize -> backfill) has been rehearsed end-to-end against a fresh test copy of prod with a clean unresolved report reviewed by the human"
  artifacts:
    - path: pb_schema_updated.json
      provides: "re-exported schema including the new products fields"
  key_links:
    - from: recipe-planner/scripts/normalize-node-units.js
      to: PocketBase test :8091
      via: "PB_URL points scripts at the test instance for rehearsal"
      pattern: "PB_URL"
---

<objective>
Add the two new `products` schema fields via the PB Admin UI on both instances, then rehearse the entire irreversible migration flow against a fresh test copy of prod (D-06 mandatory rehearsal). Nothing here touches prod data — this plan de-risks the one-shot prod run in Plan 08.

Purpose: Schema scaffolding for DATA-04/DATA-05 backfill + the D-06 rehearsal safety net. PB schema is manual (no migrations dir): fields go into Admin UI on prod :8090 AND test :8091, then `pb_schema_updated.json` is re-exported.
Output: new schema fields on both DBs, re-exported schema, a reviewed clean rehearsal on test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add canonical_unit + dimension fields via PB Admin (prod + test) and re-export schema</name>
  <action>Human adds the two nullable select fields via the PB Admin UI on both instances and re-exports pb_schema_updated.json — a manual operation with no CLI equivalent (see how-to-verify for exact steps).</action>
  <what-built>Nothing automatable — PocketBase schema in this repo is managed through the Admin UI (no `pb_migrations/` directory; `sync-to-test.js` copies records, not schema), so field creation is a manual two-instance operation.</what-built>
  <how-to-verify>
    1. In PB Admin UI on prod (http://192.168.50.95:8090/_/) open the `products` collection -> Fields.
    2. Add field `canonical_unit`, type `select`, single value, NULLABLE (not required), options = the units.ts enum members (tsp, tbsp, fl_oz, cup, pint, qt, gal, ml, l, g, kg, oz, lb, each).
    3. Add field `dimension`, type `select`, single value, NULLABLE, options = volume, mass, count.
    4. Repeat the IDENTICAL edits in the PB Admin UI on test (http://192.168.50.95:8091/_/), same field names, types, options, order.
    5. Re-export the schema from the prod instance and overwrite `pb_schema_updated.json` at the repo root (commit it in this plan).
    6. Confirm `grep -c "canonical_unit" pb_schema_updated.json` and `grep -c "dimension" pb_schema_updated.json` are both > 0.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Common Pitfalls #3" (identical manual edits on two instances; symptom if applied to only one)
    - recipe-planner/src/lib/units.ts (canonical enum members that must match the select options)
  </read_first>
  <acceptance_criteria>
    - `products.canonical_unit` and `products.dimension` select fields exist on BOTH :8090 and :8091 with identical options.
    - `pb_schema_updated.json` re-exported and contains both field names.
  </acceptance_criteria>
  <resume-signal>Type "fields added on both instances and schema re-exported" or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rehearse the full migration flow against a fresh test copy</name>
  <action>Executor runs the full migration script sequence against the TEST instance (PB_URL=:8091) and the human reviews each output before signing off (see how-to-verify for the exact ordered commands).</action>
  <what-built>The migration scripts (find-duplicates, merge-products, normalize-node-units, backfill-units) from Plans 02 and 05, plus the aggregation/container-overload code from Plans 03/06, are all in place. This task drives the complete D-06 rehearsal on the TEST instance only.</what-built>
  <how-to-verify>
    Run, in order, from `recipe-planner/` (the executor runs the node commands; the human reviews outputs and confirms):
    1. `node scripts/sync-to-test.js` — fresh copy of prod records into test :8091.
    2. `PB_URL=http://192.168.50.95:8091 node scripts/find-duplicates.js` — produces the MD report + JSON decisions skeleton against test data. Review the report; edit the JSON so a couple of real same-`type` dupes are `confirmed:true` with a `survivorId`. Confirm NO transient/stored cross-type pair appears in the skeleton.
    3. `PB_URL=http://192.168.50.95:8091 node scripts/merge-products.js <decisions.json>` — with test superuser creds in env; confirm it backs up, pre-flight-validates, repoints all four collections, verifies zero orphans, then deletes. Confirm zero-orphan message.
    4. `PB_URL=http://192.168.50.95:8091 node scripts/normalize-node-units.js --apply` — confirm the unresolved report lists only the expected junk (by, chile, medium, large, 28oz cans) and that stored-node container strings were cleared.
    5. `PB_URL=http://192.168.50.95:8091 node scripts/backfill-units.js --apply` — confirm the summary of set vs. left-null canonical_units looks sane.
    6. Optionally run the linter against test (`PB_URL=... node scripts/lint.js`) and confirm remaining null canonical_units surface as findings, not silent gaps.
  </how-to-verify>
  <read_first>
    - recipe-planner/scripts/sync-to-test.js
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-05, D-06, D-08
  </read_first>
  <acceptance_criteria>
    - The full sequence runs against test without unhandled errors.
    - merge-products prints a completed backup, zero-orphan verification, and a delete count.
    - normalize-node-units's unresolved report contains only the expected junk values and cleared stored-node container strings.
    - The human confirms the rehearsal output is correct before Plan 08 touches prod.
  </acceptance_criteria>
  <resume-signal>Type "rehearsal clean — proceed to prod" or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual Admin edit -> two independent DBs | schema drift if fields are applied to only one instance |
| rehearsal scripts -> test instance | rehearsal must target test, never prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-DRIFT | Tampering | schema applied to one instance only | high | mitigate | Task 1 requires identical edits on :8090 and :8091 and a re-export; Pitfall #3 symptom check |
| T-07-WRONGDB | Tampering | rehearsal accidentally hitting prod | high | mitigate | Scripts default PB_URL to prod but rehearsal explicitly sets PB_URL to :8091; human verifies target before --apply |
</threat_model>

<verification>
- Both new fields present on both instances and in pb_schema_updated.json.
- Full rehearsal completes on test with a clean, reviewed unresolved report.
</verification>

<success_criteria>
Schema fields exist on both DBs and the entire irreversible flow has been proven end-to-end on test before any prod mutation.
</success_criteria>

## Artifacts This Plan Produces

- PB schema fields (both :8090 and :8091): `products.canonical_unit` (select, nullable, enum options), `products.dimension` (select, nullable, volume/mass/count)
- Modified: `pb_schema_updated.json` (re-exported with the two new fields)
- Process artifact: a completed, human-reviewed rehearsal of the full migration flow against the test instance

<output>
Create `.planning/phases/01-data-hygiene/01-07-SUMMARY.md` when done.
</output>
