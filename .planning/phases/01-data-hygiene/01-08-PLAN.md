---
phase: 01-data-hygiene
plan: 08
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - pb_schema_updated.json
autonomous: false
requirements: [DATA-04, DATA-05]
user_setup:
  - service: pocketbase
    why: "Superuser access is required to snapshot prod (pb.backups.create) and run the merge against the prod instance"
    env_vars:
      - name: PB_SUPERUSER_EMAIL
        source: "PocketBase PROD instance (:8090) superuser account"
      - name: PB_SUPERUSER_PASSWORD
        source: "PocketBase PROD instance (:8090) superuser account"
    dashboard_config:
      - task: "Add the case-insensitive unique index on products after dedup succeeds"
        location: "PB Admin UI on prod :8090 AND test :8091 -> products collection -> Indexes"
must_haves:
  truths:
    - "The human reviews and confirms the prod merge-decisions JSON before any prod mutation"
    - "Prod is backed up, deduped, node-units normalized, and canonical_units backfilled, with zero orphaned relations"
    - "A case-insensitive unique index on (name, type) exists so PB rejects a same-type case-variant duplicate name, while legitimate transient/stored same-name pairs still work (D-12)"
    - "pb_schema_updated.json is re-exported and includes meal_variant_overrides and the new index"
  artifacts:
    - path: pb_schema_updated.json
      provides: "final re-exported schema with index + meal_variant_overrides"
  key_links:
    - from: recipe-planner/scripts/merge-products.js
      to: PocketBase prod :8090
      via: "confirmed merge-decisions JSON drives the destructive repoint+delete"
      pattern: "confirmed"
---

<objective>
Run the one-shot, irreversible, human-reviewed data migration against PROD, then add the case-insensitive unique name index (D-12: scoped to `(name, type)`). This is the capstone — it runs only after all phase code (aggregation fix, container split, editor enum, linter) is landed and the flow was proven on test in Plan 07.

Purpose: Completes DATA-04 (dedup + unique index) and DATA-05 (node-unit normalization) against live prod. Implements D-05 (human confirms merge map), D-06 (backup + orphan checks), D-12 (index on (name, type), not bare name).
Output: cleaned prod data, unique index on both DBs, final re-exported schema.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Human review + confirm the prod merge-decisions JSON</name>
  <action>Executor regenerates the prod dupe report; human reviews it and edits the JSON decisions file (survivorId + confirmed:true) before any prod mutation. This gate is never auto-approvable.</action>
  <decision>Which product clusters get merged, and which survivor each dupe folds into.</decision>
  <context>
    The executor runs `node scripts/find-duplicates.js` against PROD (default PB_URL) to regenerate the current MD report + JSON decisions skeleton from live prod data. The merge is irreversible once dupes are deleted. Per D-05, the JSON decisions file is the ONLY input to merge-products.js and is the durable audit trail. Per D-12/Pitfall #1, the transient/stored same-name pairs (`onion (red) sliced`, `cucumber sliced`) are legitimate and must NOT be merged — confirm they are absent from the skeleton.
  </context>
  <options>
    <option id="confirm-subset">
      <name>Confirm a reviewed subset</name>
      <pros>Only human-verified same-type dupes are merged; cross-type pairs preserved</pros>
      <cons>Requires careful per-cluster review</cons>
    </option>
    <option id="abort">
      <name>Abort / defer</name>
      <pros>No irreversible change if the report looks wrong</pros>
      <cons>Dedup and the unique index remain blocked</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-05, D-12
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Common Pitfalls #1"
  </read_first>
  <resume-signal>Provide the edited decisions JSON path with confirmed:true entries, or type "abort".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the prod migration (backup -> merge -> normalize -> backfill) with orphan checks</name>
  <files>recipe-planner/scripts/merge-products.js</files>
  <read_first>
    - recipe-planner/scripts/merge-products.js, normalize-node-units.js, backfill-units.js (behavior + flags)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-06, D-07, D-08
  </read_first>
  <action>
    With PROD superuser creds in env (`PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD`) and default `PB_URL` (prod :8090), run in this exact order from `recipe-planner/`: (1) `node scripts/merge-products.js <confirmed-decisions.json>` — it takes its own pre-flight backup via `pb.backups.create`, validates every ID, repoints the four D-07 collections, verifies zero orphans, then deletes; ABORT the plan if it reports any failure. (2) `node scripts/normalize-node-units.js --apply` — normalize node units and clear stored-node container strings; capture the unresolved report. (3) `node scripts/backfill-units.js --apply` — set canonical_unit/dimension where unambiguous, leaving the rest null. Do NOT proceed to the index step (Task 3) unless merge reported zero orphans and a completed backup. Save the unresolved-units report and the merge log into the SUMMARY.
  </action>
  <verify>
    <automated>cd recipe-planner && PB_URL=http://192.168.50.95:8090 node scripts/find-duplicates.js >/dev/null 2>&1 && echo "post-merge dupe report regenerated — inspect for zero same-type exact dupes"</automated>
  </verify>
  <acceptance_criteria>
    - merge-products.js printed a completed backup, zero-orphan verification, and a delete count; the plan did not proceed on any failure.
    - Re-running find-duplicates against prod reports zero same-`type` exact case-insensitive dupes.
    - normalize-node-units's unresolved report is captured (only expected junk remains); backfill summary captured.
  </acceptance_criteria>
  <done>Prod is deduped, node-units normalized, canonical_units backfilled, with zero orphaned relations and a pre-run backup.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Add the (name, type) unique index and re-export schema</name>
  <action>Human adds the unique (name COLLATE NOCASE, type) index via PB Admin on both instances, verifies duplicate rejection, and re-exports pb_schema_updated.json — manual, no CLI equivalent (see how-to-verify).</action>
  <what-built>Dedup succeeded in Task 2, so the unique index will now create without collision. Index creation is a manual PB Admin operation on both instances (no migrations dir).</what-built>
  <how-to-verify>
    1. In PB Admin UI on prod (:8090) -> `products` collection -> Indexes, add a UNIQUE index scoped to `(name COLLATE NOCASE, type)` per D-12 — NOT bare `name` (a bare-name index would reject the legitimate transient/stored same-name pairs). Use the raw index definition: `CREATE UNIQUE INDEX idx_products_name_type_ci ON products (name COLLATE NOCASE, type)`.
    2. Apply the IDENTICAL index on test (:8091).
    3. Verify enforcement: attempt to create a product named "Olive Oil" when a same-`type` "olive oil" exists — PB must reject it with a validation error. Confirm a transient/stored same-name pair can still coexist.
    4. Re-export the schema from prod and overwrite `pb_schema_updated.json`; confirm it now includes both the index and the `meal_variant_overrides` collection (previously omitted / stale).
    5. Confirm `grep -c "idx_products_name_type_ci" pb_schema_updated.json` > 0 and `grep -c "meal_variant_overrides" pb_schema_updated.json` > 0.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Pattern 2" (COLLATE NOCASE index) + "Common Pitfalls #1" (why (name, type) not bare name)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-12
  </read_first>
  <acceptance_criteria>
    - A unique index on `(name COLLATE NOCASE, type)` exists on BOTH :8090 and :8091.
    - PB rejects a same-type case-variant duplicate name; a transient/stored same-name pair still coexists.
    - `pb_schema_updated.json` re-exported, includes the index and `meal_variant_overrides`.
  </acceptance_criteria>
  <resume-signal>Type "index added on both instances, dup rejected, schema re-exported" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> PocketBase prod | irreversible destructive migration of live production data |
| direct Admin edit -> products.name | uniqueness must hold even against direct API/Admin writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-LOSS | Repudiation | irreversible prod delete | high | mitigate | Backup completes before mutation; human confirms merge map (Task 1); plan aborts on any orphan/failure |
| T-08-INDEX | Tampering | bare-name index rejecting legitimate pairs | high | mitigate | Index scoped to (name COLLATE NOCASE, type) per D-12, preserving transient/stored same-name pairs |
| T-08-BYPASS | Tampering | direct Admin edit bypassing app validation | medium | mitigate | Native SQLite unique index enforces uniqueness at the DB layer, not just app-side |
</threat_model>

<verification>
- Post-merge find-duplicates reports zero same-type exact dupes.
- Unique (name, type) index exists on both DBs; same-type case-variant create is rejected.
- pb_schema_updated.json includes the index and meal_variant_overrides.
</verification>

<success_criteria>
Prod product data is deduped and unit-normalized with zero orphans and a backup; a case-insensitive (name, type) unique index prevents future same-type duplicates while preserving legitimate transient/stored pairs; the schema export is current.
</success_criteria>

## Artifacts This Plan Produces

- PB index (both :8090 and :8091): `idx_products_name_type_ci` — `UNIQUE INDEX ON products (name COLLATE NOCASE, type)` (D-12)
- Cleaned prod data: deduped products, normalized `recipe_product_nodes.unit` values, backfilled `canonical_unit`/`dimension`, zero orphaned relations, pre-run backup snapshot
- Modified: `pb_schema_updated.json` (final re-export including the index and the previously-omitted `meal_variant_overrides` collection)
- Process artifacts: confirmed merge-decisions JSON (audit trail), unresolved-units report, merge log

<output>
Create `.planning/phases/01-data-hygiene/01-08-SUMMARY.md` when done.
</output>
