---
phase: 01-data-hygiene
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - recipe-planner/src/lib/linter/index.ts
  - recipe-planner/src/lib/linter/rules/cross-dimension.ts
  - recipe-planner/src/lib/linter/rules/prep-words.ts
  - recipe-planner/src/lib/linter/rules/missing-store-section.ts
  - recipe-planner/src/lib/linter/rules/missing-canonical-unit.ts
  - recipe-planner/src/lib/linter/linter.test.ts
  - recipe-planner/src/pages/registries/Products.tsx
  - recipe-planner/scripts/lint.js
autonomous: true
requirements: [DATA-06]
must_haves:
  truths:
    - "Linter flags: cross-dimension unit mismatch, prep-words in raw product names, missing store/section (Safeway-only section rule), missing canonical_unit"
    - "A known-clean product produces zero findings"
    - "Rules are pure LintFinding[] functions surfaced two ways: a Lint button+panel on Products.tsx AND a headless scripts/lint.js (D-03)"
  artifacts:
    - path: recipe-planner/src/lib/linter/index.ts
      provides: "LintFinding type + rule aggregator export"
      exports: ["LintFinding", "runLint"]
    - path: recipe-planner/src/lib/linter/rules/missing-store-section.ts
      provides: "SECTION_REQUIRED_STORES = {safeway} rule (D-04)"
    - path: recipe-planner/scripts/lint.js
      provides: "headless linter runner"
  key_links:
    - from: recipe-planner/src/pages/registries/Products.tsx
      to: recipe-planner/src/lib/linter/index.ts
      via: "Lint button handler calls runLint against loaded+expanded products"
      pattern: "runLint|LintFinding"
    - from: recipe-planner/src/lib/linter/rules/cross-dimension.ts
      to: recipe-planner/src/lib/units.ts
      via: "canConvert/getDimension imported for the cross-dimension rule"
      pattern: "canConvert"
---

<objective>
Build recipe linter v1 (DATA-06): four pure `LintFinding[]` rules surfaced on-demand two ways — a "Lint" button + findings panel on the Products registry page and a headless `scripts/lint.js` (D-03). Rules: cross-dimension mismatch, prep-words in raw names, missing store/section (Safeway-only section per D-04), missing canonical_unit (surfaces D-08 backfill gaps).

Purpose: DATA-06. Implements D-03 (dual surface, no /lint route), D-04 (SECTION_REQUIRED_STORES = safeway), and gives D-08 a surface for null canonical_unit.
Output: `src/lib/linter/` module, Products.tsx panel, `scripts/lint.js`, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@.planning/phases/01-data-hygiene/01-PATTERNS.md
@recipe-planner/src/lib/units.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Linter core — LintFinding type + 4 pure rules + tests</name>
  <files>recipe-planner/src/lib/linter/index.ts, recipe-planner/src/lib/linter/rules/cross-dimension.ts, recipe-planner/src/lib/linter/rules/prep-words.ts, recipe-planner/src/lib/linter/rules/missing-store-section.ts, recipe-planner/src/lib/linter/rules/missing-canonical-unit.ts, recipe-planner/src/lib/linter/linter.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/aggregation/utils/product-utils.ts (pure-function module shape to mirror)
    - recipe-planner/src/pages/registries/Products.tsx (ProductExpanded interface ~lines 42-48 — the expand shape store/section/container_type)
    - recipe-planner/src/lib/units.ts (canConvert, getDimension, normalizeUnit for the cross-dimension rule)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Pattern 3" (missing-store-section template) and "Common Pitfalls #2" (node units to normalize before the cross-dimension check)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-03, D-04, D-08
    - .claude/skills/recipe-import/SKILL.md lines ~60-90 (store/section conventions + prep-word list source)
  </read_first>
  <behavior>
    - cross-dimension rule: a product whose node units (after normalizeUnit) span more than one dimension, or are not convertible to its canonical_unit, produces one finding; a product with consistent-dimension units produces none.
    - prep-words rule: a `raw` product whose name contains a controlled prep verb (sliced, diced, minced, chopped, grated, shredded, or a "(raw)" suffix) produces a finding; a clean raw name produces none.
    - missing-store-section rule: a non-pantry, non-transient product with no store -> finding; a product whose store is in SECTION_REQUIRED_STORES ({"safeway"}) with no section -> finding; a Costco/Trader Joes/online product with no section -> NO finding.
    - missing-canonical-unit rule: a non-pantry product with null canonical_unit -> finding.
    - A fully-clean product (store+section set, canonical_unit set, consistent units, clean name) produces zero findings across all rules.
  </behavior>
  <action>
    Create `src/lib/linter/index.ts` exporting the `LintFinding` type (`{ severity: "error" | "warning"; rule: string; message: string; recipeId?: string; productId?: string; nodeId?: string }`) and a `runLint(products, ...)` aggregator that runs all four rules and concatenates their `LintFinding[]`. Define/relocate the `ProductExpanded` input type in `linter/index.ts` (import `Product` from `../types`; do NOT edit `types.ts` in this plan to avoid file contention) so both the linter and Products.tsx can import it. Create the four rule files as pure exported functions returning `LintFinding[]`:
    - `rules/missing-store-section.ts`: copy the 01-RESEARCH.md Pattern 3 template verbatim; `SECTION_REQUIRED_STORES = new Set(["safeway"])` (D-04); section is a finding ONLY when the lowercased store name is in that set.
    - `rules/prep-words.ts`: a controlled prep-verb list (sourced from .claude/skills/recipe-import/SKILL.md), applied to `raw`-type product names.
    - `rules/cross-dimension.ts`: use `normalizeUnit` then `getDimension`/`canConvert` from units.ts to detect a product aggregated across non-convertible units / units not convertible to canonical_unit.
    - `rules/missing-canonical-unit.ts`: non-pantry product with null `canonical_unit`.
    Create `linter.test.ts`: one test per rule (flag case + clean case) plus one "fully-clean product yields no findings" test.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/linter/linter.test.ts && npx tsc -b --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `linter.test.ts` green with a flag+clean pair for each of the 4 rules and a "clean product -> zero findings" test.
    - `SECTION_REQUIRED_STORES` contains only `"safeway"`; a Costco/TJ/online product missing a section yields no finding (asserted).
    - cross-dimension rule imports from `units.ts` (grep confirms `canConvert`).
    - `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Four pure linter rules with a shared LintFinding type and aggregator; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the linter — Products.tsx button+panel and scripts/lint.js</name>
  <files>recipe-planner/src/pages/registries/Products.tsx, recipe-planner/scripts/lint.js</files>
  <read_first>
    - recipe-planner/src/pages/registries/Products.tsx (loadItems ~lines 100-115 already expands store,section,container_type; existing "Add Product" button ~lines 233-239; existing Dialog/Alert patterns ~lines 272-276, 420-475)
    - recipe-planner/scripts/find-duplicates.js (headless script scaffold: connect, getFullList, report)
    - recipe-planner/src/lib/linter/index.ts (runLint + LintFinding from Task 1)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-03 (surface), "Claude's Discretion" (panel UX: keep it simple, findings link to the offending product)
  </read_first>
  <action>
    In `Products.tsx`, add a `runLint` handler and `const [findings, setFindings] = useState<LintFinding[]>([])`, following the existing `loadItems` try/catch/finally shape; it calls `runLint(items)` against the already-expanded product list. Add a "Lint" button copying the existing "Add Product" `<Button variant="contained" startIcon={...} onClick={...}>` shape into the same header row, and render findings as a simple panel (a `<Collapse>`/inline `<Alert severity="error">` list or a `<Dialog>` — implementer's discretion per CONTEXT.md "keep it simple"), each finding linking to the offending product. Create `recipe-planner/scripts/lint.js` following the `find-duplicates.js` scaffold: connect to prod PB, `getFullList` products with `expand: "store,section,container_type"`, call the SAME rule functions from `src/lib/linter/`, and print findings grouped by rule. Since scripts are ESM and Node v24.14 provides native TypeScript type-stripping, `node scripts/lint.js` can import the `.ts` linter modules directly; VERIFY this runs, and if the direct `.ts` import fails, add `tsx` as a devDependency and invoke via `npx tsx scripts/lint.js` (document whichever path is used in the SUMMARY). Wrap the script body in the `main().catch(...)` guard.
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/lint.js && grep -q "runLint\|linter" src/pages/registries/Products.tsx && npx tsc -b --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Products.tsx` has a Lint button, a `findings` state, and renders findings linking to products; `npx tsc -b --noEmit` clean.
    - `node --check scripts/lint.js` passes; the script imports the shared `src/lib/linter/` rules (no rule logic duplicated in the script).
    - The SUMMARY records whether `node scripts/lint.js` ran directly or required `npx tsx`.
  </acceptance_criteria>
  <done>The linter is runnable on-demand from both the Products page and the headless script, sharing one rule core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored product data -> linter | linter reads live product/store/section data to surface hygiene violations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-BYPASS | Tampering | data bypassing app-level unit validation | medium | mitigate | Linter re-checks live data on demand (not only at write time), catching values introduced via direct PB Admin edits; complements the units.ts write-path validation |
| T-04-FALSE | Information disclosure | rule false-negatives hiding bad data | low | accept | v1 rule set is the agreed data-hygiene subset (D-03); duration/pull-step rules deferred to linter v2 (Phase 5) |
</threat_model>

<verification>
- `linter.test.ts` green (flag+clean per rule; clean product zero findings).
- `node --check scripts/lint.js` passes; shared rule core.
- `npx tsc -b --noEmit` clean.
</verification>

<success_criteria>
The linter surfaces the four data-hygiene issues on demand from two surfaces sharing one pure rule core, and is silent on clean products.
</success_criteria>

## Artifacts This Plan Produces

- New module `recipe-planner/src/lib/linter/`: `index.ts` (exports `LintFinding` type, `runLint()`, `ProductExpanded`), `rules/cross-dimension.ts`, `rules/prep-words.ts`, `rules/missing-store-section.ts` (`SECTION_REQUIRED_STORES = {"safeway"}`), `rules/missing-canonical-unit.ts`, `linter.test.ts`
- Modified: `Products.tsx` (Lint button, `findings` state, `runLint` handler, findings panel)
- New file: `recipe-planner/scripts/lint.js` (headless runner sharing the same rule core)

<output>
Create `.planning/phases/01-data-hygiene/01-04-SUMMARY.md` when done.
</output>
