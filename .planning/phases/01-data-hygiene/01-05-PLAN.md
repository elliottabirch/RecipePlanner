---
phase: 01-data-hygiene
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - recipe-planner/scripts/normalize-node-units.js
  - recipe-planner/scripts/backfill-units.js
  - decisions.md
autonomous: true
requirements: [DATA-05, DATA-07]
must_haves:
  truths:
    - "normalize-node-units.js maps every enum-alias node unit to a canonical token, clears container-type strings off stored-product nodes, and REPORTS unresolved values (by, chile, medium, large, 28oz cans) rather than guessing (D-08)"
    - "backfill-units.js infers canonical_unit + dimension per surviving product, leaving genuinely ambiguous products null for the linter to surface (D-08)"
    - "decisions.md describes step aggregation as an input/output-signature match; the stale exact-name-match wording is gone (DATA-07); the D-02 container-revisit trigger is recorded"
  artifacts:
    - path: recipe-planner/scripts/normalize-node-units.js
      provides: "one-shot node.unit enum normalization + container-string clearing + unresolved report"
    - path: recipe-planner/scripts/backfill-units.js
      provides: "canonical_unit/dimension inference with review"
    - path: decisions.md
      provides: "reconciled step-aggregation wording + D-02 revisit note"
  key_links:
    - from: recipe-planner/scripts/normalize-node-units.js
      to: recipe-planner/src/lib/units.ts
      via: "UNIT_ALIASES/normalizeUnit drive the normalization"
      pattern: "normalizeUnit|UNIT_ALIASES"
---

<objective>
Build (do not yet run against prod) the node-unit normalization and canonical-unit backfill scripts, and reconcile `decisions.md` with the code's real step-aggregation behavior. `normalize-node-units.js` repairs the messy live `recipe_product_nodes.unit` corpus (~20% container-type strings + spelling variants + genuine junk); `backfill-units.js` sets `canonical_unit`/`dimension`; the doc edit closes DATA-07.

Purpose: DATA-05 (node unit normalization), DATA-07 (docs reconciliation + D-02 note). Implements D-08 (never guess; report unresolved), D-13 (can/bu alias to each). The supervised prod run of these scripts is Plan 08.
Output: `normalize-node-units.js`, `backfill-units.js`, edited `decisions.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@.planning/phases/01-data-hygiene/01-PATTERNS.md
@recipe-planner/src/lib/units.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile decisions.md with signature-based step aggregation + D-02 note</name>
  <files>decisions.md</files>
  <read_first>
    - decisions.md (the "Step Aggregation" section ~lines 41-44, the "Aggregation -> Matching keys" bullet ~lines 84-87, the prep-step line ~lines 16-18, and the "Ingredient Handling ... no conversion layer needed" line ~line 34)
    - recipe-planner/src/lib/aggregation/utils/step-utils.ts (createStepSignature lines 20-27 — the real behavior: sorted input-product-ID + output-product-ID signature)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-02 (container revisit trigger to record)
  </read_first>
  <action>
    Edit `decisions.md` so the step-aggregation wording matches `createStepSignature` (step-utils.ts:20-27): steps merge across recipes when their sorted input-product-ID + output-product-ID signature matches; step NAMES are collected for display only and do not affect merging. Update all three stale locations (the Step Aggregation section, the Aggregation matching-keys bullet, and the prep-step aggregation line) so the phrase claiming step aggregation is done by matching step names literally no longer appears. Also update the stale "Ingredient Handling — no conversion layer needed" line (~line 34) to reflect that this phase introduces the units.ts within-dimension conversion layer. Add a short note recording the D-02 revisit trigger: a per-node `recipe_product_nodes.container_type` relation will be added if/when a real recipe needs one stored product in different containers across recipes (deliberate future decision, not an implicit gap).
  </action>
  <verify>
    <automated>cd /home/ellio/code/RecipePlanner && grep -in "exact string match" decisions.md | grep -iv "product name" ; grep -qi "signature" decisions.md && grep -qi "container" decisions.md && echo DECISIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "exact string match" decisions.md` returns no line describing STEP aggregation (the phrase no longer applies to steps).
    - decisions.md mentions the input/output signature behavior and references the units.ts conversion layer.
    - decisions.md records the D-02 per-node container revisit trigger.
  </acceptance_criteria>
  <done>decisions.md matches the code's signature-based step aggregation and records the D-02 trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Create normalize-node-units.js (alias map + container clearing + unresolved report)</name>
  <files>recipe-planner/scripts/normalize-node-units.js</files>
  <read_first>
    - recipe-planner/scripts/sync-to-test.js (per-record update loop shape: count copied/failed, per-item warning cap `if (failed <= N)`, throw-on-failure; fmtError lines 33-39)
    - recipe-planner/src/lib/units.ts (normalizeUnit + UNIT_ALIASES)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Common Pitfalls #2" (the full 445-record disposition table: which values alias, which are container strings, which are unresolved)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-08, D-13
  </read_first>
  <action>
    Create `recipe-planner/scripts/normalize-node-units.js` (plain Node ESM). Read the target PocketBase base URL from a `PB_URL` env var (default prod `http://192.168.50.95:8090`) so it can be rehearsed against test `:8091` (D-06). Support a `--dry-run` flag (default to dry-run for safety; require an explicit `--apply` to write). It does a two-part job over `recipe_product_nodes`: (1) for each non-empty `unit`, run it through the alias/normalization logic — since scripts can't reliably import `.ts` in all setups, either import `normalizeUnit` from `../src/lib/units.ts` via Node 24 native TS stripping (verify it runs; fall back to a small embedded copy of the alias map sourced from units.ts UNIT_ALIASES if the import fails), mapping spelling variants (ea->each, cups->cup, Tbsp->tbsp, tbl->tbsp, cu->cup, quart->qt, clove/can/bu/bunch/whole/cube/slices/etc.->each per D-13/D-09) to canonical tokens; (2) for container-type strings on `stored`-type product nodes (quart restaurant, vacuum sealed bag, original packaging, tupperware, package, ramekin, bag, container, etc. — from the Pitfall #2 table), CLEAR the unit to empty (do NOT alias them to a measurement unit — §4.5 moves container type to products.container_type). REPORT (never silently drop) every value that matches neither an enum token, an alias, nor a recognized container-string — the genuine junk `by`, `chile`, `medium`, `large`, `28oz cans` — into an "unresolved, needs manual resolution" list printed at the end and written to a report file. Use the sync-to-test.js count-success/fail + throw-on-failure + fmtError conventions; wrap in `main().catch(...)`.
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/normalize-node-units.js && grep -q "dry-run\|dryRun\|--apply" scripts/normalize-node-units.js && grep -qi "unresolved" scripts/normalize-node-units.js && echo NORMALIZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/normalize-node-units.js` passes.
    - Script defaults to dry-run and requires `--apply` to mutate.
    - Container-type strings on stored nodes are cleared to empty, not aliased to a unit.
    - Values matching nothing (by/chile/medium/large/28oz cans) are collected into an unresolved report, not dropped or guessed (D-08).
  </acceptance_criteria>
  <done>normalize-node-units.js normalizes aliases, clears container strings off stored nodes, and surfaces unresolved values — ready for the Plan 08 supervised run.</done>
</task>

<task type="auto">
  <name>Task 3: Create backfill-units.js (canonical_unit + dimension inference)</name>
  <files>recipe-planner/scripts/backfill-units.js</files>
  <read_first>
    - recipe-planner/scripts/find-duplicates.js (read-then-report scaffold) and sync-to-test.js (update loop for writing back)
    - recipe-planner/src/lib/units.ts (getDimension, normalizeUnit; canonical_unit implies dimension)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-08 (ambiguous -> null, never guess), "Claude's Discretion" (dimension written from canonical_unit so they can't drift)
  </read_first>
  <action>
    Create `recipe-planner/scripts/backfill-units.js` (plain Node ESM, `--dry-run` default / `--apply` to write; target base URL from `PB_URL` env, default prod, so it can be rehearsed against test `:8091`). For each `products` record, infer a `canonical_unit` from that product's existing `recipe_product_nodes.unit` usage (after the same normalization logic as Task 2) and/or product type (count-like/`each` products -> `each`): when the normalized node units for a product consistently reduce to ONE dimension, pick a sensible canonical unit for it and set `dimension` from `getDimension(canonical_unit)` (write dimension FROM canonical_unit so the two never drift — CONTEXT.md discretion note); when the product's unit history is genuinely ambiguous (spans dimensions, or no usable units), leave BOTH `canonical_unit` and `dimension` null so linter rule 4 (Plan 04) surfaces it — never guess (D-08). Print a summary: how many set, how many left null and why. Use the update-loop count/fail/throw conventions; `main().catch(...)`.
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/backfill-units.js && grep -q "dry-run\|dryRun\|--apply" scripts/backfill-units.js && grep -q "canonical_unit" scripts/backfill-units.js && grep -q "dimension" scripts/backfill-units.js && echo BACKFILL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/backfill-units.js` passes.
    - Script defaults to dry-run and requires `--apply`.
    - `dimension` is always derived from the chosen `canonical_unit` (never set independently).
    - Ambiguous products are left null (not guessed) and counted in the summary.
  </acceptance_criteria>
  <done>backfill-units.js infers canonical_unit/dimension where unambiguous and leaves the rest null for the linter — ready for Plan 08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> PocketBase | one-shot mutations of node units and product canonical fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-GUESS | Tampering | silent mis-normalization of ambiguous units | high | mitigate | Unresolved/ambiguous values are reported and left null (D-08), never guessed; scripts default to dry-run and require explicit --apply |
| T-05-DRIFT | Tampering | canonical_unit/dimension drift | medium | mitigate | dimension is always written from getDimension(canonical_unit); never set independently |
</threat_model>

<verification>
- `node --check` passes for both scripts.
- Scripts default to dry-run.
- decisions.md no longer describes step aggregation as name-matching.
</verification>

<success_criteria>
Node-unit normalization and canonical backfill scripts exist with dry-run safety and never-guess reporting; decisions.md matches the code. No prod data mutated in this plan.
</success_criteria>

## Artifacts This Plan Produces

- New files: `recipe-planner/scripts/normalize-node-units.js` (`--dry-run` default/`--apply`, `PB_URL`-targetable, alias normalization + stored-node container-string clearing + unresolved report), `recipe-planner/scripts/backfill-units.js` (`--dry-run`/`--apply`, canonical_unit/dimension inference, ambiguous-left-null summary)
- Modified: `decisions.md` (step aggregation reconciled to input/output-signature behavior; conversion-layer line updated; D-02 per-node container revisit trigger recorded)

<output>
Create `.planning/phases/01-data-hygiene/01-05-SUMMARY.md` when done.
</output>
