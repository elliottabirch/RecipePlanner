---
phase: 01-data-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/scripts/find-duplicates.js
  - recipe-planner/scripts/merge-products.js
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "find-duplicates.js emits a Markdown context report AND a JSON decisions skeleton {dupeId -> survivorId, confirmed} (D-05)"
    - "Cross-type same-name collisions (transient/stored pairs) are separately flagged, NOT listed as merge candidates (D-12, Pitfall #1)"
    - "merge-products.js reads ONLY the JSON decisions file, backs up prod, pre-flight-validates every ID, repoints all four product-referencing relations, verifies zero orphans, then deletes"
  artifacts:
    - path: recipe-planner/scripts/find-duplicates.js
      provides: "Extended dupe finder: JSON + MD outputs, type-aware flagging"
    - path: recipe-planner/scripts/merge-products.js
      provides: "One-shot destructive merge with backup, pre-flight validation, orphan check"
  key_links:
    - from: recipe-planner/scripts/merge-products.js
      to: recipe-planner/scripts/find-duplicates.js
      via: "reads the JSON decisions file emitted by find-duplicates.js"
      pattern: "confirmed"
---

<objective>
Build (do not yet run against prod) the dedup tooling: extend `find-duplicates.js` to emit the D-05 review artifacts (JSON decisions file + Markdown context report) with type-aware collision flagging, and create `merge-products.js` — the destructive one-shot merge with the full D-06 safety net. These scripts are the tooling; the supervised prod run is Plan 08.

Purpose: DATA-04 dedup. Implements D-05 (JSON+MD review format), D-06 (backup, pre-flight ID validation, zero-orphan check), D-07 (live-enumerated reference collections), D-12 (cross-type collisions are legitimate, not dupes).
Output: extended `find-duplicates.js`, new `merge-products.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@.planning/phases/01-data-hygiene/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend find-duplicates.js to emit JSON + MD with type-aware flagging</name>
  <files>recipe-planner/scripts/find-duplicates.js</files>
  <read_first>
    - recipe-planner/scripts/find-duplicates.js (current console-only output; the exact-dupe grouping loop is lines 14-19)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Common Pitfalls #1" (the two live transient/stored same-name pairs that must NOT be treated as dupes)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-05, D-12
  </read_first>
  <action>
    Extend `recipe-planner/scripts/find-duplicates.js` so, in addition to its console report, it writes two files (via `fs.writeFileSync`) into `recipe-planner/scripts/` (or a `scripts/dedup-output/` subdir — pick one and print the paths): (1) a Markdown context report listing each candidate cluster with names side-by-side, each product's `type`, and per-collection reference counts (query `recipe_product_nodes`, `inventory_items`, `products.store_bought_product`, `meal_variant_overrides.replacement_product` to count references — enumerate against the live DB per D-07, never the stale schema export); (2) a JSON decisions skeleton — an array of `{ dupeId, survivorId: null, confirmed: false, name, cluster: [...] }` entries the human edits. Modify the exact-duplicate grouping so a case-insensitive name collision whose members have DIFFERENT `type` values is emitted into a SEPARATE "cross-type name collisions (NOT dupe candidates)" section and is EXCLUDED from the JSON decisions skeleton (D-12 / Pitfall #1: the `onion (red) sliced` and `cucumber sliced` transient/stored pairs are legitimate — merging them would break `shouldCreateInstances()` branching). Only same-`type` case-insensitive collisions become merge candidates. Read the target PocketBase base URL from a `PB_URL` env var (default the prod URL `http://192.168.50.95:8090`) so the same script can be pointed at test `:8091` for the D-06 rehearsal. Wrap the script body in the `main().catch(...)` guard from 01-RESEARCH.md "Code Examples".
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/find-duplicates.js && grep -q "writeFileSync" scripts/find-duplicates.js && grep -Eq "confirmed" scripts/find-duplicates.js && grep -Eq "type" scripts/find-duplicates.js && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/find-duplicates.js` passes.
    - Script writes both a `.md` report and a `.json` decisions skeleton and prints their paths.
    - JSON entries have keys `dupeId`, `survivorId`, `confirmed`.
    - Cross-`type` same-name collisions appear in a clearly-labelled separate section and are absent from the JSON decisions array (grep the produced JSON after a manual run to confirm no transient/stored pair IDs are present).
    - Reference counts are computed by live queries against all four collections named in D-07.
  </acceptance_criteria>
  <done>find-duplicates.js produces a reviewable MD report and a JSON decisions skeleton, with cross-type collisions quarantined out of the merge candidate set.</done>
</task>

<task type="auto">
  <name>Task 2: Create merge-products.js (backup, pre-flight validation, repoint, orphan-check, delete)</name>
  <files>recipe-planner/scripts/merge-products.js</files>
  <read_first>
    - recipe-planner/scripts/sync-to-test.js (analog: DB_URLS config lines 5-8, fmtError lines 33-39, count-success/fail-then-throw loop lines 50-67, top-level try/catch/exit lines 120-132)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Pre-flight prod snapshot" code example (pb.backups.create) and "Common Pitfalls #1" (shouldCreateInstances branches on type — never collapse cross-type)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-06, D-07
  </read_first>
  <action>
    Create `recipe-planner/scripts/merge-products.js` (plain Node ESM, no CLI framework). It takes the JSON decisions file (from Task 1, edited by the human) as its ONLY input — read the path from `process.argv[2]` with a sane default, and process only entries where `confirmed === true` and `survivorId` is non-null. Read the target base URL from the `PB_URL` env var (default prod `http://192.168.50.95:8090`) so the script can be rehearsed against test `:8091` (D-06). Copy from `sync-to-test.js`: the `new PocketBase(...)` setup, the `fmtError(e)` helper verbatim, and the "count successes/failures, `throw new Error` if any failure" fail-fast convention. Sequence (order is load-bearing): (1) authenticate as superuser via `pb.collection("_superusers").authWithPassword(email, password)` reading `PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD` from env; (2) **pre-flight ID validation** — assert every `dupeId` and `survivorId` in the confirmed set exists as a live `products` record, and assert no confirmed pair has mismatched `type` (abort with a clear error if any check fails) per D-06.3 and Pitfall #1; (3) **backup** via `await pb.backups.create(...)` with a timestamped basename and confirm it returns before any mutation (D-06.2); (4) **repoint** every product reference from `dupeId` to `survivorId` across exactly the four D-07 collections: `recipe_product_nodes.product`, `inventory_items.product`, `products.store_bought_product`, `meal_variant_overrides.replacement_product` (define this list as a const in the script — do NOT derive it from `pb_schema_updated.json`, which is stale); (5) **zero-orphan verification** — after repointing, re-query each collection and assert zero records still reference any `dupeId`; (6) only then `delete` each dupe. Wrap in `main().catch(...)` with `process.exit(1)`. Do NOT auto-run — the script is invoked manually in Plan 08.
  </action>
  <verify>
    <automated>cd recipe-planner && node --check scripts/merge-products.js && grep -q "backups.create" scripts/merge-products.js && grep -q "meal_variant_overrides" scripts/merge-products.js && grep -q "authWithPassword" scripts/merge-products.js && echo MERGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/merge-products.js` passes.
    - The four D-07 reference collections are all named in the script's repoint list (grep confirms `recipe_product_nodes`, `inventory_items`, `store_bought_product`, `meal_variant_overrides`).
    - `pb.backups.create` is called before any `.update`/`.delete`.
    - Pre-flight validation aborts on any missing ID or cross-type pair before mutating.
    - A zero-orphan re-query gates the delete step.
    - Superuser auth reads credentials from env vars, not hardcoded.
  </acceptance_criteria>
  <done>merge-products.js exists with the full D-06 safety net and D-07 reference list, ready for the supervised prod run in Plan 08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> PocketBase prod | destructive multi-collection writes/deletes against live production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-STALE | Tampering | merge-products.js reference list | high | mitigate | Reference collections hardcoded from D-07 live enumeration (includes meal_variant_overrides that the stale schema omits); zero-orphan re-query before delete |
| T-02-LOSS | Repudiation | irreversible product delete | high | mitigate | pb.backups.create() must complete before any mutation (D-06.2); delete gated on zero-orphan verification |
| T-02-XTYPE | Tampering | cross-type merge | high | mitigate | find-duplicates quarantines cross-type collisions; merge pre-flight aborts on any type mismatch (Pitfall #1) |
</threat_model>

<verification>
- `node --check` passes for both scripts.
- Reference list matches the four D-07 collections exactly.
- Backup precedes mutation; orphan-check precedes delete.
</verification>

<success_criteria>
Reviewable dedup artifacts are produced with cross-type collisions quarantined; the destructive merge script carries the full backup + pre-flight + orphan-check safety net. No prod data is mutated in this plan.
</success_criteria>

## Artifacts This Plan Produces

- Modified: `recipe-planner/scripts/find-duplicates.js` (now emits a Markdown context report + JSON decisions skeleton with `{dupeId, survivorId, confirmed}` entries; cross-`type` collisions quarantined)
- New file: `recipe-planner/scripts/merge-products.js` (superuser auth via `PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD`, `PB_URL`-targetable, `pb.backups.create` pre-flight, ID validation, repoint of the four D-07 reference collections, zero-orphan check, delete)
- Runtime output artifacts: dedup `.md` report + `.json` decisions file (generated at run time)

<output>
Create `.planning/phases/01-data-hygiene/01-02-SUMMARY.md` when done.
</output>
