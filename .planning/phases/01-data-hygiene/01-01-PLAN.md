---
phase: 01-data-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/package.json
  - recipe-planner/vitest.config.ts
  - recipe-planner/src/lib/units.ts
  - recipe-planner/src/lib/units.test.ts
  - recipe-planner/src/lib/types.ts
autonomous: false
requirements: [DATA-02]
must_haves:
  truths:
    - "convert() round-trips exactly within a dimension and returns null across dimensions"
    - "normalizeUnit() maps every live alias (ea, cups, Tbsp, clove, can, bu, ...) to a canonical enum token"
    - "chooseDisplayUnit()/promoteUnit() picks a deterministic display unit per D-10 (canonical when set; else largest unit keeping qty >= 1, capped at cup/lb)"
    - "Product type carries canonical_unit and dimension so aggregation and linter typecheck"
  artifacts:
    - path: recipe-planner/src/lib/units.ts
      provides: "Unit enum, UNIT_DIMENSIONS, UNIT_ALIASES, getDimension/canConvert/convert/normalizeUnit + display-unit selection"
      exports: ["Unit", "Dimension", "UNIT_DIMENSIONS", "UNIT_ALIASES", "getDimension", "canConvert", "convert", "normalizeUnit"]
    - path: recipe-planner/src/lib/units.test.ts
      provides: "Round-trip + cross-dimension-null + alias + white-bean-anchor tests"
    - path: recipe-planner/vitest.config.ts
      provides: "Vitest config (no jsdom; pure-function tests)"
  key_links:
    - from: recipe-planner/src/lib/types.ts
      to: recipe-planner/src/lib/units.ts
      via: "Product.canonical_unit typed as Unit from units.ts"
      pattern: "canonical_unit"
---

<objective>
Stand up the test harness (Vitest — none exists in the repo today) and create `src/lib/units.ts`: the single source of truth for the unit vocabulary, dimension map, alias table, exact conversion math, and deterministic display-unit selection. Add `canonical_unit` + `dimension` to the `Product` TypeScript interface so every downstream consumer (aggregation fix, linter) typechecks.

Purpose: Every other plan in this phase depends on this module. The conversion constants must be exact — a wrong factor silently corrupts every shopping list (CONTEXT risk #2). Implements D-09 (enum as-is), D-10 (display-unit selection), D-11 (units.ts stays exact; no human formatting), D-13 (can/bu alias to each).
Output: `units.ts`, `units.test.ts`, `vitest.config.ts`, `package.json` test script, `types.ts` Product fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@.planning/phases/01-data-hygiene/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve vitest install (package legitimacy gate)</name>
  <action>Pause for human approval of the vitest devDependency install before Task 2 runs it. This is a blocking package-legitimacy gate ([SUS] false-positive) — no code is written here.</action>
  <what-built>Nothing yet — this gate precedes the `npm install -D vitest` in Task 2. The package-legitimacy audit (01-RESEARCH.md "Package Legitimacy Audit") flagged `vitest` as `[SUS]`, but the flag is a false positive driven by latest-version publish recency, not package age.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest
    2. Confirm: repository is github.com/vitest-dev/vitest, weekly downloads are in the tens of millions (~68M/wk), package first published ~2021.
    3. Confirm the version to install is the current `4.x` latest.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the vitest install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Vitest and wire the test script</name>
  <files>recipe-planner/package.json, recipe-planner/vitest.config.ts</files>
  <read_first>
    - recipe-planner/package.json (current scripts + devDependencies; note "type": "module" is already set)
    - recipe-planner/tsconfig.app.json (strict mode already enabled; do not weaken it)
  </read_first>
  <action>
    Run `npm install -D vitest` inside `recipe-planner/` (the app root, where package.json lives). Add a `"test": "vitest run"` script to `recipe-planner/package.json` scripts block (alongside the existing `dev`/`build`/`lint`/`sync-to-test`). Create `recipe-planner/vitest.config.ts` with a minimal Vitest config: `test.environment` = `node` (pure-function tests only — do NOT add jsdom this phase per 01-RESEARCH.md Supporting-stack note), `test.include` = `["src/**/*.test.ts", "scripts/**/*.test.js"]`. Do not add @testing-library or jsdom.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run --reporter=dot 2>&1 | grep -iv "no test files" ; test -f vitest.config.ts && grep -q '"test": "vitest run"' package.json && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `recipe-planner/vitest.config.ts` exists and sets `test.environment` to `node`.
    - `package.json` scripts contains `"test": "vitest run"`.
    - `vitest` appears under `devDependencies` in `package.json`.
    - `npx vitest run` executes without a config error (zero tests is acceptable at this point).
  </acceptance_criteria>
  <done>Vitest is installed as a devDependency, config file exists, and `npm test` runs the runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create units.ts (enum, aliases, conversion, display selection) + tests + Product type fields</name>
  <files>recipe-planner/src/lib/units.ts, recipe-planner/src/lib/units.test.ts, recipe-planner/src/lib/types.ts</files>
  <read_first>
    - recipe-planner/src/lib/aggregation/utils/product-utils.ts (pure-function module shape to mirror: named exports, typed in/out, no PocketBase/React imports)
    - recipe-planner/src/lib/types.ts (Product interface at line 42; container_type at line 50 — you are adding two sibling fields)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Architecture Patterns > Pattern 1" (authoritative conversion table — use verbatim, do NOT re-derive constants) and "Common Pitfalls #2" (the 445-record alias disposition table — seed UNIT_ALIASES from it)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md decisions D-09, D-10, D-11, D-13
  </read_first>
  <behavior>
    - convert(1, "cup", "tbsp") === 16 (exact); convert(16, "tbsp", "cup") round-trips to 1.
    - White-bean anchor: convert(0.25, "cup", "tbsp") + 2 === 6.
    - canConvert("cup", "lb") === false; convert(1, "cup", "lb") === null.
    - convert across count (each) to volume/mass returns null.
    - normalizeUnit("ea") === "each"; normalizeUnit("cups") === "cup"; normalizeUnit("Tbsp") === "tbsp"; normalizeUnit("clove") === "each"; normalizeUnit("can") === "each"; normalizeUnit("bu") === "each"; normalizeUnit("tbl") === "tbsp"; normalizeUnit("cu") === "cup".
    - normalizeUnit("by") / normalizeUnit("chile") / normalizeUnit("medium") / normalizeUnit("28oz cans") return null (unresolved — never guessed, per D-08).
    - Display-unit selection is deterministic and independent of argument order: for a volume total below 1 cup it does not promote past cup; a mass total below 1 lb does not promote past lb; it never crosses metric<->customary within one promotion path (D-10).
  </behavior>
  <action>
    Create `recipe-planner/src/lib/units.ts` exporting: `Dimension` (`"volume" | "mass" | "count"`); `Unit` union = volume `tsp,tbsp,fl_oz,cup,pint,qt,gal,ml,l` + mass `g,kg,oz,lb` + count `each` (exactly per D-09 — NO clove/bunch/head/can members); `UNIT_DIMENSIONS: Record<Unit,Dimension>`; `TO_ML` and `TO_G` exact-factor tables copied verbatim from 01-RESEARCH.md Pattern 1 (cup=236.588236, gal=3785.411784, lb=453.59237, oz=28.349523125, etc.); `getDimension(unit)`, `canConvert(a,b)`, `convert(qty,from,to): number | null` (returns null across dimensions and for count-to-count non-equal). Add `UNIT_ALIASES: Record<string, Unit>` seeded from the 01-RESEARCH.md Common-Pitfalls-#2 table (ea->each, cups->cup, Tbsp->tbsp, tbl->tbsp, cu->cup, quart->qt, clove/cloves->each, can->each, bu/bunch->each, whole/cube/cubes/slices/pitas/sprigs->each per D-13/D-09) and `normalizeUnit(raw: string): Unit | null` that lowercases/trims, returns the token if it is already an enum member, else the alias mapping, else null (container-type strings like "quart restaurant"/"vacuum sealed bag" and junk like "by","chile","medium","28oz cans" return null — do NOT map them). Add display-unit selection implementing D-10 (CONTEXT.md wins over phase-doc §4.3): export `chooseDisplayUnit(dimension: Dimension, canonicalUnit: Unit | null, totalInBaseOrQty...)` OR `promoteUnit(qty, unit)` — when the product canonical_unit is set, that is the display unit; when null, pick the largest unit within the same system that keeps quantity >= 1, capped at `cup` for volume and `lb` for mass, never crossing metric<->customary in one promotion path. Keep the module exact and reviewable — NO fraction/human rounding here (D-11: that is a render-layer concern). Add a JSDoc header describing the module purpose and citing the NIST/BIPM factor source.

    Create `recipe-planner/src/lib/units.test.ts` covering every bullet in <behavior> (use `toBeCloseTo` for float assertions per 01-RESEARCH.md "Round-trip conversion test" example).

    Modify `recipe-planner/src/lib/types.ts` Product interface (line 42): add `canonical_unit?: Unit;` and `dimension?: Dimension;` importing `Unit`/`Dimension` from `./units`. Do not change existing fields.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/units.test.ts && npx tsc -b --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/units.test.ts` is green with tests covering: cup<->tbsp round-trip, white-bean 6-tbsp anchor, cross-dimension null, count non-convertible, and at least 8 alias mappings including the 4 unresolved-return-null cases.
    - `convert` uses the exact factors from 01-RESEARCH.md Pattern 1 (verify cup=236.588236, lb=453.59237 present in source).
    - `types.ts` Product has `canonical_unit?: Unit` and `dimension?: Dimension` and `npx tsc -b --noEmit` reports no new errors.
    - `units.ts` contains no fraction/percent human-formatting logic (D-11).
  </acceptance_criteria>
  <done>units.ts exports the full vocabulary + exact conversion + alias + deterministic display selection; tests green; Product type extended; project typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> devDependency | vitest install pulls third-party code into the build toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install -D vitest` | high | mitigate | Task 1 blocking-human checkpoint verifies vitest on npmjs.com (68M/wk, canonical repo, ~2021 origin) before install; [SUS] flag is a documented false positive |
| T-01-CONV | Tampering | conversion constants in units.ts | high | mitigate | Constants copied verbatim from NIST/BIPM factors in 01-RESEARCH.md Pattern 1; round-trip unit tests fail on any wrong factor |
</threat_model>

<verification>
- `npx vitest run src/lib/units.test.ts` green.
- `npx tsc -b --noEmit` clean.
- Conversion factors match 01-RESEARCH.md Pattern 1 exactly.
</verification>

<success_criteria>
Vitest harness runs; units.ts is the exact, tested single source of truth for the unit vocabulary and conversion; Product type carries canonical_unit + dimension.
</success_criteria>

## Artifacts This Plan Produces

- New files: `recipe-planner/src/lib/units.ts`, `recipe-planner/src/lib/units.test.ts`, `recipe-planner/vitest.config.ts`
- `units.ts` exports: types `Unit`, `Dimension`; consts `UNIT_DIMENSIONS`, `UNIT_ALIASES`, `TO_ML`, `TO_G`; functions `getDimension()`, `canConvert()`, `convert()`, `normalizeUnit()`, and the D-10 display-unit selector (`chooseDisplayUnit()`/`promoteUnit()`)
- `types.ts` additions: `Product.canonical_unit?: Unit`, `Product.dimension?: Dimension`
- `package.json`: `vitest` devDependency + `"test": "vitest run"` script

<output>
Create `.planning/phases/01-data-hygiene/01-01-SUMMARY.md` when done.
</output>
