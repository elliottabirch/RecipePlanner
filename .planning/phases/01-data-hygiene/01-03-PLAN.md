---
phase: 01-data-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - recipe-planner/src/lib/aggregation/builders/product-builder.ts
  - recipe-planner/src/lib/aggregation/builders/product-builder.test.ts
  - recipe-planner/src/lib/aggregation/builders/step-builder.ts
  - recipe-planner/src/lib/aggregation/builders/step-builder.test.ts
  - recipe-planner/src/lib/aggregation/utils/product-utils.ts
  - recipe-planner/src/lib/aggregation/types.ts
  - recipe-planner/src/lib/aggregation.ts
  - recipe-planner/src/components/outputs/ShoppingListTab.tsx
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "Same-dimension quantities combine into one converted line; cross-dimension quantities become separate stable lines (no false sum)"
    - "Split lines have distinct stable lineId (`productId` for the common case, `productId|dimension` when split) — no React duplicate-key warning, one checkbox never toggles two lines"
    - "Per-source breakdown quantities display converted into the merged line's display unit so the breakdown visibly sums to the line total"
  artifacts:
    - path: recipe-planner/src/lib/aggregation/builders/product-builder.ts
      provides: "convert-or-split merge + lineId keying"
    - path: recipe-planner/src/lib/aggregation/builders/product-builder.test.ts
      provides: "white-bean anchor + cross-dimension two-line tests"
    - path: recipe-planner/src/lib/aggregation/types.ts
      provides: "lineId on AggregatedFlowProduct/AggregatedProduct"
  key_links:
    - from: recipe-planner/src/components/outputs/ShoppingListTab.tsx
      to: recipe-planner/src/lib/aggregation/types.ts
      via: "keys rows and checkbox state by item.lineId (was item.productId)"
      pattern: "lineId"
    - from: recipe-planner/src/lib/aggregation/builders/product-builder.ts
      to: recipe-planner/src/lib/units.ts
      via: "canConvert/convert/getDimension imported for the merge decision"
      pattern: "canConvert|convert"
---

<objective>
Fix the unit-blind aggregation bug (DATA-01): the merge builders currently do `existing.totalQuantity += newProduct.totalQuantity` with no unit check. Replace with convert-or-split — combine only within a dimension (converting via units.ts), otherwise split into distinct lines keyed by a stable `lineId`. Thread `lineId` through the shopping list so split lines don't collide (this `lineId` is also the Phase 2 persisted-checkbox key — get it right here).

Purpose: DATA-01. The white-bean-stew olive oil (0.25 cup + 2 tbsp) must render as one correct converted line; a cross-dimension case must render two lines, never a summed one.
Output: fixed product-builder + step-builder, lineId on types, per-source converted display, ShoppingListTab re-keyed, new builder tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-hygiene/01-CONTEXT.md
@.planning/phases/01-data-hygiene/01-RESEARCH.md
@.planning/phases/01-data-hygiene/01-PATTERNS.md
@recipe-planner/src/lib/units.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert-or-split in product-builder + lineId on types</name>
  <files>recipe-planner/src/lib/aggregation/builders/product-builder.ts, recipe-planner/src/lib/aggregation/builders/product-builder.test.ts, recipe-planner/src/lib/aggregation/types.ts, recipe-planner/src/lib/aggregation/utils/product-utils.ts</files>
  <read_first>
    - recipe-planner/src/lib/aggregation/builders/product-builder.ts (addOrMergeProduct ~lines 79-124; blind `+=` at line 91; key computed in buildAggregatedProduct ~lines 55-72)
    - recipe-planner/src/lib/aggregation/types.ts (AggregatedFlowProduct at line 67 with productId at line 68; AggregatedProduct at line 118 with productId at line 119)
    - recipe-planner/src/lib/units.ts (canConvert, convert, getDimension, chooseDisplayUnit/promoteUnit)
    - .planning/phases/01-data-hygiene/01-RESEARCH.md "Pattern 1" (mergeInto + split-line identity sketch)
    - .planning/phases/01-data-hygiene/01-CONTEXT.md D-10 (display-unit selection), specifics (lineId = `${product.id}|${dimension}`, lineId===productId for single-line case)
  </read_first>
  <behavior>
    - Merging olive-oil nodes 0.25 cup and 2 tbsp yields one AggregatedFlowProduct whose total equals 6 tbsp (or 0.375 cup) with lineId === productId.
    - Merging a product with one node in `cup` (volume) and one in `lb` (mass) yields TWO AggregatedFlowProducts with lineIds `<pid>` and `<pid>|mass` (or both suffixed) — distinct, and neither total is a cross-dimension sum.
    - Single-line (non-split) products keep lineId === productId exactly.
    - Display unit follows D-10: canonical_unit when the product has one; else the largest unit keeping qty >= 1 capped at cup/lb, deterministic regardless of node order.
  </behavior>
  <action>
    Add `lineId: string` to `AggregatedFlowProduct` (line 67) and `AggregatedProduct` (line 118) in `aggregation/types.ts`. In `product-builder.ts`, import `canConvert, convert, getDimension` and the display-unit helper from `../../units`. Replace the unconditional `existing.totalQuantity += newProduct.totalQuantity` (line 91): when the incoming and existing units share a dimension, convert the incoming quantity into the existing line's display unit and add; when they do NOT share a dimension, the incoming product must be keyed under a compound map key `${product.id}|${dimension}` so it becomes a separate line (all convertible units collapse into ONE line per dimension family — key by dimension, not raw unit). Set `lineId` = the bare `productId` for the first/common line and `${productId}|${dimension}` for any split line, so single-line products keep `lineId === productId`. Choose the merged line's display unit per D-10 (canonical_unit if set on the product; else the deterministic largest-unit-≥1 fallback from units.ts — independent of node order). Optionally extract a shared `mergeQuantities(existingQty, existingUnit, addQty, addUnit)` helper into `aggregation/utils/product-utils.ts` (implementer's choice per CONTEXT.md). Create `product-builder.test.ts` covering the <behavior> bullets, driving the builder with fabricated nodes for the white-bean anchor and a cross-dimension case.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/aggregation/builders/product-builder.test.ts && npx tsc -b --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `product-builder.test.ts` green: white-bean anchor merges to one line (6 tbsp / 0.375 cup); cross-dimension input produces two lineId-distinct lines with no summed total.
    - `AggregatedFlowProduct` and `AggregatedProduct` both carry `lineId: string`.
    - For non-split products, `lineId === productId` (asserted in a test).
    - `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>product-builder merges within-dimension and splits cross-dimension with stable lineIds; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert-or-split in step-builder (inputs + outputs)</name>
  <files>recipe-planner/src/lib/aggregation/builders/step-builder.ts, recipe-planner/src/lib/aggregation/builders/step-builder.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/aggregation/builders/step-builder.ts (addOrMergeStep lines 128-149; blind input merge line 133, output merge line 145)
    - recipe-planner/src/lib/aggregation/utils/product-utils.ts (the mergeQuantities helper if extracted in Task 1)
    - recipe-planner/src/lib/units.ts
  </read_first>
  <behavior>
    - Merging two step inputs for the same product in convertible units combines into one converted quantity.
    - Merging two step inputs in cross-dimension units keeps them separate (no false sum).
    - Outputs merge with the identical rule as inputs.
  </behavior>
  <action>
    In `step-builder.ts`, apply the same convert-or-split logic to the two blind-merge sites: input merge at line 133 (`existingInput.quantity += input.quantity`) and output merge at line 145. Use the same `mergeQuantities`/units.ts helpers as `product-builder.ts` (reuse the extracted helper if created in Task 1). Create `step-builder.test.ts` covering convertible-merge and cross-dimension-keep-separate for both inputs and outputs.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/aggregation/builders/step-builder.test.ts && npx tsc -b --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `step-builder.test.ts` green for convertible-merge and cross-dimension-separate on both inputs and outputs.
    - Both line 133 and line 145 merge sites use the convert-or-split path (no bare `+=` remains for quantities in these blocks).
    - `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>step-builder input/output merges are unit-disciplined and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Thread lineId through shopping list + per-source converted display</name>
  <files>recipe-planner/src/lib/aggregation.ts, recipe-planner/src/components/outputs/ShoppingListTab.tsx</files>
  <read_first>
    - recipe-planner/src/lib/aggregation.ts (buildShoppingListFromFlow ~lines 231-250; sources[].unit at ~lines 243-247)
    - recipe-planner/src/components/outputs/ShoppingListTab.tsx (getShoppingCheckboxKey(item.productId) at line 181; key={item.productId} at line 191; pantry keys at 162/217/228 stay on productId)
    - recipe-planner/src/lib/units.ts
  </read_first>
  <action>
    In `aggregation.ts` `buildShoppingListFromFlow`, populate `lineId` on each emitted shopping line (carry the `lineId` computed by the builders; do not re-derive incorrectly) and set each `sources[].unit`/quantity to the value CONVERTED into the merged line's display unit (so the per-recipe breakdown visibly sums to the line total, per CONTEXT.md specifics). In `ShoppingListTab.tsx`, change the shopping-list (non-pantry) row keying at line 181 from `getShoppingCheckboxKey(item.productId)` to `getShoppingCheckboxKey(item.lineId)` and the React `key` at line 191 from `item.productId` to `item.lineId`. Leave the pantry checkbox keys (lines 162, 217, 228) on `item.productId` — pantry lines are not split. Confirm `lineId === productId` for the common single-line case so Phase 2 keys are unchanged there.
  </action>
  <verify>
    <automated>cd recipe-planner && grep -n "item.lineId" src/components/outputs/ShoppingListTab.tsx && npx tsc -b --noEmit 2>&1 | tail -3 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ShoppingListTab.tsx` shopping-list rows key on `item.lineId` at both the checkbox-key and React-`key` sites; pantry rows still key on `item.productId`.
    - `buildShoppingListFromFlow` sets `lineId` and emits per-source quantities in the merged line's display unit.
    - `npx tsc -b --noEmit` clean and full `npx vitest run` green.
  </acceptance_criteria>
  <done>Split lines have distinct stable identities in the UI; per-source breakdown sums to the line total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe node data -> aggregation output | free-text/enum node units feed the merge math that renders shopping quantities |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SUM | Tampering | convert-or-split merge | high | mitigate | Cross-dimension quantities are split, never summed; within-dimension merges go through tested units.ts convert; builder tests assert both paths |
| T-03-KEY | Information disclosure | shared lineId collision | medium | mitigate | lineId is `productId|dimension` for split lines and `productId` for single lines; test asserts distinctness so one checkbox cannot toggle another line (also protects Phase 2 persisted state) |
</threat_model>

<verification>
- Full `npx vitest run` green.
- White-bean anchor one line; cross-dimension two lines.
- ShoppingListTab keys on lineId; typecheck clean.
</verification>

<success_criteria>
Aggregation never silently sums mismatched units; same-dimension quantities combine correctly; split lines carry distinct stable lineIds through to the shopping UI.
</success_criteria>

## Artifacts This Plan Produces

- Modified: `product-builder.ts` (convert-or-split merge + lineId keying), `step-builder.ts` (input/output convert-or-split), `aggregation.ts` (lineId + per-source converted display), `ShoppingListTab.tsx` (rows keyed by `item.lineId`)
- Type additions: `AggregatedFlowProduct.lineId: string`, `AggregatedProduct.lineId: string`
- Optional new symbol: `mergeQuantities()` helper in `aggregation/utils/product-utils.ts`
- New test files: `product-builder.test.ts`, `step-builder.test.ts`

<output>
Create `.planning/phases/01-data-hygiene/01-03-SUMMARY.md` when done.
</output>
