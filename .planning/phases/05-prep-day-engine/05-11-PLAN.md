---
phase: 05-prep-day-engine
plan: 11
type: execute
wave: 5
depends_on: ["05-01", "05-09", "05-10"]
files_modified:
  - recipe-planner/src/hooks/useCookProgress.ts
  - recipe-planner/src/lib/scheduler/readiness.ts
  - recipe-planner/src/lib/scheduler/readiness.test.ts
  - recipe-planner/src/pages/CookMode.tsx
  - recipe-planner/src/components/cook-mode/NowNextCard.tsx
  - recipe-planner/src/components/cook-mode/ReadinessChip.tsx
  - recipe-planner/src/pages/Outputs.tsx
  - recipe-planner/src/App.tsx
autonomous: false
requirements: [PREP-04]
must_haves:
  truths:
    - "Cook mode shows now/next cards with scaled quantities + instructions and live countdowns for passive steps"
    - "Checking off a step re-times the remaining timeline WITHOUT reordering any card"
    - "Readiness flips from 'waiting on: X' to 'Ready' once every upstream producer is checked off (AND-semantics)"
    - "Cook progress persists per (weekly_plan, step_instance) across tablet refresh via useCookProgress"
  artifacts:
    - recipe-planner/src/hooks/useCookProgress.ts
    - recipe-planner/src/pages/CookMode.tsx
    - recipe-planner/src/components/cook-mode/NowNextCard.tsx
    - recipe-planner/src/components/cook-mode/ReadinessChip.tsx
  key_links:
    - "useCookProgress mirrors useShoppingState + reuses sync-queue.ts unchanged; keyed by (weekly_plan, step_instance)"
    - "check-off calls retimeSchedule (Plan 10), never generateSchedule; no reorder UI affordance exists"
    - "cook mode launches from Outputs page ('Start Cook Mode'), reading the same mealKeyedRecipeData Outputs.tsx already builds (line 370)"
---

<objective>
Build the interactive tablet cook mode: a `useCookProgress` optimistic hook (mirrors `useShoppingState`), a pure readiness-derivation helper, the `CookMode.tsx` page with now/next cards, live passive-step countdowns, readiness chips, and check-off that re-times (never reorders), plus a "Start Cook Mode" launch on the Outputs page (PREP-04). Includes a human checkpoint for the tablet experience + persistence.

Purpose: This is the prep-day surface that supersedes the flat BatchPrepTab checklist. Progress must survive refresh/sleep and stay calm (order stable) mid-cook.
Output: cook-progress hook, readiness helper (+test), cook mode page + 2 components, Outputs launch, route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-UI-SPEC.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/hooks/useShoppingState.ts
@recipe-planner/src/lib/sync-queue.ts
@recipe-planner/src/components/nodes/StepNode.tsx
@recipe-planner/src/components/outputs/BatchPrepTab.tsx
@recipe-planner/src/pages/Outputs.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCookProgress hook + pure readiness helper</name>
  <files>recipe-planner/src/hooks/useCookProgress.ts, recipe-planner/src/lib/scheduler/readiness.ts, recipe-planner/src/lib/scheduler/readiness.test.ts</files>
  <behavior>
    - deriveReadiness for an assembly step returns "waiting" listing each un-checked upstream producer, and "ready" only once ALL upstream producers are checked (AND-semantics, A4).
    - A step with no upstream producers is "ready" at t=0.
  </behavior>
  <read_first>
    - 05-PATTERNS.md useCookProgress section (mirror useShoppingState; CookProgressEntry { recordId, checked, checked_at }; filter weekly_plan + step_instance; reuse sync-queue.ts unchanged)
    - recipe-planner/src/hooks/useShoppingState.ts (full structural template) + recipe-planner/src/lib/sync-queue.ts (reused as-is)
    - 05-UI-SPEC.md Surface 1 readiness paragraph (binary chip, AND-semantics)
  </read_first>
  <action>
    Create useCookProgress(weeklyPlanId) as a direct analog of useShoppingState: state Map<stepInstanceKey, CookProgressEntry>, createSyncQueue over writable fields { checked, checked_at }, query-then-branch upsert filtered by `weekly_plan="${planId}" && step_instance="${key}"` against collections.cookProgress, load-on-mount by filter, optimistic setter. Do NOT modify sync-queue.ts. Separately create a pure readiness.ts exporting deriveReadiness(stepInstance, weekGraph, checkedSet) → { state: "waiting" | "ready", waitingOn: string[] } using the week-graph's incoming producer edges and the checked set (AND-semantics). Co-locate readiness.test.ts asserting the two behaviors. Keep readiness pure (no React/PB) so it is unit-testable.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/readiness.test.ts && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/hooks/useCookProgress.ts','utf8'); if(!/cookProgress/.test(t)||!/step_instance/.test(t)) throw new Error('hook not wired to cook_progress'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - readiness.test.ts passes (AND-semantics waiting/ready; no-upstream = ready).
    - useCookProgress is keyed by (weekly_plan, step_instance) against collections.cookProgress and reuses sync-queue.ts unmodified.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Persistence hook + pure readiness derivation ready for the UI.</done>
</task>

<task type="auto">
  <name>Task 2: CookMode page + NowNextCard + ReadinessChip + Outputs launch</name>
  <files>recipe-planner/src/pages/CookMode.tsx, recipe-planner/src/components/cook-mode/NowNextCard.tsx, recipe-planner/src/components/cook-mode/ReadinessChip.tsx, recipe-planner/src/pages/Outputs.tsx, recipe-planner/src/App.tsx</files>
  <read_first>
    - 05-UI-SPEC.md Surface 1 (now/next card treatment, 24px padding, tap-for-detail Collapse, 48px check-off, countdown MM:SS Display size, readiness chip colors, "no reorder affordance" contract) + Color/Typography/Copy sections ("Start Cook Mode", "No prep schedule yet", "Generate Prep Schedule")
    - 05-PATTERNS.md NowNextCard/ReadinessChip/CookMode sections (StepNode border/shadow/chip sx to reuse; BatchPrepTab checkbox 48px sx + EmptyState)
    - recipe-planner/src/pages/Outputs.tsx line ~370 (mealKeyedRecipeData already built — pass it to buildWeekGraph → generateSchedule)
    - recipe-planner/src/lib/scheduler/genetic.ts (generateSchedule), retime.ts (retimeSchedule), week-graph.ts (buildWeekGraph)
  </read_first>
  <action>
    Build CookMode.tsx: from the current weekly plan's mealKeyedRecipeData (same source Outputs.tsx builds at ~line 370), call buildWeekGraph then generateSchedule(weekGraph, schedulerConfig) to get the ordered timeline; render a now card (accent 2px primary.main border, boxShadow 3, 24px padding, step name Heading, scaled quantities via the Phase-4 scaleQuantity/peopleMultiplier path shown on tap via MUI Collapse) and a next card (1px #ccc border, no shadow). Check-off uses a 48px control (mirror CheckableListItem/BatchPrepTab sx) and calls useCookProgress + retimeSchedule(schedule.order, actualCompletions, config) — NEVER generateSchedule — so cards never reorder. Passive steps show a live MM:SS countdown at Display size in secondary.main orange, switching to a green "Ready" chip at zero. Assembly steps show ReadinessChip driven by deriveReadiness. Build NO drag/reorder/swap-position affordance. Empty state uses "No prep schedule yet" + "Generate Prep Schedule". On generateSchedule failure show "Couldn't build a schedule for this week. Check the linter for missing step metadata, then try again." NowNextCard.tsx and ReadinessChip.tsx reuse StepNode's exact border/shadow/chip sx blocks (waiting = outlined gray text.secondary, ready = filled accent green, passive = filled secondary.main orange). Add a "Start Cook Mode" button on the Outputs page and register the CookMode route in App.tsx. Do NOT delete BatchPrepTab.tsx (keep its print view per Topic 1). Never use color="success" for captions — use text.secondary.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/pages/CookMode.tsx','utf8'); if(!/retimeSchedule/.test(t)) throw new Error('check-off must retime'); if(/generateSchedule/.test(t)&&/onToggle|onCheck|handleCheck/i.test(t)&&!/retimeSchedule/.test(t)) throw new Error('check-off must not regenerate'); if(/drag|reorder|movePosition/i.test(t)) throw new Error('no reorder affordance allowed'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Check-off path calls retimeSchedule, not generateSchedule; no drag/reorder/swap affordance exists in CookMode.tsx.
    - Now/next cards reuse StepNode's border/shadow conventions; check-off control is 48px; countdown at Display size.
    - "Start Cook Mode" launch exists on Outputs; CookMode route registered; BatchPrepTab.tsx not deleted.
    - Captions use text.secondary, never color="success".
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Interactive tablet cook mode with retime-not-reorder check-off and readiness/countdown states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — tablet cook mode + persistence</name>
  <action>Human exercises cook mode on the tablet: confirms now/next cards, retime-not-reorder on check-off, passive countdown → Ready, readiness flip, and progress restored after refresh.</action>
  <what-built>Tablet cook mode: now/next cards, live countdowns, readiness chips, check-off that re-times without reordering, progress persisted to cook_progress.</what-built>
  <how-to-verify>
    1. Open Cook Mode for a weekly plan on the tablet (or a narrow window); confirm now/next cards render with scaled quantities on tap.
    2. Check off a step; confirm remaining times/countdowns recompute but NO card changes position in the sequence.
    3. Confirm a passive step shows an MM:SS countdown that turns into a green "Ready" chip at zero.
    4. Confirm an assembly step shows "waiting on: X, Y" that flips to "Ready" once its upstream steps are checked.
    5. Refresh the tablet; confirm checked-off state is restored (persistence via cook_progress).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. cards reordered on check-off, state lost on refresh).</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 11)
- Hook: `useCookProgress.ts`; pure helper: `readiness.ts` (+test)
- UI: `CookMode.tsx`, `NowNextCard.tsx`, `ReadinessChip.tsx`; "Start Cook Mode" launch on Outputs; CookMode route
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| cook mode → cook_progress write | Per-step check-off state persisted per plan/device |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11a | Tampering | concurrent cross-device check-off | low | mitigate | Query-then-branch upsert on unique (weekly_plan, step_instance) prevents duplicate records (mirrors shopping_state); sync-queue coalesces rapid writes |
| T-05-11b | Denial of Service | per-second countdown re-render | low | accept | Single interval driving lightweight card text; household-scale step count |
</threat_model>

<verification>
- `npx vitest run src/lib/scheduler/readiness.test.ts` green; `npx tsc --noEmit` clean.
- Human checkpoint: tablet cook mode, retime-not-reorder, countdown, readiness, refresh persistence.
</verification>

<success_criteria>
Interactive cook mode with persisted progress, live countdowns, binary readiness, and order-stable check-off recompute.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-11-SUMMARY.md` when done.
</output>
