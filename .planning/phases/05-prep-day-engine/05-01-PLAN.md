---
phase: 05-prep-day-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/src/lib/types.ts
  - recipe-planner/src/lib/api.ts
  - recipe-planner/pb_schema.json
  - recipe-planner/scripts/apply-phase5-schema.mjs
autonomous: true
requirements: [PREP-01, PREP-03, PREP-04, PREP-05]
must_haves:
  truths:
    - "recipe_steps carries active_minutes, passive_minutes, instructions, prep_action, resource, oven_temp_f, rack_slots (all additive + nullable) on both PB instances"
    - "cook_progress and scheduler_config collections exist on both PB instances"
    - "A single scheduler_config record exists with default weights (active weight boosted per D-06)"
    - "RecipeStep type + CookProgress + SchedulerConfig types compile and match the live schema"
  artifacts:
    - recipe-planner/scripts/apply-phase5-schema.mjs
    - recipe-planner/pb_schema.json
    - recipe-planner/src/lib/types.ts
    - recipe-planner/src/lib/api.ts
  key_links:
    - "types.ts RecipeStep field names === live PocketBase field names (round-trip depends on exact match)"
    - "api.ts collections.cookProgress/schedulerConfig string values === live collection names"
---

<objective>
Land the additive-nullable schema foundation for the whole phase: extend `recipe_steps` with 7 new fields, create the `cook_progress` and `scheduler_config` collections, seed the singleton config, and mirror everything into `types.ts` + `api.ts`. Every downstream plan (authoring, scheduler, cook mode, weights, backfill) reads these types.

Purpose: Without populated resource/duration fields there is nothing to schedule (PREP-03) and no place to persist cook progress (PREP-04) or weights (PREP-05); the existing 185 steps must validate immediately, so all additions are nullable.
Output: Live schema on `:8091` test + `:8090` prod, re-exported `pb_schema.json`, updated TypeScript types + collections map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prep-day-engine/05-CONTEXT.md
@.planning/phases/05-prep-day-engine/05-RESEARCH.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/types.ts
@recipe-planner/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply additive-nullable schema to both PocketBase instances</name>
  <files>recipe-planner/scripts/apply-phase5-schema.mjs, recipe-planner/pb_schema.json</files>
  <read_first>
    - recipe-planner/scripts/_tmp-verify-schema.mjs pattern referenced in STATE.md [Phase 04-01] (env-sourced, self-cleaning, never prints PB_SUPERUSER_* to shell)
    - recipe-planner/.env.local (gitignored superuser creds — source at runtime, never echo)
    - recipe-planner/pb_schema.json — current recipe_steps collection (pbc_1735492817) and the shopping_state collection as the additive-nullable precedent
    - 05-RESEARCH.md Code Examples "PocketBase schema addition pattern" for the exact field list
  </read_first>
  <action>
    Write an idempotent Node ESM script that authenticates as superuser (creds sourced from .env.local at runtime, never printed) against BOTH the test instance (192.168.50.95:8091, schema/code) and prod (192.168.50.95:8090, content), and applies these additions if not already present:
    (a) On recipe_steps: add active_minutes (number, nullable), passive_minutes (number, nullable), instructions (text, nullable), prep_action (select single, nullable, values sliced/diced/minced/chopped/grated/shredded), resource (select single, nullable, values oven/stovetop/blender/food_processor/instant_pot/none), oven_temp_f (number, nullable), rack_slots (number, nullable, default 1). All additive + nullable so the existing 185 steps validate unchanged (per D-01 schema-handling precedent).
    (b) Create collection cook_progress with fields: weekly_plan (relation to weekly_plans, required), step_instance (text, required), checked (bool), checked_at (text/date, nullable). Add a unique index on (weekly_plan, step_instance) mirroring shopping_state's (weekly_plan, line_key) uniqueness so the query-then-branch upsert is race-safe (D-03).
    (c) Create collection scheduler_config (singleton) with fields: seed (number), weights (json), burner_count (number), oven_rack_slots (number), appliances (json). Then upsert exactly one record with defaults: seed 12345, weights {active:8, chopping:3, grouping:3, elapsed:4, resource_pressure:3} (active deliberately boosted above the others because minimizing active-session span is the PRIMARY objective per D-06), burner_count 4, oven_rack_slots 2, appliances ["blender","food_processor","instant_pot"] (D-04).
    The script must be safe to re-run (check field/collection existence before create). Do NOT add any lead_time_minutes field (D-02 cut the day-before horizon entirely). After applying, re-export the test instance schema to recipe-planner/pb_schema.json. Delete no existing fields.
  </action>
  <verify>
    <automated>cd recipe-planner && node scripts/apply-phase5-schema.mjs && node -e "const s=require('./pb_schema.json'); const rs=s.find(c=>c.name==='recipe_steps').fields.map(f=>f.name); const need=['active_minutes','passive_minutes','instructions','prep_action','resource','oven_temp_f','rack_slots']; const miss=need.filter(n=>!rs.includes(n)); if(miss.length) throw new Error('missing '+miss); if(!s.find(c=>c.name==='cook_progress')||!s.find(c=>c.name==='scheduler_config')) throw new Error('missing collection'); console.log('schema ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Re-running the script is a no-op (prints already-present, exits 0) — proven by running it twice.
    - pb_schema.json contains all 7 new recipe_steps fields plus cook_progress and scheduler_config collections.
    - A GET on scheduler_config against test returns exactly one record whose weights.active is strictly greater than every other weight value.
    - No lead_time_minutes field exists anywhere (grep pb_schema.json for lead_time_minutes returns nothing).
    - No PB_SUPERUSER_* value appears in any committed file or script output.
  </acceptance_criteria>
  <done>Both instances carry the 7 nullable step fields + 2 new collections + seeded singleton; pb_schema.json re-exported; existing 185 steps still validate.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RecipeStep and add CookProgress + SchedulerConfig types</name>
  <files>recipe-planner/src/lib/types.ts</files>
  <read_first>
    - recipe-planner/src/lib/types.ts lines 98-116 (StepType, Timing, RecipeStep) and the ShoppingState interface (~line 244) as the new-collection template
    - 05-PATTERNS.md section "recipe-planner/src/lib/types.ts (modify)" for the exact field list and shapes
  </read_first>
  <action>
    Add the 7 new nullable fields to the RecipeStep interface exactly matching the live schema field names from Task 1: active_minutes?: number; passive_minutes?: number; instructions?: string; prep_action?: string; resource?: "oven" | "stovetop" | "blender" | "food_processor" | "instant_pot" | "none"; oven_temp_f?: number; rack_slots?: number. Add a new interface CookProgress extends BaseRecord with weekly_plan: string, step_instance: string, checked: boolean, checked_at: string | null (per D-03 — no have_quantity/resolution; cook_progress has no have-N/resolution concept). Add a new interface SchedulerConfig extends BaseRecord with seed: number, weights: { active: number; chopping: number; grouping: number; elapsed: number; resource_pressure: number }, burner_count: number, oven_rack_slots: number, appliances: string[] (D-04 singleton — no weekly_plan relation). Cite the decision IDs (D-01/D-03/D-04) in a short comment above each new interface per this codebase's module-header convention.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with no new errors.
    - RecipeStep exposes all 7 optional fields; resource is a string-literal union, not bare string.
    - CookProgress and SchedulerConfig are exported and extend BaseRecord.
  </acceptance_criteria>
  <done>types.ts mirrors the live schema; downstream plans can import RecipeStep/CookProgress/SchedulerConfig.</done>
</task>

<task type="auto">
  <name>Task 3: Register cook_progress + scheduler_config in the collections map</name>
  <files>recipe-planner/src/lib/api.ts</files>
  <read_first>
    - recipe-planner/src/lib/api.ts collections map (the `as const` object ending with templateSlots)
    - 05-PATTERNS.md section "recipe-planner/src/lib/api.ts (modify)"
  </read_first>
  <action>
    Add two entries to the collections const object following the exact camelCase-key / snake_case-value convention already used: cookProgress: "cook_progress" and schedulerConfig: "scheduler_config". The generic getAll/create/update/remove helpers already handle any collection and need no change.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/lib/api.ts','utf8'); if(!/cookProgress:\s*\"cook_progress\"/.test(t)||!/schedulerConfig:\s*\"scheduler_config\"/.test(t)) throw new Error('collections not registered'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - collections.cookProgress === "cook_progress" and collections.schedulerConfig === "scheduler_config".
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Both new collections are addressable via the collections map.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01)
- Script: `recipe-planner/scripts/apply-phase5-schema.mjs`
- Schema: 7 new `recipe_steps` fields; new collections `cook_progress`, `scheduler_config`; seeded singleton config record
- Types: `RecipeStep` (+7 fields), `CookProgress`, `SchedulerConfig`
- Collections: `collections.cookProgress`, `collections.schedulerConfig`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → PocketBase superuser API | Schema mutation on live prod content DB using superuser creds |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01a | Tampering | apply-phase5-schema.mjs on prod :8090 | high | mitigate | Additive-nullable only, no field deletes; existence-checked idempotent create; script proven re-runnable no-op before any prod run |
| T-05-01b | Information Disclosure | .env.local superuser creds | high | mitigate | Creds sourced at runtime from gitignored .env.local, never printed, never committed (STATE.md Phase 04-01 precedent) |
| T-05-01c | Denial of Service | seeded singleton scheduler_config | low | accept | Single low-frequency record; malformed weights only affect local schedule quality, not availability |
</threat_model>

<verification>
- `node scripts/apply-phase5-schema.mjs` run twice → second run is a no-op.
- `npx tsc --noEmit` clean.
- Manual/UAT (per repo convention, no PB-integration harness): edit a step in the editor against :8091, confirm the 7 fields round-trip.
</verification>

<success_criteria>
Schema live on both instances, singleton seeded with active-boosted weights, types + collections compile and match live field names, no lead_time_minutes anywhere.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-01-SUMMARY.md` when done.
</output>
