---
phase: 05-prep-day-engine
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - recipe-planner/package.json
  - recipe-planner/package-lock.json
  - recipe-planner/src/lib/scheduler/types.ts
  - recipe-planner/src/lib/scheduler/week-graph.test.ts
  - recipe-planner/src/lib/scheduler/resources.test.ts
  - recipe-planner/src/lib/scheduler/genetic.test.ts
  - recipe-planner/src/lib/scheduler/retime.test.ts
  - recipe-planner/src/lib/linter/linter.test.ts
autonomous: true
requirements: [PREP-03, PREP-04, PREP-06]
must_haves:
  truths:
    - "prando ^6.0.1 is installed and importable (approved OK on the package-legitimacy audit)"
    - "scheduler/types.ts defines StepInstance, WeekGraphEdge, WeekGraph, Schedule, ResourceTimeline, SchedulerConfig scheduler types"
    - "5 Wave-0 test files exist and FAIL (red) with concrete assertions the later waves must satisfy"
  artifacts:
    - recipe-planner/src/lib/scheduler/types.ts
    - recipe-planner/src/lib/scheduler/week-graph.test.ts
    - recipe-planner/src/lib/scheduler/resources.test.ts
    - recipe-planner/src/lib/scheduler/genetic.test.ts
    - recipe-planner/src/lib/scheduler/retime.test.ts
  key_links:
    - "StepInstance.id format `${plannedMealId}::${step.id}` === cook_progress.step_instance key === useCookProgress filter key"
    - "Scheduler test assertions encode D-06 (active-session-span), determinism, and D-05 resource rules as the acceptance contract"
---

<objective>
Install the one new dependency (`prando`), define the scheduler's shared type vocabulary, and lay down the 5 Wave-0 red test files so every later wave has a failing, executable acceptance contract to turn green. This is the phase's Nyquist backbone (05-VALIDATION.md).

Purpose: Determinism, resource constraints, week-graph edges, retime-not-reorder, and the 3 linter rules are all pure-function-testable; encoding them as red tests first prevents "plausible but wrong" scheduler output (RESEARCH Pitfall 1/4).
Output: `prando` dependency, `scheduler/types.ts`, and 5 red test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-RESEARCH.md
@.planning/phases/05-prep-day-engine/05-VALIDATION.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/aggregation/types.ts
@recipe-planner/src/lib/sync-queue.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install prando and define scheduler/types.ts</name>
  <files>recipe-planner/package.json, recipe-planner/src/lib/scheduler/types.ts</files>
  <read_first>
    - 05-RESEARCH.md Package Legitimacy Audit (prando verdict OK, v6.0.1, no postinstall) — NO human checkpoint needed; it is not [ASSUMED]/[SUS]
    - 05-RESEARCH.md Recommended Project Structure + Pattern 1/2 for the StepInstance/WeekGraph/ResourceTimeline/Schedule shapes
    - recipe-planner/src/lib/aggregation/types.ts (RecipeGraphData, MealKeyedRecipeData) — the input the week-graph consumes
    - recipe-planner/src/lib/types.ts (RecipeStep, SchedulerConfig from Plan 01)
  </read_first>
  <action>
    Run `npm install prando` in recipe-planner. Create recipe-planner/src/lib/scheduler/types.ts exporting: StepInstance { id: string (format `${plannedMealId}::${step.id}`); plannedMealId: string; step: RecipeStep; recipeName: string }; WeekGraphEdge { from: string; to: string }; WeekGraph { nodes: StepInstance[]; edges: WeekGraphEdge[] }; Schedule { order: StepInstance[]; starts: Map<string, number>; ends: Map<string, number> }; ResourceTimeline { cookBusy, ovenUsage, activeBurners, singletonAppliances } per RESEARCH Pattern 2; and re-export or reference the SchedulerConfig weights shape from lib/types. Add a module-header comment citing D-03/D-04/D-05/D-06. Do NOT route through aggregation/utils/step-utils.ts — these types describe per-instance nodes only.
  </action>
  <verify>
    <automated>cd recipe-planner && node -e "require('prando')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - prando appears in package.json dependencies at ^6.0.1.
    - scheduler/types.ts exports StepInstance, WeekGraphEdge, WeekGraph, Schedule, ResourceTimeline.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Scheduler type vocabulary compiles; prando importable.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 red tests for week-graph, resources, genetic, retime</name>
  <files>recipe-planner/src/lib/scheduler/week-graph.test.ts, recipe-planner/src/lib/scheduler/resources.test.ts, recipe-planner/src/lib/scheduler/genetic.test.ts, recipe-planner/src/lib/scheduler/retime.test.ts</files>
  <read_first>
    - 05-VALIDATION.md Per-Task Verification Map + Wave 0 Requirements (the exact behaviors each file must assert)
    - 05-RESEARCH.md Patterns 1-4, Pitfalls 1-3, Assumptions A1/A4, and D-06 (active-session-span)
    - recipe-planner/src/lib/aggregation/aggregation-lineid.test.ts for the in-repo fixture-building convention (inline Map<string, RecipeGraphData>)
  </read_first>
  <action>
    Author four Vitest files importing the not-yet-existing implementation modules (import failure or assertion failure = red, as intended). Each must assert concrete behavior:
    week-graph.test.ts: buildWeekGraph on a 2-meal fixture where meal A's stored/inventory output product equals meal B's input product produces a cross-recipe edge from A's producer instance to B's consumer instance; producer-absent leaves the consumer as a source node with no incoming edge; multiple producers fan in an edge from EVERY matching producer (AND-semantics, A4); the same recipe planned twice yields TWO separate step instances (never merged — Pitfall 3).
    resources.test.ts: isFeasibleAt rejects two overlapping active windows (single implicit cook, active-only); accepts a passive window overlapping another step's active window; rejects two different oven_temp_f steps overlapping regardless of rack capacity (Pitfall 2 / AC4); enforces oven rack_slots capacity at equal temp; enforces burner_count; enforces singleton appliance capacity 1.
    genetic.test.ts: same (seed, weights, plan) produces a byte-identical Schedule across two runs (determinism regression, tagged "determinism"); output violates no precedence edge and no resource constraint (tagged "no violations"); a schedule that packs active work into passive windows scores better on the primary objective than one that serializes all active work (encodes D-06 active-session-span = max(activeEnd) − min(activeStart)); two different weight vectors can produce different orders (PREP-05).
    retime.test.ts: given a fixed order and one late actual completion, downstream start times shift by the delta and the ORDER array is unchanged (order-preserving, D-01a.3).
    Use inline fixtures; do not depend on live PocketBase.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler 2>&1 | grep -Eq "fail|FAIL|No test files|error" && echo "red-as-expected" || (echo "tests unexpectedly green/empty" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under src/lib/scheduler/ and reference the D-06 active-session-span objective, determinism, and each D-05 resource rule by concrete assertion.
    - Running the scheduler suite fails (red) because the implementation modules do not yet exist.
    - No test uses vitest watch mode; all use `vitest run`.
  </acceptance_criteria>
  <done>Four red scheduler test files encode the acceptance contract for waves 3-4.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 red tests for the 3 linter v2 rules</name>
  <files>recipe-planner/src/lib/linter/linter.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/linter/linter.test.ts (existing suite + its fixture pattern)
    - recipe-planner/src/lib/linter/index.ts (LintFinding shape, runLint aggregator)
    - 05-CONTEXT.md D-07 (missing-pull-step runs at WHOLE-PLANNED-WEEK scope via the week-graph builder's cross-recipe edges; the other two rules stay per-recipe/per-step)
  </read_first>
  <action>
    Extend linter.test.ts with red cases for the 3 new rules: missing-durations flags a step with both active_minutes and passive_minutes null and does NOT flag a step with either populated; missing-prep-action flags a prep-type step whose prep_action is null and ignores assembly steps; missing-pull-step (week-scoped) flags a stored/inventory input consumed by an assembly step when NO recipe in the planned week produces it, and does NOT flag when another recipe in the week produces it (D-07 cross-recipe reuse of the week-graph producer→consumer edges). Import the (not-yet-existing) rule functions and the week-scoped aggregator entry point so the cases fail red.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/linter/linter.test.ts 2>&1 | grep -Eq "fail|FAIL|error" && echo "red-as-expected" || (echo "linter tests unexpectedly green" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - linter.test.ts contains red cases for missing-durations, missing-prep-action, and week-scoped missing-pull-step (positive AND negative case each).
    - The missing-pull-step case is week-scoped (a producer in a DIFFERENT recipe suppresses the finding), per D-07.
    - Suite runs red (rules not yet implemented).
  </acceptance_criteria>
  <done>Linter v2 acceptance contract is a failing test the Plan 07 implementation turns green.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 02)
- Dependency: `prando` ^6.0.1
- Types: `StepInstance`, `WeekGraphEdge`, `WeekGraph`, `Schedule`, `ResourceTimeline` (scheduler/types.ts)
- Red test files: week-graph.test.ts, resources.test.ts, genetic.test.ts, retime.test.ts; extended linter.test.ts
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → build | New dependency install (prando) |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-SC | Tampering | npm install prando | low | accept | Package legitimacy audit verdict OK (63.4K/wk, canonical github.com/zeh/prando, no postinstall script); no [ASSUMED]/[SUS] packages in this phase, so no blocking-human checkpoint required |
</threat_model>

<verification>
- `node -e "require('prando')"` succeeds.
- `npx vitest run src/lib/scheduler` and `src/lib/linter/linter.test.ts` both run RED (implementations pending).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
prando installed, scheduler types compile, 5 Wave-0 test files fail red with concrete D-06/determinism/resource/linter assertions.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-02-SUMMARY.md` when done.
</output>
