---
phase: 05-prep-day-engine
plan: 12
type: execute
wave: 6
depends_on: ["05-09", "05-11"]
files_modified:
  - recipe-planner/src/lib/scheduler/scheduler-config.ts
  - recipe-planner/src/components/cook-mode/WeightsPanel.tsx
  - recipe-planner/src/pages/CookMode.tsx
autonomous: false
requirements: [PREP-05]
must_haves:
  truths:
    - "User can tune 5 scheduler weights (active, chopping, grouping, elapsed, resource_pressure) in-app and regenerate the plan"
    - "Weight changes persist to the scheduler_config singleton on release (debounced), shared across devices"
    - "Regenerate re-invokes the GA with the same seed unless reseeded; regenerating with checked-off steps warns first and keeps checked steps"
    - "The active weight leads and defaults higher than the others (D-06 primary objective)"
  artifacts:
    - recipe-planner/src/lib/scheduler/scheduler-config.ts
    - recipe-planner/src/components/cook-mode/WeightsPanel.tsx
  key_links:
    - "WeightsPanel writes scheduler_config; both the panel and generateSchedule read the same singleton"
    - "Regenerate calls generateSchedule with the persisted seed (deterministic same seed unless reseeded)"
---

<objective>
Add the in-app scheduler tuning surface: a `scheduler_config` load/save helper and a `WeightsPanel` with 5 sliders (active leading and boosted by default per D-06) plus a "Regenerate Plan" action that re-runs the GA deterministically, with a confirmation when steps are already checked off (PREP-05).

Purpose: The GA fitness weights are user-tunable so a wrong grouping/consolidation guess is cheap to correct (A3); regenerate is deterministic given the persisted seed.
Output: `scheduler-config.ts`, `WeightsPanel.tsx`, mounted in CookMode with the regenerate flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-UI-SPEC.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/api.ts
@recipe-planner/src/lib/scheduler/genetic.ts
@recipe-planner/src/pages/CookMode.tsx
@recipe-planner/src/components/outputs/BatchPrepTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: scheduler_config load/save helper + WeightsPanel</name>
  <files>recipe-planner/src/lib/scheduler/scheduler-config.ts, recipe-planner/src/components/cook-mode/WeightsPanel.tsx</files>
  <read_first>
    - 05-UI-SPEC.md Surface 2 (5 sliders 0-10, active listed first + default-boosted, value readout at Heading size, "Regenerate Plan" full-width accent, write-on-release debounced, no live preview)
    - 05-PATTERNS.md WeightsPanel section (BatchPrepTab full-width primary Button treatment; scheduler_config does NOT need sync-queue — plain getAll/update debounced on release)
    - recipe-planner/src/lib/api.ts collections.schedulerConfig + getAll/update helpers
    - recipe-planner/src/lib/types.ts SchedulerConfig (from Plan 01)
  </read_first>
  <action>
    Create scheduler-config.ts: loadSchedulerConfig() → getAll(collections.schedulerConfig)[0] (the singleton), and saveSchedulerConfig(patch) → update on that record's id. No sync-queue needed (low-frequency singleton write). Create WeightsPanel.tsx: 5 MUI Sliders (range 0-10 integer) for active, chopping, grouping, elapsed, resource_pressure, with active listed FIRST and defaulting higher than the others (D-06); each slider labeled Body size with its current value at Heading size to the right (no separate input box). Slider changes persist to scheduler_config on release (debounced), never on every drag tick, and do NOT live-regenerate. Slider thumbs meet the 48px touch minimum. A "Regenerate Plan" button (full-width, contained, color primary, matches BatchPrepTab's Print button treatment) is the only trigger to recompute.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/components/cook-mode/WeightsPanel.tsx','utf8'); ['active','chopping','grouping','elapsed','resource_pressure'].forEach(w=>{if(!t.includes(w))throw new Error('missing slider '+w)}); if(!/Regenerate Plan/.test(t)) throw new Error('missing regenerate CTA'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - 5 sliders present, active first, range 0-10; scheduler_config write is debounced on release (no per-tick write, no live regenerate).
    - saveSchedulerConfig updates the singleton; loadSchedulerConfig returns it.
    - "Regenerate Plan" is a full-width accent contained button; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Persisted, tunable weights with an explicit regenerate trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Wire regenerate flow into CookMode with checked-off confirmation</name>
  <files>recipe-planner/src/pages/CookMode.tsx</files>
  <read_first>
    - 05-UI-SPEC.md Surface 2 "No live preview" + destructive-adjacent confirmation copy ("Regenerating will keep your checked-off steps but recalculate remaining timing. Continue?" — Cancel / Regenerate, accent not error)
    - recipe-planner/src/pages/CookMode.tsx (schedule state + generateSchedule call from Plan 11)
    - recipe-planner/src/lib/scheduler/genetic.ts generateSchedule (same seed unless reseeded)
  </read_first>
  <action>
    Mount WeightsPanel inside CookMode (panel or drawer). Wire "Regenerate Plan" to reload scheduler_config, rebuild the schedule via generateSchedule(weekGraph, config) using the persisted seed (deterministic — same seed unless the user explicitly reseeds), and replace the displayed timeline. If any step is already checked off, show the existing confirm-dialog pattern with the exact copy "Regenerating will keep your checked-off steps but recalculate remaining timing. Continue?" and an accent-green "Regenerate" action (not error red) — on confirm, regenerate while preserving checked-off cook_progress; on cancel, do nothing. Regenerating with no checked steps needs no confirmation.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/pages/CookMode.tsx','utf8'); if(!/WeightsPanel/.test(t)) throw new Error('panel not mounted'); if(!/recalculate remaining timing/.test(t)) throw new Error('missing regenerate confirm copy'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - WeightsPanel is mounted in CookMode; Regenerate re-runs generateSchedule with the persisted seed (deterministic).
    - Regenerating with checked-off steps shows the exact confirmation copy with an accent (not error) action and preserves checked progress.
    - Regenerating with no checked steps skips the confirmation.
  </acceptance_criteria>
  <done>Deterministic in-app regenerate with a non-destructive checked-off confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — weights tuning + deterministic regenerate</name>
  <action>Human tunes weights, regenerates (confirming determinism on an unchanged re-run), verifies the checked-off confirmation dialog styling/behavior, and confirms weights persist across devices.</action>
  <what-built>In-cook-mode weights panel with 5 sliders and a deterministic "Regenerate Plan" flow that persists weights to scheduler_config and warns before regenerating over checked-off steps.</what-built>
  <how-to-verify>
    1. Open Cook Mode, open the weights panel; confirm 5 sliders with "active" first and defaulting higher than the others.
    2. Change a weight, release; tap "Regenerate Plan" and confirm the timeline changes.
    3. Tap "Regenerate Plan" again WITHOUT changing weights (same seed); confirm the schedule is identical (deterministic).
    4. Check off a step, then tap "Regenerate Plan"; confirm the "Regenerating will keep your checked-off steps..." dialog appears with an accent (not red) Regenerate button, and checked steps stay checked after regenerating.
    5. Reload on another device; confirm the tuned weights persisted (scheduler_config shared).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. non-deterministic regenerate, weights not persisted, wrong confirmation styling).</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 12)
- Helper: `recipe-planner/src/lib/scheduler/scheduler-config.ts` (loadSchedulerConfig/saveSchedulerConfig)
- Component: `recipe-planner/src/components/cook-mode/WeightsPanel.tsx` (5 sliders + Regenerate)
- Regenerate flow wired into `CookMode.tsx` with checked-off confirmation
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| weights panel → scheduler_config write | User-tuned weights persisted to the shared singleton |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12a | Tampering | slider write storm to scheduler_config | low | mitigate | Debounced write-on-release (not per drag tick); plain update on the singleton, no queue needed |
| T-05-12b | Tampering | regenerate discarding progress | medium | mitigate | Confirmation dialog before regenerating over checked-off steps; checked-off cook_progress preserved through regenerate |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Human checkpoint: tune + regenerate determinism + checked-off confirmation + cross-device persistence.
</verification>

<success_criteria>
User tunes weights (active-led) in-app, regenerates deterministically with the persisted seed, and is warned (non-destructively) before regenerating over checked-off steps.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-12-SUMMARY.md` when done.
</output>
