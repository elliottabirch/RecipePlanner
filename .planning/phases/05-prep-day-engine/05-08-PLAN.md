---
phase: 05-prep-day-engine
plan: 08
type: execute
wave: 3
depends_on: ["05-01", "05-04"]
files_modified:
  - recipe-planner/src/lib/backfill/diff.ts
  - recipe-planner/src/lib/backfill/diff.test.ts
  - recipe-planner/src/pages/StepBackfill.tsx
  - recipe-planner/src/App.tsx
autonomous: false
requirements: [PREP-02]
must_haves:
  truths:
    - "The review page renders a per-recipe-batch draft-vs-current diff for every step in the draft"
    - "Saving a reviewed batch writes ONLY accepted/edited values and is idempotent — a re-run shows 0 items for an already-complete recipe"
    - "Rejecting a draft field discards only the AI suggestion (no confirmation dialog, nothing user-authored lost)"
  artifacts:
    - recipe-planner/src/lib/backfill/diff.ts
    - recipe-planner/src/pages/StepBackfill.tsx
  key_links:
    - "computeBackfillWriteSet(draft, currentSteps) is the pure, tested core; the page is a thin shell over it"
    - "write-set only touches steps still missing fields (idempotency) — keyed by recipe_steps.id from the Plan-04 draft"
---

<objective>
Build the in-app backfill review flow: a pure diff/apply core (`computeBackfillWriteSet`) plus a `StepBackfill.tsx` page that renders the Plan-04 draft against current steps per recipe batch, lets the user Accept/Edit per field, and writes only approved values idempotently (PREP-02). Includes a human checkpoint to review one batch and confirm the write + idempotent re-run.

Purpose: Drafts are estimates; the human review gate is the mitigation (RESEARCH Pitfall 4). The write must be atomic-per-batch and idempotent so re-running backfill never clobbers reviewed data.
Output: `lib/backfill/diff.ts` (+test), `StepBackfill.tsx` page + route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-UI-SPEC.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/pages/registries/Products.tsx
@recipe-planner/src/lib/api.ts
@recipe-planner/src/data/step-backfill-draft.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure computeBackfillWriteSet diff/apply core + test</name>
  <files>recipe-planner/src/lib/backfill/diff.ts, recipe-planner/src/lib/backfill/diff.test.ts</files>
  <behavior>
    - Given a draft entry and a current step already fully populated, the write-set excludes that step (idempotency).
    - Given a draft entry and a current step missing fields, the write-set includes only the accepted fields.
    - Editing a field value before accept puts the edited value (not the draft value) in the write-set.
    - A rejected field is absent from the write-set.
  </behavior>
  <read_first>
    - 05-VALIDATION.md PREP-02 row (factor the diff/apply logic into a pure testable function separate from the React page)
    - recipe-planner/src/data/step-backfill-draft.json (draft entry shape from Plan 04)
    - recipe-planner/src/lib/types.ts RecipeStep (target field names)
  </read_first>
  <action>
    Implement computeBackfillWriteSet(draftEntries, currentSteps, decisions) → a map of recipe_steps.id to the partial field object to write. A step is included only if it is still missing at least one field the draft supplies (idempotency: fully-populated steps are excluded). decisions carries per-field accept/edit/reject; only accepted (or edited) fields enter the write object; rejected fields are omitted; edited fields use the edited value. Write co-located diff.test.ts asserting the four behaviors above. Pure function only — no PocketBase calls, no React.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/backfill/diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - diff.test.ts passes all four behaviors including idempotency (fully-populated step excluded).
    - computeBackfillWriteSet is pure (no imports from api.ts / react).
  </acceptance_criteria>
  <done>The backfill write-set logic is tested and page-independent.</done>
</task>

<task type="auto">
  <name>Task 2: StepBackfill review page + route</name>
  <files>recipe-planner/src/pages/StepBackfill.tsx, recipe-planner/src/App.tsx</files>
  <read_first>
    - 05-UI-SPEC.md Surface 4 (per-recipe-batch two-column draft-vs-current diff, weight-only change highlight, Accept/Edit per field, "Save Reviewed Batch" disabled until every step decided, "All caught up" empty state, no Reject confirmation, atomic no-partial-write error copy)
    - 05-PATTERNS.md StepBackfill.tsx section (Products.tsx Lint Findings Dialog list/approve shape; recipe-import draft-then-review process)
    - recipe-planner/src/App.tsx routing (how pages are registered)
  </read_first>
  <action>
    Build StepBackfill.tsx: import the Plan-04 draft JSON, fetch current recipe_steps via getAll(collections.recipeSteps), group by recipe into batches, and for each step render a two-column draft-vs-current diff at Body size with changed fields bold-weighted (700, no color-coding — color is reserved). Per field: Accept (default) / Edit (inline TextField/Select matching the authoring dialog counterpart). "Save Reviewed Batch" is accent-green contained, disabled until every step in the batch has an explicit decision; on tap it calls computeBackfillWriteSet then update(collections.recipeSteps, id, fields) per entry, atomically per batch — on any failure write nothing and show "Couldn't save this batch. Nothing was written — try again." A saved batch drops from the queue; an all-complete recipe shows the "All caught up" empty state. Reject discards the draft with NO confirmation dialog (disposable AI draft). All tap targets 48px minimum. Register a route for the page in App.tsx.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/pages/StepBackfill.tsx','utf8'); if(!/computeBackfillWriteSet/.test(t)) throw new Error('page must use the pure core'); if(!/All caught up|Save Reviewed Batch/.test(t)) throw new Error('missing UI-SPEC copy'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Page uses computeBackfillWriteSet (thin shell over the tested core).
    - "Save Reviewed Batch" disabled until every step decided; batch write is atomic (no partial write on failure).
    - "All caught up" empty state present; Reject has no confirmation dialog.
    - Route registered; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Reviewable, idempotent, atomic backfill page wired into the app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — batched backfill review + idempotency</name>
  <action>Human reviews one recipe batch against the test DB, saves it, and confirms writes persisted, the rejected field stayed blank, and re-opening shows "All caught up" (idempotent).</action>
  <what-built>Backfill review page consuming the offline draft, writing only approved values idempotently per recipe batch against the test DB (:8091).</what-built>
  <how-to-verify>
    1. Point the app at the test instance (:8091) and open the Step Backfill page.
    2. Review one recipe batch: Accept most fields, Edit one duration, Reject one field.
    3. Tap "Save Reviewed Batch"; confirm the edited/accepted values persisted on those steps in the recipe editor and the rejected field stayed blank.
    4. Re-open Step Backfill: confirm that recipe now shows "All caught up" (0 items) — proving idempotency.
    5. Confirm no "Are you sure" dialog appeared when rejecting a field.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. non-idempotent re-run, partial write on failure).</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 08)
- Pure core: `recipe-planner/src/lib/backfill/diff.ts` (computeBackfillWriteSet) + test
- Page: `recipe-planner/src/pages/StepBackfill.tsx` + route in `App.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| draft JSON + user edits → recipe_steps write | AI-drafted + human-reviewed values written to prod content |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08a | Tampering | batch write to recipe_steps | high | mitigate | Human review gate before any write; atomic per-batch write (no partial write on failure); idempotent (only touches steps missing fields); PB select-enum validation on resource/prep_action |
| T-05-08b | Denial of Service | large draft in the browser | low | accept | ~185 steps is small; batched by recipe so the page never renders the whole set at once |
</threat_model>

<verification>
- `npx vitest run src/lib/backfill/diff.test.ts` green.
- `npx tsc --noEmit` clean.
- Human checkpoint: batch review + idempotent re-run confirmed against :8091.
</verification>

<success_criteria>
Reviewable draft-vs-current backfill with atomic, idempotent, approval-only writes; human-confirmed on the test DB.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-08-SUMMARY.md` when done.
</output>
