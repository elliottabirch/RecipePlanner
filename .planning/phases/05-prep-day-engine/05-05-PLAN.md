---
phase: 05-prep-day-engine
plan: 05
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - recipe-planner/src/lib/scheduler/week-graph.ts
autonomous: true
requirements: [PREP-03]
must_haves:
  truths:
    - "buildWeekGraph produces one node per (plannedMealId, step.id) — the same recipe planned twice yields two instances, never merged"
    - "Cross-recipe precedence edges connect a stored/inventory output product to the same product's consumer in another planned meal"
    - "Producer-absent inputs are graph sources (no edge); multiple producers fan in an edge from every match (AND-semantics)"
  artifacts:
    - recipe-planner/src/lib/scheduler/week-graph.ts
  key_links:
    - "week-graph consumes MealKeyedRecipeData directly, NEVER aggregation/utils/step-utils.ts createStepSignature/addOrMergeStep (documented anti-pattern that collapses per-meal instances)"
    - "output WeekGraph feeds both genetic.ts (Plan 09) and missing-pull-step linter (Plan 07)"
---

<objective>
Build the scheduler's per-instance week-graph from the existing `MealKeyedRecipeData`, adding intra-recipe precedence edges and cross-recipe stored/inventory producer→consumer edges. This is the DAG the GA schedules over and the graph the week-scoped pull-step linter reasons against (PREP-03).

Purpose: The scheduler must see separate step instances (so "chopping consolidation" and per-meal resource accounting are possible) and cross-recipe dependencies (the chicken-stock case). Turns green the red week-graph.test.ts from Plan 02.
Output: `lib/scheduler/week-graph.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-RESEARCH.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/aggregation/types.ts
@recipe-planner/src/lib/scheduler/types.ts
@recipe-planner/src/lib/scheduler/week-graph.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement buildWeekGraph over MealKeyedRecipeData</name>
  <files>recipe-planner/src/lib/scheduler/week-graph.ts</files>
  <read_first>
    - 05-RESEARCH.md Pattern 1 (per-instance nodes, edge cases) + Pitfall 3 (signature-merge trap)
    - 05-PATTERNS.md week-graph.ts section (build FROM RecipeGraphData; AVOID createStepSignature/addOrMergeStep)
    - recipe-planner/src/lib/aggregation/types.ts (RecipeGraphData: productNodes, steps, productToStepEdges, stepToProductEdges)
    - recipe-planner/src/lib/scheduler/week-graph.test.ts (the exact assertions to satisfy)
  </read_first>
  <action>
    Implement buildWeekGraph(mealData: MealKeyedRecipeData): WeekGraph. For each (plannedMealId, RecipeGraphData): create one StepInstance per step with id `${plannedMealId}::${step.id}`, recipeName from the recipe. Derive intra-recipe precedence edges from productToStepEdges/stepToProductEdges (a step that consumes a product produced by another step in the same meal gets an edge). Then derive cross-recipe edges: for every consuming INPUT product node whose product.type is stored or inventory, find ALL producing OUTPUT nodes across every OTHER planned meal whose product id matches, and add an edge from each producing step instance to the consuming step instance (fan-in AND-semantics, A4). If no producer is found, leave the input as a source node (no edge) — surfacing the missing producer is the linter's job (Plan 07), not a builder error. Never import or call aggregation/utils/step-utils.ts createStepSignature/addOrMergeStep — build directly from the raw per-recipe structures so two instances of the same recipe remain two nodes. Add a module-header comment citing PREP-03 + the anti-pattern avoided.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/week-graph.test.ts && node -e "const t=require('fs').readFileSync('src/lib/scheduler/week-graph.ts','utf8'); if(/createStepSignature|addOrMergeStep/.test(t)) throw new Error('routes through signature-merge anti-pattern'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - week-graph.test.ts passes (cross-recipe edge, producer-absent source, fan-in AND, twice-planned = two instances).
    - week-graph.ts does not reference createStepSignature or addOrMergeStep.
    - Node ids follow the `${plannedMealId}::${step.id}` format.
  </acceptance_criteria>
  <done>Per-instance week-graph with correct intra- and cross-recipe edges; the anti-pattern is provably avoided.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 05)
- Module: `recipe-planner/src/lib/scheduler/week-graph.ts` (buildWeekGraph)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory transform over already-fetched data |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05a | Denial of Service | buildWeekGraph on a large week | low | accept | Household scale ~10-40 instances; O(n²) product-match is trivially bounded (RESEARCH Pitfall 5) |
</threat_model>

<verification>
- `npx vitest run src/lib/scheduler/week-graph.test.ts` green.
- Anti-pattern grep clean.
</verification>

<success_criteria>
Deterministic per-instance week-graph with correct precedence and cross-recipe edges; signature-merge anti-pattern avoided.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-05-SUMMARY.md` when done.
</output>
