---
phase: 05-prep-day-engine
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - recipe-planner/src/pages/RecipeEditor.tsx
  - recipe-planner/src/components/nodes/StepNode.tsx
autonomous: true
requirements: [PREP-01]
must_haves:
  truths:
    - "The Edit Step dialog persists active_minutes, passive_minutes, instructions, prep_action, resource, oven_temp_f, rack_slots"
    - "prep_action only shows for prep steps; oven_temp_f only shows (and is required with inline error) when resource is oven"
    - "StepNode graph card shows a metadata chip when durations are populated"
  artifacts:
    - recipe-planner/src/pages/RecipeEditor.tsx
    - recipe-planner/src/components/nodes/StepNode.tsx
  key_links:
    - "handleSaveEditedStep node-data merge AND handleSave nodeData object BOTH thread all 7 fields (either omission silently drops data)"
    - "field names in nodeData === RecipeStep field names === live schema (Plan 01)"
---

<objective>
Add the 7 new step-metadata fields to the recipe authoring surface: the Edit Step dialog in RecipeEditor.tsx (with conditional prep_action/oven_temp_f and inline validation) and a display chip on StepNode.tsx. This is the human-editable path that also lets the household correct backfilled estimates (PREP-01).

Purpose: New recipes must be able to carry durations/resource metadata at authoring time (linter-enforced at import in Phase 6), and backfilled values must be editable afterward (RESEARCH Pitfall 4 mitigation).
Output: Editable + displayed step metadata in the recipe editor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-UI-SPEC.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/pages/RecipeEditor.tsx
@recipe-planner/src/components/nodes/StepNode.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 metadata fields to the Edit Step dialog + save handlers</name>
  <files>recipe-planner/src/pages/RecipeEditor.tsx</files>
  <behavior>
    - Saving a prep step with active_minutes=12, passive_minutes=0, prep_action=diced persists all three to recipe_steps.
    - Saving a step with resource=oven and blank oven_temp_f shows an inline field error and blocks save.
    - Saving a step with resource=none hides oven_temp_f and persists resource=none.
    - prep_action select is hidden when step_type=assembly.
  </behavior>
  <read_first>
    - 05-PATTERNS.md section "RecipeEditor.tsx (modify)" — the two touchpoints: Edit Step dialog (~lines 1279-1352) and handleSaveEditedStep (~424-450) + handleSave step-node branch (~629-649)
    - 05-UI-SPEC.md Surface 3 (field order, conditional rules, "Oven temperature is required for oven steps." inline error, Save label unchanged)
    - recipe-planner/src/lib/types.ts RecipeStep (field names + resource union from Plan 01)
  </read_first>
  <action>
    Thread 7 new fields through local StepNodeData component state, the Edit Step Dialog, handleSaveEditedStep's node-data merge, and the handleSave step-node nodeData object passed to create/update(collections.recipeSteps). In the dialog, append after the existing Step Type/Timing fields, in this order using the existing margin="dense"/fullWidth TextField/Select pattern: active_minutes (number), passive_minutes (number), instructions (multiline rows 3), prep_action (Select of sliced/diced/minced/chopped/grated/shredded, conditional on step_type===prep — mirror the existing timing conditional), resource (Select of oven/stovetop/blender/food_processor/instant_pot/none), oven_temp_f (number, conditional on resource===oven, with error + helperText "Oven temperature is required for oven steps." when oven is selected and the field is empty — block save in that state), rack_slots (number, default 1). Keep the Save button label as "Save" (do not rename). Do not introduce lead_time_minutes (D-02). Both save touchpoints must carry all 7 fields — verify each field is present in the nodeData object AND the node-data merge.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/pages/RecipeEditor.tsx','utf8'); ['active_minutes','passive_minutes','instructions','prep_action','resource','oven_temp_f','rack_slots'].forEach(f=>{if(!t.includes(f))throw new Error('missing '+f)}); if(!/is required for oven/.test(t))throw new Error('missing oven validation'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 fields appear in the dialog and in BOTH save paths (grep confirms field names in the file; tsc clean).
    - oven_temp_f renders only when resource===oven and blocks save with the exact UI-SPEC inline error copy.
    - prep_action renders only when step_type===prep.
    - Save button label is unchanged ("Save").
  </acceptance_criteria>
  <done>Recipe editor persists and validates all 7 step-metadata fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add a durations metadata chip to StepNode</name>
  <files>recipe-planner/src/components/nodes/StepNode.tsx</files>
  <read_first>
    - recipe-planner/src/components/nodes/StepNode.tsx lines 48-71 (existing chip row + Timing outlined chip sx block)
    - 05-UI-SPEC.md Surface 3 StepNode paragraph ("reuse the exact sx block", size="small" variant="outlined" fontSize 0.7rem height 20)
  </read_first>
  <action>
    Append a third Chip to the existing Box display="flex" gap={0.5} chip row, reusing the exact Timing-chip sx (size="small", variant="outlined", fontSize 0.7rem, height 20). Render it only when active_minutes or passive_minutes is populated on the node data, with a label like "12m active / 30m passive" (omit a side that is null/0 gracefully). Do not introduce any new chip visual style, color token, or size.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - StepNode renders a metadata chip only when durations exist; no new sx style introduced (reuses the existing chip block).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Graph step cards surface active/passive durations at a glance.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 03)
- Modified `RecipeEditor.tsx`: 7 new Edit-Step fields + conditional rendering + inline oven validation + dual save-path threading
- Modified `StepNode.tsx`: durations metadata chip
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authoring form → recipe_steps write | User-entered numeric/select metadata persisted to PB |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03a | Tampering | Edit Step save path | low | mitigate | Number fields coerced/validated; oven_temp_f required-when-oven inline validation blocks malformed oven steps at the form; PB select-enum constrains resource/prep_action server-side |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Manual/UAT: edit a step (prep + assembly + oven), confirm conditional fields and that all 7 persist and re-render after reload against :8091.
</verification>

<success_criteria>
All 7 step-metadata fields are authorable, conditionally rendered, oven-validated, dual-save-threaded, and displayed on the graph node.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-03-SUMMARY.md` when done.
</output>
