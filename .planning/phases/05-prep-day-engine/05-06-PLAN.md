---
phase: 05-prep-day-engine
plan: 06
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - recipe-planner/src/lib/scheduler/resources.ts
autonomous: true
requirements: [PREP-03]
must_haves:
  truths:
    - "The implicit single cook is busy only during a step's active_minutes window; passive windows never occupy the cook"
    - "Two oven steps at different oven_temp_f can never overlap regardless of rack capacity; same-temp steps share up to oven_rack_slots"
    - "Stovetop respects burner_count; blender/food_processor/instant_pot each have capacity 1"
  artifacts:
    - recipe-planner/src/lib/scheduler/resources.ts
  key_links:
    - "isFeasibleAt is called once per candidate step by the SSGS decoder (Plan 09) and the retime pass (Plan 10)"
    - "config capacity (oven_rack_slots, burner_count) comes from scheduler_config; per-step consumption (rack_slots) comes from recipe_steps"
---

<objective>
Implement the pure resource-feasibility model the GA decoder and retime pass consume: the implicit cook resource (active-only), oven temperature-conflict + rack-slot capacity, stovetop burner capacity, and singleton appliances. Turns green the red resources.test.ts from Plan 02 (PREP-03).

Purpose: Without an explicit cook resource the scheduler produces physically impossible schedules (two simultaneous active steps); without the temp-conflict rule it double-books the oven at conflicting temperatures (RESEARCH Pitfall 2).
Output: `lib/scheduler/resources.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-RESEARCH.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/scheduler/types.ts
@recipe-planner/src/lib/scheduler/resources.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement resource feasibility + timeline helpers</name>
  <files>recipe-planner/src/lib/scheduler/resources.ts</files>
  <read_first>
    - 05-RESEARCH.md Pattern 2 (isFeasibleAt with the implicit cook resource, oven temp-conflict short-circuit, burner + singleton capacity) and Pitfall 2
    - 05-CONTEXT.md D-05 (resource enum, oven_temp_f, rack_slots default 1, burner default 4, singleton caps) and A2 (active-then-passive within a step)
    - recipe-planner/src/lib/scheduler/resources.test.ts (the assertions to satisfy)
  </read_first>
  <action>
    Implement pure functions: overlaps(aStart,aEnd,bStart,bEnd); emptyResourceTimeline(config); isFeasibleAt(candidateStart, step, timeline, config) returning boolean; occupyResources(timeline, step, start, end); nextCandidateTime(candidate, timeline). Model a step timeline as active window [start, start+active) = cook-busy AND resource-busy, and passive window [start+active, start+active+passive) = resource-busy only (or nothing when resource=none), per A2 active-then-passive. Feasibility rules in order: (1) reject if the active window overlaps any existing cookBusy window (single implicit cook); (2) for resource=oven, first reject if any temporally-overlapping oven usage has a different oven_temp_f (temperature conflict short-circuits BEFORE capacity), then reject if summed rack_slots over the overlap window exceed config.oven_rack_slots; (3) for resource=stovetop, reject if concurrent burner usage + 1 exceeds config.burner_count; (4) for blender/food_processor/instant_pot, reject if the singleton is busy during the window (capacity 1); (5) resource=none occupies only the cook during active + blocks downstream precedence, no appliance. occupyResources records the step's footprint into the appropriate timeline arrays. Add a module-header comment citing D-05/PREP-03.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/resources.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - resources.test.ts passes: cook single-threading (active-only), passive-absorbs-active allowed, oven different-temp overlap rejected regardless of rack capacity, oven same-temp rack capacity enforced, burner_count enforced, singleton capacity 1 enforced.
    - Temperature conflict is checked before rack-slot capacity (short-circuit).
  </acceptance_criteria>
  <done>Pure, tested resource-feasibility model ready for the SSGS decoder and retime pass.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06)
- Module: `recipe-planner/src/lib/scheduler/resources.ts` (isFeasibleAt, overlaps, emptyResourceTimeline, occupyResources, nextCandidateTime)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure constraint-checking functions |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06a | Tampering | config capacity values from scheduler_config | low | mitigate | Capacities are numeric config with sane defaults; a bad value degrades schedule quality only (no availability/security impact); PB number-field validation applies |
</threat_model>

<verification>
- `npx vitest run src/lib/scheduler/resources.test.ts` green.
</verification>

<success_criteria>
Cook/oven/stovetop/appliance feasibility enforced exactly per D-05, with temperature conflict short-circuiting rack capacity.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-06-SUMMARY.md` when done.
</output>
