---
phase: 05-prep-day-engine
plan: 09
type: execute
wave: 4
depends_on: ["05-05", "05-06"]
files_modified:
  - recipe-planner/src/lib/scheduler/genetic.ts
autonomous: true
requirements: [PREP-03]
must_haves:
  truths:
    - "Same (seed, weights, plan) produces a byte-identical schedule across runs (cross-operator determinism, D-01a.1)"
    - "GA output violates no DAG precedence edge and no resource constraint"
    - "The primary fitness term minimizes the active-session span max(activeEnd) − min(activeStart) (D-06), not sum(active_minutes)"
    - "GA hyperparameters (population/generations/rates) are code constants OUTSIDE scheduler_config (D-01a.2)"
  artifacts:
    - recipe-planner/src/lib/scheduler/genetic.ts
  key_links:
    - "ONE prando PRNG threads through init/selection/crossover/mutation — no Math.random, no reliance on Array.sort stability, seeded Fisher–Yates for shuffles"
    - "SSGS decode calls resources.isFeasibleAt; fitness reads scheduler_config.weights"
---

<objective>
Implement the seeded genetic-algorithm scheduler: activity-list chromosomes, SSGS decode over the week-graph using the resource model, a weighted-sum fitness whose PRIMARY term is the active-session span (D-06), and every stochastic operator driven by ONE prando PRNG for byte-identical determinism (D-01a). Turns green genetic.test.ts from Plan 02 (PREP-03).

Purpose: This is the phase's one genuinely novel module. The user kept the full GA over a simpler alternative, so determinism, fixed hyperparameters, and the correct objective are first-class verifiable requirements, not incidental.
Output: `lib/scheduler/genetic.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-RESEARCH.md
@.planning/phases/05-prep-day-engine/05-CONTEXT.md
@recipe-planner/src/lib/scheduler/types.ts
@recipe-planner/src/lib/scheduler/week-graph.ts
@recipe-planner/src/lib/scheduler/resources.ts
@recipe-planner/src/lib/scheduler/genetic.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSGS decode + active-session-span fitness</name>
  <files>recipe-planner/src/lib/scheduler/genetic.ts</files>
  <read_first>
    - 05-RESEARCH.md Pattern 4 (SSGS decode) + Pattern 2 usage (isFeasibleAt/occupyResources/nextCandidateTime) + Pitfall 1 (active-session-span, not sum)
    - 05-CONTEXT.md D-06 (primary objective = active-session span) and A2/A3 (grouping = shared recipe/meal adjacency)
    - recipe-planner/src/lib/scheduler/resources.ts (feasibility + timeline helpers from Plan 06)
    - recipe-planner/src/lib/scheduler/genetic.test.ts (fitness + no-violation assertions)
  </read_first>
  <action>
    Implement decodeSSGS(activityList, weekGraph, config): walk the precedence-respecting activity list once; each activity's start is the earliest time >= its precedence bound (max end of scheduled predecessors) at which resources.isFeasibleAt succeeds, advancing via nextCandidateTime; record starts/ends and occupyResources; return a Schedule. Implement fitness(schedule, config) as a weighted sum over config.weights where the PRIMARY term is the active-session span = max over instances of (start+active) minus min over instances of start (D-06) — NOT sum(active_minutes), which is invariant under reordering (Pitfall 1). Secondary terms: elapsed (literal makespan min-start to max-end), chopping (adjacency of same prep_action steps), grouping (adjacency of same recipe/meal steps, A3), resource_pressure (peak concurrent resource contention). Lower fitness is better; document each term. Add a module-header comment citing D-06/PREP-03.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/genetic.test.ts -t "no violations" && node -e "const t=require('fs').readFileSync('src/lib/scheduler/genetic.ts','utf8'); if(/sum\(active/.test(t)&&!/span/.test(t)) throw new Error('objective must be span not sum'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - decodeSSGS output violates no precedence edge and no resource constraint (test "no violations" green).
    - The primary fitness term is the active-session span (max activeEnd − min activeStart), documented as such.
    - Fitness reads all 5 weights from config.
  </acceptance_criteria>
  <done>Feasible SSGS decode with the correct D-06 primary objective.</done>
</task>

<task type="auto">
  <name>Task 2: Seeded GA loop + operators + determinism</name>
  <files>recipe-planner/src/lib/scheduler/genetic.ts</files>
  <read_first>
    - 05-RESEARCH.md D-01a.1 (one seeded PRNG, no Math.random, no sort-stability reliance, seeded Fisher–Yates), Pattern 4 "valid activity list" (randomized topological sort), Anti-Patterns (no sort(()=>rng()-0.5)), A5 hyperparameter defaults
    - 05-RESEARCH.md Standard Stack (prando API: nextInt inclusive, no built-in shuffle)
    - recipe-planner/src/lib/scheduler/genetic.test.ts (determinism + weight-change assertions)
  </read_first>
  <action>
    Implement the GA: construct ONE Prando instance from config.seed and thread it through every stochastic operation. Initial population + every offspring is generated by a randomized topological sort (at each step pick uniformly-at-random via the PRNG among currently-available no-pending-predecessor nodes) so every chromosome is precedence-valid by construction. Operators: tournament selection, order crossover (OX1) with PRNG-chosen cut points, swap mutation via PRNG; any shuffle uses a seeded Fisher–Yates using prando.nextInt(0,i) — never Array.sort(()=>rng()-0.5) and never rely on Array.sort stability (always break ties on an explicit deterministic key such as StepInstance.id). Fixed hyperparameters as code constants OUTSIDE scheduler_config (D-01a.2/A5): population 60, generations 150, crossoverRate 0.85, mutationRate 0.15, elitism 2. Export generateSchedule(weekGraph, config): runs the GA and returns the best Schedule. Never call Math.random anywhere in this module.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/genetic.test.ts && grep -rn "Math.random" src/lib/scheduler/ | grep -v '^Binary' | wc -l | grep -qx 0 && echo "no-math-random-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Determinism test green: identical (seed, weights, plan) → byte-identical Schedule twice.
    - Two different weight vectors can yield different orders (weight-change test).
    - `grep -rn "Math.random" src/lib/scheduler/` returns zero matches.
    - Hyperparameters are code constants, not read from scheduler_config.
  </acceptance_criteria>
  <done>Deterministic seeded GA with fixed hyperparameters; full genetic.test.ts green.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 09)
- Module: `recipe-planner/src/lib/scheduler/genetic.ts` (decodeSSGS, fitness, generateSchedule) with fixed GA hyperparameter constants
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side computation, deterministic given inputs |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09a | Tampering | non-determinism via Math.random / sort-stability | medium | mitigate | Single prando PRNG threaded through all operators; seeded Fisher–Yates; explicit deterministic tie-break key; grep-gate forbids Math.random in src/lib/scheduler/ |
| T-05-09b | Denial of Service | GA runtime on the tablet | low | accept | ~10-40 instances × pop 60 × gen 150 is sub-100ms in-browser (RESEARCH Pitfall 5); no web worker needed |
</threat_model>

<verification>
- `npx vitest run src/lib/scheduler/genetic.test.ts` fully green (determinism + no-violation + weight-change + D-06 span).
- `grep -rn "Math.random" src/lib/scheduler/` → 0.
</verification>

<success_criteria>
Deterministic seeded GA producing feasible schedules that minimize the active-session span, with hyperparameters fixed outside the user weights.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-09-SUMMARY.md` when done.
</output>
