---
phase: 05-prep-day-engine
plan: 07
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - recipe-planner/src/lib/linter/rules/missing-durations.ts
  - recipe-planner/src/lib/linter/rules/missing-prep-action.ts
  - recipe-planner/src/lib/linter/rules/missing-pull-step.ts
  - recipe-planner/src/lib/linter/index.ts
autonomous: true
requirements: [PREP-06]
must_haves:
  truths:
    - "missing-durations flags a step with both active_minutes and passive_minutes null (per-step scope)"
    - "missing-prep-action flags a prep-type step with null prep_action (per-step scope)"
    - "missing-pull-step flags a stored/inventory input with no producing pull/thaw/make step ANYWHERE in the planned week (week scope, D-07)"
  artifacts:
    - recipe-planner/src/lib/linter/rules/missing-durations.ts
    - recipe-planner/src/lib/linter/rules/missing-prep-action.ts
    - recipe-planner/src/lib/linter/rules/missing-pull-step.ts
  key_links:
    - "missing-pull-step consumes the WeekGraph type (cross-recipe edges) — week scope, not single-recipe (D-07 resolves RESEARCH A6)"
    - "the two per-step rules slot into the existing runLint array-spread; the week-scoped rule gets its own runWeekLint entry point"
---

<objective>
Add the 3 linter v2 rules following the Phase-1 pure-rule-function pattern: two per-step rules (missing durations, missing prep_action) and one week-scoped rule (missing pull/thaw/make step) that reuses the week-graph's cross-recipe producer→consumer edges. Turns green the red linter cases from Plan 02 (PREP-06).

Purpose: On-demand authoring-time guardrails so steps carry schedulable metadata and the chicken-stock cross-recipe failure is caught (D-07). Publish-gate wiring stays Phase 6.
Output: 3 rule modules + index registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prep-day-engine/05-CONTEXT.md
@.planning/phases/05-prep-day-engine/05-PATTERNS.md
@recipe-planner/src/lib/linter/index.ts
@recipe-planner/src/lib/linter/rules/missing-store-section.ts
@recipe-planner/src/lib/linter/linter.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: missing-durations + missing-prep-action per-step rules</name>
  <files>recipe-planner/src/lib/linter/rules/missing-durations.ts, recipe-planner/src/lib/linter/rules/missing-prep-action.ts</files>
  <read_first>
    - recipe-planner/src/lib/linter/rules/missing-store-section.ts + prep-words.ts (exact pure-rule-function shape: filter/flatMap → LintFinding[])
    - recipe-planner/src/lib/linter/index.ts (LintFinding shape: severity/rule/message/nodeId)
    - 05-PATTERNS.md sections for missing-durations.ts and missing-prep-action.ts
  </read_first>
  <action>
    Create lintMissingDurations(steps): returns an error finding {rule:"missing-durations"} for each step where active_minutes == null AND passive_minutes == null (a step with either populated is fine). Create lintMissingPrepAction(steps): returns an error finding {rule:"missing-prep-action"} for each step where step_type === "prep" AND prep_action == null (ignore assembly steps). Both are pure functions over a step array (define a minimal LinterStep input type analogous to ProductExpanded, carrying id/name/step_type + the metadata fields), each finding carrying nodeId = step.id and a human message naming the step. No classes; follow the existing rule file structure and module-header comment citing PREP-06.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/linter/linter.test.ts -t "missing-durations" && npx vitest run src/lib/linter/linter.test.ts -t "missing-prep-action"</automated>
  </verify>
  <acceptance_criteria>
    - missing-durations flags null/null and does NOT flag when either duration is set.
    - missing-prep-action flags null-prep_action prep steps and ignores assembly steps.
    - Both are pure functions returning LintFinding[] with nodeId set.
  </acceptance_criteria>
  <done>The two per-step rules pass their red cases.</done>
</task>

<task type="auto">
  <name>Task 2: week-scoped missing-pull-step rule</name>
  <files>recipe-planner/src/lib/linter/rules/missing-pull-step.ts</files>
  <read_first>
    - 05-CONTEXT.md D-07 (WHOLE-PLANNED-WEEK scope; reuse the week-graph builder's cross-recipe producer→consumer edges)
    - 05-PATTERNS.md missing-pull-step.ts section (input is a WeekGraph, not a flat step array)
    - recipe-planner/src/lib/scheduler/types.ts (WeekGraph type from Plan 02)
    - recipe-planner/src/lib/linter/linter.test.ts (the week-scoped positive/negative case)
  </read_first>
  <action>
    Create lintMissingPullStep(weekGraph: WeekGraph): for every assembly step instance consuming a stored/inventory input product, verify the week-graph contains a producing edge (a pull/thaw/make step in ANY recipe of the planned week) that yields that product. Flag an error finding {rule:"missing-pull-step"} only when NO producer exists anywhere in the week (D-07 — a producer in a different recipe suppresses the finding, resolving RESEARCH A6). The finding carries nodeId = the consuming step instance id and a message naming the missing input. This rule's input shape intentionally diverges from the Phase-1 per-recipe rules — document that in the module-header comment.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/linter/linter.test.ts -t "missing-pull-step"</automated>
  </verify>
  <acceptance_criteria>
    - Flags a stored/inventory input with no week-wide producer.
    - Does NOT flag when another recipe in the week produces the input (week scope, D-07).
    - Accepts a WeekGraph as input, not a single recipe's step list.
  </acceptance_criteria>
  <done>The cross-recipe pull-step rule passes its week-scoped red case.</done>
</task>

<task type="auto">
  <name>Task 3: Register the 3 rules in the linter aggregator</name>
  <files>recipe-planner/src/lib/linter/index.ts</files>
  <read_first>
    - recipe-planner/src/lib/linter/index.ts lines 51-58 (runLint array-spread aggregator)
    - 05-PATTERNS.md linter/index.ts section (per-step rules into a step aggregator; week-scoped rule gets its own entry point)
  </read_first>
  <action>
    Add a step-scoped aggregator (extend runLint to also accept steps, or add a sibling runStepLint(steps) that spreads lintMissingDurations + lintMissingPrepAction) and a separate runWeekLint(weekGraph) entry point that returns lintMissingPullStep(weekGraph). Do NOT force the week-scoped rule into the per-recipe runLint signature (its input differs). Export both so the existing on-demand linter UI (Products.tsx dialog) and any week-scoped caller can invoke them. Keep imports type-only where the existing file does, to preserve the scripts/lint.js strip-only-run constraint noted in index.ts's header.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit && npx vitest run src/lib/linter/linter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The two per-step rules are reachable via a step aggregator; missing-pull-step is reachable via a distinct week-scoped entry point.
    - Full linter test file green; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>All 3 v2 rules are registered and invocable via the linter aggregators.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 07)
- Rules: `missing-durations.ts`, `missing-prep-action.ts`, `missing-pull-step.ts`
- Aggregator: extended `linter/index.ts` (step-scoped + week-scoped entry points)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure read-only lint functions |

## STRIDE Threat Register (ASVS L1, block-on: high)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07a | Repudiation | linter false-negative (missed missing-pull-step) | low | accept | Advisory on-demand rule; week-scope reduces false positives; publish-gate enforcement is Phase 6 |
</threat_model>

<verification>
- `npx vitest run src/lib/linter/linter.test.ts` fully green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
3 linter v2 rules pass their red cases; missing-pull-step is week-scoped per D-07; registration keeps per-step and week-scoped entry points distinct.
</success_criteria>

<output>
Create `.planning/phases/05-prep-day-engine/05-07-SUMMARY.md` when done.
</output>
