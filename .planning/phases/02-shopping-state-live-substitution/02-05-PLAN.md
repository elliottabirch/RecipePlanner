---
phase: 02-shopping-state-live-substitution
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_schema.json
  - recipe-planner/src/lib/api.ts
  - recipe-planner/src/lib/types.ts
autonomous: false
requirements: [SHOP-01, SHOP-03]
user_setup:
  - service: pocketbase
    why: "Schema changes follow the established manual admin-UI convention (no code-based ORM migration exists) — the shopping_state collection and meal_variant_overrides fields must be created by hand on both prod and test instances before hook code can be tested."
    dashboard_config:
      - task: "Create shopping_state collection (fields + unique index) and add quantity/unit to meal_variant_overrides"
        location: "PocketBase admin UI — prod http://192.168.50.95:8090/_/ and test http://192.168.50.95:8091/_/"
must_haves:
  truths:
    - "shopping_state collection exists on BOTH prod and test with fields weekly_plan (relation, cascadeDelete), line_key (text), checked (bool), have_quantity (number nullable), resolution (select buy|make|skip nullable), and a unique index on (weekly_plan, line_key)"
    - "meal_variant_overrides has nullable quantity (number) and unit (select/text) fields on BOTH instances"
    - "pb_schema.json re-exported to reflect both changes; getAll/create/update round-trip against shopping_state succeeds"
  artifacts:
    - "pb_schema.json — updated canonical export"
    - "recipe-planner/src/lib/api.ts — collections.shoppingState"
    - "recipe-planner/src/lib/types.ts — ShoppingState interface + MealVariantOverride.quantity/unit"
  key_links:
    - "shopping_state.weekly_plan cascadeDelete → deleting a weekly plan removes its shopping state"
    - "unique (weekly_plan, line_key) → idempotent upserts from useShoppingState"
    - "collections.shoppingState string → generic getAll/create/update wrappers"
---

<objective>
Create the net-new `shopping_state` collection and add `quantity`/`unit` to `meal_variant_overrides` on both PocketBase instances (D-03, D-12), following the project's established manual-admin-UI convention (there is NO code-based migration — 02-RESEARCH). Re-export the canonical `pb_schema.json`, then add the TypeScript surface (`collections.shoppingState`, `ShoppingState` interface, extended `MealVariantOverride`). Capture the pre-migration override row count (phase doc item 3a). The stored/api/types work here is the foundation the hook (02-06) depends on.

Purpose: SHOP-01 persistence and SHOP-03 swap quantity require these collections/fields to exist before any hook or dialog can be tested end-to-end. `shopping_state` does not exist today (confirmed 404 on both instances).
Output artifacts produced by this plan: `shopping_state` collection (PB); `collections.shoppingState`; `ShoppingState` interface; `MealVariantOverride.quantity`/`.unit`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md

@recipe-planner/src/lib/api.ts
@recipe-planner/src/lib/types.ts
@pb_schema.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Create shopping_state collection + meal_variant_overrides fields (manual PocketBase, both instances)</name>
  <action>Facilitate and confirm the manual PocketBase admin-UI schema changes on both prod and test instances per the how-to-verify steps: capture pre-migration override counts, create the shopping_state collection with fields + unique index + matched API rules, add quantity/unit to meal_variant_overrides, re-export pb_schema.json, and confirm a CRUD round-trip. Block until the human confirms all steps.</action>
  <what-built>Nothing automated yet — this is the manual PocketBase schema step. The executor will guide/confirm the admin-UI edits and capture the pre-migration count, but a human performs the admin-UI changes on both instances (the established project convention; there is no migration script).</what-built>
  <how-to-verify>
    1. Record the pre-migration override count on BOTH instances: `GET http://192.168.50.95:8090/api/collections/meal_variant_overrides/records?perPage=1` and `:8091/...` — read `totalItems` (expected 0 on both per 02-RESEARCH; record actual). This drives whether inherit-when-null needs backfill attention (phase doc item 3a).
    2. In the PocketBase admin UI on prod (`:8090/_/`) AND test (`:8091/_/`), create collection `shopping_state` with fields: `weekly_plan` (relation → weekly_plans, required, cascadeDelete ON), `line_key` (text, required), `checked` (bool), `have_quantity` (number, nullable), `resolution` (select single, values buy/make/skip, nullable). Add a UNIQUE index on (`weekly_plan`, `line_key`).
    3. Set `shopping_state` API rules to match the existing collections' rule level (verify what meal_variant_overrides / weekly_plans use and mirror it — do NOT leave it more permissive; threat T-02-07).
    4. On both instances, add to `meal_variant_overrides`: `quantity` (number, nullable) and `unit` (select single bound to the units enum values, or text if select is impractical — nullable).
    5. Re-export the schema and overwrite repo-root `pb_schema.json` (Phase 1 consolidated to this single file; `pb_schema_updated.json` was deleted — do not recreate it).
    6. Confirm a CRUD round-trip: create a `shopping_state` row via the SDK/admin against test, read it back, update `checked`, delete it.
  </how-to-verify>
  <resume-signal>Type "approved" once shopping_state + the two override fields exist on BOTH instances, the unique index is in place, API rules match existing collections, pb_schema.json is re-exported, and the CRUD round-trip succeeds. Report the recorded pre-migration override counts.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add collections.shoppingState + ShoppingState interface + extend MealVariantOverride</name>
  <files>recipe-planner/src/lib/api.ts, recipe-planner/src/lib/types.ts</files>
  <read_first>
    - recipe-planner/src/lib/api.ts:51-68 — `collections` object; generic `getAll`/`create`/`update`/`remove` (:5-48) work unchanged against a new collection string.
    - recipe-planner/src/lib/types.ts:195-209 — `MealVariantOverride` + `MealVariantOverrideExpanded`; `BaseRecord`-extends + separate `*Expanded` convention; `PlannedMealExpanded` as a second example.
    - recipe-planner/src/lib/units.ts — `Unit` type for `MealVariantOverride.unit` (D-12, enum not free text).
    - 02-PATTERNS "types.ts" and "api.ts" sections.
  </read_first>
  <action>
    Add `shoppingState: "shopping_state"` to the `collections` object in api.ts (naming discretion resolved to `shopping_state` per CONTEXT.md — pick truer name only if you prefer `plan_line_state`; keep it consistent with the collection actually created in the checkpoint). In types.ts, add `ShoppingState extends BaseRecord` with `weekly_plan: string; line_key: string; checked: boolean; have_quantity: number | null; resolution: "buy" | "make" | "skip";` (add a `ShoppingStateExpanded` only if a relation expansion is actually needed — for this phase's UI, likely none). Extend `MealVariantOverride` with `quantity?: number | null;` and `unit?: Unit | null;` (import `Unit` from units.ts per D-12). Follow the existing `BaseRecord`/`*Expanded` convention exactly.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `collections.shoppingState` exists; the string matches the collection created in the checkpoint.
    - `ShoppingState` interface matches the collection fields; `resolution` is the `buy|make|skip` union.
    - `MealVariantOverride` carries nullable `quantity`/`unit` with `unit` typed as the Phase-1 `Unit` enum (not `string`).
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>TypeScript surface for shopping_state and the extended override is in place and type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase | Writes to the new shopping_state collection and extended meal_variant_overrides cross into durable storage over LAN/tailnet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | shopping_state left more permissive than existing collections once reachable over tailnet | medium | mitigate | Set shopping_state API rules to match existing collections during admin-UI creation (checkpoint step 3); verify against meal_variant_overrides/weekly_plans rule level |
| T-02-08 | Tampering / data integrity | orphaned shopping_state rows on plan deletion | low | mitigate | weekly_plan relation uses cascadeDelete ON so deleting a plan removes its state (checkpoint step 2) |
| T-02-09 | Denial of Service | duplicate (weekly_plan, line_key) rows breaking upsert idempotency | low | mitigate | UNIQUE index on (weekly_plan, line_key) makes query-then-branch upsert deterministic |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit — no installs, so no supply-chain checkpoint).
</threat_model>

<verification>
- Manual: shopping_state + override fields exist on both instances; unique index present; API rules match; pb_schema.json re-exported; CRUD round-trip green (blocking checkpoint).
- `cd recipe-planner && npx tsc -b` clean.
- Pre-migration override counts recorded for both DBs (phase doc item 3a).
</verification>

<success_criteria>
The shopping_state collection and meal_variant_overrides quantity/unit fields exist on prod and test with correct constraints and rules; the TypeScript surface binds to them; pb_schema.json reflects reality.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-05-SUMMARY.md` when done (include recorded pre-migration override counts).
</output>
