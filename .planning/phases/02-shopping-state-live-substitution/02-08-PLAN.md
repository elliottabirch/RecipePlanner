---
phase: 02-shopping-state-live-substitution
plan: 08
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-05", "02-07"]
files_modified:
  - recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx
  - recipe-planner/src/components/outputs/ShopSwapDialog.tsx
  - recipe-planner/src/pages/Outputs.tsx
autonomous: true
requirements: [SHOP-03, SHOP-04, SHOP-05]
must_haves:
  truths:
    - "From a shopping line, the swap dialog lists exactly this week's meals that use the product (via getMealNodeTargetsForProduct), pre-fills each meal's quantity/unit from the original node (D-07), and applies only to checked meals"
    - "Saving a swap writes/extends meal_variant_overrides per checked meal (delete+recreate, including quantity/unit) then bumps refreshCounter so all six outputs re-derive (D-09)"
    - "Quick-create (name + store/section + unit-as-enum) runs inline in the swap dialog and returns the new product straight into the replacement picker (D-08)"
    - "Make-it is available only when product.source_recipe is set (D-10), confirms first (D-11), then adds the recipe via handleAddRecipeToPlan and sets resolution=make"
  artifacts:
    - "recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx"
    - "recipe-planner/src/components/outputs/ShopSwapDialog.tsx"
    - "recipe-planner/src/pages/Outputs.tsx — swap open/save handler, make-it confirm handler, onSwap/onMakeIt props passed to ShoppingListTab"
  key_links:
    - "getMealNodeTargetsForProduct (02-03) → ShopSwapDialog meal checklist"
    - "swap save → delete+recreate meal_variant_overrides (with quantity/unit) → refreshCounter bump → applyVariantOverrides (02-02) re-derive"
    - "QuickCreateProductDialog onCreated → ShopSwapDialog replacement Autocomplete"
    - "make-it confirm → handleAddRecipeToPlan + setResolution('make')"
---

<objective>
Build the mid-shop swap flow and make-it action (D-07..D-11, SHOP-03/04/05): the `ShopSwapDialog` (meal checklist keyed by planned_meal, pre-filled per-meal quantity/unit, replacement product search), the inline `QuickCreateProductDialog` (name + store/section + unit-as-enum, returns into the picker), and the Outputs handlers (swap save via delete+recreate with quantity/unit; confirm-first make-it wired to `handleAddRecipeToPlan` + `resolution=make`). The line-level Swap/Make-it buttons live in ShoppingListTab (02-09) and call the `onSwap`/`onMakeIt` props this plan provides.

Purpose: SHOP-03 (swap re-derives outputs), SHOP-04 (make-it), SHOP-05 (quick-create) — the store-time substitution capability. Reuses the flow-graph re-derivation for free (bump refreshCounter, no separate propagation).
Output artifacts produced by this plan: `QuickCreateProductDialog.tsx`, `ShopSwapDialog.tsx`, Outputs swap/make-it handlers + `onSwap`/`onMakeIt` props.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md
@.planning/phases/02-shopping-state-live-substitution/02-UI-SPEC.md

Depends on: 02-02 (VariantOverride.quantity/unit), 02-03 (getMealNodeTargetsForProduct + SwapTarget), 02-05 (collections.shoppingState, MealVariantOverride.quantity/unit, products.canonical_unit), 02-07 (Outputs wired to hook + setResolution available).
@recipe-planner/src/components/VariantEditorDialog.tsx
@recipe-planner/src/pages/WeeklyPlans.tsx
@recipe-planner/src/lib/units.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuickCreateProductDialog (name + store/section + unit-as-enum)</name>
  <files>recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx</files>
  <read_first>
    - 02-UI-SPEC surface 4 (Quick-create dialog): nested Dialog maxWidth="xs", DialogTitle "Create Product" (Heading 20/600), fields Name (autofocus, required), Store (optional), Section (optional), Unit (Select bound to units enum, required); "Create Product" contained/accent disabled until Name+Unit; on failure inline Alert severity="error" keeping input; on success returns created Product.
    - 02-UI-SPEC Copywriting Contract (CTA copy, quick-create failure copy).
    - 02-PATTERNS "QuickCreateProductDialog.tsx" — VariantEditorDialog Dialog shell; generic `create(collections.products, {...})`; `onCreated: (product: Product) => void` return pattern.
    - recipe-planner/src/lib/units.ts — derive unit options from `Object.keys(UNIT_DIMENSIONS)` cast to `Unit[]` (D-12, enum not free text); bind to `products.canonical_unit`.
    - recipe-planner/src/lib/types.ts — products fields (name, store, section, canonical_unit).
  </read_first>
  <action>
    Create QuickCreateProductDialog with props for `open`, `onClose`, and `onCreated(product)`. Render the four fields per UI-SPEC surface 4 (Store/Section as Select/Autocomplete over existing stores/sections; Unit as a Select over the units enum). On submit, `create(collections.products, { name, store, section, canonical_unit })` via the generic api wrapper, then call `onCreated(created)` and close. On failure show the inline error Alert with the Copywriting-Contract quick-create copy and preserve typed input (do not close). Disable the accent "Create Product" CTA until Name and Unit are set. Add explicit aria-labels where the UI-SPEC recommendation applies.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Fields, required-gating, accent CTA, and failure-keeps-input behavior match UI-SPEC surface 4; Unit is the Phase-1 enum bound to `products.canonical_unit` (D-12).
    - `onCreated(product)` returns the created product to the parent.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Minimal phone-friendly quick-create dialog that returns a Product into its caller.</done>
</task>

<task type="auto">
  <name>Task 2: ShopSwapDialog (meal checklist + pre-filled qty/unit + replacement + inline quick-create)</name>
  <files>recipe-planner/src/components/outputs/ShopSwapDialog.tsx</files>
  <read_first>
    - 02-UI-SPEC surface 3 (Mid-shop swap dialog): Dialog maxWidth="sm" fullWidth single-column; DialogTitle "Swap {productName}"; Meals section (List of checkable SwapTargets, per-meal number TextField + unit Select pre-filled from the node's qty/unit per D-07, editable only when checked); Divider; Replacement Autocomplete (reuse VariantEditorDialog pattern) + "+ Create new product" opening QuickCreateProductDialog nested; empty-state copy when no meals use the product; "Save Swap" accent, disabled until >=1 meal checked AND a replacement selected. aria-label "Swap {productName}".
    - 02-PATTERNS "ShopSwapDialog.tsx" — VariantEditorDialog imports, Autocomplete/product-search pattern (verbatim, no fuzzy search per Don't-Hand-Roll), pending-overrides Map-in-state idiom seeded from getMealNodeTargetsForProduct.
    - 02-03 exports `getMealNodeTargetsForProduct` + `SwapTarget`; 02-RESEARCH Open Question 1 — per-meal qty/unit fans out to all matching nodes in that meal.
  </read_first>
  <action>
    Create ShopSwapDialog taking `open`, `onClose`, the target `product`, `plannedMeals`, `recipeData`, the `products` list, and an `onSave(perMealOverrides)` callback. On open, seed a `Map` of per-meal state from `getMealNodeTargetsForProduct(product.id, plannedMeals, recipeData)` (02-03), pre-filling each meal's quantity/unit from its target (D-07); render one checkable row per meal with number TextField + unit Select (enum) enabled only when checked. Reuse VariantEditorDialog's Autocomplete for the replacement product; add a "+ Create new product" affordance that opens QuickCreateProductDialog (Task 1) nested and sets the returned product as the selected replacement (D-08). Show the empty-state copy when there are no targets. Disable "Save Swap" until >=1 meal checked and a replacement chosen. On save, hand the parent, per checked meal, the { plannedMealId, node targets, replacementProductId, quantity, unit } needed to write overrides. Include the aria-labels from the downstream UI recommendation.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Meal checklist is derived from getMealNodeTargetsForProduct, keyed by planned_meal, qty/unit pre-filled from the node (D-07).
    - Inline quick-create returns into the replacement Autocomplete without leaving the flow (D-08).
    - Save is gated until a meal + replacement are chosen; empty-state renders when no meal uses the product.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Store-time swap dialog wired to the mapping helper and inline quick-create.</done>
</task>

<task type="auto">
  <name>Task 3: Outputs swap-save + confirm-first make-it handlers; pass onSwap/onMakeIt props</name>
  <files>recipe-planner/src/pages/Outputs.tsx</files>
  <read_first>
    - recipe-planner/src/pages/WeeklyPlans.tsx:476-499 — `handleSaveVariants` delete+recreate-per-meal pattern to mirror (now also writing quantity/unit); keep both override writers on this shared shape (CONTEXT.md ask).
    - recipe-planner/src/pages/Outputs.tsx:562-576 (`handleAddRecipeToPlan`), :105/:316 (`refreshCounter` bump).
    - 02-UI-SPEC surface 5 (Make-it confirm flow): make-it hidden entirely when no source_recipe (D-10); confirm-first Dialog "Make it instead of buying?" / body copy / "Add to This Week" accent (D-11); after confirm the line shows resolved treatment with a "Making" chip.
    - 02-RESEARCH Pitfall 4 — do NOT wire make-it directly to handleAddRecipeToPlan like OutOfStockSection.tsx:145 (no confirmation); wrap in a confirm dialog.
  </read_first>
  <action>
    Add to Outputs.tsx: state to open ShopSwapDialog for a given shopping line; a swap-save handler that, per checked meal, deletes existing meal_variant_overrides for that (meal) and recreates them for each matching node including `quantity`/`unit` (mirroring WeeklyPlans.handleSaveVariants, extended per D-09), then bumps `refreshCounter` so the existing derive pipeline re-runs (no separate propagation). Add a confirm-first make-it handler: only offered when `product.source_recipe` is set (D-10); on confirm via a MUI confirm Dialog (D-11, UI-SPEC surface 5 copy) call the existing `handleAddRecipeToPlan(source_recipe)` and `setResolution(lineKey, 'make')`. Pass `onSwap(line)` and `onMakeIt(line)` (plus make-it eligibility) down to ShoppingListTab as props (02-09 renders the buttons). Mount ShopSwapDialog and the make-it confirm Dialog in Outputs. Keep the planning-time and store-time override writers on the shared delete+recreate shape.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b && npx vitest run src/lib/aggregation/utils/variant-utils.test.ts src/lib/shopping-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Swap save writes overrides (with quantity/unit) per checked meal and bumps refreshCounter; downstream lists re-derive (variant-utils + mapping tests back the logic).
    - Make-it is gated on source_recipe (D-10), confirms first (D-11), then adds recipe + sets resolution=make; NOT wired directly like OutOfStockSection.
    - `onSwap`/`onMakeIt` props are passed to ShoppingListTab.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Store-time swap save and confirm-first make-it are wired; buttons delegated to ShoppingListTab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase | Swap writes meal_variant_overrides; quick-create writes products; make-it inserts planned_meals + writes shopping_state.resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | make-it silently mutates the plan (adds a planned_meal) with no confirmation | medium | mitigate | confirm-first Dialog (D-11) before handleAddRecipeToPlan; not wired like OutOfStockSection (Pitfall 4) |
| T-02-15 | Input Validation | quick-create writes an invalid/blank product name or a duplicate colliding with the Phase-1 unique index | medium | mitigate | client-side required-gating (Name+Unit) + inline error Alert on the PocketBase 400 (unique-index) response; unit constrained to the enum Select |
| T-02-16 | Input Validation | negative/NaN swap quantity | low | mitigate | number TextField constrained (min 0); non-numeric coerced/ignored before write |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx tsc -b` clean.
- `cd recipe-planner && npx vitest run` full suite green.
- Manual (02-10 UAT): swap from a line updates shopping/prep/pull/containers for the chosen meals at entered quantities; quick-create mid-swap; make-it confirm + batch-prep step appearance (SHOP-03/04/05 dialogs are manual-only per 02-VALIDATION).
</verification>

<success_criteria>
A shopper can swap a product across chosen meals with per-meal quantity/unit (re-deriving all outputs), quick-create a missing product inline, and resolve a line as make-it with confirm-first — all from the shopping list.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-08-SUMMARY.md` when done.
</output>
