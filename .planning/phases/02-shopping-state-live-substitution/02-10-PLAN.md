---
phase: 02-shopping-state-live-substitution
plan: 10
type: execute
wave: 6
depends_on: ["02-07", "02-08", "02-09"]
files_modified:
  - recipe-planner/src/components/outputs/CheckableListItem.tsx
  - recipe-planner/src/components/outputs/FridgeFreezerTab.tsx
  - recipe-planner/src/components/outputs/MealContainersTab.tsx
  - recipe-planner/src/components/outputs/MicahMealsTab.tsx
  - recipe-planner/src/components/outputs/PullListsTab.tsx
  - recipe-planner/src/components/outputs/BatchPrepTab.tsx
autonomous: false
requirements: [SHOP-06, SHOP-07]
user_setup:
  - service: tailnet
    why: "Store usability (SHOP-07) requires the NAS PocketBase instances on the tailnet and db-config pointed at tailnet hostnames (env vars VITE_POCKETBASE_URL/_TEST_URL). This is the separate infra todo nas-pocketbase-tailnet — it blocks store use, NOT local dev. No code change is needed (db-config already reads env vars); it is a deployment/env-var switch once the NAS joins the tailnet."
    dashboard_config:
      - task: "Join NAS PocketBase to tailnet and set VITE_POCKETBASE_URL/_TEST_URL to tailnet hostnames"
        location: "todo .planning/todos/pending/nas-pocketbase-tailnet.md + deployment env"
must_haves:
  truths:
    - "The shared checkbox and every checkable line across the non-shopping Outputs tabs meet the 48x48 touch-target minimum with comfortable spacing (SHOP-06)"
    - "Batch prep still prints cleanly and shopping has no print affordance"
    - "End-to-end UAT confirms per-plan persistence across refresh/device, optimistic retry on a connectivity drop, and tablet touch operability"
  artifacts:
    - "recipe-planner/src/components/outputs/CheckableListItem.tsx and the non-shopping tabs — touch-target sizing"
  key_links:
    - "CheckableListItem touch target → all tabs using it (shopping/batch/fridge)"
    - "tailnet env-var switch → SHOP-07 store usability (infra todo, not code)"
---

<objective>
Finish the tablet touch pass across the remaining Outputs surfaces (SHOP-06) — the shared `CheckableListItem` checkbox (Pitfall 6: `size="small"` under-sizes the tap target) and the non-shopping tabs — confirm print stays scoped to batch prep only (phase doc item 15), and run the end-of-phase human UAT for the manual-only behaviors (SHOP-01 device switch, SHOP-06 touch, SHOP-07 connectivity drop). The tailnet hostname switch (SHOP-07 store usability) is the separate infra todo and needs no code change (db-config already reads env vars).

Purpose: Make the whole Outputs surface comfortably touch-operable on the tablet and validate the manual-only acceptance criteria that no automated test in this repo can cover.
Output artifacts produced by this plan: touch-sized CheckableListItem + non-shopping tabs; verified print scoping; phase UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-UI-SPEC.md
@.planning/phases/02-shopping-state-live-substitution/02-VALIDATION.md

@recipe-planner/src/components/outputs/CheckableListItem.tsx
@recipe-planner/src/lib/db-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Touch-target pass — CheckableListItem + non-shopping tabs</name>
  <files>recipe-planner/src/components/outputs/CheckableListItem.tsx, recipe-planner/src/components/outputs/FridgeFreezerTab.tsx, recipe-planner/src/components/outputs/MealContainersTab.tsx, recipe-planner/src/components/outputs/MicahMealsTab.tsx, recipe-planner/src/components/outputs/PullListsTab.tsx, recipe-planner/src/components/outputs/BatchPrepTab.tsx</files>
  <read_first>
    - 02-UI-SPEC Spacing Scale "Exception — tablet touch targets" (48x48 minimum via sx minWidth/minHeight on the enclosing IconButton/ListItemIcon, or size="medium"; keep the visual glyph small if desired; 8px minimum gap between adjacent targets).
    - 02-RESEARCH Pitfall 6 — CheckableListItem.tsx:27 `size="small"` under-sizes the tap target; enlarge the hit area via padding/minHeight, not just fonts.
    - CheckableListItem.tsx:32-36 (checked styling) — do not regress the resolved-line visual language (02-09 relies on matching it).
  </read_first>
  <action>
    Increase the tap target on the shared `CheckableListItem` (enclosing ListItem/ListItemIcon padding or minHeight to reach 48x48; the checkbox glyph may stay visually small) without changing its prop surface or checked-state styling. Apply matching touch sizing/spacing to the checkable rows in FridgeFreezerTab, MealContainersTab, MicahMealsTab, PullListsTab, and BatchPrepTab per the UI-SPEC exception (48x48 targets, 8px gaps). Do not edit ShoppingListTab (its controls are sized in 02-09) and do not change the checkbox key logic (owned by 02-01).
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - CheckableListItem and the five non-shopping tabs present 48x48 tap targets with comfortable spacing; checked/resolved styling unchanged.
    - `cd recipe-planner && npx tsc -b` passes and the full vitest suite is green.
  </acceptance_criteria>
  <done>Touch pass applied across the shared checkbox and non-shopping tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm print scoping (batch-prep only, no shopping print)</name>
  <files>recipe-planner/src/components/outputs/BatchPrepTab.tsx</files>
  <read_first>
    - phase doc item 15 — printStyles.css should target only batch prep (`#batch-prep-list`); ensure no shopping print affordance was added this phase.
    - Locate printStyles.css / BatchPrepPrintView usage before asserting scope.
  </read_first>
  <action>
    Verify (and adjust only if a shopping print path leaked in) that the print stylesheet and print affordances target batch prep exclusively and that the shopping tab exposes no print button/path. This is primarily a verification/scoping confirmation, not new machinery. If BatchPrepTab needs no edit, record the verification in the summary rather than forcing a change.
  </action>
  <verify>
    <automated>cd recipe-planner && grep -rn "print" src/components/outputs/ShoppingListTab.tsx | grep -vc "sprint\|footprint" </automated>
  </verify>
  <acceptance_criteria>
    - No shopping print affordance exists; batch-prep print remains scoped and functional.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Print confirmed scoped to batch prep; shopping is print-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human UAT (persistence, swap, make-it, connectivity, touch)</name>
  <action>Guide the human through the end-of-phase UAT script in how-to-verify on a real tablet + network, covering SHOP-01/02/03/04/05/06/07 and the D-01 positional-key correctness check. Collect pass/fail per item and block until the human approves or lists failures for gap closure.</action>
  <what-built>The full Phase 2 shopping surface: per-plan persisted state across all six tabs, have-N with remaining-to-buy, mid-shop swap with quick-create, confirm-first make-it, optimistic sync with a pending indicator, and a tablet touch pass. Automated tests cover the re-derivation/overlay/mapping/sync-queue logic; the following require a human on a real tablet + network.</what-built>
  <how-to-verify>
    1. Persistence (SHOP-01): on the tablet, check several items across shopping, stored, container, and pull tabs; refresh the page and confirm state survives; open the same plan on the phone and confirm the checks appear; switch to a different weekly plan and confirm its checks are independent.
    2. Positional-key correctness (D-01): with some stored/container/pull lines checked, use make-it to add a recipe to the week (which reorders those lists) and confirm no previously-checked line now marks a DIFFERENT item.
    3. Have-N (SHOP-02): set "have 2 of 5" on a line — confirm "3 to buy" and auto-complete (dim/strike) at 5.
    4. Swap (SHOP-03): swap a product across two meals with different quantities; confirm shopping/prep/pull/container lists re-derive for exactly those meals at the entered quantities; confirm a same-recipe-double-plan shares one container row (known pre-existing behavior, not a regression — 02-RESEARCH Pitfall 3).
    5. Quick-create (SHOP-05) + make-it (SHOP-04): quick-create a product mid-swap and use it as the replacement; make-it a line with a source_recipe and confirm the confirm-first dialog, then that the line drops off the export and its steps appear in batch prep.
    6. Connectivity (SHOP-07): drop the hotspot mid-check WITHOUT reloading — confirm the tap still updates the UI, the indicator shows "Saving…"/"Can't reach server — retrying", and on reconnect the queued writes land with no lost state.
    7. Touch (SHOP-06): confirm the stepper, swap/make-it buttons, chips, and checkboxes are comfortably operable on the tablet viewport.
    8. Tailnet (SHOP-07 store usability): if the nas-pocketbase-tailnet todo is done, verify from a hotspot-only device; if not, confirm it is tracked and that local dev works against LAN.
  </how-to-verify>
  <resume-signal>Type "approved" when all criteria pass, or list the failing items for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase over tailnet | Store usability adds a (trusted) tailnet path; slightly larger surface than pure LAN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Tampering | collections reachable over tailnet with overly-permissive rules | medium | mitigate | Verified in 02-05 (rules match existing collections); UAT confirms no unexpected write surface |
| T-02-20 | Denial of Service (usability) | sub-48px targets on the remaining tabs | low | mitigate | touch pass enforces 48x48 targets (this plan) |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx tsc -b` clean; full vitest suite green.
- Print confirmed batch-prep-only.
- Blocking human UAT covers SHOP-01/02/03/04/05/06/07 end-to-end on the tablet + a connectivity drop.
</verification>

<success_criteria>
The entire Outputs surface is touch-comfortable on the tablet, print stays batch-prep-only, and end-to-end UAT confirms persistence, swap re-derivation, make-it/quick-create, and optimistic-retry behavior.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-10-SUMMARY.md` when done.
</output>
