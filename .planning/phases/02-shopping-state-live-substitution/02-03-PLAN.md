---
phase: 02-shopping-state-live-substitution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/src/lib/shopping-overlay.ts
  - recipe-planner/src/lib/shopping-overlay.test.ts
  - recipe-planner/src/lib/shopping-mapping.ts
  - recipe-planner/src/lib/shopping-mapping.test.ts
autonomous: true
requirements: [SHOP-02, SHOP-03, SHOP-04]
must_haves:
  truths:
    - "Given have_quantity, the overlay computes remaining = max(0, totalQuantity - have) and marks the line resolved when remaining <= 0 (SHOP-02)"
    - "A line with resolution 'make' or 'skip' is marked resolved and excluded by the export filter (SHOP-04, D-06)"
    - "A product used by two planned meals (even of the same recipe) resolves to two distinct {plannedMealId, nodeId} swap targets (SHOP-03)"
  artifacts:
    - "recipe-planner/src/lib/shopping-overlay.ts — OverlaidShoppingItem, overlayShoppingItem(), filterForExport()"
    - "recipe-planner/src/lib/shopping-mapping.ts — SwapTarget, getMealNodeTargetsForProduct()"
    - "matching .test.ts files (both MISSING today — Wave 0)"
  key_links:
    - "shopping_state map (keyed by getShoppingCheckboxKey(lineId)) → overlayShoppingItem → visible list + export"
    - "plannedMeals × meal-keyed recipeData → getMealNodeTargetsForProduct → meal_variant_overrides {planned_meal, original_node}"
---

<objective>
Build the two pure, dependency-free modules the shopping UI joins against: the derive-then-overlay layer (have-N remaining, auto-complete, resolution + export filtering per D-04/D-05/D-06) and the shopping-line → per-meal-node mapping the swap dialog depends on (phase doc item 9). Both are the automatable core (02-VALIDATION) and MUST be built and tested before their consumers (02-07 overlay wiring, 02-08 swap dialog).

Purpose: Keep overlay + mapping logic in plain React-free modules so they are unit-testable under Vitest's existing `node` environment without adding jsdom/RTL.
Output artifacts produced by this plan: `OverlaidShoppingItem`, `overlayShoppingItem()`, `filterForExport()`; `SwapTarget`, `getMealNodeTargetsForProduct()`; two new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md

@recipe-planner/src/lib/aggregation/types.ts
@recipe-planner/src/constants/outputs.ts
@recipe-planner/src/pages/Outputs.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shopping-overlay.ts — derive-then-overlay + export filter (pure)</name>
  <files>recipe-planner/src/lib/shopping-overlay.ts, recipe-planner/src/lib/shopping-overlay.test.ts</files>
  <read_first>
    - 02-RESEARCH Architecture Patterns Pattern 2 (concrete `overlayShoppingItem`/`filterForExport` target shape).
    - recipe-planner/src/pages/Outputs.tsx:336-366 — existing pantry-only `filteredShoppingListForExport` (the pattern to generalize).
    - recipe-planner/src/lib/aggregation/types.ts — `AggregatedProduct` shape (has `lineId`, `totalQuantity`).
    - recipe-planner/src/constants/outputs.ts — `getShoppingCheckboxKey(lineId)`.
    - Resolution enum (D-04): `buy | make | skip`. isResolved = resolution !== 'buy' OR (have != null AND have >= needed).
  </read_first>
  <behavior>
    - overlayShoppingItem(item, stateMap): remaining = have != null ? max(0, totalQuantity - have) : null; resolution defaults 'buy'; isResolved true when resolution !== 'buy' OR remaining <= 0.
    - A line with no state entry → haveQuantity null, remaining null, resolution 'buy', isResolved false.
    - filterForExport(items) excludes every isResolved item (make / skip / have-complete) per D-06; keeps buy-with-remaining lines.
    - Pure functions — no React import, no PocketBase import.
  </behavior>
  <action>
    Create shopping-overlay.ts exporting `OverlaidShoppingItem` (extends `AggregatedProduct` with `haveQuantity: number | null`, `remaining: number | null`, `resolution: "buy"|"make"|"skip"`, `isResolved: boolean`), `overlayShoppingItem(item, state: Map<string, ShoppingStateEntry>)`, and `filterForExport(items)`. Key the state lookup via `getShoppingCheckboxKey(item.lineId)`. Import the `ShoppingStateEntry`/`ShoppingState` type shape — since 02-05 (types.ts) is a sibling Wave-1 plan, define a local minimal structural type here (`{ have_quantity: number | null; resolution: "buy"|"make"|"skip" }`) or import from types.ts if already present; do not hard-depend on 02-05 ordering. Do NOT re-implement unit conversion — have-N is entered/compared in the line's existing display unit (02-RESEARCH Don't-Hand-Roll). Create shopping-overlay.test.ts covering the behavior cases.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/shopping-overlay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `overlayShoppingItem` and `filterForExport` implement the remaining/isResolved/export rules above; no React or pb import.
    - Test proves remaining math, isResolved for each resolution cause, and export exclusion (D-06).
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Pure overlay module with tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shopping-mapping.ts — shopping-line → per-meal-node targets (pure)</name>
  <files>recipe-planner/src/lib/shopping-mapping.ts, recipe-planner/src/lib/shopping-mapping.test.ts</files>
  <read_first>
    - 02-RESEARCH Code Examples "Shopping-line → per-meal-node mapping" (concrete `getMealNodeTargetsForProduct` target shape) and Open Question 1 (per-meal fan-out to N nodes).
    - phase doc item 9 — aggregation output carries NO back-link to meals/nodes; must re-derive from `plannedMeals` × meal-keyed `recipeData`; two meals sharing a recipe → same original_node but distinct planned_meal, so key by planned_meal.
    - recipe-planner/src/pages/Outputs.tsx — how `recipeData` is keyed by `plannedMeal.id` (~:307) and the `PlannedMealWithRecipe`/`RecipeGraphData` shapes exported from `../lib/aggregation`.
  </read_first>
  <behavior>
    - getMealNodeTargetsForProduct(productId, plannedMeals, recipeData) returns one SwapTarget per matching recipe_product_node, carrying { plannedMealId, recipeName, nodeId, quantity, unit }.
    - A product used by two planned meals of the SAME recipe yields two targets with distinct plannedMealId and the same nodeId.
    - A product used via two nodes in ONE meal yields two targets for that meal (distinct nodeId) — supports per-meal fan-out.
    - A product used in no planned meal yields an empty array.
  </behavior>
  <action>
    Create shopping-mapping.ts exporting `SwapTarget` (`{ plannedMealId: string; recipeName: string; nodeId: string; quantity?: number; unit?: string }`) and `getMealNodeTargetsForProduct(productId, plannedMeals, recipeData)` that iterates plannedMeals, looks up each meal's `recipeData.get(meal.id)`, scans `productNodes` for `node.expand?.product?.id === productId`, and pushes a target per match (per 02-RESEARCH Code Examples). Import types from `../lib/aggregation`. Create shopping-mapping.test.ts covering the behavior cases, especially the two-same-recipe-meals distinct-target case (phase doc item 9 acceptance).
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/shopping-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getMealNodeTargetsForProduct` returns distinct targets keyed by planned_meal for two same-recipe meals (phase doc item 9 acceptance).
    - Multi-node-in-one-meal fan-out returns one target per node.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Pure mapping helper with tests green, built before the swap dialog consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure read-time joins of already-loaded client data; no boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | export list leaking resolved (make/skip) lines a shopper resolved | low | mitigate | filterForExport excludes all isResolved lines (D-06); unit-tested |
| T-02-04 | Tampering | wrong node targeted by mapping → swap applied to unintended meal | medium | mitigate | mapping keys by planned_meal + node id; test asserts distinct targets for same-recipe meals |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx vitest run src/lib/shopping-overlay.test.ts src/lib/shopping-mapping.test.ts` green.
- `cd recipe-planner && npx tsc -b` clean.
</verification>

<success_criteria>
Overlay computes remaining/isResolved and filters the export per D-04/05/06; mapping resolves a product to distinct per-meal node targets. Both pure, both tested.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-03-SUMMARY.md` when done.
</output>
