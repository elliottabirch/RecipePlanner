---
phase: 02-shopping-state-live-substitution
plan: 09
type: execute
wave: 5
depends_on: ["02-07", "02-08"]
files_modified:
  - recipe-planner/src/components/outputs/ShoppingListTab.tsx
autonomous: true
requirements: [SHOP-02, SHOP-04, SHOP-06]
must_haves:
  truths:
    - "Each non-pantry shopping line has a have-N stepper (Remove / value / Add), shows '{n} to buy' while remaining>0 and 'All set' when remaining<=0, and auto-completes (dimmed/struck) when have>=needed (SHOP-02)"
    - "Resolved lines (make/skip/have-complete) render dimmed + line-through with a resolution chip (Making/Skipped/Have enough) that un-resolves on tap, and stay visible on-screen (D-05)"
    - "Each line shows Swap and (when eligible) Make-it icon buttons wired to the onSwap/onMakeIt props from Outputs, each a 48x48 touch target with the specified aria-labels (SHOP-06)"
  artifacts:
    - "recipe-planner/src/components/outputs/ShoppingListTab.tsx — have-N stepper, remaining-to-buy, resolved treatment, resolution chip, swap/make-it buttons"
  key_links:
    - "overlaid item fields (haveQuantity/remaining/resolution/isResolved from 02-07) → ShoppingListTab rendering"
    - "stepper → setHaveQuantity; chip tap → setResolution('buy')/clear have; buttons → onSwap/onMakeIt props"
---

<objective>
Implement the shopping-line UI surfaces (UI-SPEC surfaces 1 & 2, SHOP-02/04/06): the have-N stepper with remaining-to-buy and auto-complete, the dimmed/struck resolved-line treatment with an un-resolve chip, and the per-line Swap / Make-it icon buttons (wired to the `onSwap`/`onMakeIt` props Outputs provides in 02-08), all sized for tablet touch with explicit aria-labels.

Purpose: SHOP-02 (have-N + remaining), the visible-but-dimmed resolution model (D-05), and the line-level entry points for swap/make-it — the shopper-facing surface of the phase.
Output artifacts produced by this plan: the upgraded ShoppingListTab.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md
@.planning/phases/02-shopping-state-live-substitution/02-UI-SPEC.md

Depends on: 02-07 (Outputs passes overlaid item fields + setHaveQuantity/setResolution down), 02-08 (onSwap/onMakeIt props + make-it eligibility).
@recipe-planner/src/components/outputs/ShoppingListTab.tsx
@recipe-planner/src/components/outputs/CheckableListItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Have-N stepper + remaining-to-buy + resolved-line treatment + resolution chip</name>
  <files>recipe-planner/src/components/outputs/ShoppingListTab.tsx</files>
  <read_first>
    - 02-UI-SPEC surface 1 (Have-N stepper): inline at the right edge of each non-pantry line; IconButton size="medium" Remove/value/Add each a 48x48 tap target with 8px gap; remaining caption Label 12/400 "{n} to buy" / "All set"; auto-complete visual opacity 0.55 + line-through matching CheckableListItem checked state; pantry-style lines keep the simple checkbox (no stepper).
    - 02-UI-SPEC surface 2 (resolved-line treatment): opacity 0.55 + line-through on the whole ListItem when resolution!=='buy' OR have>=needed; Chip size="small" outlined neutral "Making"/"Skipped"/"Have enough"; chip wrapped in a 48x48 tap target that un-resolves on tap; resolved lines stay in the DOM.
    - 02-UI-SPEC Copywriting Contract (have-N label, resolution chip labels) + Color/Typography contracts (no accent for chips; success/text.secondary for "All set", not accent green).
    - The downstream aria-label recommendation: have-N Remove/Add ("Increase/Decrease have quantity"), un-resolve chip ("Undo resolution for {productName}").
  </read_first>
  <action>
    Extend ShoppingListTab to consume the overlaid item fields (haveQuantity/remaining/resolution/isResolved) provided by Outputs (02-07). For each non-pantry line, render the have-N stepper (Remove IconButton, centered value min-width 40px, Add IconButton) calling `setHaveQuantity(lineKey, next)`; show the remaining caption ("{n} to buy" / "All set" using text.secondary or success.main, not accent). Apply the resolved treatment (opacity 0.55 + line-through on the ListItem) whenever `isResolved`, with a neutral outlined Chip ("Making"/"Skipped"/"Have enough") whose tap un-resolves (setResolution back to 'buy', or clear have_quantity if that was the cause). Keep pantry-style lines on the existing simple checkbox. Ensure every net-new control is a 48x48 tap target (sx minWidth/minHeight or size="medium") with 8px gaps, and add the specified aria-labels. Do not remove resolved lines from the list (D-05).
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Non-pantry lines show the stepper + remaining caption; auto-complete dims/strikes at have>=needed; resolved lines dim/strike with the correct chip and stay visible.
    - Chip tap un-resolves; controls are 48x48 with 8px gaps and the specified aria-labels; "All set" is not accent-colored.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Have-N + remaining-to-buy + resolved treatment implemented per UI-SPEC surfaces 1 & 2.</done>
</task>

<task type="auto">
  <name>Task 2: Per-line Swap and Make-it buttons wired to Outputs props</name>
  <files>recipe-planner/src/components/outputs/ShoppingListTab.tsx</files>
  <read_first>
    - 02-UI-SPEC surface 3 intro + surface 5 (make-it icon only when eligible; hide entirely when no source_recipe per D-10/UI-SPEC discretion); Copywriting Contract ("Swap", "Make it" line actions).
    - The onSwap/onMakeIt props + make-it eligibility flag passed from Outputs (02-08).
    - aria-label recommendation: Swap ("Swap {productName}"), Make-it ("Make it at home").
  </read_first>
  <action>
    Add a Swap IconButton (48x48, aria-label "Swap {productName}") to each shopping line calling `onSwap(line)`, and a Make-it IconButton (48x48, aria-label "Make it at home", e.g. HomeIcon/RestaurantIcon) rendered ONLY when the line's make-it eligibility flag is true (product has source_recipe, D-10) calling `onMakeIt(line)`. Place them alongside each other with 8px gap per the Spacing Scale. Do not implement the dialogs or handlers here — they live in Outputs (02-08); this task only renders the buttons and delegates to the props.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Swap button on every line; Make-it button only when eligible (hidden otherwise per D-10); both 48x48 with correct aria-labels; both delegate to the Outputs props.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Line-level swap/make-it entry points render and delegate to the Outputs handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational surface; all writes go through the hook/handlers owned by 02-06/02-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Input Validation | negative have-N entered via the stepper | low | mitigate | stepper clamps at 0 (Remove disabled/no-op below 0); setHaveQuantity receives a non-negative number |
| T-02-18 | Denial of Service (usability) | sub-48px touch targets cause mis-taps at the store | low | mitigate | all net-new controls are 48x48 tap targets with 8px gaps (Pitfall 6 / UI-SPEC Spacing exception) |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc -b` clean.
- Manual (02-10 UAT): "have 2 of 5" shows "3 to buy" and completes at 5; resolved lines dim in place; swap/make-it buttons open the right dialogs; touch operable on tablet (SHOP-02/06 manual per 02-VALIDATION).
</verification>

<success_criteria>
The shopping list shows have-N with remaining-to-buy and auto-complete, dims resolved lines in place with an un-resolve chip, and exposes touch-sized Swap/Make-it entry points with proper aria-labels.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-09-SUMMARY.md` when done.
</output>
