---
phase: 02-shopping-state-live-substitution
plan: 06
type: execute
wave: 2
depends_on: ["02-04", "02-05"]
files_modified:
  - recipe-planner/src/hooks/useShoppingState.ts
  - recipe-planner/src/components/outputs/SyncIndicator.tsx
autonomous: true
requirements: [SHOP-01, SHOP-07]
must_haves:
  truths:
    - "useShoppingState(weeklyPlanId) loads all shopping_state rows for the plan into a Map keyed by line_key and exposes setChecked/setHaveQuantity/setResolution with immediate optimistic local update"
    - "Writes go through the sync-queue: local state updates synchronously, PocketBase upsert happens in the background via query-then-branch (not create-then-catch-400)"
    - "isSyncing/pendingCount/failed reflect in-flight and retry-exhausted writes; SyncIndicator renders the three states"
  artifacts:
    - "recipe-planner/src/hooks/useShoppingState.ts — the optimistic persistence hook"
    - "recipe-planner/src/components/outputs/SyncIndicator.tsx — Synced / Saving…(n) / Can't reach server states"
  key_links:
    - "createSyncQueue (02-04) → useShoppingState processor → PocketBase shopping_state upsert"
    - "hook pending/failed → SyncIndicator states (per UI-SPEC surface 6)"
---

<objective>
Build the optimistic persistence hook (D-03, D-13, SHOP-01/07) that replaces the in-memory `checkedItems` Set, plus the `SyncIndicator` component that renders its sync state. The hook loads per-plan `shopping_state`, applies changes to local Map state synchronously (optimistic), and backgrounds each write through the `sync-queue` primitive (02-04) using query-then-branch upsert (02-RESEARCH Pitfall 5). Wiring into Outputs.tsx is a separate plan (02-07).

Purpose: SHOP-01 (persist per plan) and SHOP-07 (optimistic + retry + pending indicator) foundation. Modeled on `useRecipeQueue.ts` style but optimistic (that hook is naive await+refetch — 02-PATTERNS divergence note).
Output artifacts produced by this plan: `useShoppingState()` hook; `SyncIndicator.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md
@.planning/phases/02-shopping-state-live-substitution/02-UI-SPEC.md

Depends on: 02-04 (createSyncQueue in src/lib/sync-queue.ts), 02-05 (collections.shoppingState + ShoppingState interface).
@recipe-planner/src/hooks/useRecipeQueue.ts
@recipe-planner/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useShoppingState hook — optimistic load + queued upsert</name>
  <files>recipe-planner/src/hooks/useShoppingState.ts</files>
  <read_first>
    - 02-RESEARCH Code Examples `useShoppingState` skeleton (concrete target: per-plan load keyed by line_key, applyOptimistic → synchronous setState → syncLine via queue, query-then-branch upsert).
    - recipe-planner/src/hooks/useRecipeQueue.ts — style source: useCallback-wrapped async ops, try/catch/finally + console.error, no external state lib. Hook logs; page-level error state surfaces user-facing failures (02-PATTERNS error-handling note).
    - recipe-planner/src/lib/sync-queue.ts (from 02-04) — `createSyncQueue` API this hook consumes for retry/backoff/coalesce; do NOT reimplement retry inline.
    - recipe-planner/src/lib/api.ts — generic getAll/create/update + collections.shoppingState (from 02-05).
    - 02-RESEARCH Pitfall 5 — prefer query-then-branch upsert (getAll by `weekly_plan="X" && line_key="Y"` then create/update) over create-then-catch-400.
  </read_first>
  <action>
    Create useShoppingState(weeklyPlanId) that: on weeklyPlanId change, `getAll<ShoppingState>(collections.shoppingState, { filter: \`weekly_plan="${weeklyPlanId}"\` })` into a `Map<string, StateEntry>` keyed by `line_key` (StateEntry = recordId|null, checked, have_quantity, resolution); exposes `setChecked`, `setHaveQuantity`, `setResolution` that update the local Map synchronously (optimistic) then enqueue the write. Instantiate one `createSyncQueue` (02-04) whose processor performs the query-then-branch upsert (existing recordId → update; else getAll-by-filter → update-or-create, caching the created/found id back into the Map). Surface `state`, `isSyncing` (queue pending>0), `pendingCount`, and a failed/retry-exhausted signal from the queue's onChange. Guard empty weeklyPlanId. Keep last-write-wins (D-13) — no conflict resolution beyond coalescing.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Hook returns `{ state, isSyncing, pendingCount, failed, setChecked, setHaveQuantity, setResolution }` (names may vary but cover these).
    - Local Map updates synchronously before the async write (optimistic); writes route through createSyncQueue, not an inline setTimeout.
    - Upsert uses query-then-branch, not create-then-catch-400.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Optimistic per-plan persistence hook backed by the sync-queue, type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: SyncIndicator component</name>
  <files>recipe-planner/src/components/outputs/SyncIndicator.tsx</files>
  <read_first>
    - 02-UI-SPEC surface 6 (Pending-sync indicator): three states — Synced (CloudDoneIcon + "Synced", grey/action not accent), Saving… ({n}) (CircularProgress size 14 or CloudSyncIcon), Can't reach server — retrying (CloudOffIcon color="warning"). Label role 12/400, ~32px tall. No hidden/idle state.
    - 02-UI-SPEC Copywriting Contract "Sync indicator states" row for exact copy.
    - 02-PATTERNS "SyncIndicator.tsx" — no analog; small purely presentational MUI component.
  </read_first>
  <action>
    Create SyncIndicator as a presentational MUI component taking `pendingCount: number` and `failed: number` (or an equivalent status prop) and rendering the three states per UI-SPEC surface 6 with the exact copy from the Copywriting Contract. Use `warning.main` (not `error.main`) for the retry-exhausted state, and grey `color="action"` (not accent green) for Synced, to preserve the 10% accent budget. Do not include error-text; failures surface via the page-level error state elsewhere.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Renders Synced / Saving…({n}) / Can't reach server — retrying per pendingCount/failed, with UI-SPEC copy and colors (warning not error; action-grey not accent).
    - Always visible in one of the three states (no hidden idle).
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>SyncIndicator renders the three sync states per the UI contract; mounted by 02-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase | Hook reads/writes shopping_state over LAN/tailnet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering / data integrity | concurrent device edits overwrite each other (last-write-wins) | low | accept | Explicitly accepted per D-13 (single household, rare); documented, not a bug. Coalescing avoids lost double-taps on one device. |
| T-02-11 | Repudiation | write fails and shopper believes it saved | medium | mitigate | query-then-branch upsert + retry via sync-queue; retry-exhausted surfaced by SyncIndicator (this plan) |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx tsc -b` clean.
- Manual (deferred to 02-10 UAT checkpoint): toggle persists across refresh; network drop shows Saving…/Can't reach server and recovers on reconnect (SHOP-01/07 are manual-only per 02-VALIDATION — no live-PB test harness in repo).
</verification>

<success_criteria>
An optimistic per-plan persistence hook backed by the sync-queue, plus a three-state sync indicator, both type-checking clean and ready to wire into Outputs.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-06-SUMMARY.md` when done.
</output>
