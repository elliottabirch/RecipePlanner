---
phase: 02-shopping-state-live-substitution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/src/lib/aggregation/utils/variant-utils.ts
  - recipe-planner/src/lib/aggregation/utils/variant-utils.test.ts
autonomous: true
requirements: [SHOP-03]
must_haves:
  truths:
    - "A variant override carrying quantity=2, unit='cup' changes the derived line's quantity/unit for that meal to 2 cup"
    - "A variant override with null quantity/unit inherits the original node's quantity/unit unchanged (inherit-when-null)"
  artifacts:
    - "recipe-planner/src/lib/aggregation/utils/variant-utils.ts — VariantOverride.quantity/unit + inherit-when-null replacement branch"
    - "recipe-planner/src/lib/aggregation/utils/variant-utils.test.ts — new test file (none exists today)"
  key_links:
    - "VariantOverride.quantity/unit → applyVariantOverrides replacement node → all six derived outputs"
---

<objective>
Thread substitute quantity/unit through the override re-derivation pipeline (D-07, D-09) so a mid-shop swap can express "2 cup instead of 3 cup" per meal. This is the SHOP-03 core: storing the field without these edits produces zero visible effect (02-RESEARCH Pitfall 2). The override map builder (Outputs.tsx) and the swap dialog that write these fields are separate plans (02-07, 02-08); this plan makes the pure re-derivation honor them.

Purpose: Highest-value automatable core of the phase (per 02-VALIDATION) — the exact place D-09 warns storing-without-threading silently no-ops.
Output artifacts produced by this plan: `VariantOverride.quantity`, `VariantOverride.unit`; inherit-when-null replacement branch in `applyVariantOverrides`; `variant-utils.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md

@recipe-planner/src/lib/aggregation/utils/variant-utils.ts
@recipe-planner/src/lib/units.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend VariantOverride and make applyVariantOverrides inherit-when-null</name>
  <files>recipe-planner/src/lib/aggregation/utils/variant-utils.ts, recipe-planner/src/lib/aggregation/utils/variant-utils.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/aggregation/utils/variant-utils.ts — `VariantOverride` interface (:13-16, only `originalNodeId` + `replacementProduct`); `applyVariantOverrides` (:174-276); the replacement branch (~:220-236) with the "for now, preserve original" shortcut at ~:228-229; `validateOverrides` (:41-66) drops-invalid pattern.
    - recipe-planner/src/lib/units.ts — `Unit` union type (the enum unit tokens per D-12); do not use free text.
    - 02-RESEARCH Architecture Patterns Pattern 3 (concrete diff shape) and Pitfall 2; 02-PATTERNS "variant-utils.ts" section.
    - NOTE the real path is `src/lib/aggregation/utils/variant-utils.ts` — CONTEXT.md's `src/lib/variant-utils.ts` citation is wrong (02-RESEARCH correction).
  </read_first>
  <behavior>
    - applyVariantOverrides applied to a node with an override `{ quantity: 2, unit: 'cup' }` produces a replacement node with quantity 2 and unit 'cup' (plus the replacement product relation).
    - applyVariantOverrides applied with an override `{ quantity: null, unit: null }` (or omitted) produces a replacement node whose quantity/unit equal the ORIGINAL node's quantity/unit (inherit-when-null).
    - A partial override `{ quantity: 2, unit: null }` sets quantity 2 but inherits the original unit.
    - Only the product relation + quantity/unit change; other node fields are preserved via spread.
  </behavior>
  <action>
    Add optional `quantity?: number | null` and `unit?: string | null` to the `VariantOverride` interface (D-09/D-12 — `unit` holds a Phase-1 `Unit` enum token; type it as the `Unit` type imported from units.ts, or `string` if that creates an import cycle — prefer `Unit`). In the replacement branch of `applyVariantOverrides`, replace the "for now, preserve original" shortcut with `quantity: override.quantity ?? node.quantity` and `unit: override.unit ?? node.unit` on the constructed replacement node (inherit-when-null per D-07/D-09), keeping the product-relation swap and the existing spread of other node fields. Do not change `validateOverrides`' drop-invalid convention — just carry the extra fields on already-valid overrides.

    Create variant-utils.test.ts (no test file exists for this module today) covering the four behavior cases above with minimal ExpandedProductNode fixtures.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/aggregation/utils/variant-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `VariantOverride` has optional `quantity`/`unit`; `unit` uses the Phase-1 enum type (not free text) per D-12.
    - Full-override, null-override, and partial-override cases all pass; the "preserve original" comment/shortcut is removed.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>applyVariantOverrides honors substitute quantity/unit with inherit-when-null; regression test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure transform of already-loaded override + node data; no new boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | swap quantity persisted but not threaded → silent no-op (correctness, not adversarial) | medium | mitigate | This plan wires the three re-derivation touchpoints (interface here; map builder in 02-07); test asserts derived quantity actually changes |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx vitest run src/lib/aggregation/utils/variant-utils.test.ts` green.
- `cd recipe-planner && npx tsc -b` clean.
</verification>

<success_criteria>
Override quantity/unit flows into the replacement node with inherit-when-null; a null override leaves original quantity unchanged; a `{2, cup}` override changes it. Proven by the new test file.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-02-SUMMARY.md` when done.
</output>
