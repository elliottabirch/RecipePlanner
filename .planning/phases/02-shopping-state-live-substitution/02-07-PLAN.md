---
phase: 02-shopping-state-live-substitution
plan: 07
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-06"]
files_modified:
  - recipe-planner/src/pages/Outputs.tsx
autonomous: true
requirements: [SHOP-01, SHOP-03, SHOP-06]
must_haves:
  truths:
    - "Outputs.tsx uses useShoppingState in place of the in-memory checkedItems Set; all six tabs' checks persist per plan"
    - "The override→VariantOverride map builder forwards quantity/unit off the MealVariantOverride record (D-09 item 10c) so swap quantities re-derive"
    - "The overlay + export filter exclude resolved (make/skip/have-complete) lines from the export while keeping them visible on-screen (D-05/D-06)"
    - "SyncIndicator is mounted in the Outputs header"
  artifacts:
    - "recipe-planner/src/pages/Outputs.tsx — hook wiring, Set-view adapter, overlay+export integration, override map threading, SyncIndicator mount"
  key_links:
    - "useShoppingState.state → Set<string> adapter → every tab's checkedItems prop (unchanged prop surface)"
    - "MealVariantOverride.quantity/unit → override map builder → applyVariantOverrides (02-02) → all outputs"
    - "shopping-overlay.filterForExport → filteredShoppingListForExport"
---

<objective>
Wire the integration hub: replace `Outputs.tsx`'s in-memory `checkedItems` Set + `toggleChecked` with `useShoppingState` (D-03), thread `quantity`/`unit` through the override map builder (D-09 item 10c — the third re-derivation touchpoint), integrate the overlay/export filter so resolved lines drop from the export but stay visible on-screen (D-05/D-06), and mount `SyncIndicator` in the header. This connects the Wave-1 pure logic + the Wave-2 hook to the live page without changing any tab's prop surface.

Purpose: Turns the six tabs durable (SHOP-01), completes the swap re-derivation wiring (SHOP-03), and applies the export-vs-screen split (D-06).
Output artifacts produced by this plan: the wired Outputs.tsx (hook, adapter, overlay/export integration, override-map threading, SyncIndicator mount).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shopping-state-live-substitution/02-CONTEXT.md
@.planning/phases/02-shopping-state-live-substitution/02-RESEARCH.md
@.planning/phases/02-shopping-state-live-substitution/02-PATTERNS.md

Depends on: 02-01 (lineId + helpers), 02-02 (VariantOverride.quantity/unit), 02-03 (shopping-overlay), 02-06 (useShoppingState + SyncIndicator).
@recipe-planner/src/pages/Outputs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace checkedItems Set with useShoppingState + Set-view adapter</name>
  <files>recipe-planner/src/pages/Outputs.tsx</files>
  <read_first>
    - recipe-planner/src/pages/Outputs.tsx:113 (`checkedItems` useState Set), :592-602 (`toggleChecked`), :725-806 (the six tabs each receive `checkedItems`/`onToggleChecked`).
    - 02-PATTERNS "Outputs.tsx" section — the recommended cheapest path is a Set<string> view derived from the hook's Map so every tab's prop signature stays identical.
    - 02-RESEARCH — hook logs; page-level `error` state surfaces user-facing failures (Outputs.tsx:308-312 pattern).
  </read_first>
  <action>
    Replace the `checkedItems`/`toggleChecked` state (Outputs.tsx:113,592-602) with `const { state, setChecked, setHaveQuantity, setResolution, isSyncing, pendingCount, failed } = useShoppingState(selectedPlanId)`. Derive a `Set<string>` view of checked line_keys from `state` (memoized) and pass it as `checkedItems` to all six tabs so their prop surface is unchanged; route `onToggleChecked(key)` to `setChecked(key, !currentlyChecked)`. Keep have-N/resolution setters available for the ShoppingListTab wiring (02-09) and swap/make-it handlers (02-08) — expose them via the same page scope. Do not change tab prop signatures in this task.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `checkedItems` Set state and `toggleChecked` are gone; the hook drives checked state; a Set-view adapter feeds all six tabs unchanged.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Outputs persists all six tabs via the hook with the tab prop surface untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Thread quantity/unit through the override map builder; integrate overlay + export filter</name>
  <files>recipe-planner/src/pages/Outputs.tsx</files>
  <read_first>
    - recipe-planner/src/pages/Outputs.tsx:225-237 — override→VariantOverride map builder (currently forwards only originalNodeId + replacementProduct).
    - recipe-planner/src/pages/Outputs.tsx:336-366 — pantry-only `filteredShoppingListForExport`; :318-332 the useMemo derive chain.
    - 02-RESEARCH Architecture Patterns Pattern 3 step 3 (map builder forwards quantity/unit) and Pattern 2 (overlay memo); 02-PATTERNS "useMemo-derived pipeline" shared pattern.
    - shopping-overlay.ts exports from 02-03 (`overlayShoppingItem`, `filterForExport`).
  </read_first>
  <action>
    In the override map builder (Outputs.tsx:225-237), forward `quantity: override.quantity ?? null` and `unit: override.unit ?? null` off the raw `MealVariantOverride` record into each `VariantOverride` (D-09 item 10c — completes the three touchpoints with 02-02). Add a `useMemo` overlay link in the existing derive chain that maps `groupedShoppingList` items through `overlayShoppingItem(item, state)` (02-03), keyed on `[groupedShoppingList, state]`; use the overlaid list for the visible shopping list and pass overlaid fields (haveQuantity/remaining/resolution/isResolved) down to ShoppingListTab (02-09 consumes them). Generalize `filteredShoppingListForExport` to also exclude `isResolved` lines via `filterForExport` (D-06), not just pantry-checked ones — resolved lines stay in the on-screen list (D-05), only the export drops them. Mount `SyncIndicator` (from 02-06) in the Outputs header box (near the Weekly Plan Select), right-aligned per UI-SPEC surface 6, fed by `pendingCount`/`failed`.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b && npx vitest run src/lib/shopping-overlay.test.ts src/lib/aggregation/utils/variant-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Override map builder forwards quantity/unit; combined with 02-02 a `{2, cup}` override changes the derived line (covered by variant-utils test).
    - Export excludes make/skip/have-complete lines (via filterForExport) while they remain in the on-screen list (D-05/D-06).
    - SyncIndicator is mounted in the header and fed by the hook.
    - `cd recipe-planner && npx tsc -b` passes.
  </acceptance_criteria>
  <done>Swap quantities re-derive end-to-end, export mirrors the actionable buy list, and the sync indicator is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase | Outputs now reads/writes shopping_state and reads meal_variant_overrides; all over LAN/tailnet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Information Disclosure | export accidentally includes resolved lines (make/skip) | low | mitigate | filterForExport applied to the export path (D-06); covered by 02-03 overlay test |
| T-02-13 | Tampering | swap quantity silently ignored because map builder drops it | medium | mitigate | map builder forwards quantity/unit (this plan) completing the three touchpoints with 02-02; variant-utils test asserts derived change |

No new external packages this phase (02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `cd recipe-planner && npx tsc -b` clean.
- `cd recipe-planner && npx vitest run` full suite green (overlay + variant-utils prove the re-derivation).
- Manual (02-10 UAT): six-tab persistence across refresh/device (SHOP-01).
</verification>

<success_criteria>
Outputs persists all six tabs via the hook, threads swap quantity/unit through re-derivation, applies the screen-vs-export split, and shows the sync indicator — without changing any tab's prop surface.
</success_criteria>

<output>
Create `.planning/phases/02-shopping-state-live-substitution/02-07-SUMMARY.md` when done.
</output>
