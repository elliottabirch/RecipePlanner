---
phase: 03-product-registry-seeding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/package.json
  - recipe-planner/package-lock.json
  - recipe-planner/src/lib/search/product-search.ts
  - recipe-planner/src/lib/search/product-search.test.ts
  - recipe-planner/src/pages/registries/Products.tsx
  - recipe-planner/src/pages/RecipeEditor.tsx
  - recipe-planner/src/components/ProductForm.tsx
  - recipe-planner/src/components/outputs/ShopSwapDialog.tsx
autonomous: true
requirements: [REG-02]
user_setup: []

must_haves:
  truths:
    - "searchProducts('paste tomato', products) ranks a product named 'tomato paste' first (word-order independence)"
    - "searchProducts('garbonzo', products) returns a product named 'garbanzo' (typo tolerance)"
    - "The registry list filter, both RecipeEditor product Autocompletes, the ProductForm duplicate check, and the ShopSwapDialog replacement Autocomplete all route through the single searchProducts module"
  artifacts:
    - "recipe-planner/src/lib/search/product-search.ts"
    - "recipe-planner/src/lib/search/product-search.test.ts"
  key_links:
    - "All 5 call sites import the SAME searchProducts function — one fuse.js config, not five divergent ones"
    - "fuse.js is pinned in package.json and installed"
---

<objective>
Build the single client-side fuzzy/token search module (`searchProducts`, D-05) and wire it into all five product-search call sites so typos and reordered words rank the right product first (REG-02).

Purpose: Every product-search surface today uses naive substring matching (`.includes()` / default MUI Autocomplete). REG-02 requires typo + word-order tolerance everywhere from ONE reusable module — "garbonzo" finds "garbanzo", "paste tomato" finds "tomato paste".
Output: `product-search.ts` + co-located test, plus `filterOptions`/filter wiring at 5 call sites across 4 component files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-registry-seeding/03-CONTEXT.md
@.planning/phases/03-product-registry-seeding/03-RESEARCH.md
@.planning/phases/03-product-registry-seeding/03-PATTERNS.md
@.planning/phases/03-product-registry-seeding/03-UI-SPEC.md
@recipe-planner/src/pages/registries/Products.tsx
@recipe-planner/src/pages/RecipeEditor.tsx
@recipe-planner/src/components/ProductForm.tsx
@recipe-planner/src/components/outputs/ShopSwapDialog.tsx
</context>

<artifacts_produced>
- `recipe-planner/src/lib/search/product-search.ts` — `searchProducts(query, products): Product[]` fuse.js wrapper with sorted-token key
- `recipe-planner/src/lib/search/product-search.test.ts` — REG-02 behavior tests
- `fuse.js` pinned dependency
- Wiring at 5 call sites (Products.tsx, RecipeEditor.tsx ×2, ProductForm.tsx, ShopSwapDialog.tsx)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fuse.js dependency + searchProducts module (test-first)</name>
  <files>recipe-planner/package.json, recipe-planner/src/lib/search/product-search.ts, recipe-planner/src/lib/search/product-search.test.ts</files>
  <read_first>
    - .planning/phases/03-product-registry-seeding/03-RESEARCH.md (Pattern 1 — concrete searchProducts implementation, FUSE_OPTIONS, sorted-token trick)
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (product-search.ts section — verbatim base implementation)
    - recipe-planner/src/lib/units.test.ts (existing vitest convention: co-located *.test.ts, describe/it/expect)
  </read_first>
  <behavior>
    - searchProducts("paste tomato", products)[0].name === "tomato paste" (word-order independence via sorted-token key)
    - searchProducts("garbonzo", products) includes a product named "garbanzo" (per-term typo tolerance)
    - searchProducts("", products) returns the full products array unchanged (empty-query passthrough)
    - searchProducts("  ", products) (whitespace-only) also returns the full array (trim guard)
  </behavior>
  <action>
    Install `fuse.js` pinned to `7.4.2` (Approved in 03-RESEARCH.md Package Legitimacy Audit — 10.1M weekly downloads, canonical `krisk/Fuse` repo). Create `recipe-planner/src/lib/search/product-search.ts` exporting `searchProducts(query: string, products: Product[]): Product[]`, implemented exactly per 03-RESEARCH.md Pattern 1 / 03-PATTERNS.md: a `SearchableProduct` extending `Product` with a `_sortedTokens` field (name lowercased, split on whitespace, filtered, alpha-sorted, joined); FUSE_OPTIONS with `keys` weighting `name` 0.6 and `_sortedTokens` 0.4, `threshold: 0.35`, `ignoreLocation: true`, `includeScore: true`; empty/whitespace query returns `products` unchanged; otherwise build a `Fuse` over the indexed array and return `fuse.search(query).map(r => r.item)`. Import the `Product` type from `../types`. Write `product-search.test.ts` FIRST covering the four behavior cases above using a small in-memory fixture array (include at least "tomato paste", "tomato sauce", "garbanzo beans"→ use name "garbanzo", and a few distractors). Confirm the test fails before the module exists (RED), then implement to green.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/search/product-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fuse.js` 7.4.2 present in package.json dependencies.
    - `product-search.test.ts` asserts both REG-02 behavior cases plus the empty/whitespace passthrough, and passes.
    - `searchProducts` lives at `src/lib/search/product-search.ts` and returns `Product[]`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire searchProducts into all 5 call sites</name>
  <files>recipe-planner/src/pages/registries/Products.tsx, recipe-planner/src/pages/RecipeEditor.tsx, recipe-planner/src/components/ProductForm.tsx, recipe-planner/src/components/outputs/ShopSwapDialog.tsx</files>
  <read_first>
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (per-file MODIFY sections with exact current code + replacements for all 5 sites)
    - .planning/phases/03-product-registry-seeding/03-UI-SPEC.md (Surface 1 — noOptionsText copy, no match-highlighting in scope)
  </read_first>
  <action>
    Wire the single `searchProducts` module into all five call sites per D-05 / REG-02, following the exact replacements in 03-PATTERNS.md (do not diverge the config — import the one module everywhere):
    1. `Products.tsx` — replace the `filteredItems` useMemo body (currently `items.filter(item => item.name.toLowerCase().includes(query))`) with `searchProducts(searchQuery, items)`. The existing "Showing N of M products" counter and "No products match your search" empty state need no copy change.
    2. `RecipeEditor.tsx` site 1 (main product-picker Autocomplete, ~lines 900-904) — add `filterOptions={(options, { inputValue }) => searchProducts(inputValue, options)}`. Add `noOptionsText` rendering the dynamic string `No products match "{inputValue}"` (replaces MUI default "No options"). Leave renderInput/renderOption/value/onChange unchanged.
    3. `RecipeEditor.tsx` site 2 (Edit Product Node dialog Autocomplete, ~lines 1144-1148) — same `filterOptions` + `noOptionsText` wiring. Import `searchProducts` from `../lib/search/product-search` once at the top of the file, reused by both sites.
    4. `ProductForm.tsx` — replace the `potentialDuplicates` useMemo body: keep the `editingProductId` self-exclusion filter, then `return searchProducts(form.name, candidates).slice(0, 5)`. Leave `hasExactMatch` and the Alert copy unchanged (fuzzy search only changes which candidates populate the list, per 03-UI-SPEC.md).
    5. `ShopSwapDialog.tsx` (replacement-product Autocomplete, ~lines 298-324) — add the same `filterOptions` prop and `noOptionsText`; keep `groupBy` and `renderOption` unchanged.
    No match-highlighting/bolding is in scope — reordering + no-results copy is the entire visible contract (03-UI-SPEC.md Surface 1).
  </action>
  <verify>
    <automated>cd recipe-planner && test $(grep -rl 'lib/search/product-search' src/pages/registries/Products.tsx src/pages/RecipeEditor.tsx src/components/ProductForm.tsx src/components/outputs/ShopSwapDialog.tsx | wc -l) -eq 4 && npx tsc -b --noEmit 2>&1 | (grep -i error && exit 1 || echo "4 files wired, typecheck clean")</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files import `searchProducts` from `src/lib/search/product-search`; grep count is 4.
    - Both RecipeEditor Autocompletes and the ShopSwapDialog Autocomplete carry `filterOptions` routing through `searchProducts`; `noOptionsText` replaces MUI's default "No options".
    - `Products.tsx` `filteredItems` and `ProductForm.tsx` `potentialDuplicates` route through `searchProducts`; `hasExactMatch` and existing copy unchanged.
    - `npx tsc -b` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text query → client fuzzy index | Search input flows into the in-memory fuse.js matcher (no server round-trip, no persistence) |
| npm registry → project dependencies | New `fuse.js` package added to the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | searchProducts free-text query | low | mitigate | Empty/whitespace-trim guard returns the input array unchanged; fuse.js operates purely in-memory over the already-fetched Product array — no query reaches a server, no injection surface |
| T-03-SC | Tampering | npm install fuse.js | low | accept | Pre-cleared in 03-RESEARCH.md Package Legitimacy Audit (npm, 10.1M/wk, canonical krisk/Fuse, no postinstall script) — not [ASSUMED]/[SUS], no legitimacy checkpoint required; pinned to 7.4.2 |
</threat_model>

<verification>
- `npx vitest run src/lib/search/product-search.test.ts` green (both REG-02 behavior cases).
- Wiring grep count 4 + `npx tsc -b` clean.
- Full suite `npm test` stays green (no regression to existing units/linter/aggregation tests).
</verification>

<success_criteria>
- One `searchProducts` module backs all five product-search call sites; REG-02 typo + word-order behavior verified by unit test.
</success_criteria>

<output>
Create `.planning/phases/03-product-registry-seeding/03-02-SUMMARY.md` when done.
</output>