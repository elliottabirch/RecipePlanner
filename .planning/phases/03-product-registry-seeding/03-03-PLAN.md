---
phase: 03-product-registry-seeding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/scripts/build-usda-seed.js
  - recipe-planner/scripts/data/README.md
  - recipe-planner/scripts/seed-output/catalog-comparison.md
autonomous: false
requirements: [REG-01]
user_setup:
  - service: USDA FoodData Central bulk download
    why: "Foundation Foods + SR Legacy bulk files supply the fdc_id join target and (in Plan 05) the Search-USDA asset — one download, two consumers"
    dashboard_config:
      - task: "Download Foundation Foods (JSON/CSV) and SR Legacy (CSV) full-download zips"
        location: "https://fdc.nal.usda.gov/download-datasets/ — re-verify file sizes (research valid until 2026-08-06)"

must_haves:
  truths:
    - "The USDA Foundation Foods + SR Legacy bulk files are staged locally under scripts/data/ (offline artifacts, gitignored)"
    - "build-usda-seed.js can normalize a candidate ingredient name, fuzzy-join it to a USDA fdc_id (Foundation Foods first, SR Legacy fallback, null on genuine miss), and map its category to one of the 8 existing sections + one of the 4 real stores"
    - "A 50-item sample comparison of OFF-filtered vs hand-curated catalog approaches — with real name→fdc_id join-coverage stats — is produced for the user to choose from"
  artifacts:
    - "recipe-planner/scripts/build-usda-seed.js (acquisition + join + mapping scaffolding)"
    - "recipe-planner/scripts/seed-output/catalog-comparison.md"
  key_links:
    - "The name→fdc_id join uses the same fuse.js conservative-threshold logic used elsewhere this phase"
    - "The category→section map targets exactly the 8 existing sections; the store map routes across all 4 real stores (safeway, costco, trader joes, online) — not just 2"
---

<objective>
Stand up the offline seed-build pipeline (`build-usda-seed.js`), acquire the USDA bulk data, and produce a 50-item head-to-head comparison of the two candidate plain-name catalog sources so the user can make the D-01 catalog-source decision on real evidence rather than an unvalidated assumption (REG-01, Open Question 1).

Purpose: D-01 pivots the seed source from "rename verbose USDA descriptions" to an EXTERNAL plain-name catalog joined to USDA for `fdc_id`. Research left Open Question 1 (Open Food Facts categories taxonomy vs a hand-curated list) UNRESOLVED and explicitly recommended a 50-item spike before committing to the full build. This plan produces that evidence and gets the decision locked.
Output: the seed-build script skeleton (name normalization, fuzzy fdc_id join, category→section/store map, keyword→canonical_unit map), staged USDA bulk data, and a comparison report driving a decision checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-registry-seeding/03-CONTEXT.md
@.planning/phases/03-product-registry-seeding/03-RESEARCH.md
@.planning/phases/03-product-registry-seeding/03-PATTERNS.md
@recipe-planner/scripts/find-duplicates.js
@recipe-planner/scripts/find-product-matches.js
</context>

<artifacts_produced>
- `recipe-planner/scripts/build-usda-seed.js` — offline pipeline: name normalize → fuzzy fdc_id join → category→section/store map → keyword→canonical_unit map
- Staged USDA bulk files under `recipe-planner/scripts/data/` (gitignored large artifacts; a committed `README.md` documents provenance + expected filenames)
- `recipe-planner/scripts/seed-output/catalog-comparison.md` — 50-item OFF-filtered vs hand-curated comparison with join-coverage stats
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Seed-build pipeline scaffolding + USDA acquisition + 50-item catalog comparison</name>
  <files>recipe-planner/scripts/build-usda-seed.js, recipe-planner/scripts/data/README.md, recipe-planner/scripts/seed-output/catalog-comparison.md</files>
  <read_first>
    - .planning/phases/03-product-registry-seeding/03-RESEARCH.md (Summary + Open Questions 1-3 — OFF taxonomy vs hand-curated, join strategy, category→section approach; the 4-store / 8-section live facts; nutrient-id notes)
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (build-usda-seed.js section — PB client boilerplate, report artifact shape)
    - recipe-planner/scripts/find-duplicates.js (ESM client boilerplate, OUTPUT_DIR report pattern, main().catch entrypoint)
  </read_first>
  <action>
    Create `recipe-planner/scripts/build-usda-seed.js` as the offline seed-build pipeline (Claude's discretion on dataset + join details per CONTEXT.md). Follow the Phase-1 script convention (ESM imports, `main().catch((e)=>{...process.exit(1)})`; PocketBase client only needed for resolving live section/store IDs by name — use the `PB_URL` env-override so it can point at test `:8091`). Acquire the USDA bulk data: download Foundation Foods (JSON or CSV) and SR Legacy (CSV — 54M, the smaller variant per research) from FoodData Central and stage them under `scripts/data/` as offline artifacts; add `scripts/data/README.md` documenting the source URLs, download date, expected filenames, and CC0/public-domain provenance (gitignore the large data files themselves — do not commit multi-MB bulk downloads; the committed README is the provenance record). Build the pipeline's reusable pieces: (a) a name-normalizer (lowercase, trim, plain concept-level names per the "black beans" not "Beans, black, mature seeds, raw" bar); (b) a name→`fdc_id` fuzzy join reusing the same fuse.js conservative-threshold config as the rest of this phase — Foundation Foods first (higher rigor), SR Legacy fallback, `fdc_id = null` on a genuine miss (D-02 allows null as valid), recording which source matched via `usda_data_type`; (c) a category→section keyword map targeting EXACTLY the 8 existing sections, unmapped → null (linter-flagged, first-use-corrected per D-04); `frozen`/`international` have no USDA equivalent and stay null; (d) a category/keyword→store default map routing across ALL 4 real stores (`safeway`, `costco`, `trader joes`, `online` — resolve store/section relation IDs by name at runtime, do NOT hardcode the phase-doc's stale 2-store guess); (e) a keyword→`canonical_unit`/`dimension` map where a small map classifies, else null (D-04). Then RUN the pipeline over a 50-item sample under BOTH candidate catalog approaches — (1) Open Food Facts categories taxonomy filtered to English generic leaf nodes under vegetables/fruits/meats/dairies/cereals/fats, and (2) a hand-curated (LLM-assisted) 50-item household-staple list — and write `scripts/seed-output/catalog-comparison.md` reporting, per approach: the 50 candidate names, the name→fdc_id match rate, count of null-fdc_id items, section-mapping coverage, and any obvious noise/junk names. This report is the decision artifact for Task 2. Do NOT write to any PocketBase yet — this task only reads (for name→ID resolution) and emits files.
  </action>
  <verify>
    <automated>cd recipe-planner && test -f scripts/build-usda-seed.js && test -f scripts/data/README.md && test -f scripts/seed-output/catalog-comparison.md && grep -qi 'match rate\|coverage' scripts/seed-output/catalog-comparison.md && echo "pipeline + comparison report present"</automated>
  </verify>
  <acceptance_criteria>
    - `build-usda-seed.js` exists with name-normalize, fuzzy fdc_id join (FoundationFoods-first/SRLegacy-fallback/null), section+store maps (8 sections, 4 stores), and keyword→canonical_unit map.
    - USDA bulk files staged under `scripts/data/`; `scripts/data/README.md` records provenance and filenames; large data files gitignored.
    - `scripts/seed-output/catalog-comparison.md` shows both approaches with real join-coverage stats over a 50-item sample.
    - No PocketBase writes performed in this task.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Lock the catalog-source decision (D-01 / Open Question 1)</name>
  <action>Present the 50-item comparison report (scripts/seed-output/catalog-comparison.md) and have the user choose between the OFF-filtered taxonomy and a hand-curated list for the full seed build. Block until the user selects; record the choice (and any target item count) in the SUMMARY for Plan 04 to consume.</action>
  <decision>Which plain-name catalog source backs the full seed build (D-01 / Open Question 1)?</decision>
  <context>Research could not settle this without doing the curation work; it recommended a 50-item spike first. The comparison report (scripts/seed-output/catalog-comparison.md, produced in Task 1) shows both approaches with real name→fdc_id join-coverage and name-quality evidence. Foundation Foods alone yields &lt;150 net-new products, so breadth comes from whichever catalog is chosen. Either choice is valid; null fdc_id is an acceptable per-item state (D-02).</context>
  <options>
    <option id="off-filtered">
      <name>Open Food Facts categories taxonomy (filtered)</name>
      <pros>Real external breadth "for free"; ODbL-licensed; carries "we didn't just make this up" provenance; scales past 800 if wanted</pros>
      <cons>Needs a filtering/curation pass to strip branded/regional noise; mapping to the 8 sections is noisier</cons>
    </option>
    <option id="hand-curated">
      <name>Hand-curated (LLM-assisted) static list</name>
      <pros>Zero filtering cost; zero license exposure; organized directly into the 8 sections; lowest risk for a single-user personal registry</pros>
      <cons>100% manual authorship; no external credibility; breadth capped at what is authored</cons>
    </option>
  </options>
  <resume-signal>Select: off-filtered or hand-curated (optionally with a target item count for the full build).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline bulk file → build script | Multi-MB USDA/OFF files parsed by a one-time dev-machine script |
| dev-machine script → PocketBase (read-only) | Reads sections/stores to resolve relation IDs by name; no writes this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Denial of Service | build-usda-seed.js parsing oversized/malformed USDA/OFF bulk files | low | mitigate | Standard JSON.parse / CSV-parse error handling; offline dev-machine script, not a running service; prefer the smaller SR-Legacy CSV (54M) over the 205M JSON per research |
| T-03-06 | Tampering | Untrusted external catalog data seeding product names | low | accept | Names are human-reviewed in the catalog-comparison report and again at the Plan-04 dry-run checkpoint before any prod write; single-household trusted-network app |
| T-03-SC | Tampering | No new package installs this plan | n/a | accept | This plan adds no npm dependencies (fuse.js installed in Plan 02) |
</threat_model>

<verification>
- Pipeline + comparison report present (verify command).
- Decision checkpoint resolved with a chosen catalog source recorded in the SUMMARY.
</verification>

<success_criteria>
- The seed-build pipeline exists and the catalog-source decision is locked on real 50-item join-coverage evidence.
</success_criteria>

<output>
Create `.planning/phases/03-product-registry-seeding/03-03-SUMMARY.md` when done. Record the chosen catalog source and target item count in the SUMMARY (Plan 04 consumes it).
</output>
