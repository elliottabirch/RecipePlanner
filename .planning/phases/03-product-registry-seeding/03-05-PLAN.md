---
phase: 03-product-registry-seeding
plan: 05
type: execute
wave: 2
depends_on: [03-02, 03-03]
files_modified:
  - recipe-planner/scripts/build-usda-search-index.js
  - recipe-planner/src/assets/usda-sr-legacy.json
  - recipe-planner/src/lib/usda/usda-lookup.ts
  - recipe-planner/src/lib/usda/usda-lookup.test.ts
  - recipe-planner/src/lib/usda/category-section-map.ts
autonomous: true
requirements: [REG-03]
user_setup: []

must_haves:
  truths:
    - "A trimmed SR-Legacy index ({name, foodCategory, fdc_id}) is built from the same bulk file as the seed join and shipped as a bundled static asset"
    - "searchUsda(query) fuzzy-searches the bundled index and returns candidates each carrying an fdc_id"
    - "A category→section map derives a section id from an SR-Legacy foodCategory for prefill"
  artifacts:
    - "recipe-planner/scripts/build-usda-search-index.js"
    - "recipe-planner/src/assets/usda-sr-legacy.json"
    - "recipe-planner/src/lib/usda/usda-lookup.ts"
    - "recipe-planner/src/lib/usda/usda-lookup.test.ts"
    - "recipe-planner/src/lib/usda/category-section-map.ts"
  key_links:
    - "usda-lookup.ts reuses the same fuse.js matching approach as product-search.ts (one search engine, not a second)"
    - "The bundled asset and the seed's fdc_id join both consume the one SR-Legacy bulk download (D-06)"
---

<objective>
Build the bundled trimmed SR-Legacy Search-USDA index (D-06) and the client lookup module (`searchUsda`) that the quick-create "Search USDA" tab (Plan 06) queries — fully offline, no live FDC API (deferred), no network round-trip (REG-03).

Purpose: REG-03 requires quick-create to offer an on-demand "Search USDA" mode for items not in the seeded set. D-06 ships the bundled SR-Legacy index (native `fdc_id` for pre-fill) as the PRIMARY source and DEFERS the live FDC API fallback — do not build it. The same SR-Legacy bulk file downloaded for the seed join (Plan 03) feeds this asset — one download, two consumers.
Output: the index-build script, the bundled `usda-sr-legacy.json` asset, `usda-lookup.ts` + test, and a category→section map for prefill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-registry-seeding/03-CONTEXT.md
@.planning/phases/03-product-registry-seeding/03-RESEARCH.md
@.planning/phases/03-product-registry-seeding/03-PATTERNS.md
@recipe-planner/src/lib/search/product-search.ts
</context>

<artifacts_produced>
- `recipe-planner/scripts/build-usda-search-index.js` — SR-Legacy bulk → trimmed `{name, foodCategory, fdc_id}` asset
- `recipe-planner/src/assets/usda-sr-legacy.json` — bundled Search-USDA index (target ~150-250KB gzipped)
- `recipe-planner/src/lib/usda/usda-lookup.ts` — `searchUsda(query)` fuse.js wrapper over the bundled index
- `recipe-planner/src/lib/usda/usda-lookup.test.ts` — index-search behavior tests
- `recipe-planner/src/lib/usda/category-section-map.ts` — SR-Legacy foodCategory → section id map for prefill
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: build-usda-search-index.js → bundled usda-sr-legacy.json asset</name>
  <files>recipe-planner/scripts/build-usda-search-index.js, recipe-planner/src/assets/usda-sr-legacy.json</files>
  <read_first>
    - .planning/phases/03-product-registry-seeding/03-RESEARCH.md (D-06 asset shape/size; SR-Legacy CSV vs JSON size facts)
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (build-usda-search-index.js section — read → transform → write JSON, no PocketBase client)
    - recipe-planner/scripts/data/README.md (staged SR-Legacy bulk filename from Plan 03)
  </read_first>
  <action>
    Create `recipe-planner/scripts/build-usda-search-index.js` (offline transform, no PocketBase client needed): read the SR-Legacy bulk file staged under `scripts/data/` by Plan 03, map every SR-Legacy food row to a trimmed `{ name, foodCategory, fdc_id }` object (name plain/normalized where reasonable, but SR-Legacy names may stay as-is since this is the long-tail lookup surface), and write the array to `recipe-planner/src/assets/usda-sr-legacy.json`. Per D-06 the target is ~150-250KB gzipped over ~7.8k rows — confirm the raw JSON size is reasonable for a bundled asset (Vite will gzip on serve); if the raw file is unexpectedly large, keep only the three fields and drop any duplicate/blank-name rows. Do NOT build the live FDC API fallback (D-06 defers it). This script consumes the SAME bulk download the seed join uses — do not re-download.
  </action>
  <verify>
    <automated>cd recipe-planner && node -e "const a=require('./src/assets/usda-sr-legacy.json'); if(!Array.isArray(a)||a.length<1000)throw new Error('index too small: '+a.length); const bad=a.filter(x=>!x.name||!x.fdc_id||!('foodCategory' in x)); if(bad.length)throw new Error(bad.length+' rows missing name/fdc_id/foodCategory'); console.log('index rows:',a.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `usda-sr-legacy.json` exists, is an array of ~thousands of rows, each with `name`, `foodCategory`, and a non-null `fdc_id`.
    - `build-usda-search-index.js` reads the staged SR-Legacy bulk (no re-download) and emits only the three trimmed fields.
    - No live FDC API code introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usda-lookup.ts (searchUsda) + category→section map + test</name>
  <files>recipe-planner/src/lib/usda/usda-lookup.ts, recipe-planner/src/lib/usda/usda-lookup.test.ts, recipe-planner/src/lib/usda/category-section-map.ts</files>
  <read_first>
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (usda-lookup.ts sketch — mirrors product-search.ts fuse wrapper)
    - recipe-planner/src/lib/search/product-search.ts (the sibling fuse config to reuse)
  </read_first>
  <behavior>
    - searchUsda("cheddar") returns an array of UsdaEntry candidates, each with a numeric fdc_id, ranked by relevance (a "cheddar cheese" entry appears near the top).
    - searchUsda("") and searchUsda("  ") return [] (empty/whitespace passthrough — do not dump the whole index).
    - sectionIdForCategory(foodCategory) returns a section id string for a mappable category (e.g. a dairy category → the dairy section) and undefined for an unmapped category (first-use-corrected, per D-04).
  </behavior>
  <action>
    Create `recipe-planner/src/lib/usda/usda-lookup.ts` exporting `searchUsda(query: string): UsdaEntry[]` where `UsdaEntry = { name: string; foodCategory: string; fdc_id: number }`, per 03-PATTERNS.md: statically import (or lazy-import) `../../assets/usda-sr-legacy.json`, build a `Fuse<UsdaEntry>` with the same `threshold: 0.35`, `ignoreLocation: true` approach as product-search.ts (keys `["name"]`), and return `fuse.search(query).map(r => r.item)` — empty/whitespace query returns `[]` (do NOT return the full ~7.8k index). Create `recipe-planner/src/lib/usda/category-section-map.ts` exporting `sectionIdForCategory(foodCategory: string): string | undefined` — a small keyword/prefix map from SR-Legacy `foodCategory` strings to the existing section ids (the 8 real sections; unmapped → undefined, corrected at first use per D-04). Because section ids are environment data, the map may key on section NAME and resolve to id via a passed-in lookup, OR hold the known section ids — keep it simple and documented; the Plan-06 tab supplies the section id it prefills from this. Write `usda-lookup.test.ts` FIRST covering the three behaviors above (use a small inline fixture or a tiny slice of the real asset so the test is fast and deterministic).
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/usda/usda-lookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `searchUsda` returns fdc_id-bearing candidates for a real query and `[]` for empty/whitespace.
    - `sectionIdForCategory` maps known categories and returns undefined for unmapped ones.
    - Tests pass; the module reuses the fuse.js approach rather than a second matching engine.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline SR-Legacy bulk → build script | One-time dev-machine transform to a bundled asset |
| bundled asset → client runtime | Static JSON shipped in the app bundle, searched in-memory (no network) |
| user free-text query → searchUsda | Search input flows to the in-memory fuse index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Denial of Service | Oversized bundled asset bloating the app / slow tablet parse | low | mitigate | Trim to exactly `{name, foodCategory, fdc_id}`, drop blank/dup rows; verify row count + rely on Vite gzip; Plan 06 lazy-loads the asset only when the tab opens |
| T-03-04 | Tampering | searchUsda free-text query | low | mitigate | Empty/whitespace guard returns `[]`; in-memory search only, no query reaches a server; offline asset means no live FDC API surface (D-06 defers it) |
| T-03-SC | Tampering | No new package installs this plan | n/a | accept | fuse.js already installed in Plan 02; no new dependency added |
</threat_model>

<verification>
- Asset verify command prints a plausible row count with all rows carrying name/fdc_id/foodCategory.
- `npx vitest run src/lib/usda/usda-lookup.test.ts` green.
- Full suite `npm test` stays green.
</verification>

<success_criteria>
- The bundled offline Search-USDA index and its lookup module exist and are query-ready for the Plan 06 tab, with no live-API dependency.
</success_criteria>

<output>
Create `.planning/phases/03-product-registry-seeding/03-05-SUMMARY.md` when done.
</output>
