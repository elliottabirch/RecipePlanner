---
phase: 03-product-registry-seeding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/scripts/add-product-nutrition-fields.js
  - pb_schema.json
  - recipe-planner/src/lib/types.ts
autonomous: false
requirements: [REG-04]
user_setup: []

must_haves:
  truths:
    - "The products collection on both test (:8091) and prod (:8090) carries 8 new nullable fields (fdc_id, usda_data_type, usda_category, nutrient_basis_g, kcal, protein_g, fat_g, carb_g)"
    - "All 291 existing products still load in the registry page unchanged after the field additions"
    - "The Product TypeScript interface mirrors the new schema fields as optional"
  artifacts:
    - "recipe-planner/scripts/add-product-nutrition-fields.js"
    - "pb_schema.json (re-exported with new products fields)"
    - "recipe-planner/src/lib/types.ts (extended Product interface)"
  key_links:
    - "types.ts Product interface field names/types match the PocketBase field definitions exactly"
    - "The field-add script is idempotent — re-running it never duplicates or overwrites existing fields"
---

<objective>
Add the nutrition-ready nullable schema (D-07 / REG-04) to the `products` collection on both PocketBase instances, re-export the canonical schema, and mirror the fields into the `Product` TypeScript interface. This is the schema prerequisite the seed insert (Plan 04) and the Search-USDA create path (Plan 06) both depend on.

Purpose: REG-04 requires products to carry FDC ID + macro fields with NO user-facing nutrition UI. Only `fdc_id` must be populated at seed time; macros backfill in a later phase. Density/purchase-unit work stays DEFERRED — this plan only preserves the hooks (retain `fdc_id`, keep `canonical_unit`/`dimension` nullable — already nullable, unchanged here).
Output: 8 new nullable fields on `products` (both DBs), updated `pb_schema.json`, extended `Product` interface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-registry-seeding/03-CONTEXT.md
@.planning/phases/03-product-registry-seeding/03-RESEARCH.md
@.planning/phases/03-product-registry-seeding/03-PATTERNS.md
@pb_schema.json
@recipe-planner/src/lib/types.ts
</context>

<artifacts_produced>
- `recipe-planner/scripts/add-product-nutrition-fields.js` — idempotent superuser schema-migration script
- 8 new nullable fields on `products` (`pbc_7402169584`): `fdc_id`, `usda_data_type`, `usda_category`, `nutrient_basis_g`, `kcal`, `protein_g`, `fat_g`, `carb_g`
- `pb_schema.json` re-export reflecting the new fields
- `Product` interface extension in `recipe-planner/src/lib/types.ts`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent schema-migration script + apply test-then-prod + re-export pb_schema.json</name>
  <files>recipe-planner/scripts/add-product-nutrition-fields.js, pb_schema.json</files>
  <read_first>
    - recipe-planner/scripts/merge-products.js (superuser auth + PB_URL env-override + backups.create convention)
    - recipe-planner/scripts/find-duplicates.js (ESM PocketBase client boilerplate, main().catch entrypoint)
    - pb_schema.json (products collection pbc_7402169584 — existing fields array shape; canonical_unit/dimension select-field definitions as the template for usda_data_type)
  </read_first>
  <action>
    Write `recipe-planner/scripts/add-product-nutrition-fields.js` following the Phase-1 script convention: ESM `import PocketBase`, `PB_URL` env-override (test `http://192.168.50.95:8091` vs prod `http://192.168.50.95:8090`), superuser auth via `pb.collection("_superusers").authWithPassword(process.env.PB_SUPERUSER_EMAIL, process.env.PB_SUPERUSER_PASSWORD)`, and the `main().catch((e) => { console.error("ERROR:", e.message, e.status, e.url); ... process.exit(1); })` entrypoint. Per D-07/REG-04, the script adds these 8 nullable fields to the `products` collection (`pbc_7402169584`), all `required: false`: `fdc_id` (number), `usda_data_type` (select, single, values `foundation_food`/`sr_legacy`), `usda_category` (text), `nutrient_basis_g` (number), `kcal` (number), `protein_g` (number), `fat_g` (number), `carb_g` (number). Idempotency is mandatory: fetch the collection via `pb.collections.getOne("products")`, append ONLY fields whose `name` is not already present in the existing `fields` array, then call `pb.collections.update(collection.id, { fields })` with the merged array (PocketBase replaces the whole fields array, so never drop an existing field). Re-running the script must be a no-op. Do NOT touch `canonical_unit`/`dimension` — they are already nullable and stay so (density hooks preserved per CONTEXT.md deferred scope). Run the script against test `:8091` first (rehearse), then against prod `:8090`. After the prod run, re-export the live prod schema into `pb_schema.json` (the single canonical export — NOT pb_schema_updated.json, which was deleted in Phase 1), so the new products fields are captured.
  </action>
  <verify>
    <automated>cd recipe-planner && node -e "const s=require('../pb_schema.json'); const c=(Array.isArray(s)?s:s.collections).find(x=>x.id==='pbc_7402169584'); const names=(c.fields||c.schema).map(f=>f.name); const need=['fdc_id','usda_data_type','usda_category','nutrient_basis_g','kcal','protein_g','fat_g','carb_g']; const missing=need.filter(n=>!names.includes(n)); if(missing.length){console.error('MISSING',missing);process.exit(1)} console.log('all 8 fields present')"</automated>
  </verify>
  <acceptance_criteria>
    - Script exists, is idempotent (second run adds nothing), and authenticates via `.env.local` superuser vars (never printed/committed).
    - Both test `:8091` and prod `:8090` `products` collections carry all 8 new nullable fields.
    - `pb_schema.json` re-export shows the 8 fields on `pbc_7402169584`; the verify command prints "all 8 fields present".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend the Product TypeScript interface</name>
  <files>recipe-planner/src/lib/types.ts</files>
  <read_first>
    - recipe-planner/src/lib/types.ts (Product interface, ~lines 44-60, ends at store_bought_product)
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (types.ts MODIFY section — exact field list)
  </read_first>
  <action>
    Extend the `Product` interface in `recipe-planner/src/lib/types.ts` with 8 new OPTIONAL fields mirroring the schema added in Task 1, per REG-04 / D-07 and 03-PATTERNS.md: `fdc_id?: number;`, `usda_data_type?: "foundation_food" | "sr_legacy" | "";`, `usda_category?: string;`, `nutrient_basis_g?: number;`, `kcal?: number;`, `protein_g?: number;`, `fat_g?: number;`, `carb_g?: number;`. All optional so the existing 291 hand-authored products remain valid with no migration on the TS side. Do not reorder or remove existing fields; append the new ones after `store_bought_product`.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc -b --noEmit 2>&1 | (grep -i 'error' && exit 1 || echo "typecheck clean")</automated>
  </verify>
  <acceptance_criteria>
    - `Product` interface carries all 8 new optional fields with the exact names/types above.
    - `npx tsc -b` reports no type errors introduced by the change.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify schema fields on both instances + registry loads</name>
  <action>Guide the human through confirming the 8 new fields exist (non-required) on the `products` collection in both PocketBase Admin UIs and that the registry page still loads all existing products with no console errors. Block until approval.</action>
  <what-built>Added 8 nullable nutrition/USDA fields to the `products` collection on both test (:8091) and prod (:8090) via an idempotent superuser script, re-exported pb_schema.json, and mirrored the fields into the Product interface.</what-built>
  <how-to-verify>
    1. Open the prod PocketBase Admin UI: http://192.168.50.95:8090/_/ → Collections → `products`. Confirm the 8 new fields (fdc_id, usda_data_type, usda_category, nutrient_basis_g, kcal, protein_g, fat_g, carb_g) are present and NONE is marked required.
    2. Repeat for test: http://192.168.50.95:8091/_/ → `products`.
    3. Start the app (`cd recipe-planner && npm run dev`), open the Products registry page against prod, and confirm all existing products still load with no console errors (existing 291 products unaffected).
  </how-to-verify>
  <resume-signal>Type "approved" once both instances show the 8 fields and the registry loads cleanly, or describe any issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine script → prod PocketBase | Superuser-authenticated schema mutation on the live single-household DB |
| local `.env.local` → script process | PocketBase superuser credentials sourced at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | add-product-nutrition-fields.js schema update on prod | medium | mitigate | Idempotent append-only merge of the fields array (fetch-then-update, never overwrite); test `:8091` rehearsal before prod; additive nullable fields only, no data mutation, existing 291 products untouched; blocking human-verify checkpoint before acceptance |
| T-03-02 | Information Disclosure | PB superuser credentials | medium | mitigate | Reuse Phase-1's gitignored `recipe-planner/.env.local` (`PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD`); never print or commit credential values |
| T-03-03 | Denial of Service | Whole-fields-array replace dropping existing fields | high | mitigate | Read existing `fields` via `collections.getOne`, merge new fields onto it, and update with the full merged array — a bug that omitted existing fields would delete columns, so the script asserts every pre-existing field name survives the merge before calling update |
</threat_model>

<verification>
- Schema verify command prints "all 8 fields present" against the re-exported pb_schema.json.
- `npx tsc -b` clean.
- Human-verify checkpoint approved (both instances + registry load).
</verification>

<success_criteria>
- 8 nullable fields exist on `products` in both DBs, schema re-exported, Product interface mirrors them, existing products unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-product-registry-seeding/03-01-SUMMARY.md` when done.
</output>
