---
phase: 03-product-registry-seeding
plan: 06
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-05]
files_modified:
  - recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx
autonomous: false
requirements: [REG-03]
user_setup: []

must_haves:
  truths:
    - "Quick-create shows a two-tab toggle: 'Enter manually' (default, unchanged behavior) and 'Search USDA'"
    - "The Search USDA tab fuzzy-searches the bundled index, and selecting a result prefills Name + Section, records fdc_id/usda_data_type='sr_legacy', switches back to the manual tab, and leaves Unit manual + required"
    - "Creating a USDA-sourced product persists fdc_id and usda_data_type='sr_legacy'"
  artifacts:
    - "recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx (Search USDA tab added)"
  key_links:
    - "The tab queries searchUsda from usda-lookup.ts and derives the prefill section via sectionIdForCategory"
    - "isValid stays name.trim().length > 0 && unit !== '' — a USDA prefill does NOT auto-fill or bypass the required unit (Open Question 4)"
---

<objective>
Add the "Search USDA" mode to the existing Phase-2 quick-create dialog so the user can pull an item not in the seeded set on demand, prefilling name/section/fdc_id from the bundled SR-Legacy index (REG-03, D-06).

Purpose: REG-03 requires quick-create to offer a "Search USDA" mode for the long tail. Per 03-UI-SPEC.md and Open Question 4, Search-USDA prefills name + section + fdc_id but leaves `unit` MANUAL and required (SR Legacy carries no purchase-unit concept) — the dialog's existing "ruthlessly minimal" intent and its `isValid` gate are preserved.
Output: a two-tab quick-create dialog with the new Search-USDA tab wired to `searchUsda`, plus the create payload carrying `fdc_id` + `usda_data_type = "sr_legacy"`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-registry-seeding/03-CONTEXT.md
@.planning/phases/03-product-registry-seeding/03-RESEARCH.md
@.planning/phases/03-product-registry-seeding/03-PATTERNS.md
@.planning/phases/03-product-registry-seeding/03-UI-SPEC.md
@recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx
@recipe-planner/src/lib/usda/usda-lookup.ts
</context>

<artifacts_produced>
- `recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx` — extended with a two-tab (Enter manually / Search USDA) layout and USDA prefill flow
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the Search USDA tab + prefill flow to QuickCreateProductDialog</name>
  <files>recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx</files>
  <read_first>
    - recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx (whole file — existing fields, isValid at ~line 67, create payload ~lines 74-93, error-handling ~lines 83-92)
    - .planning/phases/03-product-registry-seeding/03-UI-SPEC.md (Surface 2 — Tabs layout, result-row spec, lazy-load, empty/no-match/loading copy, prefill feedback chip, focus-to-Unit behavior, color rules)
    - .planning/phases/03-product-registry-seeding/03-PATTERNS.md (QuickCreateProductDialog MODIFY section — create-payload extension)
    - recipe-planner/src/lib/usda/usda-lookup.ts (searchUsda, UsdaEntry) and category-section-map.ts (sectionIdForCategory)
  </read_first>
  <action>
    Extend `QuickCreateProductDialog.tsx` per REG-03 / D-06 and 03-UI-SPEC.md Surface 2 (do NOT touch the existing manual-entry fields' behavior). Add a MUI `Tabs value={mode} onChange variant="fullWidth"` row directly below `DialogTitle` with two tabs: "Enter manually" (default/first — today's exact form and behavior) and "Search USDA". The Search USDA tab renders: a full-width `TextField` (SearchIcon adornment, placeholder "Search USDA foods…", autofocus on tab activation) and, below it, a `List` of `ListItemButton` rows (min-height 48px tablet touch target) each showing food name (Body 16/400 primary) and category (Label 12/400 text.secondary). Lazy-load the bundled index: only the FIRST time this tab activates in a session, load/parse it and show a centered `CircularProgress` + "Loading USDA catalog…"; reuse the in-memory index on subsequent activations. Query via `searchUsda(query)` from usda-lookup.ts; cap the rendered list at the top ~20 matches. States/copy exactly per 03-UI-SPEC.md: empty-query → "Type to search the USDA catalog"; no-match → 'No USDA matches for "{query}". Try a different term, or switch to "Enter manually".' Tapping a result row: (1) switches the active tab back to "Enter manually"; (2) prefills Name (editable) and Section (editable Select, via `sectionIdForCategory(entry.foodCategory)`) from the entry; (3) records the entry's `fdc_id` and `usda_data_type = "sr_legacy"` in local state for the create payload; (4) leaves Store blank and Unit blank+required, moving focus to the Unit Select; (5) shows a neutral `Chip size="small" variant="outlined"` reading "USDA match · fdc:{fdcId}" beneath the Name field (text.secondary border/text — NOT accent green, per the Color contract). Keep `isValid` UNCHANGED (`name.trim().length > 0 && unit !== ""`) — a USDA prefill still requires the user to pick a unit; do NOT auto-derive canonical_unit (Open Question 4). Extend the `create<Product>(collections.products, {...})` payload to include `fdc_id: selectedUsdaMatch?.fdc_id` and `usda_data_type: selectedUsdaMatch ? "sr_legacy" : undefined` (undefined when the manual tab supplied the name). Preserve the existing error-handling contract (dialog stays open on failure, input preserved, no auto-close) for the new tab. Reuse existing MUI components only — no new dialog, no nutrition UI.
  </action>
  <verify>
    <automated>cd recipe-planner && grep -q 'searchUsda' src/components/outputs/QuickCreateProductDialog.tsx && grep -q 'sr_legacy' src/components/outputs/QuickCreateProductDialog.tsx && npx tsc -b --noEmit 2>&1 | (grep -i error && exit 1 || echo "USDA tab wired, typecheck clean")</automated>
  </verify>
  <acceptance_criteria>
    - Dialog shows the two-tab toggle; "Enter manually" is default and its existing behavior is unchanged.
    - Search USDA tab queries `searchUsda`, lazy-loads the index, renders result rows, and shows the correct empty/no-match/loading copy.
    - Selecting a result prefills Name + Section, records fdc_id + usda_data_type, returns to the manual tab, focuses Unit, and shows the neutral fdc chip.
    - `isValid` unchanged; create payload includes fdc_id + usda_data_type="sr_legacy" only for USDA-sourced products; `npx tsc -b` clean.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the Search-USDA prefill flow + persisted USDA fields</name>
  <action>Guide the human through the Search-USDA tab in the running app: lazy-load, ranked results, no-match copy, result-tap prefill (name/section/fdc chip, focus-to-Unit), and creating a product then confirming fdc_id + usda_data_type="sr_legacy" persisted. Block until approval or an issue list.</action>
  <what-built>A "Search USDA" tab in the quick-create dialog that fuzzy-searches the bundled SR-Legacy index and prefills name/section/fdc_id, leaving unit manual+required, and persists usda_data_type="sr_legacy" on create.</what-built>
  <how-to-verify>
    1. Start the app (`cd recipe-planner && npm run dev`), open a flow that shows the quick-create dialog (e.g. the "+ Create New Product" path or the shopping swap quick-create).
    2. Switch to the "Search USDA" tab. Confirm the "Loading USDA catalog…" state appears once, then type an item NOT in your registry (e.g. an SR-Legacy-only food). Confirm ranked results with category captions appear; confirm the no-match copy for a nonsense query.
    3. Tap a result: confirm it returns to "Enter manually" with Name + Section prefilled, the "USDA match · fdc:{id}" chip beneath Name, focus on the Unit select, and Store blank.
    4. Pick a Unit, tap "Create Product", then verify in the PB Admin UI that the new product has the matched `fdc_id` and `usda_data_type = "sr_legacy"`.
  </how-to-verify>
  <resume-signal>Type "approved" once the Search-USDA flow prefills correctly and the created product carries fdc_id + usda_data_type, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text query → searchUsda index | Search input flows to the in-memory fuse index (no network) |
| quick-create form → PocketBase create | User-driven product creation (existing Phase-2 surface, already validated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | Search USDA free-text query | low | mitigate | searchUsda empty/whitespace guard; in-memory search over the bundled offline asset — no query reaches a server, no live FDC API surface (D-06 defers it) |
| T-03-11 | Denial of Service | Eager index load on dialog mount slowing tablet | low | mitigate | Lazy-load the bundled index only on first Search-USDA tab activation; cap rendered results at ~20 rows |
| T-03-12 | Tampering | Malformed prefill data persisted as a product | low | accept | Prefilled Name/Section stay user-editable; unchanged `isValid` gate still requires an explicit Unit; existing create error-handling preserved; single-household trusted app |
| T-03-SC | Tampering | No new package installs this plan | n/a | accept | fuse.js already installed in Plan 02; no new dependency |
</threat_model>

<verification>
- Wiring grep (`searchUsda`, `sr_legacy`) + `npx tsc -b` clean.
- Full suite `npm test` stays green.
- Human-verify checkpoint approved (prefill flow + persisted fdc_id/usda_data_type).
</verification>

<success_criteria>
- Quick-create offers an on-demand Search-USDA mode that prefills name/section/fdc_id from the bundled index, keeps unit manual+required, and persists usda_data_type="sr_legacy" on create.
</success_criteria>

<output>
Create `.planning/phases/03-product-registry-seeding/03-06-SUMMARY.md` when done.
</output>
