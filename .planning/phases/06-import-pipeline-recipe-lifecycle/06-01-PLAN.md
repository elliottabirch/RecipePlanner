---
phase: 06-import-pipeline-recipe-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/src/lib/types.ts
  - recipe-planner/src/lib/api.ts
  - recipe-planner/scripts/apply-phase6-schema.mjs
  - pb_schema.json
autonomous: false
requirements: [IMP-01, IMP-05, IMP-06]
must_haves:
  truths:
    - "recipes carries a select status (draft|published) on both PB instances, and EVERY existing recipe row reads status=published (backfilled) — nothing vanishes from planning on day one (D-03)"
    - "recipe_notes collection exists on both PB instances with recipe/text/status/source_surface/draft_revision/created fields (D-09)"
    - "recipes carries a nullable revision_of relation and recipe_product_nodes carries a nullable source_node relation (D-10 write-back linkage)"
    - "Recipe.status, Recipe.revision_of, and a RecipeNote interface compile and match the live schema; collections.recipeNotes resolves to 'recipe_notes'"
    - "Re-running apply-phase6-schema.mjs is a no-op (idempotent) on an already-migrated instance"
  artifacts:
    - recipe-planner/scripts/apply-phase6-schema.mjs
    - pb_schema.json
    - recipe-planner/src/lib/types.ts
    - recipe-planner/src/lib/api.ts
  key_links:
    - "types.ts field names (status, revision_of, RecipeNote.*) === live PocketBase field names (round-trip depends on exact match)"
    - "api.ts collections.recipeNotes string value === live collection name 'recipe_notes'"
    - "recipe_notes.draft_revision and recipes.revision_of both relate to recipes (pbc_5183946072) — the evolution wizard flag + write-back target depend on these"
---

<objective>
Land the Phase 6 schema foundation: add a draft/published `status` select to `recipes` (D-03) with a full backfill to `published`, create the `recipe_notes` collection (D-09), and add the two evolution-loop linkage relations `recipes.revision_of` + `recipe_product_nodes.source_node` (D-10, Open Q2 option A). Mirror the additive TypeScript type surface and register the new collection in the api map so all downstream typed code compiles.

Purpose: `status` gates draft invisibility (IMP-01), `recipe_notes` backs one-tap notes (IMP-05), and `revision_of`/`source_node` make the note→revision write-back id-stable (IMP-06). Every later plan in this phase reads these types or this live schema.
Output: Idempotent `apply-phase6-schema.mjs`, refreshed `pb_schema.json` mirror, updated `types.ts` + `api.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/scripts/apply-phase5-schema.mjs
@recipe-planner/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Recipe.status/revision_of + RecipeNote type + api.ts registration</name>
  <files>recipe-planner/src/lib/types.ts, recipe-planner/src/lib/api.ts</files>
  <read_first>
    - recipe-planner/src/lib/types.ts:55-125 (Product nutrition fields, Recipe interface 73-77, RecipeProductNode 88-96 — mirror the additive-nullable style)
    - recipe-planner/src/lib/api.ts:51-73 (collections name map)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Schema Migration Mechanics (field shapes) and §Evolution Loop (linkage fields)
  </read_first>
  <action>
    In recipe-planner/src/lib/types.ts: add `status?: "draft" | "published"` and `revision_of?: string` to the `Recipe` interface (per D-03 and D-10). Add a nullable `source_node?: string` to the `RecipeProductNode` interface (D-10 node correspondence, Open Q2 option A). Add a new `RecipeNote` interface extending BaseRecord with fields `recipe: string` (relation id), `text: string`, `status?: "pending" | "applied" | "dismissed"`, `source_surface?: "cook_mode" | "calendar" | "recipe_card"`, `draft_revision?: string` (relation id). Add a `RecipeNoteExpanded` interface extending RecipeNote with `expand?: { recipe?: Recipe }` for the hook consumer. In recipe-planner/src/lib/api.ts: add `recipeNotes: "recipe_notes",` to the `collections` const map. Keep all new type fields optional so the existing 725 recipes and all existing code compile unchanged.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` is clean. `Recipe.status`, `Recipe.revision_of`, `RecipeProductNode.source_node`, and the `RecipeNote`/`RecipeNoteExpanded` interfaces exist; `collections.recipeNotes === "recipe_notes"`.
  </acceptance_criteria>
  <done>Recipe.status/revision_of, RecipeProductNode.source_node, RecipeNote types + recipeNotes api entry compile.</done>
</task>

<task type="auto">
  <name>Task 2: Author apply-phase6-schema.mjs and run it against TEST (8091)</name>
  <files>recipe-planner/scripts/apply-phase6-schema.mjs, pb_schema.json</files>
  <read_first>
    - recipe-planner/scripts/apply-phase5-schema.mjs (WHOLE FILE — copy its structure verbatim: DB_URLS, PB_URL env, authenticateSuperuser, getCollectionSafe, append-only field merge with drop-guard + post-update verify, reExportSchema gated to the test run, main + .catch)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Schema Migration Mechanics (concrete change 1/2/3 field JSON) and §Common Pitfalls Pitfall 1 (backfill-in-same-script)
    - pb_schema.json (recipes collection — copy the exact shape of `select_recipe_type` for the new `status` select; find the recipes collection id for the relation collectionId)
  </read_first>
  <action>
    Create recipe-planner/scripts/apply-phase6-schema.mjs mirroring apply-phase5-schema.mjs exactly (idempotent, existence-checking, PB_URL env selects test :8091 vs prod :8090 default, superuser auth from gitignored .env.local via `node --env-file=.env.local`, repo-root pb_schema.json re-export via `../../pb_schema.json` ONLY after a test run). Apply four changes: (1) append a `status` select field to `recipes.fields` mirroring `select_recipe_type`'s shape with `values:["draft","published"]`; (2) IN THE SAME SCRIPT after the field add, backfill — `getFullList()` recipes and `update(id,{status:"published"})` any whose status !== "published" (Pitfall 1: an un-set select reads as "" and would vanish under a fail-closed filter); (3) create the `recipe_notes` collection modeled on the recipe_queue relation shape (recipe relation cascadeDelete=true to the recipes collection id, text field, status select [pending/applied/dismissed], source_surface select [cook_mode/calendar/recipe_card], draft_revision relation to recipes cascadeDelete=false, autodate created + updated), open list/view/create/update/delete rules like cook_progress; (4) append a nullable `revision_of` relation to `recipes` (to recipes, cascadeDelete=false) and a nullable `source_node` relation to `recipe_product_nodes` (to recipe_product_nodes, cascadeDelete=false). Reuse the phase5 append-only merge helper (assert no existing field dropped before update). Then run against TEST only: `cd recipe-planner && PB_URL=http://192.168.50.95:8091 node --env-file=.env.local scripts/apply-phase6-schema.mjs`, which also re-exports pb_schema.json.
  </action>
  <verify>
    <automated>cd recipe-planner && PB_URL=http://192.168.50.95:8091 node --env-file=.env.local scripts/apply-phase6-schema.mjs 2>&1 | tail -20 && grep -c '"name": "recipe_notes"' ../pb_schema.json</automated>
  </verify>
  <acceptance_criteria>
    Script run against 8091 completes without error and is idempotent (a second run prints no-op messages for every change). pb_schema.json at repo root now contains the `recipe_notes` collection and the `status`/`revision_of`/`source_node` fields. Every recipe on TEST reads status=published.
  </acceptance_criteria>
  <done>Idempotent schema script applied to TEST; recipe_notes + status/revision_of/source_node live and backfilled; pb_schema.json refreshed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human checkpoint — run the migration against PROD (8090)</name>
  <action>Human runs the same idempotent script against prod (PB_URL=8090) and confirms existing recipes still appear in the recipe list + WeekWizard pool after the status backfill.</action>
  <what-built>An idempotent, test-rehearsed schema migration. Task 2 already applied it to the TEST instance (8091) and verified idempotency. This checkpoint is the PROD (8090) run — a hard-to-reverse production mutation on the live household database that adds `status` + backfills all recipes to `published`, creates `recipe_notes`, and adds `revision_of`/`source_node`.</what-built>
  <how-to-verify>
    1. Confirm the TEST run succeeded and pb_schema.json shows the new collection/fields (Task 2 output).
    2. Run the SAME script against prod: `cd recipe-planner && PB_URL=http://192.168.50.95:8090 node --env-file=.env.local scripts/apply-phase6-schema.mjs`
    3. Confirm it prints the field-add + backfill + collection-create lines with no error (or all no-op lines if re-run).
    4. Spot-check prod: open the app against Production and confirm existing recipes still appear in the recipe list and in the WeekWizard pool (nothing vanished — status backfilled to published).
  </how-to-verify>
  <resume-signal>Type "approved" once the prod migration has run cleanly and existing recipes are confirmed still present, or describe any error.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- Schema field `recipes.status` (select draft|published) on prod + test
- Schema field `recipes.revision_of` (nullable relation → recipes)
- Schema field `recipe_product_nodes.source_node` (nullable relation → recipe_product_nodes)
- Collection `recipe_notes` (recipe, text, status, source_surface, draft_revision, created, updated) on prod + test
- Script `recipe-planner/scripts/apply-phase6-schema.mjs`
- Refreshed `pb_schema.json` mirror
- Types: `Recipe.status`, `Recipe.revision_of`, `RecipeProductNode.source_node`, `RecipeNote`, `RecipeNoteExpanded`
- api map entry `collections.recipeNotes`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→PocketBase (admin) | Superuser-authenticated schema mutation crosses into the live prod DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01a | Info Disclosure | apply-phase6-schema.mjs superuser creds | medium | mitigate | Creds read from gitignored `.env.local` via `--env-file`; never printed, never committed (verbatim phase5 pattern) |
| T-06-01b | Tampering | prod backfill of `status` | high | mitigate | Backfill sets EVERY row to `published` (fail-open); test-first rehearsal on 8091; blocking human checkpoint before prod run; idempotent re-run is a no-op |
| T-06-01c | Denial of Service | drop-guard on field merge | medium | mitigate | Append-only merge asserts no existing field is dropped before `collections.update` (phase5 helper) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH §Package Legitimacy: zero installs); uses existing `pocketbase` SDK only |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean after Task 1.
- Idempotent re-run of the script on 8091 prints no-op for all four changes.
- pb_schema.json contains `recipe_notes`, `status`, `revision_of`, `source_node`.
- Prod recipes still visible in list + wizard pool after the checkpoint migration.
</verification>

<success_criteria>
`status` exists and is backfilled to `published` on BOTH instances; `recipe_notes`, `revision_of`, `source_node` exist on both; the TypeScript type surface compiles and the api map registers the new collection; re-running the script is a proven no-op.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-01-SUMMARY.md` when done.
</output>
