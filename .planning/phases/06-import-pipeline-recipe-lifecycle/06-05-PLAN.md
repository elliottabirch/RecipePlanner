---
phase: 06-import-pipeline-recipe-lifecycle
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-03"]
files_modified:
  - recipe-planner/src/components/WeekWizard.tsx
  - recipe-planner/src/pages/WeeklyPlans.tsx
  - recipe-planner/src/pages/Recipes.tsx
autonomous: false
requirements: [IMP-01]
must_haves:
  truths:
    - "The WeekWizard rotation-pool recipe load and the WeeklyPlans Add-Meal recipe picker BOTH apply the fail-open draft-exclusion filter — a draft recipe never appears in either (D-04)"
    - "The four unfiltered surfaces (Recipes list, RecipeEditor, Products source_recipe, StepBackfill) are left untouched — drafts stay visible there"
    - "A grey Draft <Chip> renders on a recipe card when status === draft, coexisting with the Batch chip, mirroring the Batch chip structure exactly (D-06 badge)"
  artifacts:
    - recipe-planner/src/components/WeekWizard.tsx
    - recipe-planner/src/pages/WeeklyPlans.tsx
    - recipe-planner/src/pages/Recipes.tsx
  key_links:
    - "Both planning call sites pass the SAME fail-open filter from draft-filter.ts (Plan 03) — a divergent inline string would risk the fail-closed bug"
    - "Draft chip gate item.status === 'draft' depends on Recipe.status from Plan 01 + live backfill"
---

<objective>
Wire draft invisibility into planning: add the fail-open draft-exclusion filter to EXACTLY the two correctness-critical planning queries (WeekWizard rotation-pool load + WeeklyPlans Add-Meal picker, D-04) and render a grey Draft badge on draft recipe cards (D-06). Leave the four intentionally-unfiltered surfaces alone (IMP-01).

Purpose: a draft must never leak into a meal plan (a missed filter is the highest-risk defect in this phase), while staying visible + badged everywhere authoring/review happens.
Output: two one-line filter additions + a Draft `<Chip>` on the recipe grid.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md
@recipe-planner/src/lib/lifecycle/draft-filter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the fail-open draft filter to the two planning call sites</name>
  <files>recipe-planner/src/components/WeekWizard.tsx, recipe-planner/src/pages/WeeklyPlans.tsx</files>
  <read_first>
    - recipe-planner/src/components/WeekWizard.tsx:100-106 (the `getAll<Recipe>(collections.recipes, { sort: "name" })` inside the Promise.all — the rotation-pool source, D-04 highest risk)
    - recipe-planner/src/pages/WeeklyPlans.tsx:150-156 (the `getAll<Recipe>(collections.recipes, { sort: "name" })` — the Add-Meal picker source)
    - recipe-planner/src/lib/lifecycle/draft-filter.ts (Plan 03 — the fail-open filter to import and pass)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Pattern 3 + Anti-Patterns (never status = "published")
  </read_first>
  <action>
    In recipe-planner/src/components/WeekWizard.tsx, change the recipes `getAll` inside the Promise.all (~line 104) from `{ sort: "name" }` to `{ sort: "name", filter: <fail-open filter from draft-filter.ts> }`, importing the filter from src/lib/lifecycle/draft-filter.ts (per D-04). In recipe-planner/src/pages/WeeklyPlans.tsx do the same to the recipes `getAll` at line 153. Use the shared fail-open filter (excludes draft, keeps unset) — never a fail-closed equals-published string (Pitfall 1). Touch ONLY these two call sites; do not add the filter to Recipes.tsx query (86), RecipeEditor, Products, or StepBackfill (drafts must stay visible there, D-04).
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -rn "draft-filter" src/components/WeekWizard.tsx src/pages/WeeklyPlans.tsx</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; both WeekWizard and WeeklyPlans import and apply the shared draft-exclusion filter on their recipes load; no other recipe query changed.
  </acceptance_criteria>
  <done>Draft-exclusion filter applied at exactly the two planning call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Draft <Chip> badge on the recipe card</name>
  <files>recipe-planner/src/pages/Recipes.tsx</files>
  <read_first>
    - recipe-planner/src/pages/Recipes.tsx:410-432 (the card header Box with the Batch chip at 418-430 — copy its exact structure)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Color (Draft badge #757575 grey, white text, out of the accent budget) + §Component Inventory item 2
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md §Draft <Chip> (grey, gated on item.status === "draft", coexists with Batch)
  </read_first>
  <action>
    In recipe-planner/src/pages/Recipes.tsx, add a Draft `<Chip>` in the same card-header flex Box (the `gap={1}` row at 414) rendered when `item.status === "draft"`, mirroring the Batch chip exactly but grey: `label="Draft"`, `size="small"`, `sx={{ backgroundColor: "#757575", color: "white", fontWeight: 600, fontSize: "0.7rem", height: 22 }}`. Draft and Batch chips may both render (orthogonal states) in the same row. Do not add the draft filter to this page's query — the list intentionally shows drafts (D-04).
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -n 'label="Draft"' src/pages/Recipes.tsx</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; a grey Draft chip is rendered on cards where status === "draft", coexisting with the Batch chip.
  </acceptance_criteria>
  <done>A grey Draft chip on draft recipe cards, coexisting with the Batch chip.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — draft hidden from planning, visible+badged in list</name>
  <action>Human sets one recipe to draft and confirms it is absent from the WeekWizard pool and Add-Meal picker but visible + grey-badged in the recipe list.</action>
  <what-built>Draft invisibility in planning + a Draft badge in the recipe list. Both are jsdom-untestable UI, so a live check confirms drafts are excluded from planning yet visible + badged in the list.</what-built>
  <how-to-verify>
    1. On Test DB, set one recipe's status to "draft" (via the app's editor after Plan 06 ships, or a quick PB edit) and leave another published.
    2. Open the Recipes list — confirm the draft recipe shows a grey "Draft" chip; the published one does not.
    3. Open the WeekWizard (Fill Week) — confirm the draft recipe is NOT in any slot's pool.
    4. Open a weekly plan's Add-Meal picker — confirm the draft recipe is NOT selectable.
    5. Confirm the draft recipe IS still visible in the Recipes list and openable in the editor.
  </how-to-verify>
  <resume-signal>Type "approved" if the draft is hidden from wizard + Add-Meal but visible+badged in the list, or describe the leak.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- Draft-exclusion filter wired at WeekWizard recipe load + WeeklyPlans Add-Meal picker
- Grey Draft `<Chip>` on the Recipes card grid
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning UI → recipes query | The two filtered call sites are the only guard keeping drafts out of a live meal plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05a | Tampering (data integrity) | a missed planning call site | high | mitigate | Exactly the two D-04 call sites filtered using the shared fail-open builder; live checkpoint verifies the draft is absent from BOTH wizard pool and Add-Meal picker |
| T-06-05b | Tampering (data integrity) | fail-closed filter hides published | high | mitigate | Uses the shared not-equals-draft filter (Plan 03), never equals-published; unset-status recipes stay plannable |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; MUI Chip + existing filter |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean.
- Both call sites reference the shared draft filter; no other recipe query changed.
- Live checkpoint: draft hidden from wizard + Add-Meal, visible + badged in list.
</verification>

<success_criteria>
Draft recipes are invisible to the two planning queries and visible + grey-badged in the recipe list; the four unfiltered surfaces are untouched.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-05-SUMMARY.md` when done.
</output>
