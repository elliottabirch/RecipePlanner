---
phase: 06-import-pipeline-recipe-lifecycle
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-04"]
files_modified:
  - recipe-planner/src/pages/Import.tsx
  - recipe-planner/src/App.tsx
  - recipe-planner/src/components/Layout.tsx
autonomous: false
requirements: [IMP-02, IMP-03]
must_haves:
  truths:
    - "A new /import route + nav entry hosts a JSON paste page; pasting recipe JSON validates via validateImportJson and surfaces problems inline without blocking (D-01, D-02)"
    - "Unmatched/low-confidence product lines are resolved inline (pick-existing / QuickCreateProductDialog / Search-USDA) BEFORE the draft lands, so every line carries a store/section/unit (D-02)"
    - "On resolve, the recipe lands directly in prod as a draft via buildRecipeGraph({status:'draft'}) and redirects into RecipeEditor for review (D-05) — no test DB, no migration script"
    - "The Import button never hard-blocks; validation + inline resolve are part of import, not a gate (first-class invariant)"
  artifacts:
    - recipe-planner/src/pages/Import.tsx
    - recipe-planner/src/App.tsx
    - recipe-planner/src/components/Layout.tsx
  key_links:
    - "Import page consumes validateImportJson + scoreProduct (Plan 02) and buildRecipeGraph (Plan 04); landing status='draft' depends on Recipe.status (Plan 01)"
    - "Inline unmatched resolution reuses QuickCreateProductDialog onCreated callback (guarantees store/section/unit — avoids the shopping-list-dropout bug)"
---

<objective>
Build the in-app import page (IMP-02) that retires the test-DB migration ritual (IMP-03): a `/import` route where the user pastes D-01 recipe JSON, sees non-blocking validation feedback, resolves any unmatched products inline (reusing QuickCreateProductDialog), and lands a draft directly in prod via the shared `buildRecipeGraph({status:"draft"})` — then redirects into RecipeEditor for review (D-05, D-02).

Purpose: adding a recipe becomes a paste-and-review flow with no PC-bound migration; the first-class invariant "importing never blocks; publishing is the only hard block" is preserved end-to-end.
Output: `Import.tsx` page + `/import` route + nav entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/lib/import/validate-import.ts
@recipe-planner/src/lib/import/build-recipe-graph.ts
@recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import page — paste, validate, inline-resolve, land draft</name>
  <files>recipe-planner/src/pages/Import.tsx</files>
  <read_first>
    - recipe-planner/src/lib/import/validate-import.ts (validateImportJson + NormalizedGraph) and recipe-planner/src/lib/search/product-search.ts (scoreProduct — Plan 02)
    - recipe-planner/src/lib/import/build-recipe-graph.ts (buildRecipeGraph — Plan 04)
    - recipe-planner/src/components/outputs/QuickCreateProductDialog.tsx (onCreated(product) callback + Search-USDA tab — the inline unmatched-resolution dialog)
    - recipe-planner/src/pages/registries/Products.tsx (page shell: Box + h6 title pattern) for layout parity
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Component Inventory item 1 + §Copywriting (Import recipe CTA, parse-OK/warning Alert copy, "Match these products" step)
  </read_first>
  <action>
    Create recipe-planner/src/pages/Import.tsx: a page container (Box + `h6` title "Import a recipe") with a full-width multiline monospace `TextField` (`minRows={10}`) for JSON paste and a primary "Import recipe" `Button variant="contained"` (green accent). On Import: run `validateImportJson(text)`. If not ok, render each error/warning as its own `Alert` (warning severity for non-blocking issues) with the UI-SPEC copy — button stays ENABLED for re-attempt (never hard-block). If ok, resolve products: for each product line, if it has a `matchProductId` hint use it; else `scoreProduct(name, allProducts)` and auto-match when the best score is at/under the confidence threshold (start ≈0.15, Claude's discretion to tune — D-02). Collect unmatched/low-confidence lines and drive an inline "Match these products" step: for each, offer pick-existing OR open `QuickCreateProductDialog` (its `onCreated(product)` fills store/section/unit; includes the Search-USDA tab) — so every line ends with a resolved productId. Once all lines are resolved, assemble the NormalizedGraph (products now carrying productId) and call `buildRecipeGraph(graph, /* status draft */)` with the recipe's status set to "draft", then `navigate('/recipes/' + recipeId)` into RecipeEditor (D-05). Show the parse-OK success `Alert` with ingredient/step/unmatched counts per UI-SPEC. Keep all non-trivial logic delegated to the Plan 02/04 pure modules — the page is thin wiring.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; Import.tsx validates paste via validateImportJson (non-blocking), resolves unmatched via QuickCreateProductDialog, lands a draft via buildRecipeGraph, and navigates to the editor.
  </acceptance_criteria>
  <done>An in-app import page: paste→validate→inline-resolve→land-draft→redirect.</done>
</task>

<task type="auto">
  <name>Task 2: /import route + nav entry</name>
  <files>recipe-planner/src/App.tsx, recipe-planner/src/components/Layout.tsx</files>
  <read_first>
    - recipe-planner/src/App.tsx:53-58 (recipes routes — add the import route alongside)
    - recipe-planner/src/components/Layout.tsx:107-151 (renderNav helper + the main nav list; "Step Backfill" at 151, Registries submenu after)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Component Inventory item 1 (route + nav placement, FileUploadIcon/PostAddIcon)
  </read_first>
  <action>
    In recipe-planner/src/App.tsx add `<Route path="import" element={<ImportRecipe />} />` (import the Import.tsx component) near the recipes routes (53-56). In recipe-planner/src/components/Layout.tsx add `renderNav("Import", "/import", <ImportIcon />, mini)` in the main list after "Step Backfill" (line 151) and before the Registries submenu, using a Material icon (FileUploadIcon or PostAddIcon) imported alongside the existing nav icons; include it in the mini-rail tooltip label mapping (see lines 226/236 pattern) so the collapsed rail shows the tooltip.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -n 'path="import"' src/App.tsx && grep -n '"/import"' src/components/Layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; /import is a registered route and appears in the nav (with a mini-rail tooltip).
  </acceptance_criteria>
  <done>/import registered as a route and nav entry with a mini-rail tooltip.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — paste→resolve→land-draft→redirect import flow</name>
  <action>Human pastes fixture recipe JSON on the /import page, resolves an unmatched product inline, lands a draft in prod, and confirms redirect into RecipeEditor; malformed paste writes nothing.</action>
  <what-built>The full in-app import flow: paste JSON → validate (non-blocking) → inline-resolve unmatched products → land a draft in prod → open in RecipeEditor. jsdom-untestable; verified live on the tablet.</what-built>
  <how-to-verify>
    1. Run the app. Navigate to /import via the nav.
    2. Paste a fixture D-01 recipe JSON with at least one ingredient NOT in the registry.
    3. Confirm the parse-OK Alert shows ingredient/step/unmatched counts; the unmatched ingredient triggers the "Match these products" step; resolve it via QuickCreateProductDialog (store/section/unit set).
    4. Click Import recipe — confirm a DRAFT recipe lands (badged Draft in the list) and the app redirects into RecipeEditor showing the full graph (nodes, steps with Phase-5 fields, edges).
    5. Paste malformed JSON — confirm warnings render and NOTHING is saved, button still enabled.
  </how-to-verify>
  <resume-signal>Type "approved" if the paste→resolve→land→redirect flow works and malformed input never writes, or describe the issue.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- Page `recipe-planner/src/pages/Import.tsx` (route component)
- Route `/import` in App.tsx
- Nav entry "Import" in Layout.tsx
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted JSON → prod draft | Untrusted paste flows through validate + resolve into a prod graph write (V5 input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07a | Tampering (data integrity) | partial write from bad paste | high | mitigate | validateImportJson fully validates before any buildRecipeGraph call; unmatched lines resolved before land — no partial/silent product create (avoids shopping-list-dropout bug) |
| T-06-07b | Tampering / DoS | oversized/garbage paste | medium | mitigate | Never-throw validator; button stays enabled but writes nothing until valid + resolved; manual single-user surface |
| T-06-07c | Elevation (bypass publish gate) | import landing plannable | medium | mitigate | Import lands status="draft" (excluded from planning by Plan 05); only Publish (Plan 06) promotes it |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; MUI + existing modules |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean.
- `/import` route + nav entry present.
- Live checkpoint: paste→resolve→land-draft→redirect works; malformed input writes nothing.
</verification>

<success_criteria>
A user pastes recipe JSON in-app and gets a draft landed directly in prod (no test DB, no migration), with unmatched products resolved inline and the draft opened in RecipeEditor; import never hard-blocks.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-07-SUMMARY.md` when done.
</output>
