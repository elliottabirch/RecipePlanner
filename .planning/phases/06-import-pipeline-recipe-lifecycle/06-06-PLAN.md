---
phase: 06-import-pipeline-recipe-lifecycle
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-03", "06-04"]
files_modified:
  - recipe-planner/src/pages/RecipeEditor.tsx
autonomous: false
requirements: [IMP-07]
must_haves:
  truths:
    - "A Publish button appears in the RecipeEditor action bar ONLY when the loaded recipe status === draft (D-06)"
    - "Clicking Publish runs runRecipeLint(id); if findings exist a lint-findings dialog opens and status is NEVER written; if clean, status flips to published and the button hides"
    - "Importing is never blocked by lint — the publish gate is the only hard block (first-class invariant)"
  artifacts:
    - recipe-planner/src/pages/RecipeEditor.tsx
  key_links:
    - "Publish handler calls runRecipeLint from Plan 03; findings dialog mirrors Products.tsx:522-561"
    - "status update to published depends on Recipe.status (Plan 01) + the draft filter (Plan 05) so a published recipe re-enters planning"
---

<objective>
Add the publish gate to RecipeEditor (D-06, IMP-07): a "Publish" button shown only for draft recipes that runs `runRecipeLint(id)` and flips status to published only on a clean pass, rendering failures in the existing findings-dialog pattern and never writing status on failure. Importing itself stays unblocked — publishing is the one hard gate.

Purpose: closes the lifecycle loop — a draft becomes plannable only after passing the linter, keeping the planning pool clean of half-authored recipes.
Output: Publish button + lint-findings dialog wired into RecipeEditor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/lib/linter/recipe-lint.ts
@recipe-planner/src/pages/registries/Products.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish button + lint-findings dialog in RecipeEditor</name>
  <files>recipe-planner/src/pages/RecipeEditor.tsx</files>
  <read_first>
    - recipe-planner/src/pages/RecipeEditor.tsx:843-947 (header/action bar — the Save button is at 940-947; add Publish beside it) and 307 (setRecipe/recipe state carries status)
    - recipe-planner/src/lib/linter/recipe-lint.ts (Plan 03 — runRecipeLint(recipeId) returns LintFinding[])
    - recipe-planner/src/pages/registries/Products.tsx:518-561 (lint findings Dialog to copy: title, one Alert severity per finding, `<strong>{rule}</strong>: {message}`, Close action)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Component Inventory item 3 + §Copywriting (Publish, "Fix N issue(s) before publishing", success = chip disappears)
  </read_first>
  <action>
    In recipe-planner/src/pages/RecipeEditor.tsx add local state `findings: LintFinding[]`, `lintDialogOpen: boolean`, `publishing: boolean`. Add a `handlePublish` async that calls `runRecipeLint(id!)`; if it returns any findings, `setFindings(findings); setLintDialogOpen(true)` and RETURN without writing status; if empty, `await update(collections.recipes, id!, { status: "published" })`, refresh the local recipe state so the button hides, and close. Render a "Publish" `Button variant="contained"` in the action bar next to Save (line ~940), shown only when `recipe?.status === "draft"` (green accent per UI-SPEC — the Publish action is one of the three accent-eligible surfaces). Add a lint-findings `<Dialog>` copied from Products.tsx:518-561 — title *"Fix {findings.length} issue(s) before publishing"*, one `<Alert severity={finding.severity}>` per finding rendering `<strong>{finding.rule}</strong>: {finding.message}`, and a "Close" action. Never write status on the failure path. Do not alter the Save button or handleSave.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -n "runRecipeLint\|handlePublish\|Fix" src/pages/RecipeEditor.tsx | head</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; a Publish button conditioned on status === "draft" calls runRecipeLint; findings open a dialog and never write status; clean pass sets status published and hides the button.
  </acceptance_criteria>
  <done>A draft-only Publish button gated on runRecipeLint with a findings dialog.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — publish blocks on lint, succeeds clean</name>
  <action>Human clicks Publish on a lint-failing draft (blocked with findings dialog, status unchanged), fixes it, and republishes (succeeds, button hides, recipe re-enters planning).</action>
  <what-built>The publish gate: a draft-only Publish button that blocks on lint findings and flips status to published only on a clean pass. jsdom-untestable UI, so a live pass/fail check is required.</what-built>
  <how-to-verify>
    1. Run the app; open a DRAFT recipe that has a lint-violating step (e.g. a step missing durations/prep_action).
    2. Click Publish — confirm the "Fix N issue(s) before publishing" dialog lists the findings and the recipe stays a draft (Draft chip remains in the list).
    3. Fix the violation, Save, click Publish again — confirm it succeeds silently, the Publish button disappears, and the recipe now appears in the WeekWizard pool / Add-Meal picker (re-entered planning).
    4. Confirm a published (non-draft) recipe shows NO Publish button.
  </how-to-verify>
  <resume-signal>Type "approved" if publish blocks on findings and succeeds on a clean pass, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- Publish button (draft-only) + `handlePublish` in RecipeEditor
- Lint-findings dialog (mirrors Products.tsx pattern) in RecipeEditor
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish action → status write | The gate that promotes a draft into the plannable set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06a | Elevation (bypass gate) | status write on lint failure | high | mitigate | handlePublish returns before any status write when findings exist; status only written on empty findings; live checkpoint verifies the block |
| T-06-06b | Tampering | publishing a non-draft | low | mitigate | Publish button only rendered when status === "draft"; a no-op otherwise |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; MUI Dialog + existing linter |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean.
- Live checkpoint: publish blocks on findings, succeeds clean, button hidden for published recipes.
</verification>

<success_criteria>
Publishing a draft is gated on runRecipeLint — findings block with a dialog and never write status; a clean pass flips to published and the recipe re-enters planning. Import remains unblocked.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-06-SUMMARY.md` when done.
</output>
