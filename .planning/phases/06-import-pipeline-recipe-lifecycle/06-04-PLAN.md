---
phase: 06-import-pipeline-recipe-lifecycle
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - recipe-planner/src/lib/import/build-recipe-graph.ts
  - recipe-planner/src/lib/import/graph-write.test.ts
  - recipe-planner/src/pages/RecipeEditor.tsx
autonomous: false
requirements: [IMP-02]
must_haves:
  truths:
    - "A pure planGraphWrites(graph, remapSeed) emits the correct create-vs-update operations per node: a ref present in remapSeed → update-in-place, a ref absent → create; edges are delete-all-then-recreate with direction inferred from the product-*/step-* ref prefix and skipped when an endpoint dbId is missing (D-01 id-remap contract)"
    - "buildRecipeGraph(graph, opts) is the single PB executor that lands recipe + product nodes + steps (all Phase-5 fields) + edges, reused by import, /suggest landing, and evolution write-back"
    - "RecipeEditor.handleSave delegates to buildRecipeGraph (builds a NormalizedGraph from its ReactFlow state) so there is exactly one write path — editing/saving an existing recipe still works unchanged"
    - "Hand-authored New Recipe (handleSave create path) sets status explicitly to published (Open Q1 / Pitfall 3); import/evolution set draft"
  artifacts:
    - recipe-planner/src/lib/import/build-recipe-graph.ts
    - recipe-planner/src/lib/import/graph-write.test.ts
  key_links:
    - "planGraphWrites edge-direction inference (edge.source.startsWith('product')) === RecipeEditor's original 789-813 logic — the JSON ref prefix convention keeps this portable"
    - "buildRecipeGraph create<Recipe>({status}) depends on Recipe.status type from Plan 01"
    - "The ref→dbId map returned by buildRecipeGraph === the seed the evolution write-back (Plan 10) pre-populates"
---

<objective>
Extract the graph-write spine (RESEARCH's highest-leverage move): a pure, unit-testable `planGraphWrites(graph, remapSeed)` that emits create/update/edge operations from a NormalizedGraph, plus a thin PB executor `buildRecipeGraph(graph, opts)` — then refactor `RecipeEditor.handleSave` to build a NormalizedGraph and delegate, collapsing to exactly one write path (D-01, D-05, IMP-02).

Purpose: import (Plan 07), /suggest landing (Plan 11), and evolution write-back (Plan 10) all reuse this one spine instead of re-implementing the subtle id-remap + edge-recreate + Phase-5 step-field handling (Anti-Pattern: a second copy will drift and reintroduce solved bugs).
Output: `build-recipe-graph.ts` (NormalizedGraph type, planGraphWrites, buildRecipeGraph), `graph-write.test.ts`, refactored `handleSave`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/lib/import/validate-import.ts
@recipe-planner/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure planGraphWrites planner + graph-write.test.ts</name>
  <files>recipe-planner/src/lib/import/build-recipe-graph.ts, recipe-planner/src/lib/import/graph-write.test.ts</files>
  <read_first>
    - recipe-planner/src/pages/RecipeEditor.tsx:657-825 (handleSave — THE write spine being extracted: recipe create/update 667-680, node create-vs-update + newNodeDbIds 704-762 incl. ALL Phase-5 step fields 743-749, edge delete-all-then-recreate + prefix direction + missing-endpoint guard 767-813)
    - recipe-planner/src/lib/import/validate-import.ts (NormalizedGraph shape from Plan 02 — align this planner to it)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Pattern 1 (buildRecipeGraph signature, the id-remap contract, positions non-load-bearing) + §Validation Architecture Wave 0 note (extract pure planGraphWrites)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md §build-recipe-graph.ts (the three copy-from excerpts)
  </read_first>
  <behavior>
    - planGraphWrites(graph, {}) on an all-new graph → one create op per product node + per step (carrying all Phase-5 fields), and create ops for every edge whose endpoints both resolve, direction chosen from the product-*/step-* prefix
    - planGraphWrites(graph, remapSeed) where a node ref is in remapSeed → an update op targeting that existing dbId (not a create)
    - An edge whose source or target ref is missing from the resolved id map is skipped (no op)
    - product→step ref pairs produce a product_to_step edge op; step→product pairs produce a step_to_product edge op
    - Positions are emitted as {x:0,y:0} (non-load-bearing; loadRecipe re-runs dagre)
  </behavior>
  <action>
    Create recipe-planner/src/lib/import/build-recipe-graph.ts. Re-export or import the `NormalizedGraph` type (keep a single source of truth — import it from validate-import.ts). Export a PURE `planGraphWrites(graph: NormalizedGraph, remapSeed: Record<string,string> = {}): GraphWritePlan` returning a plain-data description of operations (recipe create-or-update; per-node {op:"create"|"update", collection, ref, dbId?, data}; edge {collection, sourceRef, targetRef} entries) WITHOUT touching PocketBase. Port the exact contract from handleSave: a ref present in remapSeed → update op with that dbId; absent → create op; edge direction from `sourceRef.startsWith("product")`; skip an edge whose endpoint ref is not in the (seed + planned-create) ref set. Include all Phase-5 step fields (active_minutes, passive_minutes, instructions, prep_action, resource, oven_temp_f, rack_slots). Create recipe-planner/src/lib/import/graph-write.test.ts covering the behaviors above using fixture NormalizedGraphs (no live PB).
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/import/graph-write.test.ts</automated>
  </verify>
  <acceptance_criteria>
    graph-write.test.ts green; planGraphWrites emits correct create-vs-update per remapSeed, correct edge direction, and skips edges with unresolved endpoints — all without a live PocketBase.
  </acceptance_criteria>
  <done>A pure, tested planGraphWrites emitting correct create/update/edge ops.</done>
</task>

<task type="auto">
  <name>Task 2: buildRecipeGraph PB executor + refactor handleSave to delegate</name>
  <files>recipe-planner/src/lib/import/build-recipe-graph.ts, recipe-planner/src/pages/RecipeEditor.tsx</files>
  <read_first>
    - recipe-planner/src/pages/RecipeEditor.tsx:657-825 (handleSave, again — this is what delegates) + 843-947 (header/action bar for context; the Save button stays)
    - recipe-planner/src/lib/api.ts:28-73 (create/update/remove/getAll + collections map)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Common Pitfalls Pitfall 3 (handleSave create omits status; set published explicitly — Open Q1)
  </read_first>
  <action>
    In recipe-planner/src/lib/import/build-recipe-graph.ts, add `buildRecipeGraph(graph: NormalizedGraph, opts?: { recipeId?: string; remapSeed?: Record<string,string> }): Promise<{ recipeId: string; nodeDbIds: Record<string,string> }>` — the thin executor that (a) calls planGraphWrites, (b) executes the recipe create/update (passing name/notes/recipe_type/status/revision_of), the node create/update ops, and the edge delete-all-then-recreate via lib/api create/update/remove/getAll, and (c) returns the full ref→dbId map (mirrors handleSave's newNodeDbIds). Then refactor recipe-planner/src/pages/RecipeEditor.tsx handleSave to BUILD a NormalizedGraph from its ReactFlow nodes/edges + name/notes/recipeType/selectedTags + nodeDbIds and DELEGATE to buildRecipeGraph (passing recipeId + nodeDbIds as remapSeed for the existing-recipe case), then update nodeDbIds from the returned map and keep the tag-diff + navigate behavior. In the New-Recipe (isNew) path, set `status: "published"` explicitly (Open Q1 / Pitfall 3 — preserves today's behavior; only import/evolution set draft). Do NOT change the Save button UI. Keep tag handling in handleSave (tags are not part of the node/edge spine here) OR fold tagIds into the NormalizedGraph — either is acceptable as long as existing tag save/edit still works.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/lib/import/graph-write.test.ts</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; graph-write.test.ts still green; handleSave now delegates to buildRecipeGraph; the New-Recipe create path sets status="published".
  </acceptance_criteria>
  <done>One graph-write path: buildRecipeGraph executor with handleSave delegating to it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — existing-recipe edit + new-recipe create round-trip</name>
  <action>Human edits an existing recipe (step metadata + node quantity + new step/edge) and creates a new recipe via the refactored handleSave, confirming both persist and reopen correctly.</action>
  <what-built>RecipeEditor.handleSave was refactored to delegate all recipe+node+step+edge writes to the shared buildRecipeGraph spine. This is a refactor of a load-bearing, jsdom-untestable component, so a live smoke test confirms no regression in the existing editor.</what-built>
  <how-to-verify>
    1. Run the app (against Test DB is fine): `cd recipe-planner && npm run dev`
    2. Open an EXISTING recipe in the editor, change a step's active_minutes and a product node's quantity, add one new step + one edge, and Save.
    3. Reload the recipe — confirm the edited values, the new step, and the new edge all persisted (Phase-5 fields intact).
    4. Create a NEW recipe with 1 product + 1 step + 1 edge and Save — confirm it lands and reopens correctly, and that it appears in the recipe list (status published).
  </how-to-verify>
  <resume-signal>Type "approved" if existing-recipe edit and new-recipe create both round-trip correctly, or describe what broke.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- `NormalizedGraph` (single source, from validate-import) consumed here
- Pure `planGraphWrites(graph, remapSeed)` + `GraphWritePlan` type — build-recipe-graph.ts
- `buildRecipeGraph(graph, opts)` PB executor — build-recipe-graph.ts
- Refactored `RecipeEditor.handleSave` delegating to the spine
- Test: graph-write.test.ts
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized graph → PocketBase | The single write path lands recipe graphs; a bug here corrupts every import/evolution write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04a | Tampering (data integrity) | id-remap create-vs-update | high | mitigate | Pure planGraphWrites is unit-tested against the verified handleSave contract; edge skip-on-missing-endpoint guard preserved; live smoke checkpoint confirms no editor regression |
| T-06-04b | Tampering | New-Recipe status omission | medium | mitigate | isNew path sets status="published" explicitly (Pitfall 3) so hand-authored recipes never read as accidental drafts |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; extraction of existing code |
</threat_model>

<verification>
- `cd recipe-planner && npx vitest run src/lib/import/graph-write.test.ts` green.
- `cd recipe-planner && npx tsc --noEmit` clean.
- Live smoke checkpoint: existing-recipe edit + new-recipe create both round-trip.
</verification>

<success_criteria>
One graph-write path exists (planGraphWrites pure + buildRecipeGraph executor); handleSave delegates to it with no editor regression; the create path defaults status to published.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-04-SUMMARY.md` when done.
</output>
