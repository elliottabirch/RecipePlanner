---
phase: 06-import-pipeline-recipe-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - recipe-planner/src/lib/import/validate-import.ts
  - recipe-planner/src/lib/import/validate-import.test.ts
  - recipe-planner/src/lib/search/product-search.ts
  - recipe-planner/src/lib/search/product-search.test.ts
autonomous: true
requirements: [IMP-02]
must_haves:
  truths:
    - "validateImportJson(raw) never throws and never blocks — malformed JSON, a product line missing name/unit, a step missing step_type, or an edge ref pointing at a missing node all return { ok:false, errors } rather than crashing (D-01, D-02)"
    - "On valid input validateImportJson returns { ok:true, graph } where graph is a NormalizedGraph with product/step refs in the product-*/step-* convention and edges as {from,to} refs"
    - "Unknown resource/prep_action/timing enum values normalize to undefined + a warning, never rejecting the whole graph (mirrors loadRecipe's || undefined treatment)"
    - "scoreProduct(query, products) returns { product, score }[] exposing the numeric fuse score (lower = better) so the D-02 confidence gate can threshold on it"
  artifacts:
    - recipe-planner/src/lib/import/validate-import.ts
    - recipe-planner/src/lib/import/validate-import.test.ts
    - recipe-planner/src/lib/search/product-search.test.ts
  key_links:
    - "validateImportJson output NormalizedGraph shape === the input contract buildRecipeGraph (Plan 04) consumes"
    - "scoreProduct reuses the same FUSE_OPTIONS as searchProducts so import matching and app search rank identically (no near-dupe divergence)"
---

<objective>
Build the two pure import primitives: a never-throw JSON contract normalizer `validateImportJson` (D-01/D-02) and a scored product matcher `scoreProduct` (fills the gap that `searchProducts` discards the fuse score, Pitfall 5). Both are pure `src/lib/*` modules with full Vitest coverage — the testable foundation the import page (Plan 07) and /suggest skill (Plan 11) wire into.

Purpose: `validateImportJson` guarantees "importing never blocks" by surfacing problems as a list instead of throwing, and prevents partial writes by validating fully before any graph write. `scoreProduct` gives the D-02 confidence gate a number to threshold on.
Output: `validate-import.ts` (+ NormalizedGraph/ImportError types), `scoreProduct` in `product-search.ts`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/lib/search/product-search.ts
@recipe-planner/src/lib/linter/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateImportJson never-throw normalizer + NormalizedGraph type</name>
  <files>recipe-planner/src/lib/import/validate-import.ts, recipe-planner/src/lib/import/validate-import.test.ts</files>
  <read_first>
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Architecture Patterns Pattern 2 (validateImportJson signature, failure modes to surface not block) and §JSON Contract / D-01 (product line fields, step Phase-5 fields, ref/edge scheme)
    - recipe-planner/src/pages/RecipeEditor.tsx:343-368 (loadRecipe's `|| undefined` enum-normalize idiom to mirror for resource/prep_action/timing)
    - recipe-planner/src/lib/types.ts:98-125 (StepType, Timing, RecipeStep resource enum values — the allowed sets to normalize against)
    - recipe-planner/src/lib/linter/index.ts:1-30 (pure-module style: type-only imports, no React, no side effects)
  </read_first>
  <behavior>
    - Malformed JSON string → { ok:false, errors:[parse error] }, no throw
    - Product line missing `name` → error listing the line; missing `unit` → error; both surfaced, graph not returned
    - Step missing `step_type` → error
    - Edge `{from,to}` where `from` or `to` ref does not match any product-*/step-* node → error naming the dangling ref
    - Unknown `resource`/`prep_action`/`timing` value → normalized to undefined WITH a warning entry, graph still returned ok:true
    - Fully valid input → { ok:true, graph } with productNodes carrying { ref, name, unit, quantity, matchProductId?, productType?, pantry?, fdcId?, store?, section? }, steps carrying all Phase-5 fields, edges as { from, to } refs
  </behavior>
  <action>
    Create recipe-planner/src/lib/import/validate-import.ts exporting `NormalizedGraph`, `ImportError` (with a `severity: "error" | "warning"` + `message`), and `validateImportJson(raw: string | unknown): { ok: true; graph: NormalizedGraph; warnings: ImportError[] } | { ok: false; errors: ImportError[] }`. Accept either a raw JSON string (JSON.parse inside a try/catch that converts a SyntaxError to an error entry) or an already-parsed object. Structurally validate + normalize ONLY — never throw. Product lines: require `name` (non-empty string) and `unit`; carry through the optional D-01 hints. Steps: require `step_type` in {prep, assembly}; normalize unknown `resource`/`prep_action`/`timing` to undefined and push a warning (mirror RecipeEditor.tsx:359-366). Edges: verify each {from,to} ref resolves to a declared product-*/step-* node; otherwise push an error naming the ref. Assign or preserve refs in the `product-*`/`step-*` convention so the id-remap in Plan 04 ports unchanged. Do NOT adopt ajv (transitive @6.12.6; its throw-model fights the never-block invariant — RESEARCH Alternatives Considered). Keep the module pure (type-only imports from ../types).
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/import/validate-import.test.ts</automated>
  </verify>
  <acceptance_criteria>
    validate-import.test.ts is green covering: malformed JSON, missing name/unit, missing step_type, dangling edge ref, enum-normalize-to-warning, and a fully-valid graph round-trip. `validateImportJson` never throws for any input.
  </acceptance_criteria>
  <done>A never-throw JSON contract normalizer with full failure-mode coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scoreProduct scored fuzzy matcher</name>
  <files>recipe-planner/src/lib/search/product-search.ts, recipe-planner/src/lib/search/product-search.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/search/product-search.ts (WHOLE FILE — FUSE_OPTIONS 21-29, toSortedTokens 12-19, __original indexing 44-50, and the score being DISCARDED at line 53)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Code Examples "Scored product match" + Pitfall 5
  </read_first>
  <behavior>
    - scoreProduct("garbanzo", products) returns entries sorted best-first with a numeric `score` (0 = exact, higher = worse)
    - An exact-name product scores at or near 0; an unrelated product either scores high or is absent
    - Reuses FUSE_OPTIONS so ranking matches searchProducts (word-order + typo tolerance): "paste tomato" still ranks "tomato paste"
    - Empty query returns [] (no scoring on empty input)
  </behavior>
  <action>
    Add `scoreProduct<T extends Product>(query: string, products: T[]): { product: T; score: number }[]` to recipe-planner/src/lib/search/product-search.ts, reusing the existing FUSE_OPTIONS and the toSortedTokens/__original indexing. Map each Fuse result to `{ product: r.item.__original, score: r.score ?? 1 }` (keep the score instead of discarding it). Do not modify the existing `searchProducts` behavior. Extend recipe-planner/src/lib/search/product-search.test.ts (create the file if it does not yet exist) with the behavior cases above.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/search/product-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    product-search.test.ts is green; `scoreProduct` returns numeric scores best-first and reuses FUSE_OPTIONS; `searchProducts` is unchanged.
  </acceptance_criteria>
  <done>A scored fuzzy matcher exposing the numeric fuse score for the D-02 gate.</done>
</task>

</tasks>

<artifacts>
This plan produces:
- `validateImportJson(raw)` + `NormalizedGraph` + `ImportError` types — recipe-planner/src/lib/import/validate-import.ts
- `scoreProduct(query, products)` — added to recipe-planner/src/lib/search/product-search.ts
- Tests: validate-import.test.ts, product-search.test.ts
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted JSON → app | Untrusted user-pasted import JSON enters via validateImportJson (the one NEW input surface, V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02a | Tampering / DoS | validateImportJson pasted JSON | medium | mitigate | Never-throw normalizer; parse errors captured, not propagated; import is manual + single-user so unbounded-size DoS is out of scope |
| T-06-02b | Tampering (data integrity) | partial graph on invalid input | high | mitigate | validateImportJson validates fully and returns errors BEFORE any downstream buildRecipeGraph call — no partial write is possible from an invalid paste |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; reuses existing fuse.js |
</threat_model>

<verification>
- `cd recipe-planner && npx vitest run src/lib/import/validate-import.test.ts` green.
- `cd recipe-planner && npx vitest run src/lib/search/product-search.test.ts` green.
- `cd recipe-planner && npx tsc --noEmit` clean.
</verification>

<success_criteria>
`validateImportJson` provably never throws and surfaces every failure mode as a list; `scoreProduct` exposes the numeric fuse score reusing the shared config. Both covered by green Vitest files.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-02-SUMMARY.md` when done.
</output>
