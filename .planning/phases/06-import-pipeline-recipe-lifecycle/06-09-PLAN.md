---
phase: 06-import-pipeline-recipe-lifecycle
plan: 09
type: execute
wave: 3
depends_on: ["06-01", "06-05"]
files_modified:
  - recipe-planner/src/components/WeekWizard.tsx
autonomous: false
requirements: [IMP-06]
must_haves:
  truths:
    - "For each pool recipe R, the wizard checks whether any recipe exists with revision_of = R.id AND status = draft; if so it renders a low-emphasis 'Revised — review?' flag on that recipe's row (D-11)"
    - "Tapping the flag opens the linked draft revision in RecipeEditor"
    - "The flag is NOT accent green (uses a warning/info outlined chip) so it does not compete with the primary CTA"
  artifacts:
    - recipe-planner/src/components/WeekWizard.tsx
  key_links:
    - "The flag query 'revision_of != \"\" && status=\"draft\"' depends on recipes.revision_of (Plan 01) and is indexed client-side by revision_of"
    - "Tap target navigates to /recipes/<draftId> (the RecipeEditor review surface)"
---

<objective>
Surface the evolution loop's review prompt in the week wizard (IMP-06, D-11): when a published pool recipe has a linked pending draft revision (a recipe with `revision_of = R.id && status="draft"`), render a low-emphasis "Revised — review?" flag on that recipe's wizard row, tapping which opens the draft in RecipeEditor.

Purpose: closes the note→revision→review loop at the point the household plans the week — "we updated this per your note, want to review it?" without an automatic push.
Output: the revision flag in WeekWizard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/components/WeekWizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: "Revised — review?" flag in WeekWizard</name>
  <files>recipe-planner/src/components/WeekWizard.tsx</files>
  <read_first>
    - recipe-planner/src/components/WeekWizard.tsx:100-110 (the Promise.all data load — this is where to add the up-front draft-revision fetch; note the recipes load already gained the draft filter in Plan 05) and the pool-recipe row render (where each eligible recipe is listed per slot)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Evolution Loop "Wizard flag (D-11)" (one extra getAll indexed client-side by revision_of) + §Architecture Patterns EVOLUTION PATH
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Component Inventory item 5 + §Copywriting ("Revised — review?", low-emphasis warning/info outlined chip, tap opens draft in RecipeEditor)
  </read_first>
  <action>
    In recipe-planner/src/components/WeekWizard.tsx add ONE up-front fetch alongside the existing Promise.all: `getAll<Recipe>(collections.recipes, { filter: 'revision_of != "" && status = "draft"' })` (drafts are otherwise filtered out of the main recipes load by Plan 05, so this is a separate query). Build a client-side map from `revision_of` (the original recipe id) → the draft revision id. In the pool-recipe row render, when a recipe R has an entry in that map, render a low-emphasis MUI `Chip` labelled "Revised — review?" using a `warning` or `info` outlined variant (NOT accent green, per UI-SPEC — it must not compete with the primary CTA); onClick `navigate('/recipes/' + draftId)` to open the draft in RecipeEditor. Treatment details are Claude's discretion per CONTEXT §Claude's Discretion; keep it subtle.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -n "revision_of\|Revised" src/components/WeekWizard.tsx</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; the wizard fetches draft revisions once, indexes by revision_of, and renders a low-emphasis "Revised — review?" flag on recipes that have a pending draft revision, tapping which opens the draft in the editor.
  </acceptance_criteria>
  <done>A low-emphasis wizard revision flag that opens the linked draft in RecipeEditor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — wizard revision flag shows and opens the draft</name>
  <action>Human confirms a published recipe with a linked pending draft revision shows the low-emphasis 'Revised — review?' flag that opens the draft in RecipeEditor, and a recipe without one shows no flag.</action>
  <what-built>The wizard revision-review flag. jsdom-untestable; verified live with a seeded draft revision.</what-built>
  <how-to-verify>
    1. Create a draft recipe with `revision_of` set to a published pool recipe's id (via the evolution skill in Plan 10, or a quick PB edit for this check).
    2. Open the WeekWizard (Fill Week) and locate that published recipe in a slot's pool.
    3. Confirm a low-emphasis "Revised — review?" flag appears on that recipe's row and is NOT accent green.
    4. Tap it — confirm it opens the linked draft in RecipeEditor.
    5. Confirm a pool recipe with NO pending draft revision shows no flag.
  </how-to-verify>
  <resume-signal>Type "approved" if the flag shows only for recipes with a pending draft revision and opens the draft, or describe the issue.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- "Revised — review?" flag + up-front draft-revision fetch/index in WeekWizard
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard render → draft link | Reads draft revisions to flag review; navigation to a draft is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09a | Tampering (data integrity) | flagged draft entering planning | medium | mitigate | The flag only NAVIGATES to a draft for review; drafts remain excluded from the pool by Plan 05's filter — the flag never adds a draft to the plan |
| T-06-09b | Denial of Service | extra up-front query | low | accept | One additional getAll indexed client-side (RESEARCH-sanctioned); negligible on a single-household dataset |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; MUI Chip |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean.
- WeekWizard references revision_of + renders the flag.
- Live checkpoint: flag shows only for recipes with a pending draft revision and opens the draft.
</verification>

<success_criteria>
The week wizard flags published recipes that have a pending draft revision with a low-emphasis "Revised — review?" chip that opens the draft in RecipeEditor; drafts stay out of the pool.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-09-SUMMARY.md` when done.
</output>
