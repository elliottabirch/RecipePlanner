---
phase: 06-import-pipeline-recipe-lifecycle
plan: 10
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - recipe-planner/src/lib/import/write-back.ts
  - recipe-planner/src/lib/import/write-back.test.ts
  - .claude/skills/evolve-recipes/SKILL.md
autonomous: true
requirements: [IMP-06]
must_haves:
  truths:
    - "A pure planWriteBack(reviewedGraph, originalNodeIds) seeds the remap so every reviewed node carrying a source_node still present on the original UPDATES that original node id in place (overrides preserved), and reports original nodes with no corresponding reviewed node as dangling (D-10, Pitfall 2)"
    - "The evolve-recipes skill drains status=pending recipe_notes, clones the target graph into a draft (revision_of set, per-node source_node set), and on approval writes the reviewed graph back onto the ORIGINAL recipe id via buildRecipeGraph(recipeId, remapSeed) — keeping planned_meals + unchanged-node overrides valid (D-10, D-11)"
    - "The write-back never mints a new recipe id (no record swap) and never churns unchanged node ids"
  artifacts:
    - recipe-planner/src/lib/import/write-back.ts
    - recipe-planner/src/lib/import/write-back.test.ts
    - .claude/skills/evolve-recipes/SKILL.md
  key_links:
    - "planWriteBack seeds remapSeed[ref]=source_node → buildRecipeGraph updates the original node in place; recipe id stays stable so planned_meals.recipe + meal_variant_overrides.original_node survive"
    - "The skill reads recipe_notes (Plan 01) and reuses loadRecipe-equivalent read + buildRecipeGraph/planGraphWrites (Plan 04)"
---

<objective>
Build the evolution loop's integrity core (IMP-06, D-10/D-11): a pure, tested `planWriteBack(reviewedGraph, originalNodeIds)` that preserves node ids for unchanged nodes and flags dangling ones, plus a manual `evolve-recipes` skill that drains pending notes into draft revisions and, on approval, writes the reviewed graph back onto the ORIGINAL recipe id.

Purpose: keeping the recipe id AND unchanged node ids stable means already-planned weeks (`planned_meals.recipe`) and ingredient-swap overrides (`meal_variant_overrides.original_node`) stay valid automatically — the far smaller blast radius D-10 chose over a record swap. Getting node-id preservation right here is the phase's subtlest correctness requirement (Pitfall 2).
Output: `write-back.ts` (planWriteBack), `write-back.test.ts`, `evolve-recipes/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/lib/import/build-recipe-graph.ts
@.claude/skills/recipe-import/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure planWriteBack remap planner + write-back.test.ts</name>
  <files>recipe-planner/src/lib/import/write-back.ts, recipe-planner/src/lib/import/write-back.test.ts</files>
  <read_first>
    - recipe-planner/src/lib/import/build-recipe-graph.ts (NormalizedGraph + planGraphWrites from Plan 04 — planWriteBack produces the remapSeed that planGraphWrites/buildRecipeGraph consume)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Evolution Loop "Approval write-back (the D-10 crux)" (steps 1-5) + Pitfall 2 (node-id churn dangles overrides)
    - recipe-planner/src/lib/types.ts (RecipeProductNode.source_node from Plan 01)
  </read_first>
  <behavior>
    - planWriteBack(reviewedGraph, originalNodeIds): for each reviewed node whose source_node is in originalNodeIds → remapSeed[node.ref] = source_node (update in place onto the original id)
    - A reviewed node with no source_node (a genuinely new node) → not in remapSeed → will be created fresh
    - An original node id with no corresponding reviewed node (nothing maps to it) → reported in a `dangling: string[]` list (the only override-dangling case D-10 accepts)
    - Output shape: { remapSeed: Record<string,string>, dangling: string[] } — pure, no PB
    - Recipe id is never part of the remap (it is passed separately as recipeId to buildRecipeGraph)
  </behavior>
  <action>
    Create recipe-planner/src/lib/import/write-back.ts exporting a pure `planWriteBack(reviewedGraph: NormalizedGraph, originalNodeIds: string[]): { remapSeed: Record<string,string>; dangling: string[] }`. Build remapSeed from each reviewed product/step node's `source_node` when that id is in originalNodeIds (so buildRecipeGraph updates it in place). Compute `dangling` = original node ids that no reviewed node maps back to. Do NOT touch PocketBase. Create recipe-planner/src/lib/import/write-back.test.ts covering: unchanged node → remapSeed preserves its id; new node (no source_node) → absent from remapSeed (created fresh); removed node → appears in dangling; the recipe id is never remapped.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/import/write-back.test.ts</automated>
  </verify>
  <acceptance_criteria>
    write-back.test.ts green; planWriteBack preserves unchanged node ids via remapSeed, leaves new nodes to be created, and flags removed nodes as dangling — all pure.
  </acceptance_criteria>
  <done>A pure, tested planWriteBack preserving unchanged node ids and flagging dangling ones.</done>
</task>

<task type="auto">
  <name>Task 2: evolve-recipes manual skill (note-drain → draft revision + approval write-back)</name>
  <files>.claude/skills/evolve-recipes/SKILL.md</files>
  <read_first>
    - .claude/skills/recipe-import/SKILL.md (frontmatter + the run-from-inside-recipe-planner convention 246-247 + async main()/.catch gotcha — mirror the SKILL shape)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-RESEARCH.md §Evolution Loop "Draining pending notes" (steps 1-4) + "Approval write-back" (steps 1-5) + §Architecture Patterns EVOLUTION PATH
    - recipe-planner/src/pages/RecipeEditor.tsx:272-305 (loadRecipe — the 6-collection read the skill re-implements to clone the target graph)
    - recipe-planner/src/lib/import/write-back.ts (planWriteBack — Task 1) and build-recipe-graph.ts (buildRecipeGraph)
  </read_first>
  <action>
    Create .claude/skills/evolve-recipes/SKILL.md — a MANUAL skill (mirroring recipe-import's conventions: run from inside recipe-planner/ using its node_modules pocketbase, wrap script bodies in async main() + .catch, creds/URLs as in the recipe-import skill). Document the two operations. DRAIN: query recipe_notes where status="pending" grouped by recipe; for each target recipe R do a loadRecipe-equivalent read (the 6 collections by recipe="R"), build a NormalizedGraph applying the note's intent (edit step text/quantities/add nodes), set each cloned node's `source_node` = its original node id (new nodes get none), then `buildRecipeGraph(clone, { /* status */ "draft", revision_of: "R" })` and set recipe_notes.draft_revision = the new draft id (leave the note status pending until reviewed). APPROVE/WRITE-BACK: given an approved draft R', read R' + R's current node ids, call `planWriteBack(reviewedGraph, originalNodeIds)` to get { remapSeed, dangling }, then `buildRecipeGraph(reviewedGraph, { recipeId: "R", remapSeed })` (recipe id stays R → planned_meals valid; unchanged node ids stay → overrides valid); delete original nodes in `dangling` (the accepted small residual), set recipe_notes.status="applied", and delete/archive draft R'. The skill is chat-first and human-reviewable (D-11) — it proposes and confirms before the write-back. Emphasize the invariant: NEVER mint a new recipe id, NEVER churn unchanged node ids.
  </action>
  <verify>
    <automated>test -f .claude/skills/evolve-recipes/SKILL.md && grep -n "planWriteBack\|revision_of\|source_node\|recipeId" .claude/skills/evolve-recipes/SKILL.md | head</automated>
  </verify>
  <acceptance_criteria>
    evolve-recipes/SKILL.md exists documenting the drain (pending notes → draft revision with revision_of + per-node source_node) and the approval write-back (planWriteBack + buildRecipeGraph onto the original recipe id, dangling cleanup), following the recipe-import skill conventions.
  </acceptance_criteria>
  <done>A manual evolve-recipes skill documenting note-drain + approval write-back onto the original recipe id.</done>
</task>

</tasks>

<artifacts>
This plan produces:
- Pure `planWriteBack(reviewedGraph, originalNodeIds)` — recipe-planner/src/lib/import/write-back.ts
- Test: write-back.test.ts
- Skill `.claude/skills/evolve-recipes/SKILL.md` (note-drain + approval write-back)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill → prod graph write-back | The approval write-back mutates a live, possibly-planned recipe in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10a | Tampering (data integrity) | node-id churn dangles overrides | high | mitigate | planWriteBack unit-tested to preserve unchanged node ids via remapSeed; write-back reuses buildRecipeGraph update-in-place; recipe id never re-minted (planned_meals + overrides survive) |
| T-06-10b | Tampering | draining an unreviewed note silently | medium | mitigate | Skill is manual + chat-first (D-11): draft is produced for review; write-back only on explicit approval; note stays pending until applied |
| T-06-10c | Info Disclosure | superuser creds in skill script | medium | mitigate | Skill runs from inside recipe-planner/ using existing PB conventions; creds from gitignored .env.local, never printed |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; reuses buildRecipeGraph + pocketbase |
</threat_model>

<verification>
- `cd recipe-planner && npx vitest run src/lib/import/write-back.test.ts` green.
- `cd recipe-planner && npx tsc --noEmit` clean.
- evolve-recipes/SKILL.md documents drain + approval write-back with the id-stability invariant.
</verification>

<success_criteria>
The note→revision→write-back loop preserves the recipe id and unchanged node ids (overrides + planned weeks stay valid), flags only genuinely-removed nodes as dangling, and runs as a manual, human-reviewable skill.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-10-SUMMARY.md` when done.
</output>
