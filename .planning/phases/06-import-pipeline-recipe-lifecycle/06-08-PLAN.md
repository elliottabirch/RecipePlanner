---
phase: 06-import-pipeline-recipe-lifecycle
plan: 08
type: execute
wave: 3
depends_on: ["06-01", "06-05"]
files_modified:
  - recipe-planner/src/hooks/useRecipeNotes.ts
  - recipe-planner/src/pages/Recipes.tsx
  - recipe-planner/src/pages/WeeklyPlans.tsx
  - recipe-planner/src/components/cook-mode/NowNextCard.tsx
autonomous: false
requirements: [IMP-05]
must_haves:
  truths:
    - "A useRecipeNotes hook mirrors useRecipeQueue (CRUD + refresh) and writes recipe_notes rows with status=pending (D-09)"
    - "A one-tap Add-note button exists on THREE surfaces — recipe card (source_surface=recipe_card), cook-mode Now/Next card (source_surface=cook_mode), and calendar/planned-meal cell (source_surface=calendar) — each ≥44px touch target"
    - "Tapping opens a compact text entry + Save note; on save a pending recipe_notes row is created with no page navigation"
  artifacts:
    - recipe-planner/src/hooks/useRecipeNotes.ts
    - recipe-planner/src/pages/Recipes.tsx
    - recipe-planner/src/pages/WeeklyPlans.tsx
    - recipe-planner/src/components/cook-mode/NowNextCard.tsx
  key_links:
    - "useRecipeNotes writes collections.recipeNotes (Plan 01) with status=pending — the pending queue the evolution skill (Plan 10) drains"
    - "Each surface passes the correct source_surface enum value so provenance is preserved"
---

<objective>
Build note capture (IMP-05, D-09): a `useRecipeNotes` hook mirroring `useRecipeQueue`, and a one-tap "Add note" affordance on the three surfaces (recipe card, cook-mode Now/Next card, calendar cell) that writes a `status="pending"` note tagged with its `source_surface`.

Purpose: the pending-notes queue is the input to the evolution loop — the household can jot a one-tap improvement wherever they notice it, and the agent pass (Plan 10) later turns pending notes into draft revisions.
Output: `useRecipeNotes.ts` + Add-note buttons on the three surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-CONTEXT.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md
@.planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md
@recipe-planner/src/hooks/useRecipeQueue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRecipeNotes hook</name>
  <files>recipe-planner/src/hooks/useRecipeNotes.ts</files>
  <read_first>
    - recipe-planner/src/hooks/useRecipeQueue.ts (WHOLE FILE — mirror useState + useCallback refresh + create/getAll/remove shape exactly)
    - recipe-planner/src/lib/types.ts (RecipeNote / RecipeNoteExpanded from Plan 01) + recipe-planner/src/lib/api.ts (collections.recipeNotes from Plan 01)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-PATTERNS.md §useRecipeNotes.ts (the target structure)
  </read_first>
  <action>
    Create recipe-planner/src/hooks/useRecipeNotes.ts mirroring useRecipeQueue: state `notes: RecipeNoteExpanded[]` + `loading`; `refresh()` doing `getAll(collections.recipeNotes, { expand: "recipe", sort: "-created" })`; a useEffect calling refresh; `addNote(recipeId, text, source_surface)` doing `create(collections.recipeNotes, { recipe: recipeId, text, status: "pending", source_surface })` then refresh; `dismissNote(id)` doing `update(collections.recipeNotes, id, { status: "dismissed" })` then refresh (mirrors removeFromQueue). Return `{ notes, loading, addNote, dismissNote, refresh }`. Use lib/api wrappers, never pb.collection directly.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -n "addNote\|status: \"pending\"" src/hooks/useRecipeNotes.ts</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; useRecipeNotes exposes addNote writing a pending recipe_notes row via the api wrappers, plus refresh + dismissNote.
  </acceptance_criteria>
  <done>A useRecipeNotes hook writing pending recipe_notes via the api wrappers.</done>
</task>

<task type="auto">
  <name>Task 2: One-tap Add-note buttons on the three surfaces</name>
  <files>recipe-planner/src/pages/Recipes.tsx, recipe-planner/src/pages/WeeklyPlans.tsx, recipe-planner/src/components/cook-mode/NowNextCard.tsx</files>
  <read_first>
    - recipe-planner/src/pages/Recipes.tsx:410-444 (card body — add the IconButton in the card; note the Draft chip from Plan 05 is already here)
    - recipe-planner/src/components/cook-mode/NowNextCard.tsx:95-125 (header row near the ReadinessChip / expand toggle — add the note IconButton, thumb-reachable)
    - recipe-planner/src/pages/WeeklyPlans.tsx (the planned-meal cell — add an IconButton per planned meal)
    - .planning/phases/06-import-pipeline-recipe-lifecycle/06-UI-SPEC.md §Component Inventory item 4 + §Copywriting ("Add note" IconButton with NoteAddIcon + tooltip, compact TextField + "Save note", ≥44px, no navigation)
  </read_first>
  <action>
    Add a one-tap note affordance on each of the three surfaces using `useRecipeNotes().addNote`, an MUI `IconButton` with `NoteAddIcon` + a Tooltip, ≥44px touch target, opening a compact inline `TextField` + "Save note" (or a small dialog on the cook-mode card). Recipes.tsx card → `source_surface="recipe_card"` (recipeId = item.id). NowNextCard.tsx header row → `source_surface="cook_mode"` (recipeId from the step instance's recipe). WeeklyPlans.tsx planned-meal cell → `source_surface="calendar"` (recipeId = the planned meal's recipe). On save call addNote(recipeId, text, source_surface); no navigation; clear the field. Keep the additions minimal and thin — the hook owns the write. NowNextCard may need a small prop to receive the recipe id if not already in its instance data; thread it from the parent CookMode surface if required.
  </action>
  <verify>
    <automated>cd recipe-planner && npx tsc --noEmit 2>&1 | tail -3 && grep -rln "useRecipeNotes\|source_surface" src/pages/Recipes.tsx src/pages/WeeklyPlans.tsx src/components/cook-mode/NowNextCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    tsc clean; all three surfaces render an Add-note button wired to addNote with the correct source_surface; each writes a pending note with no navigation.
  </acceptance_criteria>
  <done>One-tap Add-note buttons on all three surfaces with correct source_surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — one-tap notes from all three surfaces</name>
  <action>Human taps Add note on the recipe card, cook-mode Now/Next card, and calendar cell, and confirms three pending recipe_notes rows land with the correct source_surface + recipe relation.</action>
  <what-built>One-tap note capture on three surfaces writing pending recipe_notes rows. jsdom-untestable; verified live on the tablet.</what-built>
  <how-to-verify>
    1. Run the app. On the Recipes list, tap Add note on a card, type a note, Save — confirm no navigation.
    2. In cook mode, tap Add note on a Now/Next card and save a note.
    3. On a weekly plan calendar cell, tap Add note on a planned meal and save a note.
    4. Query recipe_notes (PB admin or a quick script): confirm three pending rows exist with source_surface = recipe_card / cook_mode / calendar respectively and the correct recipe relation.
  </how-to-verify>
  <resume-signal>Type "approved" if all three surfaces write correctly-tagged pending notes, or describe the issue.</resume-signal>
</task>

</tasks>

<artifacts>
This plan produces:
- Hook `recipe-planner/src/hooks/useRecipeNotes.ts` (addNote/dismissNote/refresh)
- Add-note buttons on Recipes card, CookMode NowNextCard, WeeklyPlans calendar cell
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → recipe_notes | Free-text note input written to the new collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08a | Tampering | wrong source_surface / recipe id | low | mitigate | Each surface passes a fixed enum + the local recipe id; verified live per-surface |
| T-06-08b | Info Disclosure | note free text | low | accept | Single-household trusted-LAN app, no auth by design; notes are plain household text |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; MUI IconButton + existing hook pattern |
</threat_model>

<verification>
- `cd recipe-planner && npx tsc --noEmit` clean.
- All three surfaces reference useRecipeNotes + a source_surface value.
- Live checkpoint: three pending notes written with correct provenance.
</verification>

<success_criteria>
A one-tap note can be attached from the recipe card, cook mode, and calendar; each writes a pending recipe_notes row tagged with its source_surface, with no navigation.
</success_criteria>

<output>
Create `.planning/phases/06-import-pipeline-recipe-lifecycle/06-08-SUMMARY.md` when done.
</output>
