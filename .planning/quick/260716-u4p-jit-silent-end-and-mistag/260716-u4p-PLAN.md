---
quick_id: 260716-u4p
type: quick
description: Stop cook mode declaring "All steps complete!" while day-of cooking remains, split the `create spaghetti` mega-step in prod behind a gated write, and guard timing mis-tags with an evidence-designed linter rule
autonomous: false
files_modified:
  - recipe-planner/src/lib/scheduler/day-of-work.ts
  - recipe-planner/src/lib/scheduler/day-of-work.test.ts
  - recipe-planner/src/pages/CookMode.tsx
  - recipe-planner/src/lib/linter/rules/timing-coherence.ts
  - recipe-planner/src/lib/linter/rules/timing-coherence.test.ts
  - recipe-planner/src/lib/linter/index.ts
  - recipe-planner/scripts/split-create-spaghetti-step.js
  - .planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md
  - .planning/todos/pending/2026-07-12-day-of-steps-have-no-surface-cook-mode-ends-silently.md
  - .planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md
  - .planning/todos/resolved/2026-07-12-garlic-cube-clove-unit-conversion.md
  - .planning/todos/resolved/2026-07-16-thyme-mixes-sprigs-and-packages-in-each.md
  - .planning/STATE.md
must_haves:
  truths:
    - "When the prep-day list is exhausted and day-of work remains, cook mode names the meals and their step counts instead of asserting 'All steps complete!'"
    - "'All steps complete!' still renders — and is now TRUE — only when no planned meal has a day-of step left"
    - "`create spaghetti` becomes a `batch` sauce step plus a `just_in_time` plate step in BOTH spaghetti recipes, and the sauce→plate dependency edge actually connects"
    - "Meat Spaghetti appears in the day-of summary after the split — the end-to-end proof that the silent-end fix works on real data"
    - "The spaghetti prod write happens only after a human reads the full dry-run graph delta and approves; a PB backup and a rollback worksheet covering CREATED records exist before the first write"
    - "A published recipe that is correctly just_in_time (bake the cod, sear the patties, warm the pitas) does NOT become unpublishable when the timing rule lands"
    - "A simmer/braise tagged just_in_time with real passive time is flagged at the publish gate"
    - "The day-of collector is the exact complement of the week-graph exclusion — no step can be dropped by both"
    - "Every todo and STATE.md entry describing the bourguignon/spaghetti timing data matches the verified 2026-07-16 prod probe"
  artifacts:
    - recipe-planner/src/lib/scheduler/day-of-work.ts
    - recipe-planner/src/lib/linter/rules/timing-coherence.ts
    - recipe-planner/scripts/split-create-spaghetti-step.js
    - recipe-planner/scripts/dedup-output/split-create-spaghetti.json
    - recipe-planner/scripts/dedup-output/split-create-spaghetti.md
    - .planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md
  key_links:
    - "`collectDayOfWork` predicates on `step.timing === Timing.JustInTime` ONLY — deliberately NOT `isJustInTimeStep` (step-utils.ts:43), which also requires `step_type === Assembly` and would recreate the invisibility hole for a prep step tagged JIT"
    - "`lintTimingCoherence` joins `runStepLint` (index.ts:88), reaching BOTH `composeRecipeFindings` -> `runRecipeLint` (publish gate) AND CookMode.tsx:559 — and RecipeEditor.tsx:767 gates on `lintFindings.length > 0`, so a `warning` HARD-BLOCKS publish exactly like an `error`"
    - "The split's new `meat sauce` product MUST carry `canonical_unit` — `lintMissingCanonicalUnit` has no type exemption, so omitting it would make both spaghetti recipes unpublishable (they lint clean today)"
    - "The split script writes PocketBase records directly, bypassing `planGraphWrites`' (build-recipe-graph.ts:132) normalize-at-write boundary — so it must write already-canonical units itself"
    - "The bourguignon retag this plan was originally scoped to perform is ALREADY APPLIED by hand; that checkpoint step verifies state, it does not write. The spaghetti split IS a real write and carries the full gated pattern."
---

<objective>
**The bourguignon retag is already done — and how it got done is the finding.**
Verified against prod 2026-07-16: `Pull garlic cubes`, `Brown mushrooms` and
`Simmer bourguignon` all read `timing=batch`, and the live week graph contains the
`Pull garlic cubes → Simmer bourguignon` edge intact. It was applied **by hand in
the RecipeEditor, after 2026-07-12, by an unrecorded actor, with no commit and no
script** — which is exactly why three todos and four STATE.md entries still claim
it is pending, and why this task was scoped to redo it. That is a process finding,
not just a data one (planning_findings #1). There is nothing to write for the
retag; the checkpoint verifies it.

**`create spaghetti` is the write that matters, and it is worse than a mis-tag.**
It was retagged `batch` **wholesale rather than split** (both `meat spaghetti` and
`meat spaghetti (micah)`), so the app now schedules boiling noodles, plating and
parmesan for **Saturday**. Invisible became wrongly-scheduled — not an
improvement. meat spaghetti is planned in the live week and does **not** appear in
the day-of list. Only the split fixes it, so the split is in scope, behind the
full gated-write pattern: read-only audit → human-confirmed worksheet →
`pb.backups.create()` → explicitly gated `--apply` → rollback worksheet. Unlike
the garlic sweep this write **creates records**, so rollback must delete, not just
revert fields (planning_findings #4–#6).

**Cook mode is not "ending silently" — it is affirmatively lying.** The todo
describes a list that "just stops". It does not. `CookMode.tsx:704-712` renders
**"All steps complete! / Every prep-day step in this plan has been checked off."**
Silence is ambiguous; an affirmative completion claim is not. It fires while four
planned meals still need day-of cooking — five once the split lands. That is the
harm; Task 1 is the fix.

**The linter rule as scoped would brick ten published recipes.** "An `assembly`
step with meaningful `passive_minutes` tagged `just_in_time` is at least a
warning" produces **10 findings against live prod, all false positives** — every
one a step the source todo blesses by hand. And a warning is not advisory:
`RecipeEditor.tsx:767` is `if (lintFindings.length > 0)`, so a warning
**hard-blocks publish**. Task 2 ships the conjunction the evidence supports
(`passive >= 5` AND a simmer/braise/stew verb): **0 live findings, 3 of 5
historical mis-tags caught**, `roast` deliberately dropped. The severity-blind
gate itself is captured as a new todo rather than fixed here — it has its own
blast radius (planning_findings #8–#9).

**Explicitly NOT in scope, by deliberate call — do not quietly re-derive these:**

- **The day-of cook surface** (piece 2 of the day-of todo: a per-meal
  inverted-filter graph + view). That is a phase. Correctly-JIT steps stay
  invisible after this plan; the end-of-list message only *names* them. The
  `day-of-steps-have-no-surface-cook-mode-ends-silently` todo **STAYS PENDING** to
  carry it.
- **Fixing the severity-blind publish gate.** `RecipeEditor.tsx:767` treating a
  `warning` as a hard block is a real defect, but changing it re-opens what every
  existing rule's severity is allowed to mean. Own blast radius, own task —
  captured as a new todo in Task 2, not fixed.
- **The print-view / `buildWeekPullList` rewrite.** Only one of its three
  independent causes is the JIT exclusion; the raw-only filter and the
  `display:none` print mount are unrelated. Untouched.
- **The 17 `prep` steps carrying a `timing`.** Verified: all 17 are `batch`, zero
  are `just_in_time`, and `week-graph.ts` treats a `batch` prep step identically to
  a blank one. The hole this could open (planning_findings #7) is latent. The todo
  calls it low priority and it is.
- **Component-test infrastructure.** There is no jsdom, no testing-library and not
  one `.test.tsx` in the repo. End-state testing is done with pure functions
  (planning_findings #12). Standing up a component harness is not a quick task.
</objective>

<planning_findings>
**Read this before starting. Produced by probing LIVE PROD
(`http://192.168.50.95:8090`) read-only on 2026-07-16, and by running the REAL
`buildWeekGraph` via `npx tsx` against the live week plan `71ukhycp2v4s0fw`
("week of 7/13", 18 planned meals). These SUPERSEDE all three todos and the
original task scope. Two of those todos already contain corrections to their own
earlier drafts; this is the third round. Do NOT restate their claims as fact.**

1. **The retag is DONE, and the PROCESS is the finding.** Mushroom Bourguignon
   (Simple) `5t10ugwgxb166ll`: `h0bv7k5c2ez5db5` "Pull garlic cubes" 1a/0p →
   **batch**; `n43rl6q3yv2b610` "Brown mushrooms" 12a/0p → **batch**;
   `0u01dfyme13z65m` "Simmer bourguignon" 8a/35p → **batch**. Only
   `nn4q2350q9169z2` "Cook egg noodles" (2a/8p) and `54c491n5xh0l314` "Serve over
   noodles" (3a/0p) remain JIT — correctly. Published JIT steps now total **24**,
   not the todo's 29; the difference is exactly the 5 retagged records.
   **`git log --all` over `*retag*`/`*timing*` is empty and no such script exists
   in `scripts/`.** So a prod data change was made by hand in the RecipeEditor by
   an unrecorded actor at an unrecorded time, and the only reason we know is that
   this plan probed. Every downstream artifact — three todos, four STATE.md
   entries, and the scope of this very task — was built on the stale claim. The
   mis-tag todo must be **rewritten to verified prod state**, not silently closed:
   a todo that disappears teaches nothing, and this is the second time in two days
   (cf. `260716-rpp`) that the backlog's diagnosis was itself the defect.

2. **The pull-with-simmer hazard did NOT fire — verified on the real graph.**
   `buildWeekGraph` over the live plan yields 40 nodes / 20 edges and contains
   `Pull garlic cubes → Simmer bourguignon`, `Brown mushrooms → Simmer
   bourguignon`, `Quarter mushrooms → Brown mushrooms`, `Slice carrots → Simmer
   bourguignon`, `Prep onion (yellow) (diced ×2) → Simmer bourguignon`. All five
   bourguignon batch steps are nodes; the `includedIds` edge guard dropped nothing.
   (`Dice onion` appears as the week-wide merged prep node from pass (4) —
   expected.) Also: `Pull garlic cubes` is 1a/0p and the spurious-pull elision at
   pass (3b) requires `active_minutes === 0 && passive_minutes === 0`, so it is
   **not** elided — it survives as a real card.

3. **`create spaghetti` was retagged `batch` wholesale, NOT split.** Both
   `fn45gc06h1lm61i` (meat spaghetti `5909e91t6vuet1c`) and `9b55jwq1j78v7pm`
   (meat spaghetti (micah) `b7jdp83pa09y4o7`) are 8a/15p, `resource=stovetop`,
   `assembly/batch`, instructions *"Brown ground beef, combine with marinara, and
   simmer over cooked spaghetti; top with parmesan."* meat spaghetti IS planned in
   week 7/13 and does **not** appear in the computed day-of list — it has zero JIT
   steps. The whole mega-step sits on the Saturday prep schedule. The failure mode
   changed from *invisible* (cook stranded) to *wrongly scheduled* (cook boils
   spaghetti on Saturday). No message fixes that; only the split does.

4. **The exact graph delta for the split — probed, not guessed.**

   *Reference shape* (Mushroom Bourguignon, the model to mirror):
   `Simmer bourguignon` (assembly/**batch**, 8a/35p, stovetop) → `mushroom
   bourguignon [transient] 0 cup`; `Cook egg noodles` (assembly/**JIT**, 2a/8p,
   stovetop) ← `egg noodles [raw] 12 oz` + `salt` → `egg noodles cooked
   [transient]`; `Serve over noodles` (assembly/**JIT**, 3a/0p) ← `mushroom
   bourguignon` + `egg noodles cooked` → `mushroom bourguignon over egg noodles
   [stored] 0 serving`. **Note the batch→JIT direction is the SAFE one:** the JIT
   consumer leaves the week graph entirely, so no step is left scheduled without
   its dependency. The dangerous direction (JIT producer → batch consumer) is what
   the mis-tag todo warned about, and the split must not create it.

   *Current state, IDENTICAL in both spaghetti recipes* (ids given for
   `meat spaghetti`; micah's parallel ids belong in the second column of the
   worksheet):
   - step `fn45gc06h1lm61i` "create spaghetti"
   - inputs (product→step edges): `0361eubwrzqh5g6` ground beef [stored] 1,
     `qg6mi5vbf6c229q` marinara sauce [inventory] 1 each, `k04vxmlko52shxg`
     spaghetti noodles dry [inventory] 0.5 lb, `iixadk53ys65t7q` parmesan cheese
     [inventory] 0.5 cup
   - output (step→product edge): `26s1u652eio75y4` → `a85h2pw20tox65m` meat
     spaghetti stored [stored] 2

   *Target delta per recipe* — reuse the existing step for the sauce so its id and
   any `source_node` correspondence survive; create ONE new step for the day-of
   work:
   - **RETAG+RENAME** `fn45gc06h1lm61i` → name "Simmer meat sauce", `timing` stays
     `batch`, keeps `resource: stovetop`, instructions narrowed to browning the
     beef and simmering it with the marinara. Keeps inputs ground beef + marinara.
   - **NEW step** "Boil spaghetti and plate", `assembly` / **`just_in_time`**,
     `resource: stovetop`.
   - **RETARGET** the `spaghetti noodles dry` and `parmesan cheese` product→step
     edges from the old step to the new step (update `target`).
   - **NEW product node** on each recipe pointing at the new `meat sauce` product.
   - **NEW step→product edge**: "Simmer meat sauce" → the new meat-sauce node.
   - **NEW product→step edge**: the new meat-sauce node → "Boil spaghetti and
     plate". **This is the load-bearing edge — if it is missing, the plate step has
     no dependency on its sauce.**
   - **RETARGET** the step→product edge `26s1u652eio75y4` (→ meat spaghetti stored)
     `source` from the old step to the new step.
   - Durations: the original is 8a/15p. Proposed apportionment — sauce **6a/15p**,
     plate **2a/8p** (mirroring bourguignon's `Cook egg noodles` 2a/8p). **This is
     a judgement call, not derivable from the data (D-08): it is seeded in the
     worksheet as a proposal and must be human-confirmed, not auto-applied.**

5. **The new `meat sauce` product — type, fields, and a publish-gate trap.**
   No `meat sauce` product exists; it must be created once and referenced by both
   recipes. **`lintMissingCanonicalUnit` filters `(p) => !p.pantry &&
   !p.canonical_unit` with NO type exemption** — so a product created without
   `canonical_unit` emits a finding, and because the gate is severity-blind (#8) it
   would make **both spaghetti recipes unpublishable**. They lint clean today
   (0 findings each; verified). 82 of 83 transient and 68 of 68 stored products
   carry a `canonical_unit`. This is the single most likely way to break something
   with this write.
   **Type: `stored`.** Both the source todo and the task scope say "a stored
   meat-sauce product", and there is an exact precedent: `tahini sauce`
   `7263w152yj3fher` = `type=stored, canonical_unit="cup",
   storage_location="fridge", container_type="b387fv2f026m2up" (large ramekin),
   pantry=false, store=""`. Mirror it. **Be aware this diverges from the
   bourguignon reference, which models its make-ahead sauce as `transient` with
   `canonical_unit="cup"`** — arguably a mis-model, since a transient reads as a
   within-session intermediate while this sauce sits in the fridge for days.
   `stored` is also the strictly more capable choice: `week-graph.ts` pass (3) fans
   in cross-recipe edges only for `stored`/`inventory`, so a stored sauce could
   later be produced once and consumed by both spaghetti recipes, which a transient
   can never do. `determineStorageLocation` returns Fridge either way, so nothing
   regresses. **Surface this divergence in the worksheet for human confirmation —
   do not resolve it silently.** `missing-store-section` exempts stored, so no
   store is needed.

6. **The script bypasses the normalize-at-write boundary.** `planGraphWrites`
   (`build-recipe-graph.ts:132`) is the single write path for both
   `RecipeEditor.handleSave` and the /import page, and since `997c74c` it
   normalizes units on the way in. A script writing PocketBase records directly
   goes around it. Consequence: **the script must write already-canonical units
   itself** (`cup` for the sauce node, matching the product's `canonical_unit`) —
   there is nothing downstream to catch an alias.

7. **Zero `prep` steps are tagged `just_in_time`** — so the predicate gap is
   LATENT, not live. `step_type × timing` over all 222 steps: `prep/(blank)` 90,
   `assembly/batch` 91, `assembly/just_in_time` 24, `prep/batch` 17. **But the gap
   is real and Task 1 must not widen it:** `week-graph.ts:158` excludes on
   `step.timing === Timing.JustInTime` alone, while `isJustInTimeStep`
   (`step-utils.ts:43`, feeding `buildPullLists`) also requires
   `step_type === Assembly`. A prep step tagged JIT would be dropped from the week
   graph AND not reported as day-of work — invisible on every surface.
   `collectDayOfWork` must predicate on `timing === JustInTime` only, so it is the
   exact complement of the exclusion it compensates for. Do NOT reuse
   `isJustInTimeStep`.

8. **A `warning` is not advisory — it blocks.** `RecipeEditor.tsx:767` is
   `if (lintFindings.length > 0) { … return; }` before any status write: the
   publish gate is **severity-blind**. `mixed-denomination.test.ts` carries a
   comment reading "A heuristic must not block a publish"; that is misleading, and
   only harmless because that rule is inert at the publish gate (it needs
   registry-wide nodes; the recipe-scoped caller passes one recipe's and it finds
   nothing — its own header says so). **A step rule has no such escape hatch.**
   `lintTimingCoherence` joins `runStepLint`, reached by BOTH
   `composeRecipeFindings` → `runRecipeLint` (publish gate) AND `CookMode.tsx:559`
   (cook-mode lint dialog). Every false positive is a wall.
   **Measured blast radius, so the new todo does not overstate it:** simulating
   every rule against all 67 recipes, only **2 would block on re-publish** (one
   `missing-canonical-unit`, one `missing-store-section`) and **there are 0 drafts
   in prod**, so the gate is barely exercised today. The 268 products failing
   `missing-canonical-unit` are overwhelmingly unreferenced registry entries. So:
   a genuine design defect with a long tail, **not** a live outage. Do not write
   the todo as if publishing is broken.

9. **The linter rule's signal — load-bearing. Do not weaken the conjunction.**
   Measured against the 24 live JIT steps and the 5 historical mis-tag records:

   | predicate | live findings | historical caught |
   |---|---|---|
   | `passive >= 5` alone (**the rule as originally scoped**) | **10 — all false positives** | 3/5 |
   | `/simmer\|braise\|stew/` alone | 1 FP ("Serve with topping", 0p — "stew" as a *noun* naming the dish) | 3/5 |
   | **`passive >= 5` AND `/simmer\|braise\|stew/`** | **0** | **3/5** |
   | `passive >= 5` AND `…\|roast` | 2 FPs | 3/5 |
   | `passive >= 5` AND `…\|roast\|bake` | 5 FPs | 3/5 |

   - The 10 FPs: `cook salmon` 8p, `cook hamburger patties` 10p, `cook lasagna`
     45p, `cook garlic chicken` 20p, `assemble roasting veg and cook salmon` 8p,
     `assemble tuna salad sandwiches` 5p, `Warm pitas in oven` 5p, `bake french
     fries` 22p, `Cook egg noodles` 8p, `Bake cod (day-of)` 12p — the mis-tag todo
     names most of these by hand as correct. Passive duration cannot separate them:
     `cook lasagna` is 45p and correctly JIT while `Simmer bourguignon` was 35p and
     wrong.
   - **`roast` must be dropped** — it adds `cook garlic chicken` and `assemble
     roasting veg and cook salmon` and catches nothing extra. `bake` adds 5 FPs.
   - Threshold is **not** FP-constrained: 1, 5 and 10 all give 0 live findings with
     the verb conjunction. 5 is the smallest round value that still reads as
     "meaningful passive" and clears the 0p prose-noun case with margin.
   - **The rule catches 3 of 5 and cannot do better.** `Brown mushrooms`
     (12a/**0p**) and `Pull garlic cubes` (1a/**0p**) carry zero passive time, so
     no passive-gated rule reaches them, ever.

10. **The end state is an affirmative claim, not silence.** `CookMode.tsx:704-712`:
    `visibleOrder.length === 0` → a Paper reading "All steps complete! / Every
    prep-day step in this plan has been checked off." It fires only when every node
    is checked off (`visibleOrder` = `orderedByTime` minus `checkedIds`). The JIT
    steps are already available to fix it: CookMode fetches `recipe_steps`
    unfiltered (`:205-207`) into `mealKeyedRecipeData`, and only `buildWeekGraph`
    filters them. Nothing needs re-fetching.

11. **What the message says, before and after the split** (computed, not guessed).
    *Now:* `Harissa Cod with Chickpeas (1 step)`, `Mushroom Bourguignon (Simple)
    (2 steps)`, `salad and salmon (2 steps)`, `Creamy Tomato Soup (1 step)` — four
    meals. Not the todo's predicted "Mushroom Bourguignon (5 steps), Meat Spaghetti
    (1 step)". *After Task 3's split:* `meat spaghetti` gains a JIT plate step and
    becomes the fifth entry — **that is the end-to-end proof of the whole plan** and
    the checkpoint's headline check. Note `Creamy Tomato Soup (1 step)` is a garnish
    swirl, not a cook; the message does not rank importance and should not pretend to.

12. **No component-test infrastructure — do not add any.** `vite.config.ts` has no
    `test` key; `package.json` has no `jsdom`, no `happy-dom`, no
    `@testing-library/*`; there is not one `.test.tsx` in `src/`. Every test is a
    pure node-env `.test.ts`. "Tests for the cook-mode end state" therefore means
    **pure-function tests** over `collectDayOfWork` / `formatDayOfSummary`, with
    CookMode reduced to a thin render of their output.

13. **`PullListsTab` is dark for this plan.** `buildPullLists`
    (`aggregation.ts:117`) filters `plannedMeals.filter((meal) => meal.day)`, and
    **all 18** meals in week 7/13 are week-spanning with no `day` set. So it renders
    nothing and is **not a usable verification surface** for the day-of work — do
    not send the human there at the checkpoint, and do not cite it as a mitigation
    in the day-of todo.

14. **Script pattern.** `scripts/audit-node-quantities.js` is the live gated-write
    reference (the `260716-rpp` artifact, generalized): `PB_URL` env defaulting to
    prod, `--apply` (dry-run default), `--label`, superuser auth via
    `PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD`, `fmtError`, `preflightValidate`,
    `backupBeforeApply` → `pb.backups.create()`, `writeRollbackWorksheet`,
    `applyConfirmedRows`, worksheets under `scripts/dedup-output/`.
    `scripts/sync-to-test.js` copies prod → test (`:8091`) for the rehearsal and
    documents PocketBase's children-before-parents delete constraint.
</planning_findings>

<tasks>

<task type="auto">
  <name>Task 1: Stop cook mode claiming completion while day-of cooking remains</name>
  <files>recipe-planner/src/lib/scheduler/day-of-work.ts, recipe-planner/src/lib/scheduler/day-of-work.test.ts, recipe-planner/src/pages/CookMode.tsx</files>
  <action>
    This is the harm fix and the reason the plan exists. Ship it first and commit
    it on its own.

    **New pure module `src/lib/scheduler/day-of-work.ts`.** It is the deliberate
    complement of the exclusion at `week-graph.ts:158`; its header must say so and
    point back at that line — the two must be read together or a future edit
    re-opens the hole.

    Export `interface DayOfMeal { plannedMealId: string; recipeName: string;
    steps: RecipeStep[]; }` and
    `collectDayOfWork(mealData: MealKeyedRecipeData): DayOfMeal[]`. Walk `mealData`
    in iteration order (matching `buildWeekGraph`'s own pass-1 order, so output is
    deterministic without a sort), collect each meal's steps where
    `step.timing === Timing.JustInTime`, and emit one `DayOfMeal` per planned meal
    that has at least one.

    **Predicate on `step.timing === Timing.JustInTime` and NOTHING else.** Do NOT
    reuse `isJustInTimeStep` from `aggregation/utils/step-utils.ts` — it also
    requires `step_type === StepType.Assembly`, and a prep step tagged JIT would
    then be excluded by `week-graph.ts` AND unreported here, i.e. invisible on
    every surface (planning_findings #7). Prod has zero such steps today; the point
    is that this stays a true complement if that changes. Put that reasoning in a
    comment on the predicate — it is the single most likely thing for a future
    reader to "simplify".

    One `DayOfMeal` **per planned meal**, keyed on `plannedMealId`, not per recipe:
    the same recipe planned twice is two dinners and two day-of cooks. Mirrors
    `buildWeekGraph`'s per-instance node ids.

    Also export `formatDayOfSummary(meals: DayOfMeal[]): string | null` — `null`
    for an empty array, otherwise the one-line summary:
    `4 meals still need day-of cooking: Harissa Cod with Chickpeas (1 step), Mushroom Bourguignon (Simple) (2 steps), salad and salmon (2 steps), Creamy Tomato Soup (1 step)`
    Singularise both nouns: `1 meal` / `(1 step)`. Keeping the formatter pure and
    separate from the component is what makes the end state testable at all
    (planning_findings #12).

    **Wire into `CookMode.tsx`.** Add a `useMemo` over `mealKeyedRecipeData`
    calling `collectDayOfWork` (it already holds every step unfiltered,
    planning_findings #10 — do not re-fetch, do not touch the loader). Rewrite the
    `visibleOrder.length === 0` Paper at `:704-712`:
    - `collectDayOfWork` empty → keep the existing "All steps complete! / Every
      prep-day step in this plan has been checked off." wording unchanged. It is
      true in that case and only that case.
    - Otherwise → headline **"Prep day complete."**, then `formatDayOfSummary`'s
      sentence, then a per-meal list of the recipe name and its remaining step
      names, so the cook can see `Cook egg noodles` / `Serve over noodles` and act.
      Plain MUI `Typography`/`List` consistent with the surrounding Paper; no new
      component, no new dependency.

    Do NOT add a day-of scheduler, route, `NowNextCard` reuse, readiness chips,
    countdowns or check-off for these steps — that is the deferred day-of surface.
    This message names the work; it does not run it.

    **Tests — `day-of-work.test.ts`, pure, node env.** Mirror the inline fixture
    convention at the top of `week-graph.test.ts` (`makeStep`, inline
    `Map<string, RecipeGraphData>`; no live PocketBase, no jsdom — see
    planning_findings #12; do NOT add a component harness). Cover:
    (a) a meal with mixed batch + JIT steps yields one `DayOfMeal` carrying ONLY
        the JIT steps;
    (b) a meal with no JIT steps is absent entirely;
    (c) blank-timing steps are never reported (they are prep-day per
        `week-graph.ts`'s header);
    (d) **a `prep`-typed step tagged JIT IS reported** — pins planning_findings #7.
        Comment it: this is the case `isJustInTimeStep` would silently drop, and it
        is why this module does not reuse that helper;
    (e) two planned meals of the SAME recipe yield two entries;
    (f) `formatDayOfSummary([])` is `null`; singular "1 meal"/"(1 step)" and plural
        forms both render;
    (g) **the live-plan fixture** — reconstruct week 7/13's four day-of meals from
        planning_findings #11 as `WEEK_7_13_PRE_SPLIT` and assert the exact summary
        string. Comment that this is the verified PRE-split prod reality and that
        Task 3's split adds `meat spaghetti` as a fifth entry — the post-split
        behaviour is proven end-to-end at the checkpoint, against the real app, not
        here. This fixture is hand-built and offline; it must not read prod.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/day-of-work.test.ts && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>Cook mode names the remaining day-of meals and steps instead of asserting completion; "All steps complete!" survives only for the genuinely-complete case; the collector is the exact complement of `week-graph.ts:158` and pins the prep+JIT case; full suite green, tsc clean, build succeeds. Independently committable — commit this task on its own.</done>
</task>

<task type="auto">
  <name>Task 2: timing-coherence linter rule + capture the severity-blind publish gate as a todo</name>
  <files>recipe-planner/src/lib/linter/rules/timing-coherence.ts, recipe-planner/src/lib/linter/rules/timing-coherence.test.ts, recipe-planner/src/lib/linter/index.ts, .planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md</files>
  <action>
    **Read planning_findings #8 and #9 before writing a line.** The rule as
    originally specified produces 10 findings against prod, all false positives,
    and a warning hard-blocks publish — shipping it literally would make ten
    correct recipes unpublishable. Ship the conjunction instead. Commit on its own.

    Follow `rules/mixed-denomination.ts` as the pattern — including its habit of a
    long header stating the evidence, the rejected alternatives, and the rule's own
    blind spot. That header is as much the deliverable as the code.

    **New `rules/timing-coherence.ts`**, step-scoped like `missing-durations.ts`.
    Export `MIN_PASSIVE_MINUTES = 5` and `MAKE_AHEAD_VERBS = /simmer|braise|stew/i`
    as named constants (mirroring `MIN_SUBUNIT_COUNT`) so tests assert against them
    rather than duplicating magic numbers. Export
    `lintTimingCoherence(steps: RecipeStep[]): LintFinding[]`.

    Flag a step when ALL of:
    - `step.step_type === StepType.Assembly`; and
    - `step.timing === Timing.JustInTime`; and
    - `(step.passive_minutes ?? 0) >= MIN_PASSIVE_MINUTES`; and
    - `MAKE_AHEAD_VERBS` matches `` `${step.name} ${step.instructions ?? ""}` ``.

    Emit `severity: "warning"`, `rule: "timing-coherence"`, `nodeId: step.id`
    (matching `missing-durations`' use of `nodeId` for a step id). The message must
    carry the evidence so a human can dismiss it in one read: the step name, its
    passive minutes, the matched verb, and that a weekly meal's cook steps are
    `batch` unless the dish is ruined by being made ahead — "simmering" is the
    import skill's own canonical `batch` example (`SKILL.md:415-426`).

    **The header must record, because none of it is re-derivable from the code:**
    - Why the conjunction. Passive time alone cannot separate a make-ahead braise
      from a day-of bake — `cook lasagna` is 45p and correctly JIT while `Simmer
      bourguignon` was 35p and wrong. The verb alone hits `Serve with topping`,
      where "stew" is a noun naming the dish ("Top the stew with the lemon-parsley
      mixture"), not the action. Only both together are 0-FP.
    - Why `roast` and `bake` are excluded despite "roast" being suggested: `roast`
      adds `cook garlic chicken` and `assemble roasting veg and cook salmon`;
      `bake` adds `cook lasagna`, `bake french fries` and `Bake cod (day-of)`. All
      five are steps the mis-tag audit blessed by hand. **A future reader must not
      widen the verb list without re-running the prod measurement.**
    - Why 5. Not FP-constrained — 1, 5 and 10 are all 0-FP with the verb
      conjunction. 5 is the smallest round value that still reads as meaningful
      passive and clears the 0p prose-noun case.
    - **Its blind spot, plainly:** it catches 3 of the 5 historical mis-tag records
      (`Simmer bourguignon`, `create spaghetti` ×2) and CANNOT catch the other two
      — `Brown mushrooms` is 12a/**0p** and `Pull garlic cubes` is 1a/**0p**, so no
      passive-gated rule ever reaches them. A prose-based detector is the natural
      companion and is not built. Mirror `mixed-denomination.ts`'s "READ FIRST —
      this rule would NOT have caught garlic, and that is not a bug in it."
    - That `warning` blocks publish here (`RecipeEditor.tsx:767` is
      severity-blind), unlike `mixed-denomination` which is inert at that gate — so
      the 0-FP bar is a hard requirement. Point at the new todo below.

    **Wire into `index.ts`:** import and add `...lintTimingCoherence(steps)` to
    `runStepLint`. Do NOT touch `runLint` or `runWeekLint`. This single edit reaches
    both surfaces: `composeRecipeFindings` → `runRecipeLint` (publish gate,
    `recipe-lint.ts:31`) and `CookMode.tsx:559`.

    **New todo `.planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md`.**
    Frontmatter mirroring the sibling todos (`created: 2026-07-16`, `title`,
    `area: ui`, `severity`, `files` listing `RecipeEditor.tsx:744-782`,
    `linter/index.ts:37-44` (the `LintFinding.severity` union) and
    `linter/recipe-lint.ts:27-32`). Problem: `handlePublish` gates on
    `lintFindings.length > 0`, so the `severity` field every rule carefully sets is
    **never read** — a `warning` is a hard block, which makes "warning" a lie and
    forces every new step rule to be tuned to zero false positives before it can
    ship (this plan's `timing-coherence` is the worked example — cite it). Note the
    misleading comment in `mixed-denomination.test.ts` ("A heuristic must not block
    a publish"), true only because that rule is inert at the recipe-scoped gate.
    **Scope it honestly using planning_findings #8 — do NOT overstate it:**
    simulated against all 67 recipes only **2 would block on re-publish** (one
    `missing-canonical-unit`, one `missing-store-section`), and there are **0 drafts
    in prod**, so the gate is barely exercised. The 268 products failing
    `missing-canonical-unit` are overwhelmingly unreferenced registry entries. This
    is a design defect with a long tail, **not** a live outage — say so, or the next
    reader will over-prioritise it. Sketch the fix (block on `error`, surface
    `warning` non-blockingly) and name the reason it is not done here: it re-opens
    what every existing rule's severity means, which is its own blast radius.

    **Tests — `timing-coherence.test.ts`.** Mirror `mixed-denomination.test.ts`'s
    shape (small `step(...)` factory, `describe` block, assertions on rule id +
    severity + message evidence). Cover:
    (a) `Simmer bourguignon` as it was pre-retag (assembly, JIT, 8a/35p,
        instructions containing "simmer") → exactly 1 finding, severity `warning`,
        rule `timing-coherence`, message naming the step;
    (b) `create spaghetti` pre-retag (8a/15p, "…and simmer over cooked spaghetti")
        → flagged via the INSTRUCTIONS, not the name — the name alone has no verb.
        Comment that this is why the regex reads name + instructions;
    (c) the same steps at `timing: batch` (their real current prod state) → no
        finding. This is the regression proving the landed retag is clean;
    (d) **the prod false-positive fixture — the most valuable test here.** Encode
        the blessed live JIT steps from planning_findings #9 verbatim as
        `PROD_JIT_2026_07_16` (`cook salmon` 8p, `cook hamburger patties` 10p,
        `cook lasagna` 45p, `cook garlic chicken` 20p, `assemble roasting veg and
        cook salmon` 8p, `assemble tuna salad sandwiches` 5p, `Warm pitas in oven`
        5p, `bake french fries` 22p, `Cook egg noodles` 8p, `Bake cod (day-of)`
        12p, plus `Serve with topping` 0p with its "Top the stew with…"
        instructions) and assert **zero findings**. Comment above it: these are real
        published recipes; a finding here is a recipe the user can no longer
        publish. This fixture is the executable form of the design argument — widen
        the verb list or drop the passive gate and it fails.
    (e) `Brown mushrooms` (12a/0p, JIT) → **no finding**, commented as a KNOWN,
        ACCEPTED miss per the header, not an oversight to fix by relaxing the
        passive gate.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/linter/rules/timing-coherence.test.ts && npx vitest run src/lib/linter && npx tsc --noEmit && npx vitest run && npm run build && test -f ../.planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md && grep -q 'lintFindings.length > 0' ../.planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md && echo "severity-blind todo captured"</automated>
  </verify>
  <done>A simmer/braise tagged JIT with real passive time is flagged at the publish gate and in cook mode's lint dialog; zero of the 24 live JIT steps are flagged, so no published recipe becomes unpublishable; the rejected verb variants, the 0-FP measurement and the two uncatchable zero-passive mis-tags are recorded in the header and pinned by the prod fixture; the severity-blind gate is captured as a pending todo scoped to its real (small) blast radius. Independently committable — commit this task on its own.</done>
</task>

<task type="auto">
  <name>Task 3: `create spaghetti` split — read-only audit + graph-delta worksheet, rehearsed on :8091</name>
  <files>recipe-planner/scripts/split-create-spaghetti-step.js</files>
  <action>
    **This task runs READ-ONLY against prod and must NOT be invoked with `--apply`
    against prod here — that is the checkpoint, behind the human gate.** Commit on
    its own.

    Follow `scripts/audit-node-quantities.js` as the established pattern
    (planning_findings #14): `PB_URL` env defaulting to prod
    (`http://192.168.50.95:8090`), **dry-run unless `--apply`**, superuser auth via
    `PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD` read from the gitignored
    `.env.local` (**never print the values**), a `fmtError` helper, worksheets under
    `scripts/dedup-output/`.

    **Report phase (no writes).** Resolve both recipes by id — `5909e91t6vuet1c`
    (meat spaghetti) and `b7jdp83pa09y4o7` (meat spaghetti (micah)) — and locate
    each one's `create spaghetti` step by name, **asserting** it is
    `assembly`/`batch`/8a/15p/stovetop before proposing anything. If a recipe's step
    does not match, abort with a loud message rather than guessing: the shape in
    planning_findings #4 is what the delta is computed against, and if prod has
    moved again (it has once already, silently — #1) the plan must be re-derived,
    not forced.

    Emit `scripts/dedup-output/split-create-spaghetti.json`, a single worksheet
    describing the **whole graph delta** with `confirmed: false` at the top level
    (nothing applies until a human flips it), containing per recipe:
    - `updateStep`: `{ stepId, before: {name, instructions, active_minutes,
      passive_minutes, timing}, after: {...} }` for the reused sauce step —
      rename to "Simmer meat sauce", instructions narrowed to browning the beef and
      simmering it with the marinara, timing stays `batch`, resource stays
      `stovetop`, proposed **6a/15p**.
    - `createStep`: the new "Boil spaghetti and plate", `assembly`,
      `timing: just_in_time`, `resource: stovetop`, proposed **2a/8p**, instructions
      covering boiling the spaghetti, plating and topping with parmesan.
    - `createNode`: the new `meat sauce` product node on this recipe —
      `quantity: 0`, `unit: "cup"` (**canonical, matching the product's
      `canonical_unit`** — the script bypasses `planGraphWrites`' normalize, so it
      must write canonical itself, planning_findings #6; a 0-qty intermediate is the
      convention).
    - `retargetEdges`: the `spaghetti noodles dry` and `parmesan cheese`
      product→step edges (`target` → new step) and the `meat spaghetti stored`
      step→product edge (`source` → new step), each carrying its edge id and
      before/after.
    - `createEdges`: "Simmer meat sauce" → meat-sauce node (step→product), and
      meat-sauce node → "Boil spaghetti and plate" (product→step). **Mark the second
      one `loadBearing: true` in the worksheet** — without it the plate step has no
      dependency on its sauce, which is the silent-edge-drop failure this whole
      cluster is about.
    Plus one global `createProduct` entry for `meat sauce`: `type: stored`,
    `canonical_unit: "cup"`, `storage_location: "fridge"`, `container_type`
    mirroring `tahini sauce` (`b387fv2f026m2up`), `pantry: false`, no store —
    created ONCE and referenced by both recipes' nodes.

    **Seed `confirmed: false` and surface both judgement calls explicitly in the
    worksheet, do not resolve them silently (D-08):**
    (i) the 6a/15p + 2a/8p duration apportionment is a proposal — the source is one
        8a/15p step and the data cannot say how it divides; and
    (ii) `meat sauce` is typed `stored` per the todo and the task scope, which
         **diverges from the bourguignon reference** (that models its make-ahead
         sauce as `transient`). Record both readings and the rationale from
         planning_findings #5 in the worksheet so the human rules on it rather than
         discovering it later.

    Also emit a Markdown companion `scripts/dedup-output/split-create-spaghetti.md`
    (same convention as the `-node-quantities.md` reports) rendering the delta as a
    readable before/after per recipe — this is what the human actually reads at the
    checkpoint, so make the edge changes legible, not raw ids.

    **`--apply` path.** Pre-flight validate before any mutation (mirroring
    `preflightValidate`): top-level `confirmed === true`; every referenced step,
    node, edge and product id still resolves and still matches its recorded
    `before` (abort on ANY drift — prod moved once already); durations finite and
    `>= 0`; the new product's `canonical_unit` non-empty (**omitting it makes both
    spaghetti recipes unpublishable — planning_findings #5; this is the single most
    likely way to break something here**). Then, before the first write, call
    `pb.backups.create()`, print the returned backup id, and write
    `split-create-spaghetti.rollback.json`.

    **The rollback worksheet must cover CREATES, not just field reverts** — this is
    the difference from the garlic sweep. Capture: every created record's collection
    + id so rollback DELETES them (product, 2 steps, 2 nodes, 4 edges), and every
    updated/retargeted record's BEFORE values so rollback restores them. Order the
    rollback deletes children-before-parents (edges → nodes → steps → product), the
    same constraint `sync-to-test.js` documents for PocketBase relation deletes.
    Write records in dependency order on apply: product → step → node → edges.

    **Rehearsal (required before the checkpoint).** Run the report against prod
    read-only, then use `scripts/sync-to-test.js` to copy prod to test and rehearse
    the FULL `--apply` path against `PB_URL=http://192.168.50.95:8091` with the
    worksheet confirmed, proving create + retarget + backup + rollback-worksheet all
    work. Then **execute the rollback on test** and confirm the graph returns to its
    original shape — a rollback path that has never been run is not a rollback path.
    Finally, re-run `buildWeekGraph` against test via `npx tsx` and confirm the
    `Simmer meat sauce → Boil spaghetti and plate` dependency is expressed in the
    recipe graph and that the plate step is correctly ABSENT from the week graph (it
    is JIT — the safe batch→JIT direction, planning_findings #4), while
    `collectDayOfWork` now reports meat spaghetti.
  </action>
  <verify>
    <automated>cd recipe-planner && node scripts/split-create-spaghetti-step.js 2>&1 | tee /tmp/split-audit.txt && test -f scripts/dedup-output/split-create-spaghetti.json && test -f scripts/dedup-output/split-create-spaghetti.md && node -e "const w=require('./scripts/dedup-output/split-create-spaghetti.json');if(w.confirmed!==false)throw new Error('worksheet must seed confirmed:false');const rs=w.recipes||[];if(rs.length!==2)throw new Error('expected both spaghetti recipes, got '+rs.length);for(const r of rs){if(!r.updateStep||!r.createStep)throw new Error('missing step delta for '+r.recipeId);if(r.createStep.timing!=='just_in_time')throw new Error('plate step must be just_in_time');if(r.updateStep.after.timing!=='batch')throw new Error('sauce step must stay batch');if(!(r.createEdges||[]).some(e=>e.loadBearing))throw new Error('the sauce->plate edge must be marked loadBearing');}if(!w.createProduct||!w.createProduct.canonical_unit)throw new Error('new product must carry canonical_unit or both recipes become unpublishable');if(w.createProduct.type!=='stored')throw new Error('product type must be stored per planning_findings #5');console.log('split worksheet OK: 2 recipes, product',w.createProduct.name,w.createProduct.canonical_unit)"</automated>
  </verify>
  <done>Read-only audit emits a full before/after graph delta for both spaghetti recipes plus the one shared `meat sauce` product; the worksheet seeds `confirmed:false`, flags the duration apportionment and the stored-vs-transient divergence for a human, and marks the sauce→plate edge load-bearing; the `--apply` path AND its rollback are both rehearsed green on :8091, with the week graph re-verified there. Prod is untouched. Independently committable — commit this task on its own.</done>
</task>

<task type="auto">
  <name>Task 4: Backlog hygiene — file the two resolved todos, correct four stale STATE.md entries, rewrite both source todos against the probe</name>
  <files>.planning/todos/resolved/2026-07-12-garlic-cube-clove-unit-conversion.md, .planning/todos/resolved/2026-07-16-thyme-mixes-sprigs-and-packages-in-each.md, .planning/STATE.md, .planning/todos/pending/2026-07-12-day-of-steps-have-no-surface-cook-mode-ends-silently.md, .planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md</files>
  <action>
    A wrong diagnosis in the backlog is itself a defect — an unrecorded hand-edit to
    prod is how this plan's scope came to be built on an already-fixed bug
    (planning_findings #1). Commit on its own.

    **1. File the two resolved todos.** `git mv` to `.planning/todos/resolved/`
    (the directory exists and is empty):
    - `2026-07-12-garlic-cube-clove-unit-conversion.md` — title already
      `[RESOLVED 2026-07-16]`, prod sweep landed (commit `5039568`).
    - `2026-07-16-thyme-mixes-sprigs-and-packages-in-each.md` — title already
      `[RESOLVED 2026-07-16]`, prod write landed (commit `1a3cbc9`).
    Use `git mv` so history follows. Do not edit their contents.

    **2. Correct STATE.md's "Pending Todos" section (`:266-281`). FOUR entries are
    stale, not the two originally named** — the extra two are invalidated by this
    plan's own probe, and leaving them recreates the very defect being fixed:
    - **`alias-units-break-cross-recipe-aggregation`** (`:274`) — "The aggregation
      read path never calls `normalizeUnit`" is RETRACTED; the normalize shipped in
      `997c74c` and the split is `""`-driven, not alias-driven. Rewrite to match the
      todo file's own corrected Problem and drop the "Must land with
      `garlic-cube-clove-unit-conversion`" claim. Stays pending — it carries the
      deferred `|undefined` merge-semantics work.
    - **`garlic-cube-clove-unit-conversion`** (`:280`) — "Modeling gap, not
      arithmetic" is RETRACTED; it was destroyed data. **Remove the entry from
      Pending Todos entirely** — it is resolved and now lives in `resolved/`.
    - **`mis-tagged-just-in-time-timing-on-make-ahead-steps`** (`:271`) — claims the
      steps "are tagged `just_in_time`" and cites "29 published JIT steps". Both
      false as of 2026-07-16: the retag landed by hand, 24 remain. Rewrite per (4).
      Stays pending.
    - **`day-of-steps-have-no-surface-cook-mode-ends-silently`** (`:272`) — claims
      "Mushroom Bourguignon has 3 batch steps shown and 5 JIT steps hidden (incl. a
      35-min braise)". Now 5 batch shown / 2 JIT hidden, no braise among them. Also
      says "The step data is CORRECT", which the sibling todo already retracted.
      Rewrite per (4). Stays pending.
    - **Add an entry for the new `publish-gate-is-severity-blind` todo** (Task 2),
      scoped as in planning_findings #8: a design defect, 2 of 67 recipes affected,
      0 drafts — not a live outage.
    - The `thyme-mixes-sprigs-and-packages-in-each` todo is absent from the list
      entirely; since it is resolved, do not add it to Pending Todos — confirm the
      `260716-rpp` row in "Quick Tasks Completed" (`:295`) covers the thyme sweep,
      and extend that row if not.
    Add a `260716-u4p` row to the **Quick Tasks Completed** table matching the
    existing format.

    **3. Do NOT touch** the other pending todos or the `Blockers/Concerns` section.
    Only entries this plan actually disproved.

    **4. Rewrite both source todos against the probe. Both STAY PENDING** — each now
    carries exactly one deferred piece and neither can honestly be resolved here.
    Give each a `## Corrected 2026-07-16 (u4p)` section at the top recording what
    the todo previously claimed, what the probe found, and the date. Do not just
    delete the wrong text — that is how the hand-retag became invisible.

    - **`day-of-steps-have-no-surface-cook-mode-ends-silently.md`** — retract the
      premise that cook mode "ends silently": it renders an affirmative "All steps
      complete!" (`CookMode.tsx:704-712`), which is worse, and Task 1 fixed it.
      Retract the step tables — they predate the retag (bourguignon is now 5 batch /
      2 JIT). Retract the predicted message text: the real one names four meals
      (planning_findings #11), five after the split. Correct the file-reference line
      numbers: the exclusion is `week-graph.ts:158`, header comment `:146-153` (the
      todo says 150-155 / 144-149). Rewrite Solution to mark piece 1 SHIPPED
      (pointing at `day-of-work.ts` and the CookMode Paper) and keep **piece 2 — the
      day-of cook surface — as the live remainder**, which is why this stays
      pending. Carry planning_findings #13 across: `PullListsTab` is dark for week
      7/13 because all 18 meals are week-spanning, so it is **not** the entry point
      the todo assumes.
    - **`mis-tagged-just-in-time-timing-on-make-ahead-steps.md`** — retitle around
      what is left. **Record the process finding prominently (planning_findings
      #1):** three of the four rows were applied **by hand** in the RecipeEditor
      after 2026-07-12 with no commit and no script, which is why this todo still
      claimed they were pending and why a later task was scoped to redo them; prod
      data changes made outside the repo are invisible to the backlog. Retract the
      table against verified state (brown/simmer/pull already `batch`; "29 published
      JIT steps" is now 24). Retract structural problem #1 (pull-with-simmer) as
      RESOLVED with the graph evidence from planning_findings #2 — the edge exists.
      **Keep structural problem #2 and update it to the post-split reality:**
      `create spaghetti` was retagged `batch` wholesale rather than split, so the
      failure moved from invisible to *wrongly scheduled on Saturday*; this plan's
      Task 3 + checkpoint split it, so mark that piece SHIPPED once the write lands
      and point at `scripts/split-create-spaghetti-step.js`. Update Solution: item 1
      (retag) done outside the repo, uncommitted; item 2 (linter) SHIPPED — point at
      `rules/timing-coherence.ts` and carry its stated blind spot (`Brown mushrooms`
      and `Pull garlic cubes` are 0-passive and uncatchable) so nobody assumes the
      class is covered; item 3 (tighten the import skill) still open — **this is
      what keeps the todo pending.** Correct the "17 prep steps carry a timing" note:
      all 17 are `batch`, zero are `just_in_time`, and `week-graph.ts` treats a batch
      prep step exactly like a blank one — confirmed harmless, still drift.
  </action>
  <verify>
    <automated>cd /home/ellio/code/RecipePlanner && bash -e -c '
fail() { echo "FAIL: $1"; exit 1; }
for f in 2026-07-12-garlic-cube-clove-unit-conversion.md 2026-07-16-thyme-mixes-sprigs-and-packages-in-each.md; do
  test -f ".planning/todos/resolved/$f" || fail "$f not filed in resolved/"
  test -e ".planning/todos/pending/$f" && fail "$f still in pending/"
done
for f in 2026-07-12-day-of-steps-have-no-surface-cook-mode-ends-silently.md 2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md; do
  p=".planning/todos/pending/$f"
  test -f "$p" || fail "$f must STAY pending — it carries a deferred piece"
  grep -q "^## Corrected 2026-07-16" "$p" || fail "$f missing the Corrected 2026-07-16 section"
done
grep -q "week-graph.ts:158" .planning/todos/pending/2026-07-12-day-of-steps-have-no-surface-cook-mode-ends-silently.md || fail "day-of todo still cites the wrong exclusion line (was 150-155, is 158)"
grep -qi "by hand" .planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md || fail "mis-tag todo must record the unrecorded hand-edit process finding"
grep -q "260716-u4p" .planning/STATE.md || fail "STATE.md missing the 260716-u4p quick-task row"
grep -q "publish-gate-is-severity-blind" .planning/STATE.md || fail "STATE.md missing the new severity-blind todo entry"
grep -q "garlic-cube-clove-unit-conversion" .planning/STATE.md && fail "resolved garlic todo still referenced in STATE.md"
echo "hygiene OK"
'</automated>
  </verify>
  <done>Both [RESOLVED] todos live in `resolved/` with history preserved; STATE.md's four stale entries match the verified probe, the resolved garlic entry is gone, the severity-blind todo is listed, and a `260716-u4p` row exists; both source todos carry a `## Corrected 2026-07-16 (u4p)` section, cite `week-graph.ts:158`, record the unrecorded hand-edit, and stay pending carrying exactly one deferred piece each. Independently committable — commit this task on its own.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 replaced cook mode's false "All steps complete!" with a named list of the
    meals still needing day-of cooking, behind a pure, tested `collectDayOfWork` /
    `formatDayOfSummary`. Task 2 added the `timing-coherence` publish-gate rule,
    designed against the real 24 live JIT steps and pinned at zero false positives,
    and captured the severity-blind publish gate as a todo. Task 3 produced a
    read-only graph-delta worksheet for the `create spaghetti` split, rehearsed
    `--apply` AND rollback on the test instance. Task 4 corrected the backlog.

    **Prod data is still untouched.** Two different things happen below and they are
    not the same: **step 1 VERIFIES** the bourguignon retag that was already applied
    by hand (nothing to write — planning_findings #1); **steps 3-5 are the real,
    irreversible write** that splits `create spaghetti`.
  </what-built>
  <how-to-verify>
    1. **Verify the retag — read-only, no auth, no write.** This is the claim
       everything else rests on:
       `curl -s 'http://192.168.50.95:8090/api/collections/recipe_steps/records?perPage=500&filter=(recipe="5t10ugwgxb166ll")' | python3 -m json.tool | grep -E '"name"|"timing"'`
       Expect `Pull garlic cubes`, `Brown mushrooms`, `Simmer bourguignon` all
       `batch`; only `Cook egg noodles` and `Serve over noodles` `just_in_time`. If
       so, there is nothing to retag and no rollback to plan for that half.

    2. **Read the split worksheet.** Open
       `recipe-planner/scripts/dedup-output/split-create-spaghetti.md` and read the
       whole before/after for both recipes. Two calls are yours, not the script's:
       - **Durations.** The original is one 8a/15p step; the proposal is sauce
         6a/15p + plate 2a/8p (mirroring bourguignon's `Cook egg noodles` 2a/8p).
         The data cannot derive this. Change it if you disagree.
       - **`meat sauce` type.** Proposed `stored` (per the todo's wording and the
         `tahini sauce` precedent: `stored`/`cup`/fridge/large-ramekin). Note the
         bourguignon models its own make-ahead sauce as **`transient`**, so prod is
         inconsistent here. `stored` is the more capable choice — only
         stored/inventory get cross-recipe edges in `week-graph.ts` pass (3), so a
         stored sauce could later be made once for both recipes. Rule on it now.
       Then set `confirmed: true` in
       `scripts/dedup-output/split-create-spaghetti.json`.

    3. **Dry-run against prod and read the FULL delta before any write:**
       `cd recipe-planner && node scripts/split-create-spaghetti-step.js`
       Confirm every create/update/retarget matches your intent and that the
       sauce→plate edge is present. **If the script aborts on drift, stop** — prod
       has moved again (it has once already, silently) and the delta must be
       re-derived, not forced.

    4. **Apply:** `cd recipe-planner && node scripts/split-create-spaghetti-step.js --apply`
       Confirm it prints the PB backup id and wrote
       `split-create-spaghetti.rollback.json`.

    5. **Read back:** re-run the read-only report and confirm both recipes now show
       `Simmer meat sauce` (batch) → `meat sauce` → `Boil spaghetti and plate`
       (just_in_time) → `meat spaghetti stored`.

    6. **Hard-refresh the tablet** — the NAS deploy caches the old JS bundle and you
       will otherwise be testing the previous build.

    7. **The end-to-end check.** Open cook mode for the week of 7/13
       (`71ukhycp2v4s0fw`) and check off every step. Expect **"Prep day complete."**
       followed by *5 meals still need day-of cooking:* — **including `meat
       spaghetti (1 step)` with `Boil spaghetti and plate`**, alongside Harissa Cod
       (1), Mushroom Bourguignon (2), salad and salmon (2), Creamy Tomato Soup (1).
       Meat spaghetti appearing is the whole point: before this plan it was
       scheduled for Saturday and invisible as day-of work.
       Note `Boil spaghetti and plate` must NOT appear in the prep-day list itself —
       it is JIT, so it is correctly excluded from the week graph.
       **Do not check the Pull Lists tab** — it is dark for this plan (all 18 meals
       are week-spanning and `buildPullLists` filters on `meal.day`), so it proves
       nothing either way.

    8. **Confirm the linter blocks nothing you can still publish.** Open cook mode's
       Lint button for the week (it runs `runStepLint` over every planned step,
       `CookMode.tsx:559`). Expect **no `timing-coherence` finding** — every live
       JIT step is correctly tagged. A finding here means a recipe you can no longer
       publish and the rule must not ship as-is. (There are 0 drafts in prod, so the
       Publish button is not a usable surface for this check.)

    9. Optional negative check: in the RecipeEditor, temporarily flip `Simmer
       bourguignon` to `just_in_time`, hit Lint, confirm the `timing-coherence`
       warning fires and names the step — **then flip it back to `batch`**. A
       deliberate, self-reverted test.

    **Rollback:** re-run the deletes/reverts from
    `split-create-spaghetti.rollback.json` (created records are deleted
    children-first: edges → nodes → steps → product; updated records restore their
    BEFORE values), or restore the PB backup from step 4 via the PocketBase admin
    UI. The rollback path was rehearsed on `:8091` in Task 3, not just written. The
    code is four independent commits; `git revert` any one without the others.
  </how-to-verify>
  <resume-signal>Type "approved" once the split has been applied, meat spaghetti appears in the day-of summary on the tablet, and no live step trips the new rule — or describe what looked wrong in the delta</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean, full `npx vitest run` green, `npm run build` succeeds.
- `day-of-work.test.ts` pins the prep+JIT complement case and the pre-split week-7/13
  summary; `timing-coherence.test.ts` pins zero findings against the 11 real
  published JIT steps a naive rule would have flagged.
- The split worksheet's `--apply` and rollback paths are both rehearsed green on
  `:8091`; prod is written only behind the blocking checkpoint, after a backup id is
  printed and a rollback worksheet covering created records exists.
- Post-split, both spaghetti recipes express `Simmer meat sauce` (batch) → `meat
  sauce` → `Boil spaghetti and plate` (JIT), and meat spaghetti appears in the
  day-of summary.
- Both [RESOLVED] todos are in `resolved/`; STATE.md's four stale entries match the
  2026-07-16 probe; both source todos carry a `## Corrected 2026-07-16 (u4p)` section
  and remain pending, each carrying exactly one deferred piece.
- Bourguignon's three retagged steps still read `batch` — verified, not written.
</verification>

<success_criteria>
Cook mode no longer tells a cook they are finished while five dinners still need
cooking — it names the meals and the steps. `create spaghetti` is split, so the
sauce is made Saturday and the noodles are boiled the night you eat, and Meat
Spaghetti finally shows up as day-of work. A simmer or braise mis-tagged
`just_in_time` is caught at the publish gate, while every one of the 24
correctly-JIT steps in prod stays publishable. The backlog states what is true.

**The SUMMARY must state plainly:**
1. **The bourguignon retag was already applied — by hand, with no commit and no
   script, by an unrecorded actor.** That is why three todos, four STATE.md entries
   and this task's own scope all still described it as pending, and why the scoped
   audit/backup/`--apply`/rollback machinery had nothing to write. **A prod data
   change made outside the repo is invisible to the backlog** — that is the process
   finding, and it is the second time in two days the backlog's diagnosis was itself
   the defect (cf. `260716-rpp`). The durable guard is the linter rule, not a script.
2. **Cook mode was not ending silently — it was asserting "All steps complete!"**
   A stronger claim than the todo described, and a worse failure.
3. **The `create spaghetti` deferral was correctly overturned.** It had been
   retagged `batch` wholesale rather than split, so the app scheduled boiling
   noodles and plating for Saturday: invisible became wrongly-scheduled, which is
   not an improvement. The split shipped behind the full gated-write pattern, with a
   rollback that deletes created records — rehearsed on `:8091`, not just written.
   The load-bearing detail was the sauce→plate edge; without it the plate step
   schedules with no dependency on its sauce, reproducing the exact silent-edge-drop
   failure this cluster is about.
4. **The linter rule as scoped would have bricked ten published recipes.** A
   `warning` hard-blocks publish (`RecipeEditor.tsx:767` is severity-blind), and
   `passive_minutes > 0` + JIT flags 10 live steps, all correct. The shipped rule is
   a conjunction with a simmer/braise/stew verb: 0 false positives, 3 of 5
   historical mis-tags caught. **`roast` was deliberately excluded** despite being
   requested — it adds 2 false positives and catches nothing. `Brown mushrooms` and
   `Pull garlic cubes` are 0-passive and structurally uncatchable by any
   passive-gated rule; the header says so and the tests pin it.
5. **The severity-blind publish gate is captured, not fixed** — and scoped honestly:
   2 of 67 recipes would block on re-publish, 0 drafts exist, so it is a design
   defect with a long tail rather than a live outage. Fixing it re-opens what every
   rule's severity means, which is its own blast radius.
6. **Still deferred, deliberately:** the day-of cook surface (correctly-JIT steps
   are named by the new message but remain un-runnable — the day-of todo stays
   pending carrying it) and the print-view `buildWeekPullList` rewrite (only one of
   its three causes is the JIT exclusion).
</success_criteria>
