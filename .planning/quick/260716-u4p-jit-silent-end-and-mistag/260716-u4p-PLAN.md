---
quick_id: 260716-u4p
type: quick
description: Stop cook mode declaring "All steps complete!" while day-of cooking remains, guard timing mis-tags with an evidence-designed linter rule, and split the `create spaghetti` mega-step into 4 overlappable batch steps behind a gated prod write
autonomous: false
files_modified:
  - recipe-planner/src/lib/scheduler/day-of-work.ts
  - recipe-planner/src/lib/scheduler/day-of-work.test.ts
  - recipe-planner/src/pages/CookMode.tsx
  - recipe-planner/src/lib/linter/rules/timing-coherence.ts
  - recipe-planner/src/lib/linter/rules/timing-coherence.test.ts
  - recipe-planner/src/lib/linter/index.ts
  - recipe-planner/scripts/split-create-spaghetti-step.js
  - recipe-planner/scripts/dedup-output/split-create-spaghetti.json
  - recipe-planner/scripts/dedup-output/split-create-spaghetti.md
  - .planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md
  - .planning/todos/pending/2026-07-12-day-of-steps-have-no-surface-cook-mode-ends-silently.md
  - .planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md
  - .planning/todos/resolved/2026-07-12-garlic-cube-clove-unit-conversion.md
  - .planning/todos/resolved/2026-07-16-thyme-mixes-sprigs-and-packages-in-each.md
  - .planning/STATE.md
must_haves:
  truths:
    - "When the prep-day list is exhausted and day-of work remains, cook mode names the meals and their step counts instead of asserting 'All steps complete!'"
    - "meat spaghetti has ZERO just_in_time steps and stays OUT of the day-of summary — its ABSENCE is a correctness assertion, not an omission (the user cooks the noodles on prep day and stores the dish combined)"
    - "`create spaghetti` becomes FOUR `batch` steps — Cook spaghetti, Cook meat, Simmer meat sauce, Combine all — with every edge in the converging dependency chain actually connected"
    - "The scheduler CAN express the 8p noodle boil overlapping the 15p simmer — both stovetop, and prod's burner_count is 2"
    - "The spaghetti prod write happens only after a human reads the full dry-run graph delta and approves; a PB backup and a rollback worksheet covering CREATED records exist before the first write, and the rollback has been executed on :8091"
    - "A stale worksheet encoding the superseded day-of-split model cannot be applied"
    - "A published recipe that is correctly just_in_time (bake the cod, sear the patties, warm the pitas) does NOT become unpublishable when the timing rule lands"
    - "The day-of collector is the exact complement of the week-graph exclusion — no step can be dropped by both"
    - "Every todo and STATE.md entry describing the bourguignon/spaghetti timing data matches verified prod AND the 2026-07-17 user interview"
  artifacts:
    - recipe-planner/src/lib/scheduler/day-of-work.ts
    - recipe-planner/src/lib/linter/rules/timing-coherence.ts
    - recipe-planner/scripts/split-create-spaghetti-step.js
    - recipe-planner/scripts/dedup-output/split-create-spaghetti.json
    - recipe-planner/scripts/dedup-output/split-create-spaghetti.md
    - .planning/todos/pending/2026-07-16-publish-gate-is-severity-blind.md
  key_links:
    - "`collectDayOfWork` predicates on `step.timing === Timing.JustInTime` ONLY — deliberately NOT `isJustInTimeStep` (step-utils.ts:43), which also requires `step_type === Assembly` and would recreate the invisibility hole for a prep step tagged JIT"
    - "`lintTimingCoherence` (timing-coherence.ts:77) short-circuits on `step.timing !== Timing.JustInTime`, so the new all-`batch` spaghetti steps never trip it — verified; `Simmer meat sauce` (1a/15p, name carries 'Simmer') would fire only if someone later flipped it to JIT, which is exactly the guard wanted"
    - "Reusing the existing `create spaghetti` step id as `Combine all` preserves TWO edges (the parmesan input AND the terminal `meat spaghetti stored` output) — every alternative reuse preserves at most one, and losing the terminal edge would orphan the recipe's stored product"
    - "The split's 3 new transient products MUST each carry `canonical_unit` — `lintMissingCanonicalUnit` has no type exemption, so omitting it would make both spaghetti recipes unpublishable (they lint clean today)"
    - "The split script writes PocketBase records directly, bypassing `planGraphWrites`' (build-recipe-graph.ts:132) normalize-at-write boundary — so it must write already-canonical units itself"
    - "prod `scheduler_config.burner_count = 2` is load-bearing for the whole point of the split: two stovetop steps hold burners through their passive windows (resources.ts:(3)), so at burner_count=1 the noodle/simmer overlap would be IMPOSSIBLE and the split would make the dish slower"
---

<objective>
**Status: Tasks 1, 2 and 4 of the previous revision are SHIPPED and committed
(`c63d799`, `9f47692`, `b6f009f`, `c23aafd`). The prod write never happened.
This revision rewrites the split from scratch and amends two shipped artifacts.**

**The spaghetti model was wrong, and the correction is total.** A direct user
interview (2026-07-17) established how the dish is actually cooked: **the
spaghetti is boiled on prep day, combined with the sauce, and stored combined.
There is no day-of work for this dish at all.** Every consequence follows:

- **`create spaghetti` being `batch` is CORRECT.** The unrecorded hand-retag was
  right. Its all-`batch` tagging is not a defect and must not be "fixed".
- **The source todo's own proposal is FALSE.** It claims the mega-step "conflates
  make-ahead sauce with day-of work (boil the spaghetti, plate, top with
  parmesan)". Boiling the spaghetti is *not* day-of work for this user. The todo
  was written from a misreading of how the user cooks — not from the data.
- **The "wrongly scheduled noodles" framing is dead.** The previous revision of
  this plan called it a bug. It is the desired behaviour. Discarded.
- **The batch-sauce + JIT-plate split is WRONG and must not be built.** The script
  committed in `28c9102` encodes exactly that model and is **rewritten, not
  amended** — and a **stale worksheet encoding it is sitting on disk right now**,
  appliable if someone flips `confirmed` (planning_findings #6).
- **The previous headline verification is inverted.** "Meat spaghetti becomes the
  5th day-of entry" must NOT happen. Its **absence** from the day-of summary is
  now a correctness assertion — the shipped `day-of-work.test.ts` carries a
  comment predicting the opposite and is amended in Task 2.

**The real defect is narrow and different: `create spaghetti` is a coarse
mega-step hiding two overlappable passive windows.** One 8a/15p step cannot
express that the 8-minute noodle boil and the 15-minute sauce simmer can run at
the same time. Split into four `batch` steps, the scheduler can. Verified
load-bearing: prod's `burner_count` is **2**, and `resources.ts` holds a burner
through a step's passive window — at `burner_count: 1` this split would make the
dish *slower*, not faster (planning_findings #4).

**Still true and still shipping:** cook mode was asserting "All steps complete!"
while four meals needed day-of cooking (fixed, `c63d799`), and the timing rule had
to be narrowed to a `passive >= 5` AND simmer/braise/stew conjunction because the
rule as originally scoped flags 10 live steps, all false positives, and a
`warning` hard-blocks publish (shipped, `9f47692`).

**Explicitly NOT in scope, by deliberate call — do not quietly re-derive these:**

- **The day-of cook surface.** Correctly-JIT steps (`assemble tacos`, `Bake cod`,
  `Serve over noodles`) are *named* by the new message but remain un-runnable.
  The `day-of-steps-have-no-surface-cook-mode-ends-silently` todo **STAYS PENDING**
  to carry it.
- **Any day-of split for spaghetti, forever.** Recorded loudly in the mis-tag todo
  (Task 3) so nobody re-derives it in three months from the same misreading.
- **Fixing the severity-blind publish gate** (`RecipeEditor.tsx:767`). Captured as
  a todo in `9f47692`; changing it re-opens what every rule's severity means.
- **The print-view / `buildWeekPullList` rewrite.** Only one of its three causes is
  the JIT exclusion.
- **The 17 `prep` steps carrying a `timing`.** All 17 are `batch`, zero are
  `just_in_time`; `week-graph.ts` treats a batch prep step identically to a blank
  one. Latent, low priority.
- **Component-test infrastructure.** No jsdom, no testing-library, zero `.test.tsx`.
  End-state testing stays pure-function.
</objective>

<planning_findings>
**Read this before starting. Sources: a direct USER INTERVIEW (2026-07-17) that
supersedes the todos' model of the dish; live PROD probes
(`http://192.168.50.95:8090`, read-only, 2026-07-16/17); and the REAL
`buildWeekGraph` run via `npx tsx` against week plan `71ukhycp2v4s0fw`. The todos
have now been corrected THREE times — do NOT restate their claims as fact.**

1. **The bourguignon retag is DONE, and the PROCESS is the finding.**
   `h0bv7k5c2ez5db5` "Pull garlic cubes" 1a/0p, `n43rl6q3yv2b610` "Brown mushrooms"
   12a/0p and `0u01dfyme13z65m` "Simmer bourguignon" 8a/35p all read **batch**.
   Only `Cook egg noodles` (2a/8p) and `Serve over noodles` (3a/0p) remain JIT —
   correctly. Published JIT steps total **24**, not the todo's 29; the difference is
   exactly the 5 retagged records. **`git log --all` over `*retag*`/`*timing*` is
   empty and no such script exists.** A prod data change was made by hand in the
   RecipeEditor by an unrecorded actor, and the only reason we know is that this
   plan probed. Three todos, four STATE.md entries and this task's own scope were
   all built on the stale claim. **A prod data change made outside the repo is
   invisible to the backlog** — that is the durable lesson.

2. **The pull-with-simmer hazard did NOT fire.** `buildWeekGraph` over the live plan
   yields 40 nodes / 20 edges and contains `Pull garlic cubes → Simmer bourguignon`,
   `Brown mushrooms → Simmer bourguignon`, `Quarter mushrooms → Brown mushrooms`,
   `Slice carrots → Simmer bourguignon`, `Prep onion (yellow) (diced ×2) → Simmer
   bourguignon`. The `includedIds` edge guard dropped nothing. `Pull garlic cubes`
   is 1a/0p and the pass-(3b) spurious-pull elision needs `0a/0p`, so it survives as
   a real card.

3. **How the user actually cooks meat spaghetti (interview 2026-07-17) — this
   supersedes the todo and the two previous revisions of this plan.** The spaghetti
   is **boiled on prep day**, combined with the sauce, and **stored combined**.
   There is **no day-of work**. Therefore: `create spaghetti` at `batch` is correct;
   the todo's "conflates batch sauce with day-of plating" claim is false, having been
   derived from an assumption about the dish rather than from the user; and meat
   spaghetti having **zero JIT steps is correct by design**, so its absence from the
   day-of summary is a property to assert, not a gap to close. The only real defect
   is granularity: one 8a/15p step cannot express two overlappable passive windows.

4. **`burner_count: 2` is load-bearing for the entire point of the split — verified
   in prod.** `resources.ts` case (3): stovetop is metered by
   `scheduler_config.burner_count` **across the full active+passive window** ("a
   simmering pot still occupies its burner"). The implicit cook, by contrast, is busy
   only during `active_minutes` — which is what lets one step's passive window absorb
   another's active work. `Cook spaghetti` (2a/8p, stovetop) and `Simmer meat sauce`
   (1a/15p, stovetop) therefore need **two concurrent burners** to overlap. Prod's
   `scheduler_config` reads `burner_count: 2`, `oven_rack_slots: 2` → **the overlap
   is expressible.** At `burner_count: 1` the two would serialize and the split would
   make the dish *slower* than the mega-step. If this ever changes, the split's
   rationale evaporates — record it in the script header.
   **Honest limit:** burners are a week-wide resource and other stovetop steps
   compete for them. The split makes the overlap **expressible**; the GA decides
   whether it happens. Do not promise a specific schedule.

5. **The user-confirmed target — 4 steps, all `batch`.**

   | step | times | resource | consumes | produces |
   |---|---|---|---|---|
   | Cook spaghetti | 2a/8p | stovetop | spaghetti noodles dry | spaghetti noodles cooked *(transient)* |
   | Cook meat | 5a/0p | stovetop | ground beef [stored] | ground beef browned *(transient)* |
   | Simmer meat sauce | 1a/15p | stovetop | ground beef browned + marinara | meat sauce *(transient)* |
   | Combine all | 2a/0p | none | spaghetti noodles cooked + meat sauce + parmesan | meat spaghetti stored *(existing)* |

   Durations are user-confirmed. Total moves **8a/15p → 10a/23p**: the mega-step was
   **under-counting** active time. Expected and accepted — the win is expressible
   overlap, not a smaller number. **Parmesan goes into `Combine all`**, not a serving
   step. The sauce is **`transient`**, not `stored` — the user ruled on the
   divergence surfaced last revision, and the bourguignon precedent (`mushroom
   bourguignon [transient] cup`) was the right read, not the todo's wording.
   Apply to BOTH recipes with identical structure; micah differs only in scale.

6. **Two live hazards from the superseded model — deal with both.**
   - `scripts/split-create-spaghetti-step.js` (`28c9102`, 34KB) encodes the
     batch-sauce/JIT-plate model. **Rewrite it, do not amend.** Its scaffolding
     (`PB_URL`, `--apply` gating, superuser auth, `pb.backups.create()`, worksheet +
     rollback emission, drift assertions) is sound and worth keeping; its graph delta
     is not.
   - **`scripts/dedup-output/split-create-spaghetti.{json,md}` exist on disk right
     now** (written 22:48, encoding the wrong delta). If anyone flips
     `confirmed: true` and runs `--apply`, they apply the superseded model. Delete
     them and **version-stamp the worksheet** (`model: "batch-4step-v2"`) with an
     `--apply` assertion, so a stale worksheet is rejected rather than mis-read.

7. **The exact graph delta — probed, and prod has NOT drifted since planning**
   (re-verified 2026-07-17: both `fn45gc06h1lm61i` and `9b55jwq1j78v7pm` still read
   `create spaghetti | assembly/batch | 8a/15p | res=stovetop`).

   *Current state, identical in both recipes* (ids: `meat spaghetti`
   `5909e91t6vuet1c` / `meat spaghetti (micah)` `b7jdp83pa09y4o7`):
   - step `fn45gc06h1lm61i` / `9b55jwq1j78v7pm` "create spaghetti"
   - inputs (product→step): ground beef [stored] `0361eubwrzqh5g6` / `bc932vsij50il7x`;
     marinara [inventory] `qg6mi5vbf6c229q` / `sy7zc1e06wa89z1`; spaghetti noodles dry
     [inventory] `k04vxmlko52shxg` / `ii4k066h8kn0z8u`; parmesan [inventory]
     `iixadk53ys65t7q` / `y768798c2885910`
   - output (step→product): `26s1u652eio75y4` / `kw68239j5pn48zm` → meat spaghetti
     stored [stored] `a85h2pw20tox65m` / `y0147vg4911c66y`
   - upstream: "take ground beef out of freezer" → ground beef [stored] (unchanged)

   ***REUSE the existing step id as `Combine all` — hypothesis CONFIRMED, and it is
   the maximum-preservation choice.*** It keeps **two** edges: the parmesan input AND
   the terminal `meat spaghetti stored` output. Every alternative keeps at most one
   (reuse-as-Simmer keeps only marinara; reuse-as-Cook-meat keeps only beef), and
   would additionally require retargeting the terminal output edge — the one whose
   loss orphans the recipe's stored product. Reuse-as-Combine also leaves the
   upstream `take ground beef out of freezer → …` intra-recipe edge to re-derive
   itself correctly through the retargeted beef node.

   *Delta per recipe:* **UPDATE** the reused step (name → "Combine all", 2a/0p,
   `resource: none`, stays `batch`, instructions rewritten to the combine);
   **RETARGET 3** product→step edges (beef → Cook meat, marinara → Simmer meat sauce,
   noodles → Cook spaghetti); **CREATE 3** steps, **3** product nodes, **3**
   step→product edges (Cook spaghetti→cooked, Cook meat→browned, Simmer→meat sauce)
   and **3** product→step edges (browned→Simmer, meat sauce→Combine,
   cooked→Combine). **KEEP** the parmesan input and the stored output untouched.

8. **The 3 new products — global, shared by both recipes, and a publish-gate trap.**
   All `transient`, following prod's `<product> <past-participle>` convention
   (`mushrooms browned`, `egg noodles cooked`): **`spaghetti noodles cooked`**,
   **`ground beef browned`**, **`meat sauce`** (named plainly, mirroring `mushroom
   bourguignon`). **Every one MUST carry `canonical_unit`** —
   `lintMissingCanonicalUnit` filters `!p.pantry && !p.canonical_unit` with **no type
   exemption**, and the publish gate is severity-blind, so omitting it makes **both
   spaghetti recipes unpublishable**. They lint clean today (0 findings each,
   verified). Use `cup` for all three: `egg noodles cooked`, `mushrooms browned` and
   `mushroom bourguignon` are all `cup`. (`soba noodles cooked` is `oz` — prod is not
   perfectly consistent; `cup` is the closer analog and the quantity is 0 anyway, so
   this is a low-stakes worksheet choice.) `missing-store-section` and `prep-words`
   both exempt non-raw types, so no store is needed and "browned"/"cooked" in a
   transient name is safe (`prep-words.ts` scopes to `raw`).

9. **The script bypasses the normalize-at-write boundary.** `planGraphWrites`
   (`build-recipe-graph.ts:132`) is the single write path for `RecipeEditor.handleSave`
   and /import, and since `997c74c` it normalizes units on the way in. A script
   writing PocketBase records directly goes around it, so it **must write canonical
   units itself** (`cup`) — nothing downstream will catch an alias.

10. **`timing-coherence` cannot fire on the new steps — sanity check CONFIRMED.**
    The shipped rule (`timing-coherence.ts:77`) short-circuits on
    `step.timing !== Timing.JustInTime` before any name/passive test. All four new
    steps are `batch`, so none can trip it — including `Simmer meat sauce`, despite
    carrying "Simmer" in its name and 15p passive. Better: that step is a latent
    **beneficiary** — if anyone ever flips it to `just_in_time`, the rule fires and
    names it (1a/15p, verb "simmer", passive ≥ 5). The guard points the right way.

11. **No step in the split is elided or merged.** Pass (3b) spurious-pull elision
    needs `active_minutes === 0 && passive_minutes === 0` + a single `inventory`
    input; all four steps have nonzero active time. The pass-(4) prep merge needs a
    single **RAW** input and `resource: "none"`; `Cook meat`'s lone input is
    `ground beef [stored]` (not raw) and its resource is stovetop, and `Combine all`
    has three inputs. So all four survive as distinct nodes: **the week graph gains
    exactly 3 nodes per recipe** (2 steps → 5), which is check (b) at the checkpoint.

12. **Zero `prep` steps are tagged `just_in_time`** — the predicate gap is LATENT.
    `step_type × timing` over all 222 steps: `prep/(blank)` 90, `assembly/batch` 91,
    `assembly/just_in_time` 24, `prep/batch` 17. But the gap is real:
    `week-graph.ts:158` excludes on `timing === JustInTime` alone, while
    `isJustInTimeStep` (`step-utils.ts:43`) also requires `step_type === Assembly`, so
    a prep step tagged JIT would be dropped from the week graph AND unreported as
    day-of work — invisible everywhere. The shipped `collectDayOfWork` predicates on
    `timing` only, which is correct; do not "simplify" it onto `isJustInTimeStep`.

13. **A `warning` is not advisory — it blocks.** `RecipeEditor.tsx:767` is
    `if (lintFindings.length > 0) { … return; }` before any status write: the publish
    gate is **severity-blind**. That is why `timing-coherence` had to be tuned to zero
    false positives. **Measured blast radius, so nobody over-prioritises the todo:**
    simulating every rule across all 67 recipes, only **2 would block on re-publish**
    (one `missing-canonical-unit`, one `missing-store-section`) and there are **0
    drafts in prod**. A design defect with a long tail, **not** a live outage.

14. **The linter signal (shipped, `9f47692`) — do not weaken the conjunction.**
    Measured against the 24 live JIT steps and the 5 historical mis-tag records:
    `passive >= 5` alone → **10 findings, all false positives**, 3/5 caught;
    verb alone → 1 FP ("Serve with topping", 0p — "stew" as a noun), 3/5;
    **`passive >= 5` AND `/simmer|braise|stew/` → 0 findings, 3/5**;
    adding `roast` → 2 FPs; adding `bake` → 5 FPs. Passive duration cannot separate
    the cases (`cook lasagna` 45p is correctly JIT; `Simmer bourguignon` 35p was
    wrong). `Brown mushrooms` (12a/**0p**) and `Pull garlic cubes` (1a/**0p**) are
    uncatchable by any passive-gated rule — stated blind spot, pinned by tests.

15. **The day-of summary for week 7/13, final** (computed): `Harissa Cod with
    Chickpeas (1 step)`, `Mushroom Bourguignon (Simple) (2 steps)`, `salad and salmon
    (2 steps)`, `Creamy Tomato Soup (1 step)` — **four meals, and meat spaghetti is
    correctly NOT among them, before AND after the split.** The shipped
    `day-of-work.test.ts:155-161` fixture data is right but its comment predicts a
    fifth entry; Task 2 inverts that.

16. **`PullListsTab` is dark for this plan.** `buildPullLists` (`aggregation.ts:117`)
    filters `plannedMeals.filter((meal) => meal.day)`, and all 18 meals in week 7/13
    are week-spanning. It renders nothing and is **not a usable verification
    surface** — do not send the human there.
</planning_findings>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the split script for the 4-step all-batch model</name>
  <files>recipe-planner/scripts/split-create-spaghetti-step.js, recipe-planner/scripts/dedup-output/split-create-spaghetti.json, recipe-planner/scripts/dedup-output/split-create-spaghetti.md</files>
  <action>
    **This task runs READ-ONLY against prod and must NOT be invoked with `--apply`
    against prod here — that is the checkpoint, behind the human gate.** Commit on
    its own.

    **Rewrite `scripts/split-create-spaghetti-step.js`, do not amend it.** The
    committed version (`28c9102`) encodes the superseded batch-sauce/JIT-plate model
    (planning_findings #3). **Keep its scaffolding** — `PB_URL` env defaulting to
    prod, dry-run unless `--apply`, superuser auth via
    `PB_SUPERUSER_EMAIL`/`PB_SUPERUSER_PASSWORD` from the gitignored `.env.local`
    (**never print the values**), `fmtError`, the exact-shape drift assertions,
    `pb.backups.create()`, worksheet + rollback emission under `scripts/dedup-output/`
    — and **replace the graph delta entirely**.

    **First, delete the stale worksheets** `scripts/dedup-output/split-create-spaghetti.json`
    and `.md`. They are on disk encoding the wrong delta and would apply the wrong
    model if someone flipped `confirmed` (planning_findings #6). Then **version-stamp
    the new worksheet**: a top-level `model: "batch-4step-v2"` field that `--apply`
    asserts before doing anything. A worksheet without it, or with another value, is
    rejected loudly. This is what makes the stale one unusable rather than merely
    deleted.

    **Header must record why this shape exists**, because none of it is re-derivable:
    the user cooks the spaghetti on prep day and stores the dish combined (interview
    2026-07-17), so all four steps are `batch` and **there is no day-of step —
    anyone who re-derives a JIT plate step is repeating the todo's original
    misreading**; and the split's entire value is that `burner_count: 2` lets the
    noodle boil overlap the simmer, which one 8a/15p step cannot express — **at
    `burner_count: 1` this split would make the dish slower** (planning_findings #4).

    **Report phase (no writes).** Resolve `5909e91t6vuet1c` and `b7jdp83pa09y4o7`,
    locate each `create spaghetti` step, and **assert** it is
    `assembly`/`batch`/8a/15p/stovetop with exactly the four inputs and one output in
    planning_findings #7 before proposing anything. On any mismatch, abort loudly —
    prod moved once already, silently (#1), and a delta computed against a stale
    shape must be re-derived, not forced.

    Emit `scripts/dedup-output/split-create-spaghetti.json` seeded
    `confirmed: false`, `model: "batch-4step-v2"`, describing the whole delta:
    - **`createProducts` (3, global, created once and shared by both recipes):**
      `spaghetti noodles cooked`, `ground beef browned`, `meat sauce` — each
      `type: transient`, **`canonical_unit: "cup"`**, `pantry: false`, no store.
      **Preflight-assert `canonical_unit` is non-empty on every one** — omitting it
      makes both recipes unpublishable (planning_findings #8), the single most likely
      way to break something here.
    - **Per recipe, `updateStep`:** the reused step → name "Combine all",
      **2a/0p**, `resource: none`, timing stays `batch`, instructions rewritten to
      combining the cooked spaghetti, meat sauce and parmesan for storage. Carry
      `before`/`after` for every field touched.
    - **Per recipe, `createSteps` (3):** `Cook spaghetti` 2a/8p stovetop;
      `Cook meat` 5a/0p stovetop; `Simmer meat sauce` 1a/15p stovetop — all
      `assembly`/`batch`.
    - **Per recipe, `createNodes` (3):** one per new product, `quantity: 0`,
      `unit: "cup"` (**canonical — the script bypasses `planGraphWrites`' normalize,
      planning_findings #9**).
    - **Per recipe, `retargetEdges` (3):** ground beef → `Cook meat`; marinara →
      `Simmer meat sauce`; spaghetti noodles dry → `Cook spaghetti`. Each with its
      edge id and before/after.
    - **Per recipe, `createEdges` (6):** step→product — Cook spaghetti→cooked,
      Cook meat→browned, Simmer→meat sauce; product→step — browned→Simmer,
      meat sauce→Combine, cooked→Combine. **Mark all six `loadBearing: true`.**
    - **Per recipe, `preserved` (2), recorded explicitly so a reviewer can see they
      were considered and deliberately untouched:** the parmesan input edge and the
      terminal `meat spaghetti stored` output edge. Record the reuse justification
      from planning_findings #7 — reuse-as-Combine preserves two edges where every
      alternative preserves one.

    **Assert the full converging chain, in the script, on both dry-run and apply.
    This is the highest-risk part of the task** — a 4-step converging graph has six
    edges to drop where the old 2-step model had two, and a dropped edge here
    reproduces the exact silent-edge-drop failure this whole cluster is about:
    - `ground beef [stored] → Cook meat → ground beef browned → Simmer meat sauce → meat sauce → Combine all → meat spaghetti stored`
    - `spaghetti noodles dry → Cook spaghetti → spaghetti noodles cooked → Combine all`
    - `marinara → Simmer meat sauce`; `parmesan → Combine all`
    Walk it from the recipe's edge records — not from the worksheet's intentions —
    and fail if any hop is missing.

    Emit the Markdown companion `split-create-spaghetti.md` rendering the delta as a
    readable before/after per recipe plus the chain diagram. This is what the human
    reads at the checkpoint, so make the edges legible, not raw ids.

    **`--apply` path.** Preflight: `model === "batch-4step-v2"`; `confirmed === true`;
    every referenced record still resolves and still matches its recorded `before`
    (abort on ANY drift); durations finite and `>= 0`; every new product's
    `canonical_unit` non-empty. Then `pb.backups.create()`, print the backup id, and
    write `split-create-spaghetti.rollback.json` **covering CREATES, not just field
    reverts**: every created record's collection + id so rollback DELETES it (3
    products, 6 steps, 6 nodes, 12 edges across both recipes), and every
    updated/retargeted record's BEFORE values so rollback restores them. Order
    rollback deletes children-before-parents (edges → nodes → steps → products), the
    constraint `sync-to-test.js` documents. Write in dependency order on apply:
    products → steps → nodes → edges. Provide an explicit `--rollback` flag that
    executes the worksheet.

    **Rehearsal (required before the checkpoint).** Report against prod read-only,
    then `scripts/sync-to-test.js` to copy prod → test, then rehearse the FULL
    `--apply` against `PB_URL=http://192.168.50.95:8091`, then **actually execute
    `--rollback` on test** and confirm the graph returns to its original shape — a
    rollback path that has never been run is not a rollback path. Then re-apply and,
    via `npx tsx` against test, confirm: (a) `buildWeekGraph` gains **3 nodes per
    spaghetti recipe** (2 → 5) with the chain's precedence edges present; (b)
    `collectDayOfWork` still reports **four** meals and **NOT** meat spaghetti — it
    has zero JIT steps by design (planning_findings #3/#15); and (c) the GA can place
    `Cook spaghetti`'s passive window inside `Simmer meat sauce`'s. For (c) report
    what the scheduler actually did rather than asserting a fixed schedule — burners
    are a week-wide resource and other stovetop steps compete (planning_findings #4).
  </action>
  <verify>
    <automated>cd recipe-planner && node scripts/split-create-spaghetti-step.js 2>&1 | tee /tmp/split-audit.txt && node -e "
const w=require('./scripts/dedup-output/split-create-spaghetti.json');
if(w.model!=='batch-4step-v2')throw new Error('worksheet must be version-stamped batch-4step-v2, got '+w.model);
if(w.confirmed!==false)throw new Error('worksheet must seed confirmed:false');
const ps=w.createProducts||[];
if(ps.length!==3)throw new Error('expected 3 new transient products, got '+ps.length);
for(const p of ps){if(p.type!=='transient')throw new Error(p.name+' must be transient per the user ruling');if(!p.canonical_unit)throw new Error(p.name+' missing canonical_unit — both recipes would become unpublishable');}
const rs=w.recipes||[];
if(rs.length!==2)throw new Error('expected both spaghetti recipes, got '+rs.length);
for(const r of rs){
  if((r.createSteps||[]).length!==3)throw new Error('expected 3 new steps for '+r.recipeId);
  for(const s of [...(r.createSteps||[]),r.updateStep.after])if(s.timing!=='batch')throw new Error('every spaghetti step must be batch — there is no day-of work for this dish');
  if(r.updateStep.after.name!=='Combine all')throw new Error('reused step must become Combine all (max edge preservation)');
  if((r.retargetEdges||[]).length!==3)throw new Error('expected 3 retargeted input edges for '+r.recipeId);
  if((r.createEdges||[]).length!==6)throw new Error('expected 6 new edges for '+r.recipeId);
  if((r.createEdges||[]).some(e=>!e.loadBearing))throw new Error('every new edge in the converging chain is load-bearing');
  if((r.preserved||[]).length!==2)throw new Error('parmesan input + stored output must be recorded as preserved');
}
console.log('split worksheet OK: model',w.model,'| 3 transient products | 2 recipes x (1 update + 3 creates + 3 retargets + 6 edges)');
" && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>The stale day-of-model worksheet is gone and cannot be applied (version-stamped `batch-4step-v2`, asserted on apply); the rewritten script emits a full 4-step all-batch delta for both recipes plus 3 shared transient products with canonical units, reuses the existing step as `Combine all` preserving the parmesan input and terminal output edge, and asserts the whole converging chain from the edge records; `--apply` AND `--rollback` are both executed green on :8091, with the week graph (+3 nodes/recipe), the day-of summary (still 4 meals, no spaghetti) and the burner overlap re-verified there. Prod is untouched. Independently committable.</done>
</task>

<task type="auto">
  <name>Task 2: Invert the meat-spaghetti day-of expectation (amends c63d799)</name>
  <files>recipe-planner/src/lib/scheduler/day-of-work.test.ts</files>
  <action>
    `day-of-work.ts` and the CookMode wiring shipped correctly in `c63d799` and need
    **no change** — the collector, the predicate and the message are all still right.
    Only the test's *expectation about spaghetti* was written against the superseded
    model. Commit on its own.

    `day-of-work.test.ts:155-161` carries a comment stating "Task 3's split later
    adds `meat spaghetti` as a FIFTH entry; that post-split behaviour is proven
    end-to-end at the checkpoint". **That is now false** (planning_findings #3): meat
    spaghetti has zero JIT steps by design and must never appear in the day-of
    summary. The fixture *data* — four meals, no spaghetti — is correct and
    permanent; only the framing is wrong.

    - Rename `WEEK_7_13_PRE_SPLIT` to something that does not imply it changes (e.g.
      `WEEK_7_13_DAY_OF`). "PRE_SPLIT" invites a future reader to add a fifth entry
      after the split lands.
    - Replace the comment with the correction: the user cooks the spaghetti on prep
      day and stores the dish combined (interview 2026-07-17), so meat spaghetti
      carries **zero JIT steps by design**; its absence here is **correct and
      permanent**, before and after the split. Say plainly that the earlier
      expectation of a fifth entry was based on a misreading of how the user cooks,
      so nobody restores it.
    - **Turn the absence into a real assertion rather than an incidental.** Add
      `meat spaghetti` to the fixture with its actual post-split all-`batch` steps
      (Cook spaghetti, Cook meat, Simmer meat sauce, Combine all) and assert it is
      **excluded** from `collectDayOfWork`'s result and absent from
      `formatDayOfSummary`'s string, while the other four meals still render exactly.
      That converts "we didn't list it" into "an all-batch meal is provably never
      reported as day-of work" — the property the split depends on.
    - Keep the summary assertion at **four** meals.

    Do not touch `day-of-work.ts`, `CookMode.tsx`, or any other test.
  </action>
  <verify>
    <automated>cd recipe-planner && npx vitest run src/lib/scheduler/day-of-work.test.ts && ! grep -q 'PRE_SPLIT' src/lib/scheduler/day-of-work.test.ts && ! grep -qi 'FIFTH entry' src/lib/scheduler/day-of-work.test.ts && grep -qi 'zero .*just_in_time\|zero JIT' src/lib/scheduler/day-of-work.test.ts && grep -qi 'meat spaghetti' src/lib/scheduler/day-of-work.test.ts && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>The fixture asserts that an all-batch meal (meat spaghetti, with its real post-split steps) is provably never reported as day-of work; the summary still names exactly four meals; the fifth-entry prediction and the PRE_SPLIT naming are gone, replaced by the interview-backed reason its absence is correct and permanent. Independently committable.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the mis-tag todo AGAIN + correct STATE.md (amends b6f009f)</name>
  <files>.planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md, .planning/STATE.md</files>
  <action>
    The backlog hygiene in `b6f009f` was written against the superseded model. The
    mis-tag todo now claims a split that the user does not want. **A wrong proposal
    sitting in the backlog is exactly the defect this plan keeps tripping over** —
    this is the third correction to this file. Commit on its own.

    **Rewrite `2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md`.**
    Keep a `## Corrected` section (append to the existing one, dated 2026-07-17 —
    do not delete the previous correction; the sequence of wrong readings is the
    point). It must record, **loudly enough that nobody re-derives the day-of split
    in three months:**
    - **`create spaghetti` is correctly `batch`.** The hand-retag was right.
    - **The todo's own batch-sauce/day-of-plate proposal was based on a misreading of
      how the user cooks**, verified by direct interview 2026-07-17: the spaghetti is
      **boiled on prep day, combined with the sauce, and stored combined. There is no
      day-of work for this dish.** Quote that plainly. The proposal was never derived
      from the data — it was assumed from the dish's name.
    - **meat spaghetti having zero JIT steps is correct by design**, so its absence
      from cook mode's day-of summary is a property, not a gap. Anyone "fixing" that
      absence is re-introducing this bug.
    - **The real defect was only granularity:** one 8a/15p mega-step hid two
      overlappable passive windows and under-counted active time (real: 10a/23p).
      Split into 4 `batch` steps so the scheduler can overlap the 8p noodle boil with
      the 15p simmer — expressible only because `burner_count: 2`.
    - Mark that piece SHIPPED once the write lands, pointing at
      `scripts/split-create-spaghetti-step.js`.
    Also carry forward, from the previous correction: the retag was applied **by hand**
    with no commit and no script (planning_findings #1) — keep that; the
    pull-with-simmer hazard is RESOLVED with the graph evidence (#2); the linter piece
    is SHIPPED (`rules/timing-coherence.ts`) with its stated blind spot (`Brown
    mushrooms` and `Pull garlic cubes` are 0-passive and uncatchable); the "17 prep
    steps carry a timing" note is confirmed harmless (all `batch`, zero JIT). **Item 3
    — tighten the import skill so a weekly meal's cook steps default to `batch` — is
    what keeps this todo PENDING.**

    **Correct STATE.md.** Its `mis-tagged-just-in-time-timing-on-make-ahead-steps`
    entry (rewritten in `b6f009f`) restates the day-of-split proposal; bring it in
    line with the above — `create spaghetti` correctly `batch`, split into 4 batch
    steps for overlap, no day-of work for the dish. Update the `260716-u4p` row in
    Quick Tasks Completed to match the delivered scope. Do not touch the other pending
    todos or `Blockers/Concerns`.
  </action>
  <verify>
    <automated>cd /home/ellio/code/RecipePlanner && bash -e -c '
fail() { echo "FAIL: $1"; exit 1; }
p=".planning/todos/pending/2026-07-12-mis-tagged-just-in-time-timing-on-make-ahead-steps.md"
test -f "$p" || fail "mis-tag todo must STAY pending (import-skill hardening is unshipped)"
grep -q "^## Corrected" "$p" || fail "missing Corrected section"
grep -q "2026-07-17" "$p" || fail "missing the interview date"
grep -qi "no day-of work" "$p" || fail "must state plainly that this dish has no day-of work"
grep -qi "by hand" "$p" || fail "must retain the unrecorded hand-edit process finding"
grep -qi "burner" "$p" || fail "must record why the split is worth it (burner_count=2 overlap)"
grep -qi "just_in_time" .planning/STATE.md || true
grep -q "260716-u4p" .planning/STATE.md || fail "STATE.md missing the 260716-u4p quick-task row"
grep -q "garlic-cube-clove-unit-conversion" .planning/STATE.md && fail "resolved garlic todo still referenced in STATE.md"
echo "hygiene OK"
'</automated>
  </verify>
  <done>The mis-tag todo records that `create spaghetti` is correctly `batch`, that its own day-of-plate proposal came from a misreading of how the user cooks (interview 2026-07-17, quoted), and that meat spaghetti's absence from the day-of summary is correct by design — loudly enough to stop a future re-derivation. The real defect is restated as granularity + the burner-overlap win. It stays pending on the import-skill item. STATE.md matches. Independently committable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Already shipped and committed: cook mode names the remaining day-of meals instead
    of asserting "All steps complete!" (`c63d799`); the `timing-coherence` rule, tuned
    to zero false positives against the real 24 live JIT steps, plus the severity-blind
    publish-gate todo (`9f47692`); backlog hygiene (`b6f009f`).

    This revision: Task 1 rewrote the split script for the **4-step all-batch** model
    and deleted the stale worksheet that encoded the superseded day-of split; Task 2
    inverted the meat-spaghetti expectation in the day-of tests; Task 3 rewrote the
    mis-tag todo against the 2026-07-17 interview.

    **Prod data is still untouched.** Step 1 below VERIFIES the bourguignon retag that
    was already applied by hand (nothing to write). Steps 3-5 are the real,
    irreversible write.
  </what-built>
  <how-to-verify>
    1. **Verify the retag — read-only, no auth, no write:**
       `curl -s 'http://192.168.50.95:8090/api/collections/recipe_steps/records?perPage=500&filter=(recipe="5t10ugwgxb166ll")' | python3 -m json.tool | grep -E '"name"|"timing"'`
       Expect `Pull garlic cubes`, `Brown mushrooms`, `Simmer bourguignon` all
       `batch`; only `Cook egg noodles` and `Serve over noodles` `just_in_time`.

    2. **Read the split worksheet.** Open
       `recipe-planner/scripts/dedup-output/split-create-spaghetti.md`. Confirm it is
       stamped `batch-4step-v2` (an unstamped worksheet is the deleted, superseded
       day-of model and `--apply` will reject it). Check the four steps against how
       you cook it — Cook spaghetti 2a/8p, Cook meat 5a/0p, Simmer meat sauce 1a/15p,
       Combine all 2a/0p with the parmesan, everything `batch`, nothing day-of.
       Two things to note rather than decide: total active goes **8a → 10a** (the
       mega-step was under-counting, expected); and `Combine all` is proposed
       `resource: none` — if you actually combine in the pot on a burner, say so and
       it changes. Then set `confirmed: true` in `split-create-spaghetti.json`.

    3. **Dry-run against prod and read the FULL delta:**
       `cd recipe-planner && node scripts/split-create-spaghetti-step.js`
       Confirm the converging chain prints intact — beef → Cook meat → browned →
       Simmer → meat sauce → Combine, and noodles → Cook spaghetti → cooked →
       Combine. **If the script aborts on drift, stop** — prod has moved again (it has
       once already, silently) and the delta must be re-derived, not forced.

    4. **Apply:** `cd recipe-planner && node scripts/split-create-spaghetti-step.js --apply`
       Confirm it prints the PB backup id and wrote `split-create-spaghetti.rollback.json`.

    5. **Read back:** re-run the read-only report; both recipes should show all five
       steps (`take ground beef out of freezer` + the four new ones), every one
       `batch`, chain intact.

    6. **Hard-refresh the tablet** — the NAS deploy caches the old JS bundle.

    7. **The end-to-end checks — note (a) is an ABSENCE:**
       - **(a)** Open cook mode for the week of 7/13 (`71ukhycp2v4s0fw`) and check off
         every step. Expect **"Prep day complete."** and *4 meals still need day-of
         cooking:* — Harissa Cod (1), Mushroom Bourguignon (2), salad and salmon (2),
         Creamy Tomato Soup (1). **meat spaghetti must NOT be listed.** It has zero
         day-of work by design; its absence is the correct result.
       - **(b)** The prep-day list should now show meat spaghetti as **four** separate
         cards (Cook spaghetti, Cook meat, Simmer meat sauce, Combine all) instead of
         one "create spaghetti" — 3 more nodes per recipe.
       - **(c)** In the Full Schedule view, check whether `Cook spaghetti`'s 8-minute
         boil lands inside `Simmer meat sauce`'s 15-minute passive window. This is the
         point of the split, but it is **not guaranteed** — burners are a week-wide
         resource (`burner_count: 2`) and other stovetop steps compete. If it did not
         overlap this week, that is scheduling pressure, not a broken split.
       - **Do not check the Pull Lists tab** — it is dark for this plan (all 18 meals
         are week-spanning), so it proves nothing either way.

    8. **Confirm the linter blocks nothing.** Run cook mode's Lint button. Expect no
       `timing-coherence` finding — every live JIT step is correctly tagged, and the
       four new spaghetti steps are `batch`, which the rule short-circuits on before
       any name/passive test. (0 drafts exist in prod, so Publish is not a usable
       surface for this check.)

    **Rollback:** `node scripts/split-create-spaghetti-step.js --rollback` — deletes the
    created records children-first (edges → nodes → steps → products) and restores the
    updated ones from their BEFORE values. This path was **executed** on `:8091` in
    Task 1, not just written. Or restore the PB backup from step 4 via the PocketBase
    admin UI.
  </how-to-verify>
  <resume-signal>Type "approved" once the split is applied, meat spaghetti shows four prep-day cards and is correctly ABSENT from the day-of summary, and no live step trips the new rule — or describe what looked wrong in the delta</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean, full `npx vitest run` green, `npm run build` succeeds.
- `day-of-work.test.ts` asserts an all-batch meal (meat spaghetti with its real
  post-split steps) is provably never reported as day-of work, and that the summary
  names exactly four meals; `timing-coherence.test.ts` pins zero findings against the
  11 real published JIT steps a naive rule would have flagged.
- The stale day-of-model worksheet is deleted and unusable — the new one is stamped
  `batch-4step-v2` and `--apply` asserts the stamp.
- The split worksheet's `--apply` AND `--rollback` are both executed green on `:8091`,
  with the week graph (+3 nodes/recipe), the day-of summary (4 meals, no spaghetti)
  and the burner overlap re-verified there; prod is written only behind the blocking
  checkpoint, after a backup id is printed.
- Post-split, both spaghetti recipes express the full chain — beef → Cook meat →
  browned → Simmer → meat sauce → Combine → meat spaghetti stored, and noodles → Cook
  spaghetti → cooked → Combine — with all four steps `batch` and meat spaghetti still
  absent from the day-of summary.
- Bourguignon's three retagged steps still read `batch` — verified, not written.
</verification>

<success_criteria>
Cook mode no longer tells a cook they are finished while four dinners still need
cooking. `create spaghetti` becomes four `batch` steps whose passive windows the
scheduler can overlap, while meat spaghetti stays correctly out of the day-of
summary. A simmer or braise mis-tagged `just_in_time` is caught at the publish gate,
while every one of the 24 correctly-JIT steps stays publishable. The backlog states
what is true.

**The SUMMARY must state plainly:**
1. **The spaghetti model was wrong three times, and only a user interview settled
   it.** The todo assumed the dish had day-of work because of its name; the user
   boils the spaghetti on prep day and stores it combined. `create spaghetti` at
   `batch` was **correct**, the hand-retag was **right**, and meat spaghetti's
   **absence** from the day-of summary is a correctness property, not a gap. The
   batch-sauce/JIT-plate split was designed, planned, scripted (`28c9102`) and
   **never shipped** — the script was rewritten and its stale worksheet deleted
   before it could be applied. **Ask the user how they cook it before modelling the
   graph.**
2. **The real defect was granularity, not timing.** One 8a/15p mega-step hid two
   overlappable passive windows and under-counted active time (real 10a/23p). The
   4-step split makes the noodle/simmer overlap **expressible** — and only because
   prod's `burner_count` is 2; `resources.ts` holds a burner through a passive
   window, so at `burner_count: 1` the split would have made the dish slower. The GA
   still decides whether the overlap happens; the split does not promise it.
3. **The bourguignon retag was already applied by hand, with no commit and no
   script.** That is why three todos, four STATE.md entries and this task's own scope
   described it as pending. **A prod data change made outside the repo is invisible
   to the backlog** — the second time in two days the backlog's diagnosis was itself
   the defect (cf. `260716-rpp`).
4. **Cook mode was not ending silently — it was asserting "All steps complete!"**
   A stronger claim than the todo described, and a worse failure.
5. **The linter rule as scoped would have bricked ten published recipes.** A
   `warning` hard-blocks publish (`RecipeEditor.tsx:767` is severity-blind), and
   `passive_minutes > 0` + JIT flags 10 live steps, all correct. Shipped as a
   conjunction with a simmer/braise/stew verb: 0 false positives, 3 of 5 historical
   mis-tags caught. **`roast` was deliberately excluded** — 2 false positives, no
   gain. `Brown mushrooms` and `Pull garlic cubes` are 0-passive and structurally
   uncatchable; the header says so and the tests pin it. The new `Simmer meat sauce`
   is `batch`, so the rule cannot fire on it — but it would if anyone flipped it to
   JIT, which is the guard pointing the right way.
6. **The severity-blind publish gate is captured, not fixed** — 2 of 67 recipes would
   block on re-publish and 0 drafts exist: a design defect with a long tail, not a
   live outage.
7. **Still deferred, deliberately:** the day-of cook surface (correctly-JIT steps are
   named but remain un-runnable — the day-of todo stays pending), the import-skill
   hardening (keeps the mis-tag todo pending), and the print-view `buildWeekPullList`
   rewrite.
</success_criteria>
